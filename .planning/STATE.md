# State

## Commits sin procesar
- `2026-08-05` `75ef9be` fix(scrapers): un scraper bloqueado dejaba de verse como cero publicaciones
- `2026-08-05` `9e5a980` feat(salud): health check de fuentes externas del camino crítico
- `2026-08-05` `6685cf8` perf(demografia): cachear isócrona+censo (10,4s → 0,34s) y limpiar sii-lookup
- `2026-08-05` `649ba71` refactor(copiloto): unificar en getContextoNormativo y eliminar el duplicado DDU
- `2026-08-05` `76fd1f7` fix(ai): tolerar null en los 6 schemas que parsean salida de LLM
- `2026-08-05` `6e0c295` fix(copiloto): tolerar null en los campos de texto del diagnóstico OGUC/LGUC
- `2026-08-05` `26bbc46` fix(e2e): eliminar flakiness y auto-skip silencioso de los smokes de detalle
- `2026-08-05` `6371398` fix(normativa): conectar LGUC al copiloto + corregir falso positivo del detector
- `2026-08-04` `9d201de` chore(calidad): "wired or it doesn't count" + smoke E2E de rutas
- `2026-08-04` `942ad7b` feat(cabida): motor de análisis + siembra de muestra — primer veredicto real
- `2026-08-04` `eeedf93` feat(cabida): Fase 16 — isócronas reales vía Valhalla, reemplazando ORS
- `2026-08-04` `ba21579` feat(visual): KpiCard/RankingBarChart en Señales de ubicación y Posicionamiento
- `2026-08-04` `e5f35f6` feat(proyecto): rediseñar DemografiaConsumoCard con KpiCard + RankingBarChart
- `2026-08-04` `8a293a2` feat(oportunidades): extender demografía/consumo al tab Historial
- `2026-08-04` `f7eefeb` feat(terrenos): conectar demografía y consumo (INE/CASEN) al detalle de terreno
- `2026-08-04` `cdecf19` feat(mercado-locales): segunda fuente — Doomos.cl junto a Portalinmobiliario
- `2026-08-04` `272c585` feat(terrenos): comparador terreno vacante vs. local ya construido
- `2026-08-04` `db3a740` feat(terrenos): reintento masivo de señales de ubicación pendientes
- `2026-08-03` `3784589` feat(zonificacion): ampliar cobertura de 19 a 31 comunas RM
- `2026-08-03` `ab43f3a` fix(zonificacion): conectar fallback de centroide de comuna para direcciones no geocodificables
- `2026-08-03` `94ad4a7` fix(config): corregir fallback de puerto dev en self-fetch server-side
- `2026-08-03` `9de1987` feat(terrenos): reintento masivo de zonificación pendiente
- `2026-08-03` `fad486e` feat(terrenos): mostrar equivalente en UF junto al precio y al $/m²
- `2026-08-03` `7ce88bf` refactor(terrenos): mejora de densidad visual de la tabla de terrenos
- `2026-08-03` `1ea8632` feat(proyectos): dashboard de zonificación a nivel portafolio
- `2026-08-03` `b0009fc` fix(copiloto): el checklist nunca persistía + regenerar cuando la zonificación llega tarde
- `2026-08-03` `a1b73df` fix(zonificacion): backlog 11-08 — cita usa la comuna manual, no proyecto.municipio
- `2026-08-03` `cac349a` fix(zonificacion): backlog 11-08 — reparar residuo de doble-corrupción mojibake
- `2026-08-03` `e468bef` docs: record commit log entry
- `2026-08-03` `67570af` docs(16-01): dedupe self-check section in SUMMARY.md
- `2026-08-03` `3b3318e` docs(16-01): reintento bloqueado — ORS/HeiGIT sigue en 403 tras horas
- `2026-08-03` `17cf1e1` docs(19-03): plan bloqueado por prerequisito de tres vías (Phase 16/17-03/18-07), sin fabricar código
- `2026-08-03` `c12cd94` docs(19-01): complete núcleo puro de veredicto de cabida comercial plan
- `2026-08-03` `2cd20f7` refactor(19-01): extraer textos de motivo a funciones nombradas
- `2026-08-03` `46c049d` feat(19-01): implement calcularVeredictoCabida con banding por terciles
- `2026-08-03` `8198a9b` feat(19-02): add CabidaComercialMapa component
- `2026-08-03` `87e238b` test(19-01): add failing test for calcularVeredictoCabida
- `2026-08-03` `29308a8` docs(19): apply plan-checker revision — tercile banding, no invented threshold
- `2026-08-03` `186db32` docs(19): create phase plan
- `2026-08-03` `423a579` docs(19): research phase domain — three-way gate pattern, verdict/map buildable now, tab creation not needed
- `2026-08-03` `0a2e958` fix(deploy): cap backfill-ubicacion maxDuration to Hobby's 300s ceiling
- `2026-08-03` `ca252c7` fix(deploy): consolidate 12 vercel.json crons into 1 dispatcher
- `2026-08-03` `9e1f39e` docs(17-03): plan bloqueado por prerequisito de Phase 16, sin fabricar código
- `2026-08-03` `125391c` docs(18-06): complete orquestador de competencia por formato plan
- `2026-08-03` `bd6f681` test(18-06): add tests for obtenerCompetenciaPorFormato orquestador
- `2026-08-03` `64406f2` feat(18-06): implement obtenerCompetenciaPorFormato orquestador async
- `2026-08-03` `e4b93f9` docs(17-02): complete consumo macro-zona (EPF + CASEN) plan
- `2026-08-03` `f385653` test(17-02): add unit tests para obtenerConsumoEstimado + data-sources.yaml
- `2026-08-03` `0e442fc` feat(17-02): poblar CASEN_POBREZA_POR_COMUNA con 36 comunas RM reales
- `2026-08-03` `c334303` docs(17-01): append self-check result to summary
- `2026-08-03` `5a68662` docs(17-01): complete módulo de población censal plan
- `2026-08-03` `3128360` test(17-01): add unit tests para obtenerPoblacionEnPoligono + guard de URL
- `2026-08-03` `7789fdf` feat(17-01): add lib/censo-manzana-server.ts + migración aditiva demografia_*
- `2026-08-03` `ea8be0b` feat(17-02): add consumo-macro-zona con tabla EPF parcial y esqueleto CASEN
- `2026-08-03` `65e8310` docs(17): create phase plan
- `2026-08-03` `6f7f1a7` docs(17): research phase domain — corrected INE census FeatureServer URL, gated composition pattern
- `2026-08-03` `b6deec9` docs(16): ORS key set but HeiGIT returns 403, likely propagation delay
- `2026-08-02` `23a3a99` docs(18-02,18-03): complete overpass extension and strip/power seed list plans
- `2026-08-02` `512a835` feat(18-03): add lib/strip-power-centers-chile.ts seed list + data-sources.yaml entry
- `2026-08-02` `3924cc1` feat(18-02): implement obtenerCompetidoresOverpass con parseo y filtro de isócrona
- `2026-08-02` `3eae0aa` docs(18-05): complete núcleo puro de degradación de confianza plan
- `2026-08-02` `d03774a` feat(18-03): add geocode-strip-power-seed script auditable
- `2026-08-02` `26256fe` feat(18-02): scaffold lib/overpass-competencia.ts (query, throttle, tipos)
- `2026-08-02` `51b4f36` refactor(18-05): extraer textos de disclosure a constantes nombradas
- `2026-08-02` `2b2048c` feat(18-05): implement calcularResultadoCompetencia() con degradación de confianza
- `2026-08-02` `3286596` test(18-05): add failing test for calcularResultadoCompetencia()
- `2026-08-02` `7f924c4` docs(18-04): complete geocoding on-demand de cadenas SII plan
- `2026-08-02` `35eeea8` feat(18-04): add obtenerCadenasGeocodificadasPorComuna con cache-through
- `2026-08-02` `396d2c3` docs(18-01): complete tipos de competencia por formato plan
- `2026-08-02` `e88ca70` feat(18-04): add cadenas_sucursales geocoding migration
- `2026-08-02` `bc7eb0e` test(18-01): add guard automatizado contra colisión FormatoComercial
- `2026-08-02` `340680e` feat(18-01): extend lib/cabida-comercial.ts con tipos de competencia por formato
- `2026-08-02` `149731f` docs(18): add plan 18-08 closing COMPE-01/02/06 user-facing gap
- `2026-08-02` `23b35df` docs(18): create phase plan
- `2026-08-02` `5a41214` docs(research): seed list of strip/power centers in Chile RM (Phase 18 prep, COMPE-04)
- `2026-08-02` `06077ca` docs(16): pause 16-01 pending ORS_API_KEY, resume execution paused
- `2026-08-02` `dbbda8c` docs(16-03): complete migración de cabida_comercial_cache plan
- `2026-08-02` `b310d93` docs(16-02): complete geocoding de centroide de comuna plan
- `2026-08-02` `436aa6d` feat(16-03): add cabida_comercial_cache migration
- `2026-08-02` `9816e4c` test(16-02): add geocodeComunaCentroide unit tests
- `2026-08-02` `60f8aba` feat(16-02): add lib/cabida-comercial.ts client-safe types + fetch helper
- `2026-08-02` `f2f771b` feat(16-02): add geocodeComunaCentroide() fallback en lib/geocoding.ts
- `2026-08-02` `b9080e7` docs(16): apply plan-checker revision (naming collision, mock chain, key_link fix)
- `2026-08-02` `e542cdd` docs(16): create phase plan
- `2026-08-02` `163c9fc` docs: create milestone v1.7 roadmap (4 phases)
- `2026-08-02` `81df899` docs: define milestone v1.7 requirements
- `2026-08-02` `0550c80` docs: complete project research
- `2026-08-02` `f56b13c` docs: start milestone v1.7 Cabida Comercial
- `2026-08-02` `8124061` docs: record commit log entry
- `2026-08-02` `785c891` chore: complete v1.6 milestone
- `2026-08-02` `d8ed8ed` docs: record commit log entry
- `2026-08-02` `48b1d44` docs(phase-15): complete phase execution, close milestone v1.6
- `2026-08-02` `706452c` docs(15-03): complete informe de comparación plan
- `2026-08-02` `9d257e0` docs(15-02): complete informe individual plan, close phase 15
- `2026-08-02` `0385318` docs(15-02): record checkpoint progress (Tasks 1-2 done, Task 3 human-verify pending)
- `2026-08-02` `c7132ff` feat(15-02): wire "Exportar informe" entry point en ficha de detalle
- `2026-08-02` `87723d8` docs(15-03): record checkpoint progress (Tasks 1-2 done, Task 3 human-verify pending)
- `2026-08-02` `9805c8b` feat(15-03): wire "Exportar informe" entry point en comparar/page.tsx
- `2026-08-02` `eab3286` feat(15-02): add /oportunidades/[id]/informe printable report page
- `2026-08-02` `d6280ea` docs(15-01): complete infraestructura compartida del informe exportable plan
- `2026-08-02` `a8a53c9` feat(15-01): add PortadaInforme + MetodologiaInforme shared components
- `2026-08-02` `b4b8e07` feat(15-01): add PrintButton + PreparadoPorPara client islands
- `2026-08-02` `d3c2813` feat(15-01): print-safe dashboard layout + promote formatTimestampCorto
- `2026-08-02` `25846d3` docs(15): create phase plan
- `2026-08-02` `c427982` docs(phase-14): fix REQUIREMENTS.md traceability (phase-complete script matched wrong Requirements line)
- `2026-08-02` `bd32e17` docs(phase-14): complete phase execution
- `2026-08-02` `2f128b2` docs(14-03): complete ruta y tabla de comparación plan
- `2026-08-02` `70e7482` docs(14-03): record checkpoint progress (Tasks 1-2 done, Task 3 human-verify pending)
- `2026-08-02` `0d2a10b` feat(14-03): add /oportunidades/comparar route with server-side homogeneity validation
- `2026-08-02` `07a5a06` feat(14-03): add TablaComparacion component
- `2026-08-02` `9cafa1e` docs(14-01): complete capa de datos para comparación en lote plan
- `2026-08-02` `55ecf32` docs(14-02): complete selector de comparación plan
- `2026-08-02` `ecf1a71` feat(14-02): wire SelectorComparacion en oportunidades/page.tsx
- `2026-08-02` `8cc0137` feat(14-01): add obtenerOportunidadesPorIds(ids) con fetch batched
- `2026-08-02` `36f2c6d` refactor(14-01): extraer construirOportunidadDetalle() de obtenerOportunidadPorId
- `2026-08-02` `07ef7f7` feat(14-02): add SelectorComparacion client island
- `2026-08-02` `76b1d2a` docs(14): create phase plan
- `2026-08-02` `1a55385` docs(phase-13): complete phase execution
- `2026-08-02` `cf3e8cf` docs(13-07): complete integración final de la ficha de detalle plan
- `2026-08-02` `0e0b1e3` feat(13-07): wire lista de oportunidades hacia la ficha completa
- `2026-08-02` `006e428` feat(13-07): add oportunidad detalle Server Component page
- `2026-08-02` `e1ad70c` docs(13-05): complete tab de posicionamiento plan
- `2026-08-02` `f1fb3a9` docs(13-06): complete tabs historial y comparables plan
- `2026-08-02` `a1eb931` feat(13-05): add rentabilidad implicita de zona (DETA-07) to PosicionamientoTab
- `2026-08-02` `8950c72` feat(13-06): add ComparablesTab with clickable mini-cards
- `2026-08-02` `6f2b881` feat(13-06): add HistorialTab with price history, reason codes, and cross signals
- `2026-08-02` `2d02c39` feat(13-05): add PosicionamientoTab banda + banner de muestra chica
- `2026-08-02` `d120f9f` docs(13-03): complete capa de datos ficha de detalle plan
- `2026-08-02` `0becbc1` docs(13-04): complete resumen ejecutivo IA plan
- `2026-08-02` `7f24561` feat(13-03): add obtenerComparablesOportunidad()
- `2026-08-02` `0530389` feat(13-04): add ResumenTab on-demand executive summary component
- `2026-08-02` `8ed59c6` feat(13-04): add POST /api/oportunidades-resumen SSE route
- `2026-08-02` `08ef64e` feat(13-03): add obtenerOportunidadPorId + historial + labels
- `2026-08-02` `983281b` docs(13-02): complete utilidades compartidas plan
- `2026-08-02` `3e6a533` docs(13-01): complete evaluarOportunidad extraction plan
- `2026-08-02` `182dd0e` feat(13-02): add resumen ejecutivo IA prompt builders
- `2026-08-02` `c3e920a` feat(13-02): add streamConContexto without web search tool
- `2026-08-02` `017fc96` feat(13-02): add shared formatFechaCorta date formatter
- `2026-08-02` `025e3f0` feat(13-01): extract evaluarOportunidad() as single source of truth for scoring
- `2026-08-02` `e97818d` test(13-01): add failing test for evaluarOportunidad()
- `2026-08-02` `aa9e797` docs(13): create phase plan
- `2026-08-02` `684cb8a` docs(state): record phase 13 context session
- `2026-08-02` `e815a65` docs(13): capture phase context

_(Ninguno pendiente — los 4 commits de inicio de milestone v1.6 (research, requirements, config bump, roadmap) ya están descritos en "Current Position" y en los propios PROJECT.md/ROADMAP.md/REQUIREMENTS.md — son el ciclo estándar de `/gsd:new-milestone`, no necesitan bullet propio en Accumulated Context. Sección alimentada automáticamente por `.githooks/post-commit` — cada commit agrega una línea acá. Un comando/sesión futura la digiere hacia una narrativa real en "Current Position"/"Accumulated Context" y la limpia. Ver Key Decisions en PROJECT.md: "captura automática, curación cuando se pueda".)_

## Current Position

Phase: 19 of 19 (Veredicto, Metodología, Mapa y Tab) — en paralelo con Fase 18 (Competencia por Formato) aún en progreso
Plan: 19-01 (1 de 4) completo, 19-02 completo, 19-03 INTENTADO Y BLOQUEADO (gate de tres vías: tab Phase 16/16-05 + demografia 17-03 + competencia 18-07, ver abajo — no cuenta como completo, cero archivos de código tocados, ver 19-03-SUMMARY.md) — calcularVeredictoCabida() pura en lib/veredicto-cabida.ts, ver 19-01-SUMMARY.md. Fase 18 en paralelo: 06 de 8 completo (18-01, 18-04, 18-05, 18-06) — Fase 16 en paralelo: 03 de 5 completo (16-02, 16-03), 16-01 PAUSADO (checkpoint human-action, no bloqueante) — Fase 17 en paralelo: 2 de 4 completo (17-01, obtenerPoblacionEnPoligono() + migración demografia_* aplicada; 17-02, obtenerConsumoEstimado() con EPF parcial + CASEN 2024 real para 36 comunas RM — ver 17-01-SUMMARY.md / 17-02-SUMMARY.md), 17-03 INTENTADO Y BLOQUEADO (gate de prerequisito de Phase 16, ver abajo — no cuenta como completo, cero archivos de código tocados, ver 17-03-SUMMARY.md)
Status: En progreso — Plan 19-01 (calcularVeredictoCabida(), núcleo de síntesis VERE-01 a VERE-04) completo vía TDD RED→GREEN→REFACTOR, sin deviations de arquitectura (1 fix mecánico de tipos en fixture de test, ver 19-01-SUMMARY.md). Plan 19-02 (CabidaComercialMapa) completo. Plan 19-03 (orquestador final, gateado por diseño sobre un chequeo de TRES vías: tab de Phase 16/16-05 + campo demografia de 17-03 + campo competencia de 18-07) ejecutado hasta su Task 1: los tres checks fallaron (`TAB NO EXISTE`, `lib/cabida-comercial-server.ts` no existe en absoluto todavía) — detenido limpiamente sin fabricar `lib/veredicto-cabida-server.ts` ni ninguna migración, tal como el plan lo prescribe; Task 2 (terciles reales vía obtenerPercentilesGapScore) y Task 3 (wiring del campo veredicto) quedan pendientes para cuando Phase 16 (16-04 y 16-05) Y Plan 17-03 Y Plan 18-07 completen los tres. Plan 19-04 (tab visible) permanece gateado adicionalmente por 19-03, sin cambios. 18-06 (obtenerCompetenciaPorFormato(), orquestador async que compone Overpass + seed list + geocoding SII + calcularResultadoCompetencia) completo, sin deviations. Plan 17-02 completo: checkpoint humano de Task 2 (CASEN) originalmente bloqueado por fallo de DNS del executor hacia datasocial.ministeriodesarrollosocial.gob.cl, resuelto por el orquestador sourceando el XLSX oficial real desde observatorio.ministeriodesarrollosocial.gob.cl — 36/36 comunas RM objetivo con dato real, ninguna pendiente. Plan 17-03 (gateado por diseño sobre `lib/cabida-comercial-server.ts` de Phase 16) ejecutado hasta su Task 1: `ls lib/cabida-comercial-server.ts` → "ARCHIVO NO EXISTE", `grep obtenerAnalisisCabidaComercial` → "FUNCION NO ENCONTRADA" — detenido limpiamente sin fabricar stub, tal como el plan lo prescribe; Task 2 (wiring de demografía) queda pendiente para cuando Phase 16/Plan 16-04 complete. Fase 16 sigue paused: `ORS_API_KEY` ya seteada en `.env.local` (2026-08-03, cuenta HeiGIT creada, "Basic Key" activa con cupo completo 500/500 en Isochrones V2 según el dashboard), y el placeholder de `.env.local.example` ya está committeado (`c50cc6f`). Un segundo intento de ejecución (mismo día, ~09:xx, horas después de la creación de la key) reverificó la API en vivo y confirmó el mismo 403 `"Access to this API has been disallowed"` en 3/3 combinaciones probadas: `api.openrouteservice.org` (raw header), `api.heigit.org` (raw header), `api.heigit.org` (`Bearer`). La hipótesis de "delay de propagación" queda descartada — el error es idéntico horas después, no un transitorio. Plan 16-01 se detuvo de nuevo en el mismo punto (Task 2, sin payload 200 que justifique escribir el script de diagnóstico, `lib/isocrona-server.ts` o su test — cero código fabricado), documentado en 16-01-SUMMARY.md. Se requiere que el usuario revise manualmente el dashboard de HeiGIT (verificación de cuenta pendiente, scope de la key limitado a otro servicio, o paso de activación no estándar) o contacte soporte — no reintentar el agente ejecutor sin haber confirmado un 200 real con un `curl` manual primero. Wave 2/3 de Fase 16 (16-04, 16-05) NO pueden ejecutarse hasta que 16-01 cierre — y por transitividad, tampoco Plan 17-03/17-04, 18-07, ni 19-03/19-04.
Last activity: 2026-08-03 — Plan 16-01 reintentado y bloqueado de nuevo: reverificación en vivo de ORS/HeiGIT confirma 403 persistente pese a `ORS_API_KEY` configurada (ver 16-01-SUMMARY.md, no cuenta como completo, cero código de `lib/isocrona-server.ts` fabricado). Plan 19-03 ejecutado hasta el gate de Task 1: chequeo de tres vías (tab `components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-tab.tsx` NO EXISTE, `lib/cabida-comercial-server.ts` NO EXISTE — por lo tanto ni demografia ni competencia pueden estar poblados), detenido limpiamente sin fabricar código, documentado en 19-03-SUMMARY.md, listo para reintentarse sin cambios cuando Phase 16 (16-04+16-05) Y Plan 17-03 Y Plan 18-07 completen. Plan 19-01 (calcularVeredictoCabida() en lib/veredicto-cabida.ts: función pura de 3 estados con banding por terciles provistos por parámetro, gapScore null cuando faltan datos base, tope duro de confianza en 'media', cold-start explícito con MUESTRA_MINIMA=10 — 14/14 tests, tsc limpio — ver 19-01-SUMMARY.md) completo. Plan 18-06 (obtenerCompetenciaPorFormato() en lib/competencia-formato.ts: ramifica seed list vs. Overpass+SII geocoding en paralelo con Promise.all, cruce espacial turf.distance radio 150m sustituye tag OSM por nombre real de cadena y sube confianza a 'alta', delega degradación final a calcularResultadoCompetencia de Plan 18-05 — 14/14 tests, tsc limpio, sin deviations — ver 18-06-SUMMARY.md) completo. Plan 18-07 (integración gateada por Fase 16) queda desbloqueado en su lógica de negocio, pendiente solo de que exista lib/cabida-comercial-server.ts. Plan 17-03 ejecutado hasta el gate de Task 1: `lib/cabida-comercial-server.ts` todavía no existe (Phase 16/16-04 pendiente), detenido limpiamente sin fabricar código, documentado en 17-03-SUMMARY.md, listo para reintentarse sin cambios cuando Phase 16 cierre.

Progress: [█████░░░░░] 52% (milestone v1.7 — Fase 16, 2/5 planes ejecutados (1 pausado, reintentado 2 veces, sigue bloqueado por 403 externo de ORS/HeiGIT); Fase 17, 2/4 planes ejecutados (17-03 intentado y bloqueado, no cuenta); Fase 18, 6/8 planes ejecutados; Fase 19, 2/4 planes ejecutados (19-01, 19-02; 19-03 intentado y bloqueado, no cuenta))

**Para retomar:** `ORS_API_KEY` ya seteada, pero HeiGIT sigue devolviendo 403 `"Access to this API has been disallowed"` horas después de crear la key — la hipótesis de delay de propagación ya no es plausible. Antes de relanzar el agente ejecutor: (1) revisar el dashboard de HeiGIT/openrouteservice.org directamente — ¿cuenta verificada por email? ¿la key incluye el scope "Isochrones" y no solo "Directions"? ¿hay un paso de activación adicional?; (2) si el dashboard se ve correcto, contactar soporte de HeiGIT; (3) confirmar un HTTP 200 real con un `curl` manual ANTES de reintentar — no relanzar el agente "a ciegas", ya van dos intentos con el mismo resultado. Una vez responda 200, `/gsd:execute-phase 16` retoma desde 16-01 (Task 2: escribir `scripts/verify-ors-isochrone.mjs`, capturar el payload real, y Task 3: `lib/isocrona-server.ts` + test).

## Phases Status

| Phase | Title | Status |
|---|---|---|
| 16 | Ubicación e Isócrona (Motor Desacoplado) | 🔄 En progreso (2/5 planes) — 16-02 ✅ (geocodeComunaCentroide() en lib/geocoding.ts, fallback de centroide de comuna con parámetros Nominatim estructurados city=/country=Chile + lib/cabida-comercial.ts con los tipos client-safe canónicos UbicacionCabida/IsocronaResultado/FormatoComercial y consultarCabidaComercial() fetch helper — ver 16-02-SUMMARY.md) 16-03 ✅ (migración cabida_comercial_cache — tabla angosta, índice único lat_r/lng_r/modo/minutos, RLS de solo lectura, 3 CHECK constraints — escrita y aplicada en vivo vía Supabase MCP, checkpoint humano confirmado — ver 16-03-SUMMARY.md) 16-01 🔒 BLOQUEADO (reintentado 2 veces, `ORS_API_KEY` configurada pero ORS/HeiGIT devuelve 403 "Access to this API has been disallowed" en ambos dominios y estilos de header, hipótesis de delay de propagación descartada tras horas sin cambio; cero código fabricado — `lib/isocrona-server.ts` no existe — requiere revisión manual del dashboard de HeiGIT o soporte antes de reintentar — ver 16-01-SUMMARY.md) |
| 17 | Demografía y Consumo | 🔄 En progreso (2/4 planes) — 17-01 ✅ (lib/censo-manzana-server.ts con obtenerPoblacionEnPoligono() puro por geometría, URL correcto services9.arcgis.com/kKJR3Qt68ohAWuet (49.974 manzanas RM) verificado en vivo contra el URL incorrecto de STACK.md/ARCHITECTURE.md (services3.arcgis.com/cTnMkBRk4HWkUCRo, 0 cobertura RM) + migración aditiva demografia_* aplicada en Supabase real — ver 17-01-SUMMARY.md) 17-02 ✅ (lib/consumo-macro-zona.ts: obtenerConsumoEstimado() puro, EPF con 3/12-13 categorías reales (INE IX EPF) + CASEN 2024 real para las 36 comunas RM con oportunidades activas — checkpoint de Task 2 resuelto por el orquestador con el XLSX oficial real del Observatorio Social tras fallo de DNS del executor — ver 17-02-SUMMARY.md) 17-03 🔒 BLOQUEADO (gateado por diseño sobre `lib/cabida-comercial-server.ts` de Phase 16/16-04, todavía inexistente — Task 1 lo verificó en vivo (`ls`/`grep` con evidencia literal) y detuvo la ejecución sin fabricar stub ni código parcial, cero archivos modificados, listo para reintentarse sin cambios apenas Phase 16 complete 16-04 — ver 17-03-SUMMARY.md) |
| 18 | Competencia por Formato | 🔄 En progreso (6/8 planes) — 18-01 ✅ (lib/cabida-comercial.ts extendido con CompetidorDetectado/ResultadoCompetenciaFormato/FuenteCompetidor/NivelConfianza + AnalisisCabidaComercial.competencia? opcional + guard de tipos contra colisión de nombres con lib/terrenos-comercial.ts — ver 18-01-SUMMARY.md) 18-02 ✅ (lib/overpass-competencia.ts, obtenerCompetidoresOverpass() con throttle 5s, verificado en vivo con 291 y 204 competidores reales en Providencia/Las Condes — ver 18-02-SUMMARY.md) 18-03 ✅ (lib/strip-power-centers-chile.ts, 25 filas RM del research SEED-STRIP-POWER-CENTERS-CHILE.md, 18 pre-geocodificadas reales vía script auditable, entrada nueva en data-sources.yaml documentando gap de Grupo Patio/Más Center — ver 18-03-SUMMARY.md) 18-04 ✅ (migración aditiva lat/lng/geocodificado_el en cadenas_sucursales aplicada en vivo + obtenerCadenasGeocodificadasPorComuna() con cache-through en lib/cadenas-sucursales-server.ts, verificado en vivo dos veces con coordenadas reales de Maipú — ver 18-04-SUMMARY.md) 18-05 ✅ (calcularResultadoCompetencia() pura en lib/competencia-formato.ts, TDD RED→GREEN→REFACTOR, tope duro de confianzaGlobal en 'media' y coberturaConocida siempre false verificados por test — ver 18-05-SUMMARY.md) 18-06 ✅ (obtenerCompetenciaPorFormato() orquestador async: Promise.all Overpass+SII geocoding, seed list para strip/power, cruce espacial turf.distance 150m sustituye tag OSM por nombre real de cadena — ver 18-06-SUMMARY.md) |
| 19 | Veredicto, Metodología, Mapa y Tab | 🔄 En progreso (2/4 planes) — 19-01 ✅ (calcularVeredictoCabida() pura en lib/veredicto-cabida.ts, banding por terciles p33/p66 pasados como parámetro, tope duro de confianza 'media', 14/14 tests — ver 19-01-SUMMARY.md) 19-02 ✅ (CabidaComercialMapa en components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa.tsx: clona zonificacion-mapa.tsx con polígono verde #16a34a (distinto del azul de zonificación) + pines rojos de competidores con popup nombre/distancia + fitBounds automático, acepta Polygon\|MultiPolygon, cero dependencia de lib/cabida-comercial-server.ts/isocrona-server.ts, checkpoint humano verificado en vivo por el orquestador vía Playwright contra fixture real de Providencia — ver 19-02-SUMMARY.md) 19-03 🔒 BLOQUEADO (gateado por diseño sobre un chequeo de TRES vías — tab de Phase 16/16-05, campo demografia de 17-03, campo competencia de 18-07 — los tres fallaron: `TAB NO EXISTE`, `lib/cabida-comercial-server.ts` no existe en absoluto todavía; Task 1 lo verificó en vivo con evidencia literal de comando y detuvo la ejecución sin fabricar `lib/veredicto-cabida-server.ts` ni migración, cero archivos modificados, listo para reintentarse sin cambios apenas Phase 16 (16-04+16-05) Y 17-03 Y 18-07 completen — ver 19-03-SUMMARY.md) — 19-04 (tab) gateado adicionalmente por 19-03, pendiente |
| 13 | Refactor de Scoring + Dashboard de Detalle | ✅ Completa (7/7 planes) — 13-01 ✅ (evaluarOportunidad() extraída y testeada en lib/mercado-locales-server.ts, fuente única de verdad de scoring — ver 13-01-SUMMARY.md) 13-02 ✅ (lib/formato-fecha.ts + streamConContexto() en lib/ai.ts sin tool web_search_preview + lib/resumen-oportunidad-prompts.ts — ver 13-02-SUMMARY.md) 13-03 ✅ (obtenerOportunidadPorId/obtenerComparablesOportunidad/obtenerHistorialPrecioListing + REASON_LABEL_DETALLE en lib/mercado-locales-server.ts — ver 13-03-SUMMARY.md) 13-04 ✅ (POST /api/oportunidades-resumen + ResumenTab, DETA-06 sin auto-disparo — ver 13-04-SUMMARY.md) 13-05 ✅ (PosicionamientoTab: banda de precio vs. cohorte + banner amber de advertencia + rentabilidad implícita de zona, DETA-02/DETA-07 — ver 13-05-SUMMARY.md) 13-06 ✅ (HistorialTab + ComparablesTab, DETA-03/DETA-04/DETA-05 — ver 13-06-SUMMARY.md) 13-07 ✅ (Server Component [id]/page.tsx integra las 4 tabs con datos reales + wiring de la lista, checkpoint humano end-to-end aprobado, DETA-01 a DETA-07 completos — ver 13-07-SUMMARY.md) |
| 14 | Comparación Lado a Lado | ✅ Completa (3/3 planes) — 14-01 ✅ (obtenerOportunidadesPorIds batched en lib/mercado-locales-server.ts — ver 14-01-SUMMARY.md) 14-02 ✅ (SelectorComparacion client island: checkbox + tope 5 + botón "Comparar (N)" + wireado en oportunidades/page.tsx — ver 14-02-SUMMARY.md) 14-03 ✅ (TablaComparacion + /oportunidades/comparar con validación server-side de rango/existencia/homogeneidad, checkpoint humano aprobado en vivo — COMPA-01 a COMPA-04 completos, ver 14-03-SUMMARY.md) |
| 15 | Informe Exportable | ✅ Completa (3/3 planes) — 15-01 ✅ (fix print:hidden/print:pl-0 global, formatTimestampCorto compartido, PrintButton/PreparadoPorPara/PortadaInforme/MetodologiaInforme en components/mercado-inmobiliario/informe/ — ver 15-01-SUMMARY.md). 15-02 ✅ (`/oportunidades/[id]/informe` + link "Exportar informe" en `[id]/page.tsx`, INFO-01/03/04 informe individual, checkpoint humano aprobado en vivo, commits `eab3286`/`c7132ff` — ver 15-02-SUMMARY.md). 15-03 ✅ (`/oportunidades/comparar/informe` con re-validación independiente de rango/existencia/homogeneidad — Pitfall 5 — + link "Exportar informe" en comparar/page.tsx, INFO-02/03/04 informe de comparación, checkpoint humano aprobado en vivo, commits `eab3286`/`9805c8b` — ver 15-03-SUMMARY.md). Milestone v1.6 (Reportes Profesionales de Oportunidades) completo — INFO-01 a INFO-04 verificados. |
| 10 | Motor de Zonificación | ✅ Completa — 10-01 ✅ (migración zonificacion_cache + proyectos.zona_* aplicada vía Supabase MCP) 10-02 ✅ (lib/zonificacion-comunas.ts, registro 4 comunas) 10-03 ✅ (lib/geocoding.ts, Nominatim geocoder) 10-04 ✅ (lib/zonificacion.ts + ruta GET /api/zonificacion/lookup, orquestación completa) 10-05 ✅ (lib/zonificacion-server.ts + after() en POST/PATCH /api/proyectos, persistencia automática de zona_* sin UI) |
| 11 | Vista de Zonificación en el Proyecto | ✅ Completa (8/8 planes) — 11-01 ✅ (types/index.ts zona_* + lib/zonificacion-format.ts + migración 20260730_zonificacion_v2.sql) 11-02 ✅ (lib/zonificacion-zonas.ts, GET /api/zonificacion/zonas) 11-03 ✅ (lib/zonificacion-compat.ts + POST /api/proyectos/[id]/compatibilidad) 11-04 ✅ (leaflet + CSP img-src + components/proyecto/zonificacion-mapa.tsx) 11-05 ✅ (lib/zonificacion-geo.ts + geometría real WGS84 + ?force=true vía upsert) 11-06 ✅ (GET-POST /api/proyectos/[id]/zonificacion + zona_codigo) 11-07 ✅ (ZonificacionCard + disclaimer, integrados en la página) 11-08 ✅ (fallback manual + compat checker + checkpoint humano verificado en vivo — ver 11-08-SUMMARY.md) |
| 12 | Integración con Motores de Decisión | ✅ Completa (4/4 planes) — 12-01 ✅ (ViaDecision alerta citada de incompatibilidad de uso — INTEG-01) 12-02 ✅ (FuenteHallazgo='PRC' en lib/due-diligence.ts + route ensanchado — INTEG-02) 12-03 ✅ (seccionZonificacion() inyectado en buildOgucPrompt/buildChecklistPrompt del copiloto — INTEG-03) 12-04 ✅ (tsc/vitest/git-diff limpios + checkpoint humano verificado en vivo por el orquestador — alerta real con destino_sii incompatible, no-regresión confirmada para sin-zonificación y caso Ñuñoa, Due Diligence y Copiloto sin errores — ver 12-04-SUMMARY.md) |
| 7 | Foundation | ✅ 07-01 service client, 07-02 checklist table, 07-03 Sheet component |
| 8 | Copiloto Core | ✅ 08-01 ✅ (API) 08-02 ✅ (UI: drawer, trigger, 4 tabs) 08-03 ✅ (page integration: permisos, patentes, proyectos/[id]) |
| 9 | Automatizaciones | ✅ 09-01 (DOM scraper idempotency + WA guard decoupled) 09-02 (after() SII enrichment on patente_comercial creation) 09-03 (AI tip in weekly email + schedule fix) |
| 6 | Dashboard Timeline View | ✅ app/(dashboard)/dashboard/page.tsx — Timeline View con 4 secciones |
| 1 | Stripe Billing | ✅ app/api/billing/{checkout,portal,webhook}, lib/stripe.ts, /configuracion/billing |
| 2 | Feature Gating | ✅ lib/plan-limits.ts, lib/usage.ts, upgrade prompt on /proyectos, API usage gate |
| 3 | Landing Page | ✅ app/(marketing)/page.tsx — hero + 6 features + 3 pricing tiers + toggle anual |
| 4 | Onboarding | ✅ app/(dashboard)/onboarding/page.tsx — wizard 3 pasos |
| 5 | PWA | ✅ public/manifest.json + install prompt component |

## Project Reference

See: .planning/PROJECT.md (updated 2026-08-02 al iniciar milestone v1.7)
**Core value:** El copiloto IA del arquitecto chileno — acelera y automatiza la tramitación de permisos DOM
**Current focus:** Milestone v1.7 (Cabida Comercial — Demografía y Consumo), Fase 16 (Ubicación e Isócrona) lista para planear.

## Accumulated Context

- Path contains accented char (`/Estefanía/`) — Turbopack panics on routes whose hash lands on the multi-byte boundary. Workaround: avoid dashes in route folder names (e.g. use `calculadora` not `calculadora-derechos`).
- Supabase middleware has `process.env.NODE_ENV === 'development'` bypass — auth is not enforced locally. Production (Vercel) uses `NODE_ENV=production` so auth IS enforced.
- `params` in dynamic routes is a Promise in Next.js 16 — use `React.use(params)` client, `await params` server.
- Dev server runs on port 7891 via `permisohub/package.json` start script with Turbopack.
- `ANTHROPIC_API_KEY` must be set in Vercel env vars by user — never hardcoded.
- Twilio WhatsApp: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_WHATSAPP_NUMBER` needed in Vercel.
- `GET /api/usage?metric=ai_chats|pdf_extractions` — returns {used, limit, plan} for current user. Used by Chat OGUC page to show "X/20 consultas este mes" badge. Returns 401 in dev (no session).
- Chat OGUC usage badge: only shows for plans with finite limits (Free/Starter). Pro/Estudio users see nothing. Badge turns amber at 80%, red at 100%.
- [v1.3] CRIT live bug RESOLVED (07-01): cron routes now use createServiceClient() from lib/supabase/service.ts with SUPABASE_SERVICE_ROLE_KEY — bypasses RLS. NEXT_PUBLIC_SUPABASE_ANON_KEY must be set in Vercel for crons to work. Commits: 30a88b2, 775e417.
- [v1.3] AI provider is OpenAI GPT-4o via `lib/ai.ts` — `@anthropic-ai/sdk` installed but dormant. Do NOT migrate provider during this milestone.
- [v1.3] Copiloto analysis uses `Promise.all` for 4 concurrent AI calls — set `export const maxDuration = 90` on the route segment to avoid Vercel timeout.
- [v1.3] `document_checklist_items` table live in Supabase (07-02, c0121e5) — FOUND-02 resolved. RLS policy checklist_items_own active. SKILL-04 (Phase 8) is unblocked.
- [v1.3] DOM write-back complete (09-01, f51dee4) — daily-check section 4: outer `isWhatsAppAvailable()` guard removed, `.neq('estado', estadoNuevo)` added for idempotency, `etapa_actual` field written on every estado transition. `results.domStatusChanges++` runs unconditionally.
- [v1.3] SII auto-enrichment live (09-02, 1f1ed83) — POST /api/proyectos: after() block fires on patente_comercial creation when numero_expediente present. Calls GET /api/sii/lookup?rol=..., writes superficie_terreno_m2, superficie_construida_m2, destino_sii via createServiceClient(). giro_sii NOT auto-populated (manual field). HTTP 200 returns immediately, enrichment is fire-and-forget.
- [v1.3] Weekly email (AUTO-04) sends to `ADMIN_EMAIL` only for MVP — external recipient opt-in blocked until unsubscribe flow exists (CAN-SPAM compliance).
- [v1.3] `SUPABASE_SERVICE_ROLE_KEY` must NOT have `NEXT_PUBLIC_` prefix — server-only secret.
- [v1.3] Sheet component live (07-03, d855f60) — `@/components/ui/sheet` exports Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetFooter, SheetClose, SheetTrigger. Primitive: `@base-ui/react/dialog` (base-nova style). No new npm deps. FOUND-03 resolved, SKILL-01 (Phase 8) unblocked.
- [v1.3] Copiloto API live (08-01, 2726f33) — POST /api/ai/copiloto runs 4 concurrent aiComplete calls via Promise.all with maxDuration=90. Checklist idempotency: queries document_checklist_items before AI, skips call and returns DB rows if count > 0. PATCH /api/ai/copiloto/checklist/[itemId] toggles estado. TIPO_PERMISO_TO_OBRA lookup map used for type coercion. stats?.tiempoPromedioHabiles (NOT plazoTipicoDias). await params pattern (Next.js 16). SKILL-02/03/04/05 (Phase 8) unblocked.
- [v1.3] Copiloto UI live (08-02, 18eaa75) — CopilotoDrawer: idle shows 4 skill cards, card-click→loading→loaded state machine, Map<string,CopilotoResult> cache by proyectoId. CopilotoTrigger: thin Bot-icon button. 4 tab components: TabOguc (articles+cumple), TabObservaciones (riesgoGlobal+predictions), TabChecklist (optimistic PATCH toggle, 'pendiente'|'ok' union), TabEstimacion (plazo+derechos CLP/UF). All interfaces exported from copiloto-drawer.tsx. No shadcn Tabs. tsc exits 0. SKILL-01 complete.
- [v1.3] Copiloto page integration live (08-03, a8576f7) — CopilotoTrigger per row in permisos + patentes list pages, CopilotoTrigger in PageHeader action div in proyectos/[id]. Shared CopilotoDrawer at page level (single instance). State pattern: copilotoProyecto (nullable Pick) + copilotoOpen (bool). Desarchivo covered by proyectos/[id] — no dedicated desarchivo list page exists. SKILL-01 fully operational across all views.
- [v1.3] Weekly email AI tip live (09-03, ef5972e + e99aeb9) — sendResumenSemanal accepts tipSemanal?: string, renders blue card (#EFF6FF border #BFDBFE) via escapeHtml. weekly-summary route generates tip with isAIAvailable() guard + try/catch fallback to ''. vercel.json weekly-summary schedule corrected to 0 11 * * 1 (08:00 Santiago UTC-3, was 12:00 UTC). Pattern: isAIAvailable() guard + try/catch = safe AI feature degradation.
- [v1.4] Roadmap decision (2026-07-30): 3 phases (10-12), not 4 as research suggested — merged research's Phase 1 (schema/registry/geocoder) + Phase 2 (adapter/lookup route/persistence) into a single Phase 10, since neither alone is user-observable and splitting them risked a horizontal-layers feel. Phase 10 has zero directly-mapped requirements (pure enabling infrastructure, verified via API/DB rather than UI) — ZONE-01 is owned by Phase 11 because the requirement text explicitly requires the architect to *see* the zone.
- [v1.4] No existing geocoder in the codebase — `lib/geocoding.ts` (Nominatim) is a hard Phase 10 prerequisite, not incidental.
- [v1.4] No PostGIS — zonificacion cache uses plain Supabase columns (lat/lng double precision + jsonb), matching the `proyectos_sii` convention. ArcGIS does the spatial math server-side (`returnGeometry=false`).
- [v1.4] Explicit 3-state pattern required everywhere: lookup status (`encontrado`/`sin_cobertura`/`error`) and compatibility check (`Permitido`/`No permitido`/`No especificado`) must never collapse to a boolean — this was the #1 pitfall theme in research (PITFALLS.md).
- [v1.4] New citation type needed: PRC/GIS data must NOT reuse `normativa-retrieval.ts`'s `verificado: true` badge (different trust axis) — needs its own `FuenteNormativa` value and disclaimer wording.
- [v1.4] Phase 12 (via-tramitacion.ts, due-diligence.ts, copiloto integration) is strictly additive — `recomendarVia()`'s deterministic core must never be modified; sequenced last so an immature zoning feature can't corrupt engines that already work.
- [v1.4] Map library selection (MapLibre vs Leaflet) is an open spike for Phase 11 planning — no mapping library exists in the codebase today, this is the one new frontend dependency in the milestone.
- [v1.4] Nominatim geocoder live (10-03, c134add) — `lib/geocoding.ts` exports `geocodeDireccion(direccion, comuna)`, server-side only (reuses `fetchWithTimeout` from `lib/scraper.ts`, custom Nominatim User-Agent). Live-verified: `lat`/`lon` return as strings (parsed via `parseFloat`), and `address.suburb` holds the real comuna while `address.city` collapses to "Santiago" — `comunaDetectada` reads `suburb` first, `city` only as fallback. In-module throttle (1.1s) respects Nominatim's 1 req/sec policy, no new dependency. Never throws — resolves `{ok:false, error}` on any failure. Comuna cross-check vs. requested comuna is deferred to Plan 10-04's caller (soft warning, not a gate).
- [v1.4] Zonificación comuna registry live (10-02, 380798f) — `lib/zonificacion-comunas.ts` exports `ZONIFICACION_COMUNAS` (4 verified entries: las-condes/providencia/vitacura `dedicada` lowercase fieldMap, nunoa `agregada` UPPERCASE fieldMap against shared `PrcCuencaMaipo` layer), `resolveComunaZonificacion(nombreOMunicipio)` (display-name or slug → entry or `null`, never an empty-but-truthy object), and `getComunasConCobertura()` for Phase 11's manual-fallback UI. Ñuñoa flagged `usosDisponibles: false` (UPERM/UPROH structurally empty in source, confirmed 0/200 filled) — must be disclosed, not inferred from nullability. Kept fully separate from `lib/comunas-chile.ts`, same small-deep-registry pattern as `lib/municipios-stats.ts`. Pure data + one pure function, no new dependency.
- [v1.4] Zonificación schema live (10-01, checkpoint closed 2026-07-30) — `zonificacion_cache` (17 cols, RLS on, `zonificacion_cache_read` policy) + `proyectos.zona_*` (9 cols) + `zona_status_check` CHECK constraint, all applied to Supabase project `nojejnebedjpbdlynrqs` via the Supabase MCP server's `apply_migration` (not the dashboard SQL Editor — MCP became available mid-phase). Supabase MCP is now configured at `user` scope in `~/.claude.json` (fresh Personal Access Token — the one reused from `permisohub`'s prior project-scoped config had expired) — reusable across sessions without reconfiguration going forward.
- [v1.4] Zonificación lookup route live (10-04, 687c6d9 + ec7c5ea) — `lib/zonificacion.ts` (client-safe `ZonaStatus`/`ZonaData`/`ZonaLookupResponse` types, `ArcGISQueryResponseSchema` Zod boundary validation, `lookupZonificacion()` fetch helper) + `GET /api/zonificacion/lookup` (registry short-circuit → `geocodeDireccion()` → `zonificacion_cache` read-through by `(comuna_id, lat_r, lng_r)` → ArcGIS point-in-polygon query with explicit `geometry=lng,lat&inSR=4326` → Zod validation → cache upsert). No auth check on the route (precedent: `app/api/utils/uf/route.ts` — public data, must be callable from session-less `after()` in Plan 10-05), rate-limited by IP via `checkRateLimit()`. Live-verified against all 4 covered comunas via curl on a local dev server: Las Condes/Providencia/Vitacura return `usosDisponibles:true`, Ñuñoa returns `usosDisponibles:false` with `uperm:null`/`uproh:null`; repeat query hits cache (`cacheHit:true`, identical `consultadoEl`); Temuco returns `sin_cobertura` with zero network calls. Empty ArcGIS `features[]` deliberately maps to `status:'error'`, never `'sin_cobertura'` or a false `'encontrado'`. Some ArcGIS text fields contain source-side mojibake (pre-existing upstream encoding, out of scope) — flagged for Phase 11 UI awareness.
- [v1.4] RESOLVED — Plan 11-01 Task 1 migration (2026-07-30): `supabase/migrations/20260730_zonificacion_v2.sql` (`zonificacion_cache.geometria` jsonb + `proyectos.zona_origen` text + CHECK) was created by the 11-01 executor but that subagent found `mcp__supabase__*` tools unbound in its own session (unlike the orchestrator's session, where the same MCP server has worked since Phase 10). The orchestrator applied it directly via `mcp__supabase__apply_migration` immediately after, verified live via `execute_sql` (columns + constraint confirmed). **Lesson for remaining Phase 11 waves: gsd-executor subagents spawned via the Agent tool do NOT reliably inherit the orchestrator's MCP tool bindings — any future plan requiring `mcp__supabase__apply_migration` (e.g. 11-06's zona_codigo follow-up migration) should be applied by the orchestrator directly, not delegated to the executor subagent, or at minimum the executor should be instructed to report back rather than dead-end if the tool is missing.**
- [v1.4] Zonificación auto-persistence live (10-05, 5d61f6c + ed60d54) — Phase 10 is now fully complete. `lib/zonificacion-server.ts` exports `persistZonificacionParaProyecto(proyectoId, direccion, municipio)`, the single server-only write path from the lookup route's response to `proyectos.zona_*`, writing an explicit `zona_status` on every branch (`encontrado`/`sin_cobertura`/`error`) including the outer catch — no code path leaves a row silently stuck at `pendiente`. Wired via `after()` into both `POST /api/proyectos` (fires on creation when `direccion`+`municipio` present) and `PATCH /api/proyectos/[id]` (re-reads the current row and re-fires when either field changes, since a PATCH may touch only one). Shared helper deliberately used at both call sites instead of duplicating logic, to prevent create/update drift (the exact failure shape of PITFALLS.md Pitfall 6). One auto-fixed bug during execution: the plan's own doc-comment snippet contained a literal `*/` that prematurely closed the JSDoc block — reworded, not a logic change. `npx tsc --noEmit` and `npx eslint` clean on all 3 touched files; `GET /api/zonificacion/lookup` re-curled directly to confirm no regression (Las Condes, cache hit). **Verification gap:** full end-to-end POST/PATCH-through-HTTP-with-a-real-session was not exercised in this run — `GET /api/proyectos` returns `{"error":"No autenticado"}` on localhost even in dev (this repo's routes check `supabase.auth.getUser()` directly, not just the dev middleware bypass), so no session was available to drive the authenticated create/update flow. Recommend one manual UI smoke test (create + edit a project's address while logged in, confirm `zona_status`/`zona_nombre` populate within seconds) before Phase 11 relies on this data being fresh.
- [v1.4] Zonificación manual-fallback data source live (11-02, 54e05e3 + 983db08) — `lib/zonificacion-zonas.ts` exports `fetchZonasDisponibles(comunaId)` (ArcGIS `returnDistinctValues=true&returnGeometry=false` query, no point/geocode needed) and `fetchZonaDetalle(comunaId, zona)` (single-zone verbatim usos), both read-only against the Phase 10 registry (`lib/zonificacion-comunas.ts`), returning `null` never throwing. `GET /api/zonificacion/zonas` is public/unauthenticated/IP-rate-limited (third route on this pattern, after `/api/zonificacion/lookup` and `/api/utils/uf`): no `?comuna=` lists the 4 covered comunas, `?comuna=<x>` returns its live zone list or 404 with zero ArcGIS calls if uncovered. Live-verified via curl: Las Condes returned 71 real distinct zone codes (some names carry pre-existing upstream mojibake, e.g. "Ãreas Verdes" — same source-side encoding issue already flagged in 10-04, out of scope, now a UI-rendering awareness note for whichever plan displays these). `fetchZonaDetalle` is exported but intentionally not yet wired into any route — reserved for Plan 11-08's two-step picker. This plan is additive-only: does not modify `lib/zonificacion-comunas.ts` or `/api/zonificacion/lookup`.
- [v1.4] COMPAT-01 backend live (11-03, 8578820 + 3623dfe) — `lib/zonificacion-compat.ts` exports `verificarCompatibilidadUso(usoPretendido, uperm, uproh, usosDisponibles)`: hard deterministic short-circuit to `no_especificado` BEFORE any AI call whenever `usosDisponibles` is false or both `uperm`/`uproh` are empty/whitespace — reuses `lib/ai.ts`'s existing `aiComplete({json:true})` pattern (same as due-diligence/predictor/oguc-chat), gates the model's raw `estado` field through `CompatEstadoSchema.safeParse()` before trusting it, never throws (every failure path — no data, AI unavailable, JSON.parse failure, network error — resolves to `no_especificado` with a `justificacion`). `POST /api/proyectos/[id]/compatibilidad` exposes it: `ownedProject()` auth+ownership pattern copied from `via-tramitacion/route.ts` (401 no-auth, 404 non-owner), rate-limited, 400 on missing `usoPretendido`, deliberately does NOT persist the result (uso pretendido is transient free text, re-checked each time, not a fixed project attribute). Live-verified directly against real `zonificacion_cache` rows (service role) — Las Condes/Providencia return valid AI-classified 3-state JSON, Ñuñoa short-circuits at 0ms with zero OpenAI calls. Same environment limitation as 10-05/11-01: no authenticated session available in this run to curl the full HTTP path end-to-end against a real owned project (both existing local projects are still `zona_status: 'pendiente'`) — 401-unauthenticated path was curl-verified directly; recommend covering the authenticated path in the same future manual smoke test already flagged for 10-05.
- [v1.4] Zone polygon geometry + force-refresh live (11-05, d199611 + cb999fd) — `lib/zonificacion-geo.ts` exports `esriRingsToGeoJSON()`, a deliberately narrow (single-ring-only, no donuts/curves) Esri→GeoJSON Polygon converter matching what the 4 registered PRC layers actually return. `lib/zonificacion.ts`'s `ZonaData` gained `cacheId: string` (real `zonificacion_cache` row uuid on every `encontrado` response; `''` only on the one edge case where the cache write itself fails — downstream callers must treat `''` as falsy/no-id). `GET /api/zonificacion/lookup` now sets `returnGeometry=true` + explicit `outSR=4326` (outSR is a DIFFERENT param from inSR — omitting it silently returns Web Mercator coordinates unusable by Leaflet), accepts `?force=true` to bypass the cache read-through, and switched cache writes from `.insert()` to `.upsert(onConflict: 'comuna_id,lat_r,lng_r')` unconditionally (safe for both cache-miss and forced-refresh paths, one fewer branch than insert-vs-upsert). Live-verified end-to-end against a real dev server (port 3001, not 7891 this run — 3000 was occupied by another process and 7891 isn't hardcoded anywhere in `package.json`), real Nominatim/ArcGIS/Supabase: `cacheId` real UUID, `force=true` on an already-cached address succeeds (no unique-constraint throw, refreshed `consultadoEl`), persisted `geometria` confirmed correct WGS84 Polygon (-70.x/-33.x range). Same MCP-tool-binding gap as 11-01 recurred (no `mcp__supabase__*` tools in this executor session) but did not block anything — no migration was needed (schema already live from 11-01), and the one DB-inspection verification step was done via a Node script using `@supabase/supabase-js` + the service-role key from `.env.local` instead of MCP. **Also confirmed:** `state advance-plan`/`update-progress`/`add-decision`/`record-metric` gsd-tools commands all fail silently against this project's STATE.md — it uses a prose format, not the `**Field:**` key-value format those commands expect (Progress bar has been stuck at 45% since 10-05 for this exact reason — 11-01 through 11-04 hit the same wall and updated STATE.md by hand instead). Continuing manual-edit convention; Progress bar left at 45% for consistency, not attempted to "fix" globally since that's outside a single plan's scope.
- [v1.4] Proyecto-scoped zonificación route + force-refresh persistence live (11-06, 2777d8d + abc3d6f) — `lib/zonificacion-server.ts`'s `persistZonificacionParaProyecto` gains an optional 4th `{ force }` param (threaded to `/api/zonificacion/lookup?force=true`), and its `'encontrado'` branch now writes `zona_origen:'automatico'`, a real `zona_cache_id` (from Plan 11-05's `ZonaData.cacheId`), and `zona_codigo` (from the 11-06 Task 0 migration, `20260731_zonificacion_codigo.sql`) — both existing Phase 10 `after()` call sites (`proyectos/route.ts`, `proyectos/[id]/route.ts`) untouched, still pass exactly 3 args. New `GET/POST /api/proyectos/[id]/zonificacion`: `GET` serves polygon geometry lazily via a `zona_cache_id` join to `zonificacion_cache` (never embedded in `GET /api/proyectos/[id]`'s fast payload), degrading gracefully to `geometria:null`; `POST` covers ZONE-04 ("Actualizar" forced refresh, reuses `persistZonificacionParaProyecto(..., {force:true})`, never a raw client-side `lookupZonificacion()` call per `11-RESEARCH.md`'s explicit rejection) and ZONE-05 (manual comuna/zona fallback via `fetchZonaDetalle`, always `zona_origen:'manual'` + `zona_cache_id:null`). Both auth+ownership gated (401/404), rate-limited. **Live-discovered schema drift, logged not fixed (deferred-items.md):** `supabase/migrations/20260705_proyectos_sii.sql` (adds `proyectos.rol_sii`/`destino_sii`/`avaluo_fiscal_clp`/`superficie_terreno_m2`/`superficie_construida_m2`/`lat`/`lng`) was never applied to the live Supabase project despite existing in the repo and being referenced by live SII-enrichment code (09-02) and several AI routes — selecting `lat`/`lng` in this route's `ownedProject()` threw Postgres `42703` and silently turned every legitimate request into a false 404 until fixed by dropping those two columns from the select (GET's lat/lng now sourced only from `zonificacion_cache.lat_r`/`lng_r`). **Recommend orchestrator apply `20260705_proyectos_sii.sql` via `mcp__supabase__apply_migration`** (idempotent) before/alongside 11-07, and smoke-test that SII enrichment actually persists those fields once applied. Same recurring MCP-tool-binding gap as 11-01/11-05 (no `mcp__supabase__*` in this executor session) — worked around via a Node script using `@supabase/supabase-js` + service-role key, same as 11-05. Live-verified end-to-end via a real dev server on port 7891 + real Supabase (unauthenticated 401 via curl; GET/POST query shapes verified via service-role script simulating the exact route queries — no authenticated browser session available in this environment, same standing gap as 11-01/11-03/11-05); two live project rows used for verification were reverted to their original `pendiente` state immediately after.
- [v1.4] ZonificacionMapa component live (11-04, 35acfdb + d7e250e) — `leaflet@^1.9.4` + `@types/leaflet@^1.9.21` installed (the milestone's one new frontend dependency; `react-leaflet` deliberately NOT installed per `11-RESEARCH.md`'s Stack section — a plain `useEffect`+`useRef` mount suffices for one static non-interactive map). CSP `img-src` in `next.config.ts` extended with `https://*.tile.openstreetmap.org` only (no `connect-src`/`worker-src` change — raster `<img>` tiles, no Web Worker). `components/proyecto/zonificacion-mapa.tsx` exports `ZonificacionMapa` (purely presentational, `{ lat, lng, geometria }` props, zero internal fetch, zero dependency on any other Phase 11 route) + `ZonaPolygon` type: dynamic `import("leaflet")` inside `useEffect` (avoids SSR `window`/`document` crash), custom `L.divIcon` marker (sidesteps Leaflet's default-PNG-icon bundler-path breakage), graceful degrade to a placeholder when `lat`/`lng` are null and to marker-only-plus-note when `geometria` is null, `fitBounds` on the GeoJSON polygon when present. Live-verified with a real headless browser (Playwright, launched against an already-cached Chromium build since the expected `chromium_headless_shell-1228` wasn't installed) on a throwaway route (removed before finalizing, along with the stale `.next/dev/types` entry it left behind): zero console errors, all OSM tile requests returned HTTP 200 (no CSP block, confirmed via Network events not just visual), both marker and polygon SVG path rendered. Ready for Plan 11-07 to import with real `GET /api/proyectos/[id]/zonificacion` data with zero further map-library work.

- [v1.4] ZonificacionCard automatic-path UI live (11-07, 2142d9a + b994878) — `components/proyecto/zonificacion-disclaimer.tsx` (static, always-rendered, exact ZONE-06 string) + `components/proyecto/zonificacion-card.tsx` (`ZonificacionCard`, 4-state UI over `proyecto.zona_status`: `pendiente`/`sin_cobertura`/`error`/`encontrado`, renders `zona_codigo` + `zona_nombre` + `zona_sector` directly from the already-loaded `proyecto` payload with zero extra fetch, embeds `ZonificacionMapa` fed by a *separate* lazy `GET /api/proyectos/[id]/zonificacion` fetch for the heavier polygon geometry, `fixMojibakeArcGIS()` applied to `zona_nombre`/`zona_uperm`/`zona_uproh` before render, citation block with explicit "sin link directo" text when `zona_fuente_url` is null, `handleActualizar()` round-trips the Plan 11-06 `POST` route then refetches `/api/proyectos/[id]` and calls `onUpdated`). Wired into `app/(dashboard)/proyectos/[id]/page.tsx`'s Resumen tab right column, directly below the existing `PredioMap` card, same `proyecto.direccion` conditional gate, `onUpdated` bound to the page's existing `setProyecto`. **Followed both plan-checker fixes exactly as pre-written into the plan:** `zona_codigo` rendered alongside nombre (not just nombre/sector), and the polygon-fetch `useEffect` depends on `[proyecto.id, proyecto.zona_consultada_el]` rather than `[proyecto.id]` alone — since `proyecto.id` never changes but `zona_consultada_el` is bumped server-side by every successful auto/manual lookup (including the "Actualizar" POST), this is what makes the map visibly refresh after a forced update instead of staying stuck on the first-mount polygon. `npx tsc --noEmit` and `npx eslint` clean on all 3 touched files (only pre-existing unrelated warnings remain on the page file). No `mcp__supabase__*` or schema work needed this plan. **Same recurring environment gap as 11-01/03/05/06:** no authenticated browser session available to click through the full manual flow (open a covered-comuna project, click Actualizar, watch the map visibly refresh) — compensated with a real dev server request to `/proyectos/[uuid]` confirming the page (including `ZonificacionCard`'s import graph and `ZonificacionMapa`'s dynamic `leaflet` import) compiles and renders with HTTP 200 and no crash/error-page markers. Recommend the next authenticated manual smoke test also cover this map-refresh-after-Actualizar behavior specifically, since it was the exact bug the plan-checker caught before this plan ran.

- [v1.4] Phase 11 COMPLETE (11-08, both tasks, 2026-07-30) — `components/proyecto/zonificacion-manual-fallback.tsx` + `components/proyecto/uso-compatible-check.tsx` live, wired into `zonificacion-card.tsx` (commit 6166d74). Task 2's human-verify checkpoint was resolved by the ORCHESTRATOR using a real browser (Playwright) against a real `npm run dev` session — all 6 steps passed with real ArcGIS/OpenAI data. See `11-08-SUMMARY.md` for full detail. **New codebase gotcha surfaced:** this repo's `Select` (`components/ui/select.tsx`, base-ui primitive) types `onValueChange` as `(value: string | null, eventDetails: SelectRootChangeEventDetails) => void` — passing a bare `useState` setter directly fails `tsc` (`TS2322`, null not assignable). Always wrap as `onValueChange={(v) => setX(v as string)}` — same pattern already used in `app/(dashboard)/proyectos/nuevo/page.tsx`.
- [v1.4] **IMPORTANT — this project HAS a working dev-auth bypass, resolving the "no authenticated session" gap every Phase 10/11 executor hit** (10-05, 11-01, 11-03, 11-05, 11-06, 11-07, 11-08 all separately flagged this as an unresolvable limitation): `.env.local` has `BYPASS_AUTH=true`, and `app/auth/dev-login/route.ts` (gated by that flag + `NODE_ENV!=='production'`, never active in prod) establishes a REAL Supabase session for `tomas@aiaiai.cl` via `admin.generateLink({type:'magiclink'})` + `verifyOtp` — no password needed. Navigate to `/auth/dev-login?next=/wherever` (or let middleware redirect there from any protected page) to get a real authenticated session for manual/Playwright-driven testing. **Future plans/executors: use this instead of reporting "no browser session available" as a blocker** — it was available the whole time, just not discovered until Phase 11's final checkpoint. `tests/e2e/expediente.smoke.spec.ts` already relies on this same mechanism.
- [v1.4] Two minor, non-blocking bugs found during 11-08's live checkpoint verification (Playwright, real data), deferred as polish (not fixed, don't affect any of Phase 11's 5 success criteria): (1) a subset of Las Condes zone *names* (not codes) in the manual-fallback list have mojibake that survived `fixMojibakeArcGIS()` because the upstream ArcGIS/OCUC source text was corrupted through TWO rounds of UTF-8-as-Latin1 misinterpretation, not one — the existing single-pass repair can't safely reverse it (confirmed: a 2nd pass fails to decode as valid UTF-8) without risking corrupting correctly-encoded text elsewhere; (2) `zonificacion-card.tsx`'s "Fuente: capa oficial {municipio}" fallback text uses `proyecto.municipio` (correct for the automatic/geocoded path) but is WRONG for a manually-selected zone in a different comuna than the project's registered municipio (no field currently threads the manually-picked `comunaId` back to the card for display — `zona_sector` holds a sub-sector name like "El Bosque", not the comuna). Both are cosmetic/label-accuracy issues in the ZONE-05 manual-fallback secondary path only; `uperm`/`uproh` legal text is always correct for whichever zone was actually selected.
- [v1.4] Also fixed during Phase 11 (unrelated pre-existing bug, discovered by 11-06's live verification): `supabase/migrations/20260705_proyectos_sii.sql` (proyectos.rol_sii/destino_sii/avaluo_fiscal_clp/superficie_terreno_m2/superficie_construida_m2/lat/lng) was committed 2026-07-05 but NEVER applied to the live Supabase project — Phase 9's SII-enrichment `after()` had been silently failing (`42703`) on every write to these columns since it shipped. Applied live by the orchestrator via `mcp__supabase__apply_migration`, verified. Full end-to-end SII-enrichment behavioral smoke test (create a `patente_comercial` project with `numero_expediente`, confirm `after()` persists the fields) NOT performed — schema-level fix confirmed, behavioral follow-up recommended but not blocking.
- [v1.4] INTEG-02 live (12-02, e1e563d + ca3302e) — `lib/due-diligence.ts` gains `FuenteHallazgo = FuenteNormativa | 'PRC'` (deliberately a LOCAL union type, NOT an extension of `FuenteNormativa` in `lib/normativa-retrieval.ts` — that would force `getArticuloById`/`getContextoNormativo`/`flagUnverifiedCita` to handle a source they structurally can't resolve, per this phase's research and Phase 11's prior decision), `ProyectoContexto` extended with `destino_sii` + the 7 `zona_*` fields, `seccionZonaProyecto()` injects a "## Zonificación (PRC) del proyecto" block into `buildSynthesisPrompt()` guarded by the same compound `zona_status==='encontrado' && zona_usos_disponibles===true` pattern used across Phase 11/12 (never `zona_status` alone), with `fixMojibakeArcGIS()` applied to `zona_uperm`/`zona_uproh`/`zona_nombre` before injection. `resolverRefNormativa()` now takes `proyecto` as a 2nd arg and branches on `fuenteRaw==='PRC'` BEFORE the existing `FUENTES_VALIDAS` check — builds a `verificado:true` citation directly from `zona_codigo`/`zona_nombre`/`zona_fuente_url` when the zone is usable, silently drops the citation via `continue` when it isn't (never a half-built citation); the OGUC/LGUC/DDU branch is behaviorally unchanged, only reordered. `app/api/ai/due-diligence/route.ts`'s `ProyectoRow` interface + the `proyectoContexto` literal in `procesar()` threaded with the same 8 fields in lockstep (the route's `select('*')` already returned this data — only the TS narrowing was missing). `components/proyecto/due-diligence-report.tsx` required zero changes (confirmed via empty `git diff --stat`) since `CitaBadges` is already generic over `fuente`. `npx tsc --noEmit`/`eslint` clean on both touched files (the one pre-existing unrelated `pmo-panel.tsx` error belongs to the concurrent 12-01 plan, out of scope). No live/manual smoke test of the PRC citation path was run this session (would require an authenticated project with a usable zone + a document set that actually triggers a destino/uso mismatch through the AI synthesis) — recommend covering in the phase's eventual checkpoint.
- [v1.4] INTEG-03 live (12-03, b3f5c13) — `app/api/ai/copiloto/route.ts` gains `seccionZonificacion(p: Proyecto): string`, guarded by the same compound `zona_status === 'encontrado' && zona_usos_disponibles === true` pattern used throughout Phase 11 (never `zona_status` alone — Ñuñoa has `encontrado` but `usos_disponibles:false` and must get no section), applying `fixMojibakeArcGIS()` to `zona_uperm`/`zona_uproh`/`zona_nombre` before injecting into the prompt. Wired into `buildOgucPrompt()` and `buildChecklistPrompt()` only — `buildObservacionesPrompt()`/`buildEstimacionPrompt()` deliberately untouched (literal requirement scope, per `12-RESEARCH.md`'s Open Question 1). No route/query/type changes needed — this route already did `select('*')` and cast to `Proyecto`, unlike due-diligence's harder case in the same phase. `npx tsc --noEmit`/`eslint` clean on the touched file (an unrelated pre-existing `tsc` error in `components/proyecto/pmo-panel.tsx` from the concurrent 12-01 plan's in-progress work was left alone, out of scope). **Process note for future parallel Wave executions:** this repo has no git-worktree isolation between concurrently-running plan executors sharing one working directory/index — a bare `git commit` (no pathspec) briefly swept up plan 12-01's staged-but-uncommitted `via-decision.tsx` changes; caught immediately via `git show --stat HEAD` and fixed with `git reset --soft HEAD~1` + unstage + recommit using an explicit file pathspec. Recommend all executors always commit with an explicit pathspec, never a bare `git commit`, when running in a Wave with sibling plans.
- [v1.5] **MILESTONE v1.5 (Fusión PROPRA·BI) SHIPPED 2026-08-01** — 17 commits directo a `main`, sin feature branch, sin fases GSD. Resumen completo en `.planning/MILESTONES.md` § v1.5; puntos clave: PROPRA·BI absorbido como módulo "Mercado Inmobiliario" (8 fases), módulo Terrenos (5 scrapers, construido pero sin cron propio), instrumentos IPT, checklist dinámico (9 formularios MINVU reales), fusión de `permisohub-enterprise`, copiloto conversacional (primer uso de function/tool-calling OpenAI en el repo), rediseño UX/IA separando módulos Permisos/Mercado Inmobiliario, fix transversal de un bug de contraste en modo oscuro (`bg-white` hardcodeado en ~35 archivos). Deuda técnica real encontrada en la auditoría posterior (no bloqueante, catalogada en `.planning/data-sources.yaml`): 2 duplicados del scraper SII, 4 puntos de entrada al dato de UF, 3 listas de comunas no sincronizadas, un pipeline RAG construido pero nunca cableado, 5 scrapers de terrenos sin ingesta programada.
- [torre-control] **Torre de Control iniciado 2026-08-01** (rama `feature/torre-de-control`, no fusionada) — gobernanza de datos y decisiones, motivada directamente por el hallazgo de arriba (17 commits invisibles a este sistema). Checkpoint A ✓ (`data-sources.yaml` con 32 fuentes catalogadas + `scripts/check-data-sources.mjs`, validador determinista de 4 chequeos wireado en CI no-bloqueante + `reportError`/`reportWarning` reemplazando `console.warn` en los 7 scrapers de `lib/scrapers/*` que nunca reportaban a Sentry). Checkpoint B ✓ (tabla `data_source_runs` — migración `20260807_data_source_runs.sql` aplicada en Supabase vía `mcp__supabase__apply_migration` — + `recordSourceRun()` en `lib/observability.ts` + página `/admin/salud-datos`, wireado en mercado-locales, instrumentos-ipt, terrenos (5 fuentes) y cadenas-sucursales. **Corrección (2026-08-01, sesión posterior): esta nota decía "pendiente de aplicar" por error — verificado en vivo vía `mcp__supabase__list_tables` que la tabla existe, tiene RLS y ya acumula 13 filas reales. La nota quedó desactualizada tras aplicarse y no se corrigió al digerir — lección: verificar contra la DB real antes de repetir un pendiente heredado de un resumen de sesión.**). Checkpoint C ✓ (puesta al día de `.planning/`, git hook `.githooks/post-commit` de captura automática — verificado en vivo, disparó correctamente en los commits `89a8444` y `6125fff` — comando project-local `.claude/commands/torre-control.md`). Hallazgo adicional durante el diseño: los propios comandos de gobernanza previos de la founder (`~/.claude/commands/propra-analisis.md`, `propra-gap-audit.md`, `propra-tasacion.md`, `~/.claude/skills/propra-pipeline-health/`) ya estaban obsoletos, apuntando a los repos standalone ya fusionados — archivados en `~/.claude/commands/_archived-propra/` y `~/.claude/skills/_archived-propra-pipeline-health/` — la evidencia más directa de que este es un patrón recurrente, no un incidente aislado. **Piloto §6 ✓** (`6125fff`) — `lib/tasacion-prompts.ts` gana una sección "## 🎯 Resumen Ejecutivo" obligatoria y siempre primera (precio recomendado, uso óptimo, "Confianza del análisis" Alta/Media/Baja justificada contra criterios explícitos, no una sensación del modelo); `components/mercado-inmobiliario/informe-ejecutivo.tsx` la extrae y la promueve visualmente con badges honestos de trazabilidad de fuente (avalúo SII disponible/no, búsqueda web), degradando con gracia al markdown completo si la sección no aparece. Verificado en vivo con una llamada real a OpenAI (Playwright, dirección real "Av. Apoquindo 4700, Las Condes"): resumen ejecutivo renderizó arriba, confianza "Baja" correctamente justificada por falta de comparables/Rol SII, 0 errores de consola. No extendido a Predictor/Auditor/Due Diligence todavía — piloto en una sola herramienta antes de generalizar. **Torre de Control (checkpoints A-C + piloto) completo, 4 commits en `feature/torre-de-control`, mergeado a `main` checkpoint por checkpoint el mismo día.**
- [expansion-cadenas] **Expansión de Cadenas vía SII (2026-08-01, rama `feature/expansion-cadenas-sii`, commit `3268b20`)** — investigación de mercado (2 agentes en paralelo) descartó backtest de precios reales (Chile no tiene fuente pública de transacciones — CBR no digitalizado, avalúo SII ≠ precio de venta, índice del Banco Central excluye propiedad comercial; el único vendor real, TocToc, requiere venta enterprise) y descartó patente comercial municipal (fragmentada en ~345 sistemas). Hallazgo ganador: la nómina nacional de direcciones del SII (`PUB_NOM_DIRECCIONES.zip`, ~111MB, actualizada mensualmente) — verificada en vivo descargándola y parseándola directamente, no confiando en la documentación de la página. `lib/scrapers/sii-nomina-sucursales.ts` + `lib/cadenas-sucursales-server.ts` + `supabase/migrations/20260808_cadenas_sucursales.sql` + panel `/mercado-inmobiliario/cadenas`. RUTs de Falabella y Cencosud Shopping verificados contra la nómina de razones sociales del SII; Walmart Chile y SMU marcados `needs-decision` en `data-sources.yaml` — su RUT público de holding solo trae oficinas corporativas (13 y 2 direcciones respectivamente), no las tiendas reales, que operan bajo RUTs de subsidiarias aún no identificados. **Dos hallazgos reales solo visibles al correrlo contra datos de producción** (no en teoría): (1) un bug de Postgres — "ON CONFLICT DO UPDATE cannot affect row a second time" — porque el SII trae más de una fila para la misma dirección cuando difieren en un campo fuera de la clave natural, corregido deduplicando antes del upsert; (2) el flag `vigente` del SII es un estado administrativo-tributario, no confirmación de tienda abierta — de 91 direcciones históricas de Falabella S.A., solo 3 están vigentes hoy. Documentado explícitamente en la página y en el registro en vez de presentar el conteo como más preciso de lo que es. Verificado: 144 direcciones encontradas, 111 únicas tras dedup, segunda corrida idempotente (0 nuevas), 0 errores de consola. También se restauró una corrupción real y no relacionada del servidor de desarrollo local (causada por `rm -rf .next` repetido mientras el servidor persistente seguía corriendo, colateral de la verificación de Torre de Control) — reiniciado limpio en el puerto 7891. **Continuación (`b11d1c9`, 2026-08-01):** cerrado el loop entre este dato y la toma de decisión real — `obtenerSenalesExpansionPorComuna()` en `lib/cadenas-sucursales-server.ts` cruza (normalizando nombre de comuna vía `normalizarNombreComuna()`, reutilizada de `instrumentos-ipt.ts`) las comunas presentes en `/mercado-inmobiliario/oportunidades` contra `cadenas_sucursales`, agregando un badge dorado ("Cencosud Shopping tiene sucursal en la comuna (registrada oct 2018)") a cada oportunidad cuando aplica. Verificado en vivo: badge visible en la mayoría de las tarjetas, 0 errores de consola.

- [informe-ejecutivo] **Rollout de InformeEjecutivo — extendido a Due Diligence, luego cerrado (2026-08-01, `b251dfa` + `222248b`)** — segunda herramienta en adoptar el patrón tras el piloto en Tasación: el antiguo "## 🚦 Semáforo General" de `lib/due-diligence-propiedad-prompts.ts` se fusionó en "## 🎯 Resumen Ejecutivo" (mismo veredicto APTO/CONDICIONADO/OBSERVADO como primera línea, para no romper el regex de `handleDescargarChecklist()`), agregando confianza justificada por criterios explícitos (dominio individual + CBR al día + posesión efectiva vs. datos "No informado") y trazabilidad honesta (avalúo SII, posesión efectiva/CBR declaradas, búsqueda web). Verificado en vivo (Playwright, dirección real): resumen ejecutivo con confianza "Alta" renderizado, checklist de documentos sin regresión, 0 errores. **Antes de seguir generalizando, se auditaron las 4 herramientas restantes nombradas en el plan original (Predictor, Auditor, Compliance-check, Reportes de Mercado) más Memoria Descriptiva — decisión explícita de NO extender:** las 4 primeras ya devuelven JSON estructurado con UI a medida que expresa el mismo principio (bottom-line-up-front + verificación) en su propio idioma — score circular+veredicto en Auditor, riesgo global+mes óptimo en Predictor, badge "Dato verificado"/"Estimado" por KPI en Reportes — retrofitear el componente genérico ahí sería downgrade visual, no mejora. Memoria Descriptiva es un documento formal (texto para la DOM), no un informe de decisión con veredicto — el framing no aplica a su género. Decisión registrada en Key Decisions de `PROJECT.md`. **Rollout de InformeEjecutivo considerado completo: 2/2 candidatos reales (Tasación, Due Diligence).**

- [decisiones-founder] **3 decisiones pendientes de founder resueltas (2026-08-01, `86ce868`)** — TocToc: pendiente, founder pidió más información (pricing/cobertura) antes de comprometerse, no descartado. Unimarc: founder decidió NO seguir invirtiendo tiempo de agente en encontrar su RUT operativo — cobertura parcial de SMU (Alvi + Super10) aceptada, ver `sii-nomina-sucursales-holdings-sin-tiendas` en `data-sources.yaml` (status cambiado de `needs-decision` a `active`, 7→6 needs-decision). LGUC/OGUC/DDU: founder eligió cadencia fija trimestral en vez de decidir ad-hoc — las 3 bases ahora tienen `freshness_sla_days: 90`, el validador (`check-data-sources.mjs`) avisará solo cuando venza. Las 3 registradas en Key Decisions de `PROJECT.md`. Founder confirmó además una política general (no específica de TocToc): no pagar por ninguna aplicación/dato de terceros por ahora — aplica a cualquier vendor pago que surja en la investigación de mercado que sigue.
- [ine-permisos-edificacion] **INE — permisos de edificación históricos integrados (2026-08-01, `d6fc2b4`)** — primer hallazgo construido de la investigación de mercado pedida por la founder ("busca qué hay en el mercado... sigamos buscando fuentes"), que corrió 2 agentes en paralelo (modelos de negocio proptech por segmento inversionista/rentista/administrador/fondo, y fuentes de datos gratuitas nuevas). Del segundo agente: el servicio ArcGIS oficial del INE (cuenta `publicaciones_geodatos`, la misma que publica los Censos) expone permisos de edificación agregables por comuna/año/uso_destino, verificado con peticiones HTTP reales, no solo documentación. **Due diligence de licencia que casi se pasa por alto**: el primer candidato encontrado (mismo dato, republicado por el Observatorio de Ciudades UC) está bajo CC BY-NC 4.0 — no comercial, inservible para un SaaS pagado; el servicio oficial del INE es CC BY-SA 4.0 (uso comercial permitido, solo exige atribución "Fuente: INE"). Cobertura real 2010-2022, 121 comunas (capitales regionales/provinciales + >50.000 hab., no las 346) — HISTÓRICO, nunca presentar como actividad de hoy. Wireado en Reportes de Mercado: `zona.tendencia` (antes 100% adivinada) ahora se basa en la variación real de superficie no-habitacional/mixta de los últimos 3 años vs. los 3 anteriores, citando "INE" en fuentes. **Dos bugs reales encontrados y corregidos verificando en vivo, no en teoría**: (1) el `maxRecordCount` de 2000 de ArcGIS truncaba las 4.243 combinaciones agregadas reales a solo 2000 — corregido paginando con `resultOffset`. (2) el cap server-side de PostgREST (max-rows) NO se puede levantar con un `.limit()` del cliente — un `.limit(5000)` seguía devolviendo solo 1000 filas, truncando comunas alfabéticamente tardías como Providencia (justo la comuna usada para verificar); corregido agregando una columna `comuna_normalizada` indexada en la ingesta en vez de traer todo y filtrar en cliente. Verificado en vivo end-to-end (Playwright, llamada real a OpenAI): KPI "Tendencia 12M — Estable — INE histórica, -1%" y "INE — Datos de actividad histórica" citado en fuentes, 0 errores de consola. Sin cron propio — dato histórico que no cambia seguido, re-correr manualmente si INE publica un año nuevo. **Continuación (mismo día, badge en Oportunidades):** `obtenerTendenciasConstruccionPorComuna()` (versión en lote, misma columna `comuna_normalizada`) agregada a `/mercado-inmobiliario/oportunidades` — badge "Actividad constructiva histórica en alza (INE, +X%)" solo cuando la tendencia es `creciente` (nunca estable/decreciente, sería ruido en una lista de oportunidades). Verificado en vivo filtrando por Cerrillos (+476% real): badge visible en las 7 oportunidades de la comuna, 0 errores. **Dos pendientes de la investigación de fuentes, investigados y descartados por ahora (no builds, decisión explícita):** (1) INE FUE (estadísticas de edificación mensuales, más recientes que el histórico ArcGIS hasta 2022) — el catálogo de descarga de `ine.gob.cl/estadisticas/economia/edificacion-y-construccion/permisos-de-edificacion` se renderiza vía JS, sin enlaces de archivo estáticos ni API documentada encontrada con peticiones HTTP reales; no vale la pena forzar scraping frágil sobre un mecanismo no confirmado. (2) Expandir la query CKAN de datos.gob.cl (ya usada para planes reguladores) hacia permisos municipales — al revisar de cerca, solo 4 comunas publican voluntariamente (La Reina, Maule, San Bernardo, Coquimbo), formato no uniforme entre municipios, y el schema de `plan_reguladores` (documentos PDF) no sirve para datos estructurados de permisos — construir una tabla+scraper nuevo para cobertura tan parcial no se justifica frente al valor entregado. Ambos quedan como investigación documentada, no un TODO vago. Queda pendiente (no bloqueante, para una próxima sesión): sintetizar el resto de ambas investigaciones (ideas de modelo de negocio por segmento que no se han construido — checklist de vencimientos regulatorios para administradores, dashboard de portafolio para fondos, etc.) hacia una vista consolidada si la founder la pide.

- [mi-cartera] **Mi Cartera — portafolio persistente (2026-08-01, `bdea158`)** — la founder pidió explícitamente atacar la pieza de infraestructura que faltaba: todo el módulo evaluaba un activo a la vez (Pricing, Tasación, Calculadora), sin forma de que un rentista/administrador/fondo trackee lo que YA tiene. Nueva tabla `propiedades_portafolio`, workspace-scoped, mismo patrón RLS que `terrenos` (`es_miembro()`), con CRUD completo (`GET/POST/PATCH/DELETE /api/propiedades-portafolio`). **Casi se construye relleno antes de esto**: la primera idea fue una página separada "¿tu arriendo está bien puesto?" para rentistas — se abortó al notar que Pricing ya resuelve exactamente esa pregunta para cualquier usuario; habría sido UI duplicada. La pieza real y distinta es `compararConMercado()`: reusa el mismo motor de bandas P25/mediana/P75 de Pricing, pero la pregunta cambia de "¿a qué precio publico?" a "¿mi renta YA vigente está desalineada del mercado?" — cada propiedad se compara en UF/m² contra la mediana real de su comuna+tipo, veredicto bajo/a/sobre mercado con umbral ±10%. También reutiliza sin duplicar lógica los dos badges de señal ya probados en Oportunidades (expansión de cadenas SII, actividad constructiva INE). Verificado en vivo end-to-end (Playwright): propiedad de prueba en Cerrillos (20 UF/100 m² vs. mediana real 0.6086 UF/m²) mostró "Bajo mercado -67% vs. mediana real (N=15)" + ambos badges; eliminar la quitó de la lista y de la base de datos (confirmado con consulta directa). 0 errores de consola en todo el flujo crear→listar→eliminar. Nuevo grupo de sidebar "Mi Cartera" agregado antes de "Análisis" — primer punto de entrada, no un análisis puntual más. **Continuación (mismo día, `e785e3b`)**: agregada `fecha_vencimiento_contrato` (columna nueva, migración incremental — la tabla ya estaba mergeada/pusheada, no se amendeó la original) con badge de urgencia (vencido/por vencer en 60d/normal, solo para propiedades arrendadas) y un resumen agregado de portafolio (total, superficie, renta mensual — solo arrendadas, para no mezclar con precios de referencia de venta — conteo bajo/a/sobre mercado, contratos vencidos/por vencer), todo calculado client-side desde los datos ya cargados, sin API nueva. Cierra la última gap de segmento que quedaba sin nada (fondos, que hasta ahora no tenían ninguna vista propia). Verificado en vivo: propiedad con contrato a 24 días mostró el badge y el resumen correctamente, 0 errores.
- [tema-consultora] **Rediseño "Tema Consultora" (2026-08-01, `d7774a8`)** — la founder pidió detener la construcción de features y rediseñar el frontend "estilo McKinsey worldclass consulting". Aclarado antes de tocar código (AskUserQuestion): (1) reskin **literal** (navy/grises, no evolución del blueprint verde/dorado), (2) alcance **solo Mercado Inmobiliario** — Permisos/DOM no se toca (ya tuvo su propio rediseño "idioma del arquitecto" el 5 jul). Investigación previa (Explore agent) encontró: solo Recharts 3.10.1 instalado, solo 2/9 páginas con algún gráfico (Pricing, Macro), Reportes de Mercado y Mi Cartera (las más densas en KPIs) mostraban todo como texto plano. Mecanismo elegido: scope CSS `.tema-consultora` (nuevo bloque en `globals.css` + variante `.dark`) aplicado vía `app/(dashboard)/mercado-inmobiliario/layout.tsx` nuevo (`display:contents`, scoping automático por routing) — overridea variables crudas (`--primary`, `--blueprint`, `--modulo-mercado`, `--border`, `--line-*`, `--radius`), así que TODA utilidad Tailwind ya existente cambia sola, mismo mecanismo que ya usa `.dark`, sin tocar la mayoría de componentes. Dos wrinkles reales encontrados y corregidos: (1) `page-header.tsx:54` tenía el acento hardcodeado como oklch literal en vez de variable — único punto compartido que no habría respondido al scope, fix trivial a `via-primary/30`; (2) casi todas las páginas usaban `rounded-[4px]` (valor arbitrario Tailwind, no responde a overridear `--radius`) — find/replace mecánico a `rounded-lg` en 14 archivos. Librería de gráficos nueva (`components/mercado-inmobiliario/charts/`): `KpiCard` (cifra + tendencia + sparkline + "so what"), `RankingBarChart` (generaliza el `BandaPrecioChart` de Pricing), `DistribucionDonut`, `DesviacionBar`. Aplicado a las 2 páginas sin ningún gráfico y más densas en KPIs: Reportes de Mercado (KPIs de texto → `KpiCard`, con sparkline real de precio vía nueva `obtenerHistorialMedianaUfM2()` — hoy solo 1 día de historia, crecerá solo con el cron diario, nunca se infla; tabla de rubros → `RankingBarChart` coloreado por demanda) y Mi Cartera (strip de KPIs → `KpiCard`+`DistribucionDonut`; `variacionPct` por propiedad → `DesviacionBar`). Verificado en vivo (Playwright, datos reales): ambas páginas en navy/gris con gráficos reales, 0 errores de consola; `/dashboard` (Permisos) confirmado sin ningún cambio visual (sigue verde/dorado); dark mode funciona en ambos temas. Fase 3 (restyle Pricing/Macro/Oportunidades/Cadenas/Calculadora) y Fase 4 (gráficos reales en Tasación/Due Diligence — requiere extender el prompt para emitir un JSON estructurado junto al markdown) quedan como próximo paso natural, descritas en el plan (`~/.claude/plans/quiero-integrar-tutto-dentro-floofy-platypus.md`), no ejecutadas esta vez. **Fase 3 completada (mismo día, `d4bfc95`)**: Pricing y Macro confirmados sin cambios de código necesarios (ya heredan el tema vía variables CSS, verificado visualmente). Cadenas: `RankingBarChart` de sucursales vigentes por cadena. Oportunidades: nuevo componente `Histograma` (bins continuos — distinto de `RankingBarChart`, que es para categorías con nombre) mostrando la distribución de precios de las oportunidades listadas. Calculadora: nuevo `GaugeArc` (SVG semicircular, sin recharts — no lo justificaba) reemplazando el punto de color + texto del semáforo en Cap Rate y ROI. **Bug real encontrado y corregido antes de verificar (no solo en teoría)**: Cadenas y Oportunidades son Server Components — pasarles una función inline (`formatValor`/`formatTramo`) como prop a un Client Component rompe la frontera de serialización de RSC (mismo error ya documentado en este proyecto en sesiones anteriores), causó un 500 real en `/mercado-inmobiliario/cadenas`; corregido usando el formato por defecto de ambos componentes de gráfico en vez de una función custom desde un Server Component. Verificado en vivo (Playwright): las 3 páginas cargan y muestran gráficos con datos reales, 0 errores de consola. **Fase 4 completada (mismo día, `1d65e51`)**: en vez de cambiar el prompt para pedir un JSON estructurado en paralelo (más tokens, riesgo de que texto y gráfico diverjan), `lib/informe-charts.ts` parsea directamente las tablas/listas markdown que los prompts YA generan con formato fijo — `extraerScoreLiquidez()`/`extraerComparables()` (tablas de Tasación) y `extraerRiesgosPorNivel()` (lista `**[ALTO/MEDIO/BAJO]**` de Due Diligence), mismo criterio de degradación elegante que `InformeEjecutivo`: si la sección no aparece, devuelve null y no se muestra nada, nunca se fabrica un dato. Verificado en dos capas: `tests/unit/informe-charts.test.ts` (6 tests con fixtures del formato exacto del prompt, no depende de que el LLM complete el informe) + Playwright con llamadas reales a OpenAI (Comparables en Tasación y Riesgos por nivel en Due Diligence, ambos con datos reales coincidiendo con el texto, 0 errores). **Hallazgo real durante la verificación, no un bug de este cambio**: en una corrida de Tasación el modelo cortó la respuesta antes de llegar a "Score de Liquidez" (sección 8 de 10 mandatadas) — el parser degradó correctamente a null sin mostrar nada ni fallar; queda como comportamiento pre-existente de la generación agéntica con búsqueda web, fuera de alcance de este cambio de UI, documentado acá por si se quiere investigar el límite de tokens/turnos en `streamConBusquedaWeb` más adelante. Con esto, las 9 páginas de Mercado Inmobiliario del plan original de rediseño quedan con el Tema Consultora aplicado — las 4 fases completas. **Bug pre-existente investigado y corregido a pedido de la founder (mismo día, `fc6d0e0`)**: `streamConBusquedaWeb()` (`lib/ai.ts`, usada por Tasación y Due Diligence) nunca fijaba `max_output_tokens` en la Responses API — confirmado con investigación real (no supuesto) que el default de gpt-4o sin ese parámetro es 4.096 tokens, y que las llamadas de búsqueda web + sus resultados comparten el mismo presupuesto de salida que el texto final en esa API. Con 10 secciones + varias tablas + 8+ búsquedas, ese default no alcanzaba — exactamente lo que causó el corte antes de "Score de Liquidez" documentado arriba. Fix: `max_output_tokens: 12000` explícito. Verificado en vivo con la misma dirección que expuso el bug (Av. Apoquindo 4700, Las Condes): el informe ahora completa las 10 secciones hasta "Fuentes Consultadas" (7.541 caracteres vs. 5.123 truncados antes), Score de Liquidez y Comparables ambos con su gráfico real, 0 errores de consola. De paso se corrigió un comentario desactualizado en el mismo archivo que decía "usado solo por Tasación" — Due Diligence también la usa desde la fusión PROPRA·BI.
- [resolucion-deuda] **Resolución de deuda del registro (2026-08-01, rama `feature/torre-control-resolver-deuda`, commits `4f80753`/`0f18e7c`/`5d72911`/`1089215`)** — pasada completa sobre los `needs-decision`/`duplicate` de `data-sources.yaml`: 15→7 needs-decision, 3→0 duplicate. Walmart Chile y SMU (marcados needs-decision en la sesión anterior) resueltos: cruzando la nómina de razones sociales del SII contra las marcas públicas de cada grupo se encontraron sus RUTs operativos reales — Walmart vía "Administradora de Supermercados Hiper/Express Limitada" (211 direcciones vigentes, 68/54 comunas, alta confianza — mismo día de registro que la reestructuración post-D&S), SMU vía "Alvi Supermercado Mayorista S.A." y "Super 10 S.A." (124 vigentes, 48/41 comunas, confianza media-alta — smu.cl confirma esas marcas como filiales pero sin RUT oficial publicado); Unimarc (la marca más grande de SMU) sigue sin identificar, cobertura de SMU queda parcial. Duplicado real de SII (`/api/enrich/sii`) eliminado tras confirmar cero callers funcionales (solo una mención en una página de documentación estática). UF/macro: auditados los 5 callers reales del endpoint legacy (el registro solo tenía 1 de 5 documentado) — decisión de NO consolidar, casos de uso genuinamente distintos; `/api/utils/indicadores` (duplicado sin callers) eliminado. **Dos bugs reales de producción encontrados y corregidos, no solo en teoría**: (1) el índice de dedup de `plan_reguladores` era parcial (`WHERE dataset_id IS NOT NULL`), lo que rompía el `ON CONFLICT` del cliente JS de Supabase — el scraper de datos.gob.cl encontraba 67 registros reales y no insertaba ninguno, en silencio, desde que se construyó (`20260630_plan_reguladores.sql`); corregido con un índice no-parcial equivalente, verificado: 67 filas insertadas, idempotente. (2) Terrenos: se agregó cron semanal (5 rutas, una por fuente, no combinadas — un intento combinado excedió 280s con 1 solo workspace) pero el primer intento por-fuente TAMBIÉN excedió el límite, porque el tope de enriquecimiento (`enriquecerTerreno()`: Overpass rate-limited a 2 slots/IP + IA + SII) era solo por comuna, no por corrida — con 19 comunas nuevas simultáneas eso significa hasta ~95 enriquecimientos en una sola invocación. Se agregó un tope GLOBAL por corrida (20), verificado: 300 encontrados, 20 enriquecidos, 69 diferidos a la próxima corrida, ~3 minutos, 0 errores. RAG semántico (`lib/rag.ts`): la migración pgvector ya estaba aplicada (de una sesión anterior, sin registrar) pero la tabla estaba vacía — se corrió el backfill (37 embeddings: 14 OGUC + 9 LGUC + 14 DDU), verificado, pero deliberadamente NO cableado a las rutas activas (seguiría siendo `normativa-retrieval.ts` por keywords) — decisión de calidad que requiere evaluación aparte, no una que se deba apurar. Días hábiles/feriados y comunas-chile-base: investigados y decididos explícitamente NO reemplazar por fuentes externas (API oficial del gobierno deprecada, alternativas no confiables para un cálculo de plazo legal; comunas-chile-base no tiene ninguna fuente automatizable — MINVU no publica un listado de comunas con DOM en Línea, confirmado en vivo) — quedan needs-decision pero como investigación manual documentada, no un TODO vago. Limpieza menor: función `addDays()` muerta y variable `remaining` sin uso eliminadas (-2 warnings, 56→54).
- [auditoria-max-tokens] **Auditoría de límites de tokens en el resto de llamadas de IA (2026-08-01, rama `fix/auditoria-max-tokens`, `495c398`)** — a pedido de la founder, tras el fix de `streamConBusquedaWeb` documentado arriba en [tema-consultora]: se revisaron los 5 exports de `lib/ai.ts` (los otros 4 — `aiComplete`/`aiCompleteWithTools`/`aiCompleteWithPDF`/`aiCompleteWithImages` — ya tenían default seguro cuando el caller omite `max_tokens`, `streamConBusquedaWeb` era el único sin ninguno) y los ~22 call sites reales con `max_tokens` explícito (`grep -rn "max_tokens:" app/ lib/`). Criterio de priorización: distinguir "falla visible" (JSON.parse lanza, 500 real) de "falla silenciosa" (fallback `?? default` por campo, sin ningún error) — la segunda es más grave porque el usuario ve un resultado que parece válido pero no lo es. **Hallazgo real, no solo defensivo:** `lib/due-diligence.ts`'s `synthesizeDueDiligence()` arma un JSON que escala con el N° de documentos del expediente (hallazgos, inventario, vigencias, próximos pasos) — si el JSON se corta a medias, `extractJson()` devuelve `null` y CADA campo cae a su default (`hallazgos: []`, `riesgoGlobal: 'MEDIO'`) sin ningún error visible: un expediente con hallazgos reales podía mostrarse como "sin hallazgos, riesgo medio". Corregido subiendo `max_tokens` 4096→8000 y agregando `reportWarning()` (Sentry, vía `lib/observability.ts`) cuando `extractJson` devuelve null — no se expandió a nuevos campos de error en la UI (fuera del pedido literal), solo se hizo visible en logs si vuelve a pasar. Otros dos ajustes de margen, menor urgencia porque ya fallan visible: `app/api/ai/audit-expediente/route.ts` (3000→4500, el array `documentos` también escala con PDFs subidos) y `app/api/ai/memoria-descriptiva/route.ts` (4096→6000, documento formal de 7 secciones genuinamente extenso). El resto de call sites (`anotacion-plano` ya tiene este exacto bug diagnosticado y resuelto en una sesión anterior con retry+fallback propio; `reportes-mercado`, `cadenas/ai-consulta`, `compliance-check`, `cuadro-sugerido`, `ai/chat`, `generate-communication`, `predict-observations`, `observation-response`, `pre-revision`, `asesor-tramitacion`, `copiloto` (4 llamadas paralelas), `declaracion-jurada`, `cron/weekly-summary`, `pricing`, `zonificacion-compat`) revisados y dejados sin cambio — tareas de tamaño fijo o alcance pequeño, sin el patrón de riesgo "escala con input variable". `npx tsc --noEmit`, `npx eslint .` (55 warnings, mismo baseline), `node scripts/check-data-sources.mjs`, `npm test` (164 tests) limpios.
- [obligaciones-regulatorias] **Mi Cartera — checklist de vencimientos regulatorios + vista de portafolio para fondos (2026-08-01, rama `feature/mi-cartera-obligaciones-regulatorias`, `3ad8003`)** — segunda parte del pedido "1 y 2" de la founder (la primera fue [auditoria-max-tokens] arriba). Cierra dos gaps de negocio quedados pendientes de la investigación de mercado del 1 ago ([mi-cartera] y [ine-permisos-edificacion] arriba): (a) un checklist de obligaciones RECURRENTES de un edificio ya en operación — distinto del checklist de expediente del Copiloto, que es para permisos en trámite — y (b) una vista agregada de portafolio para fondos, más allá del strip de KPIs ya existente. **Antes de construir el checklist se investigó explícitamente con un agente (WebSearch), sin fabricar requisitos** (mismo criterio que llevó a eliminar los 2 sistemas hardcodeados en [checklist-dinámico]): 6 obligaciones sobrevivieron la verificación con base legal real — certificación de ascensores (Ley 20.296/DS 37/2015 MINVU, periodicidad **variable** 1-2 años según destino/capacidad, sin cifra única confirmada en el reglamento), plan de emergencia y evacuación + asamblea ordinaria (Ley 21.442 arts. 40/14, anual), seguro colectivo contra incendio de bienes comunes (Ley 21.442 art. 43, vigencia continua/ciclo práctico anual), inspección de gas Sello Verde (DS 66/2007 + Res. 1.250/2009 SEC, 2 años fija), mantención y recarga de extintores (DS 44/2018, NCh 2056, anual). Descartados explícitamente por NO tener una obligación periódica real confirmada: recepción definitiva (trámite único, Art. 145 LGUC), certificado TE1 (solo instalaciones nuevas/modificadas), fondo de reserva y libro de actas (cargos/registros estructurales, no vencimientos). `lib/obligaciones-regulatorias.ts` modela esto con un tipo `PeriodicidadObligacion` que distingue `'fija'` (meses) de `'variable'` (nota textual) — `calcularEstadoObligacion()` devuelve el estado `'verificar'` en vez de fabricar una fecha calculada cuando la periodicidad es variable (ascensores), y `'sin_registro'` cuando el usuario aún no declaró cumplimiento — nunca un dato inventado. 7 tests unitarios cubren las 5 transiciones de estado. Nueva tabla `propiedad_obligaciones` (RLS `es_miembro()`, aplicada en vivo vía `mcp__supabase__apply_migration`) + columnas `tiene_ascensor`/`tiene_gas` en `propiedades_portafolio` para filtrar aplicabilidad (los 4 ítems "siempre" se muestran a toda propiedad, ascensor/gas solo si el checkbox correspondiente está marcado). Nueva ruta `PATCH /api/propiedades-portafolio/[id]/obligaciones` (upsert por `(propiedad_id, obligacion_slug)`). **Vista de portafolio para fondos** (lo que ResumenPortafolioStrip no cubría): KPI de "Obligaciones regulatorias" en riesgo agregadas across todo el portafolio + `RankingBarChart` de exposición por comuna + `DistribucionDonut` de mezcla por tipo de propiedad (paleta de 4 tonos vía `color-mix` sobre `--blueprint`, mismo patrón que `DEMANDA_COLOR` en Reportes) — deliberadamente no duplica el strip existente (precio vs. mercado, contratos), agrega la dimensión de riesgo regulatorio consolidada y el breakdown estructural que un fondo necesita ver de su cartera completa, no activo por activo. **Bug real encontrado y corregido antes de dar por terminado, no solo en teoría (Playwright)**: el `Input` de fecha usaba `defaultValue` sin `key` — al registrar una obligación y actualizar el estado local, React mutaba el `defaultValue` del mismo input ya montado (uncontrolled), lo que Base UI marca como error real de consola ("changing the default value state of an uncontrolled FieldControl"), no solo un warning cosmético. Corregido con `key={fechaUltimoCumplimiento}` para forzar remount en vez de mutación in-place. Verificado en vivo end-to-end con datos reales (Playwright, dev-login): propiedad de prueba con ascensor+gas mostró correctamente 6 obligaciones aplicables, registrar fecha de mantención de extintores cambió el estado a "Al día" con "Próximo vencimiento: 2027-06-15" calculado correctamente, sobrevivió un reload completo de la página (persistencia en Supabase confirmada, no solo estado de React), 0 errores de consola antes y después del fix. `npx tsc --noEmit`, `npx eslint .` (55 warnings, mismo baseline), `node scripts/check-data-sources.mjs`, `npm test` (171 tests) limpios. **Continuación (mismo día, rama `feature/mi-cartera-enriquecimiento-sii`, `6c4c993`)**: tras terminar lo anterior, se investigó con un agente qué construir después en Mi Cartera (leyendo el código real, no adivinando) — se descartó "recomendación mantener/vender con comparables reales" (ya investigado y bloqueado el mismo día en [expansion-cadenas]: sin fuente pública de transacciones en Chile, único vendor pago descartado por política de la founder) y se eligió la opción de menor esfuerzo y mayor certeza: `rol_sii` existía en la tabla desde su creación (el POST ya lo aceptaba) pero no estaba cableado a ninguna UI ni lógica — dato muerto. Cerrado reusando `buscarDatosSIIPorRol()` (`lib/sii-lookup-server.ts`), el mismo lookup ya probado en producción por Tasación y Due Diligence, sin construir scraping nuevo. Campo "Rol SII" agregado al formulario; nueva ruta `POST /api/propiedades-portafolio/[id]/enriquecer-sii` (ownership vía RLS con cliente de sesión, no service client) persiste destino + avalúo fiscal UF + fecha de consulta, best-effort — 502 explícito si el SII no responde, nunca fabrica un resultado. `destinoSiiCoincideConTipo()` cruza el destino declarado por el SII contra el tipo de propiedad vía keywords, devolviendo `null` (no fabrica un mismatch) cuando el destino es ambiguo — 3 tests unitarios. Verificado en vivo (Playwright): el campo y botón funcionan, y el camino de fallo (rol de prueba sin datos reales) degrada correctamente con error explícito, sin dato fabricado. tsc/eslint/check-data-sources/tests (174) limpios.
- [auditoria-mi-cartera] **Auditoría de código de Mi Cartera — 1 falla de seguridad real + 5 bugs (2026-08-02, rama `fix/mi-cartera-code-review`, `cd32928`)** — a pedido de la founder ("revisa lo que hemos construido y encuentra mejoras a implementar"): un agente code-reviewer revisó todo lo construido en [obligaciones-regulatorias] (checklist regulatorio + vista de portafolio + enriquecimiento SII), cada hallazgo verificado leyendo el código real antes de corregir. **Hallazgo P0 (seguridad real, no teórica)**: la policy RLS de `propiedad_obligaciones` solo validaba `es_miembro(workspace_id)` contra la columna `workspace_id` de la fila que se está insertando — un valor que el propio caller controla en el body del PATCH — sin validar que `propiedad_id` realmente perteneciera a ESE workspace. Un usuario del workspace A podía hacer `PATCH .../<uuid-de-B>/obligaciones` con su propio `workspace_id=A` y el `WITH CHECK` pasaba igual, pisando (vía el upsert por `propiedad_id`+`obligacion_slug`) el registro real de B. Corregido con el mismo patrón ya usado en el proyecto para tablas hijas derivadas (`documentos_workspace`/`etapas_workspace`, `20260731_workspace_compartir.sql`): la policy ahora valida vía `EXISTS` contra el workspace REAL del padre (`propiedades_portafolio`), no contra una columna rellenable por el caller — migración aplicada en vivo, verificada contra `pg_policy` directamente. Ruta también gana verificación de ownership explícita antes de escribir. **5 bugs más, todos corregidos y verificados en vivo (Playwright)**: (1) PATCH/DELETE de propiedades devolvían 200 `ok:true` aunque RLS filtrara todo en silencio (Postgres no lanza error con 0 filas) — agregado `.select('id')` + chequeo 404; (2) el input de fecha de obligación disparaba un PATCH por cada segmento tipeado (el fix de esta mañana evitaba el warning de Base UI pero el remount por `key` cortaba el foco a mitad de tipeo) — movido el guardado a `onBlur`, verificado con Playwright que 0 requests disparan durante el tipeo y exactamente 1 al hacer blur; (3) `calcularEstadoObligacion` podía marcar "vencido" en la tarde de un día que debía ser "por_vencer" (comparaba contra la hora exacta, no medianoche) y desfasar la fecha un día en husos UTC+ (`toISOString()` da la vuelta por UTC sobre una fecha en calendario local) — normalizado a medianoche + formateo desde campos locales, sin pasar por UTC; (4) el badge "destino SII no coincide" desaparecía al recargar (solo vivía en estado de sesión) y `destinoSiiCoincideConTipo` podía confirmar un match falso cuando el destino calzaba con el tipo declarado Y con otro a la vez (ej. "BODEGA Y LOCAL COMERCIAL") — el GET ahora recalcula el campo desde datos persistidos con la misma función pura, y la heurística devuelve `null` (no fabrica certeza) ante esa ambigüedad; (5) N+1 real en `GET /api/propiedades-portafolio` — bandas de mercado y obligaciones se consultaban una vez POR PROPIEDAD (un fondo con 30 propiedades en 3 comunas hacía ~90 round-trips) — `compararPortafolioConMercado`/`obtenerObligacionesPortafolio` cachean/agrupan por request y corren en paralelo. 4 tests unitarios nuevos (boundary de medianoche, ambigüedad de keywords). `npx tsc --noEmit`, `npx eslint .` (55 warnings, mismo baseline), `node scripts/check-data-sources.mjs`, `npm test` (177 tests) limpios.
- [auditoria-modulo-mercado] **Auditoría de código del resto de Mercado Inmobiliario — SSE roto, honestidad de datos, tool-loop, N+1 (2026-08-02, rama `fix/mercado-inmobiliario-code-review-modulo`, `0fa2f15`)** — continuación de [auditoria-mi-cartera] sobre las 10 páginas restantes del módulo, 3 agentes code-reviewer en paralelo (Tasación/Due Diligence/Calculadora · Reportes/Macro/Noticias/Cadenas · Pricing/Oportunidades/Copiloto), cada hallazgo verificado contra el código real antes de corregir. **Bug compartido más serio**: las 4 páginas que streamean IA (Tasación, Due Diligence, Pricing, Copiloto) repetían el mismo parseo de SSE roto — `decoder.decode(value)` sin `{stream:true}` (rompe UTF-8 multibyte partido entre chunks) + `split("\n")` sin buffer entre lecturas (un `data:{...}` cortado a la mitad da JSON inválido) + un catch INVERTIDO que relanzaba exactamente los SyntaxError que debía absorber, abortando informes ya generados. `lib/sse-client.ts` centraliza el fix (bufferea hasta el separador `\n\n` completo — un parse que falle ahora es un bug real, no un artefacto de corte). **Copiloto**: el loop de tool-calling descartaba la última ronda si se agotaban las 4 rondas con tool_calls aún pendientes de responder — usuario recibía "no pude resolver" con datos reales ya en mano; agregada una ronda final `tool_choice:'none'` que fuerza texto desde lo ya reunido. También rol de mensaje del cliente casteado sin validar (inyección de un `role:'system'` falso después del prompt real) — ahora filtrado y acotado en historial/largo. **3 casos de honestidad de datos corregidos** (un valor de respaldo presentado como real, contra el principio central del producto): Reportes de Mercado afirmaba `"verificado":true` incluso con banda de respaldo RM; Macro nunca persistía/pasaba al prompt el `ufFuente` ya calculado (columna `uf_fuente` nueva + badge "respaldo"); Reportes recibía la comuna como texto libre y un typo caía en silencio al respaldo citywide (`resolverComunaCanonica()`, reusa `normalizarNombreComuna`). **2 bugs de fecha** (mismo patrón que [obligaciones-regulatorias] hoy temprano): `obtenerHistorialMedianaUfM2` pedía ascendente+limit (Postgres aplica ORDER BY antes de LIMIT → trae los MÁS ANTIGUOS, sparkline congelado pasados los primeros N días) — ahora descendente+reverse; Macro mostraba `fecha_captura` un día antes (verificado contra el TZ real del entorno, America/Santiago) por no forzar medianoche local, mismo fix que ya tenía cadenas/page.tsx. **informe-charts.ts**: `extraerComparables` leía la columna ajustada por índice hardcodeado (rompe si el modelo omite "#") → ahora por texto de encabezado; separador de miles chileno mal parseado (`"1.250"` → 1.25 en vez de 1250) → reutiliza la lógica de `parseChileanNumber`; conteo de riesgos sin anclar contaba menciones inline dentro de bullets de descripción → anclado al inicio de línea. **Histograma**: pérdida real del valor máximo ~5% de las veces (verificado con barrido de 4000 datasets aleatorios) por acumulación de punto flotante en desde/hasta por tramo → indexado directo por posición (`binarValores`, extraída pura y testeada); de paso se eliminó el prop `formatTramo` (nadie lo usaba, misma clase de bug RSC función-a-Client-Component ya vista dos veces). **N+1 real en `obtenerOportunidadesMercadoLocales`**: bandas y listings sin order/limit, PostgREST corta en 1000 filas sin orden garantizado (la llamada sin comuna del dashboard podía perder listings reales de forma no determinística) — cohortes acotadas a 10 días, listings paginados explícitamente (con `reportWarning` si se alcanza el tope, nunca un corte silencioso), `.in()` de historial de precio troceado en tandas de 200. **Quota bypass en las 4 rutas de streaming**: `recordUsage` corría después de que el stream drenara — un cliente que abortaba justo antes de `[DONE]` dejaba un informe ya generado y pagado sin contar contra el cupo; movido a justo después del rate-limit gate en las 4, más orden auth→parse-body corregido y guard de doble-throw en el catch. Menores: Calculadora mostraba la UF de respaldo en el tab por defecto hasta que el usuario tocaba algo (`key={ufActual.valor}` fuerza remount); Pricing reetiquetaba el gráfico ya renderizado si se cambiaba el selector sin re-consultar (snapshot de lo realmente consultado); timeout agregado al self-fetch de `sii-lookup-server`/`obtenerValorUF`; `Promise.all` en `construirContextoReal` (5 llamadas independientes corrían en serie). Verificado en vivo (Playwright, contra el TZ real del entorno para los 2 bugs de fecha): Copiloto resolvió una comparación real de 2 comunas sin caer al mensaje genérico (contador de uso exactamente 1/5, recordUsage no duplicado); Pricing mantuvo la etiqueta correcta tras cambiar el selector sin re-consultar; Reportes con "providencia" en minúscula devolvió datos reales de Providencia con "Verificado"; Macro mostró "1 ago 2026" para `fecha_captura=2026-08-01` (el bug habría mostrado "31 jul"); Calculadora mostró el valor real desde el primer render. 0 errores de consola en todos los flujos. tsc/eslint (55 warnings, mismo baseline)/check-data-sources/tests (184, +7 nuevos) limpios.
- [auditoria-permisos-dom] **Auditoría de código de Permisos/DOM — RLS cross-tenant, workspace-sharing roto, fabricación financiera, IA sin honestidad (2026-08-02, rama `fix/permisos-dom-code-review`, `8fd63f1`)** — continuación de [auditoria-mi-cartera]/[auditoria-modulo-mercado] sobre el lado NO Mercado Inmobiliario: proyecto CRUD, documentos, copiloto OGUC, zonificación, scraping DOM, CRM, billing, admin (~34.8k líneas, 4 grupos vía Explore agent, cada hallazgo de los reviewers verificado contra el código real antes de corregir — un hallazgo del reviewer de Group C resultó FALSO, verificado y corregido igual por hallazgo propio). **Mismo patrón de escape hatch RLS que [auditoria-mi-cartera], encontrado dos veces más**: `propiedad_obligaciones` no era el único — `observaciones_dom`/`due_diligence_reports` tenían el mismo `OR user_id=auth.uid()` colado en el `WITH CHECK` de una policy `FOR ALL` (permite INSERT cross-tenant porque el caller siempre controla su propio `user_id`); migración separa en 4 policies por comando, removiendo el escape SOLO de `WITH CHECK` (se mantiene en `USING` para filas legacy). **Workspace-sharing roto en 4 rutas**: `via-tramitacion`/`cuadro-calculo`/`etapas`/`aplicar-dd` tenían un check de ownership a nivel de app (`proyecto.user_id !== user.id`) escrito ANTES de que `proyectos` se migrara a RLS workspace-scoped — más estricto que el RLS real, bloqueando a miembros legítimos del workspace; fix subtractivo (se confía en `!proyecto`, RLS ya es la fuente de verdad). Mismo patrón de "200 ok con 0 filas afectadas" de sesiones anteriores, ahora en `clientes/[id]`, `prospectos/[id]`, `proyectos/[id]` PATCH — `.select('id')` + 404 si vacío. **3 instancias independientes del mismo bug de nombre de campo** (`expediente` vs. `expedienteNumero`) rompiendo el tracking DOM en silencio — una de ellas (`permisos/page.tsx`) el reviewer la había marcado como "correcta"; se encontró igual verificando el código directamente en vez de confiar en el reporte del agente. **Fabricación financiera** ("nunca fabricar", mismo principio que [obligaciones-regulatorias]): `cuadros-calculo.ts`'s `incompleto` solo cubría predio/edificada, no `ocupadaSuelo` ni altura — un nivel sin ocupadaSuelo reportada caía a 0 vía `num()` e imprimía "cumple" en verde contra un máximo real del PRC; ampliado el flag para cubrir ambos casos. `derechos-municipales.ts`: un `presupuestoObra` NaN saltaba el mínimo municipal (`NaN < minimoCLP` es `false`) y propagaba hasta `montoDerechos: NaN`; guardado con advertencia explícita. Copiloto: presupuesto estimado a 800k CLP/m² (o 50M plano) cuando no hay avalúo fiscal real, ahora declarado en `derechosAdvertencias` en vez de presentarse como cálculo real. **Confiabilidad IA**: `lib/ai.ts` ganó el mismo fix de `finish_reason==='length'` ya aplicado a `streamConBusquedaWeb` en [tema-consultora] — ahora también en `aiComplete`/`aiCompleteWithPDF`/`aiCompleteWithImages` (afecta ~17 callers vía su try/catch→500 existente, deliberadamente NO aplicado a `aiCompleteWithTools` por su control de flujo más delicado). `pre-revision`/`predict-observations`: un parseo fallido fabricaba un acta "MEDIO/50%/0 observaciones" indistinguible de un diagnóstico real con 0 riesgos — ahora error 502 explícito (rutas standalone, pueden fallar entero). `copiloto`'s sección `observaciones` (misma fabricación, pero el endpoint combina 4 secciones en una respuesta) degrada a `riesgoGlobal:'ALTO'` + resumen explícito en vez de fallar toda la request — sesgo hacia "revisa de más" en vez de una falsa calma. `ChecklistItemSchema.obligatorio` por defecto en `false` volvía indistinguible "no obligatorio" de "el modelo omitió el campo" — cambiado a `true` (mismo criterio: ante duda, tratar como obligatorio). `ai/chat` + `oguc-chat`: mismo guard de validación de `role`/`content` del cliente ya aplicado al copiloto de mercado en [auditoria-modulo-mercado] (rol `system` inyectable sin validar), y reemplazo del parseo SSE manual roto por `lib/sse-client.ts` (mismo bug de esa sesión, encontrado de nuevo acá). `audit-expediente` migrado a `parseAiJson`+zod, cap de 30 archivos, tokens escalados con la cantidad. **Otros**: 5 sitios más con el bug de timezone UTC-medianoche ya visto en [auditoria-mi-cartera] (`dias-habiles.ts`, `desarchivo-panel.tsx`, `permisos/route.ts`, `proyectos/[id]/page.tsx`, `cartera/page.tsx`); `plano-escala.ts`'s regex de escala truncaba miles (`"1:1.000"` → 50 en vez de 1000, mismo patrón que el bug de miles ya visto en `informe-charts.ts`); `export-zip` sin tope de documentos/bytes; `informe-pdf.ts` sin `onerror`/timeout en carga de imágenes (promesa colgada para siempre) ni `res.ok` en fetch de plano, y rasterizaba TODAS las páginas de TODOS los planos antes de aplicar el recorte a 6 láminas. `dom-en-linea` ganó auth dual (cron secret O sesión+rate limit) — antes sin ninguna verificación real. Admin: `salud-datos`/`sla` sin gate de admin (fuga de datos), `notify` con relay de email abierto (zod discriminated union reemplaza el passthrough), WhatsApp sin metering de plan, checkout con `priceId` arbitrario, invites con downgrade de rol y bypass de límite de asientos, webhook de Stripe sin guard de replay (nueva columna `stripe_event_created_at`). 3 migraciones SQL aplicadas en vivo. Verificado: `npx tsc --noEmit`/`npx eslint .` (55 warnings, mismo baseline)/`node scripts/check-data-sources.mjs`/`npm test` (188 tests) limpios + verificación en vivo (Playwright, dev-login): Chat OGUC con streaming SSE real completo sin errores de consola, badge "Estimado" en Municipios renderiza con tooltip correcto.
- [mi-cartera-reajuste-caprate] **Mi Cartera — alerta de reajuste de renta UF/IPC + Cap Rate en el resumen (2026-08-02, rama `feature/mi-cartera-reajuste-caprate`, `f205bc6`+`41c62de`)** — tras cerrar [auditoria-permisos-dom], la founder pidió seguir construyendo en Mi Cartera; antes de elegir qué, un agente investigó (código real + WebSearch) qué necesita de verdad un fondo/administrador que hoy no esté cubierto, mismo criterio "no fabricar requisitos" que [obligaciones-regulatorias]. Se descartaron 3 candidatos sin base sólida o de bajo valor (patente comercial del arrendatario — sin fuente pública unificada; aviso de reavalúo trienal SII — evento único; calendario de contribuciones — quedó para después) y se eligieron los 2 de mayor leverage: **reajuste de renta** (Ley 18.101 Art. 13 — el reajuste debe estar pactado, ataca el problema real de arriendos "congelados" que nadie detecta) y **Cap Rate** (práctica estándar de industria, la fórmula ya existía en Calculadora — "casi gratis"). **Ambas se construyeron con 2 agentes en paralelo, cada uno en su propio git worktree** (mismos 3-4 archivos compartidos: `propiedades-portafolio-server.ts`, la ruta API, `mi-cartera/page.tsx`) para evitar que se pisaran mientras trabajaban — primera vez que este patrón de paralelización se usa en el proyecto. **Cap Rate**: `calcularCapRatePropiedad()` delega a la fórmula ya probada (`calcularCapRate` de `lib/calculadora-inversion.ts`, no reimplementada), solo aplica a propiedades en arriendo con avalúo fiscal SII ya consultado (proxy de valorización — el portafolio no tiene campo propio de "valor de mercado"), `null` si falta cualquier dato — nunca fabrica un cap rate parcial. **Reajuste**: `calcularEstadoReajuste()` puro/determinista (mismo patrón que `obligaciones-regulatorias.ts`), y además — verificado en vivo el shape real de `mindicador.cl/api/ipc/{año}` antes de codificar (serie mensual, no acumulada; el año en curso devuelve `serie: []` hasta que el INE lo publica) — una estimación OPCIONAL de renta reajustada que compone el IPC mes a mes y devuelve `null` completo si falta un solo mes de la ventana, nunca un parcial disfrazado de completo. **Integración manual no trivial**: como ambos agentes no committearon (dejaron el working tree del worktree listo), la integración fue vía `git apply --reject` de los diffs de cada worktree sobre una rama nueva — 4 puntos de solape real (interfaz `FilaPortafolio`, destructuring de `filas.map`, el grid de `KpiCard` en el resumen, el objeto combinado del GET) resueltos a mano combinando ambos cambios, verificados con `tsc` tras cada fix. **Efecto secundario real detectado y corregido**: los dos worktrees (con sus propios `node_modules`, ~200MB c/u) seguían montados dentro del repo tras terminar los agentes — `npx eslint .` pasó de 0 a **2055 errores** porque no hay ignore para `node_modules` anidados fuera de la raíz; los worktrees se removieron con `git worktree remove` (no un `rm -rf` a mano) y eslint volvió al baseline exacto (0 errores, 55 warnings). Verificado en vivo (Playwright) tras la integración: los 7 KPI cards del resumen renderizan juntos sin romper el layout, Cap Rate muestra el estado "sin datos" correctamente para una propiedad sin avalúo SII, el badge de reajuste pasa de "vencido" → (click "marcar aplicado hoy") → "al día" con la fecha correcta, el KPI de reajustes pendientes se actualiza en cada paso, 0 errores de consola. Migración de 3 columnas (`reajuste_aplica`/`reajuste_periodicidad_meses`/`reajuste_fecha_ultimo`) aplicada en vivo. `tsc`/`eslint` (0 errores, 55 warnings)/`npm test` (204, +16 nuevos)/`check-data-sources` limpios.
- [torre-control-cierre-deuda] **Cierre de deuda del registro de fuentes — 6→1 needs-decision (2026-08-02, rama `fix/torre-control-cierre-deuda`, `35c3e10`)** — tras cerrar [mi-cartera-reajuste-caprate], la founder pidió cerrar la deuda conocida de `.planning/data-sources.yaml`. Investigación reveló que 4 de los 6 needs-decision YA tenían decisión tomada (o estaban resueltos de facto) pero el registro nunca se actualizó para reflejarlo — se cerraron con solo un cambio de `status`/nota: `plan-reguladores-portal-transparencia` (decisión de 1 ago de no perseguir bypass anti-bot), `macro-uf-fallback-constant` (el monitoreo automático de 30 días ya estaba activo — SLA seteado, la nota decía "pendiente" por error), `rag-embeddings` (refleja la decisión de esta misma sesión, ver [mi-cartera-reajuste-caprate] arriba en el tiempo), y `rut-enrich` (decisión founder: los 5 RUTs hardcodeados — Parque Arauco, Mall Plaza, Cencosud, Mall Viña, Plaza Vespucio — se aceptan como lista final). **Hallazgo real no buscado**: 3 entradas (`rut-enrich`, `dom-digital`, `mapa-formularios`) tenían `called_from: []` (huérfano) FALSO — sí tienen callers reales en el código (`cadenas-comerciales/page.tsx`, `dom-digital-badge.tsx`, y 3 páginas de formularios respectivamente); corregido el campo en las 3, y `mapa-formularios` ganó además `freshness_sla_days: 90` (no lo tenía) para quedar bajo el mismo monitoreo trimestral automático que sus 3 hermanos stale-kb (lguc/oguc/circulares-ddu). **`dom-digital` cerrado tras investigación real de viabilidad** (agente con WebSearch + verificación en vivo en los 6 municipios de mayor volumen: Santiago, Providencia, Las Condes, Ñuñoa, Maipú, Puente Alto): domenlinea.minvu.cl confirmado sin ningún endpoint público (exige Clave Única a solicitante Y municipio); de los 6, Ñuñoa niega explícitamente poder consultar el estado de un expediente, Providencia/Santiago exigen login, Puente Alto (FileDOM) es solo para certificados ya emitidos (no expedientes en trámite), Maipú no tiene portal propio; el único hallazgo real — Las Condes (`lascondesonline.cl/Permisos Obras`) — sí permite consulta pública sin login, pero es tecnología ASP legacy sin API (requeriría scraping HTML frágil), cubre 1 de 6 municipios, y exige el RUT del propietario (dato que PermisoHub no siempre tiene) — no justifica construir ni una integración general ni un piloto de un municipio; cerrado como "no ahora", el endpoint placeholder honesto (503 explícito en producción) se mantiene. **`comunas-chile-base` sigue abierto** (el único de los 6 sin decisión posible aún) — otro agente ejecutó el spot-check manual recomendado desde el 1 ago sobre 23 comunas grandes/relevantes, vía WebSearch de noticias/comunicados municipales + fetch directo a sitios oficiales (domenlinea.minvu.cl sigue sin directorio público, reconfirmado). Encontró 2 correcciones reales con evidencia directa — `nunoa`: `dom_en_linea`→`presencial` (el sitio oficial nunoa.cl dice explícitamente que la entrega de permisos es presencial en el mesón de DOM, sin mención de plataforma alguna, contradiciendo el valor de 2023) y `puente-alto`: `presencial`→`probable` (confirmadas 2 plataformas propias operativas, `puentealto.filedom.cl` y `puentealto.smartdom.cl`) — aplicadas directo en `lib/comunas-chile.ts`. Las otras 18 comunas revisadas se dejaron SIN CAMBIO por no encontrar evidencia que contradijera el valor de 2023 — disciplina de "nunca fabricar un cambio sin evidencia real" aplicada también acá, igual que en las auditorías de código de esta sesión. El universo completo de 345 comunas sigue sin una fuente automatizable — recomendación registrada: próximo spot-check en 6-12 meses o cuando MINVU publique un directorio público, lo que ocurra primero. Verificado: `tsc`/`eslint` (0 errores, 55 warnings)/`npm test` (204)/`check-data-sources.mjs` (1 needs-decision, 0 fallos duros, bajado de 6) limpios.
- [v1.4] **MILESTONE v1.4 (Zonificación) COMPLETE** — Phase 12's final checkpoint (12-04 Task 2) was resolved live by the orchestrator (same pattern as 11-08): real Playwright browser, `/auth/dev-login` session, and — new this time — `mcp__supabase__execute_sql` used to temporarily mutate a real project's `destino_sii`/`zona_status`/`zona_usos_disponibles` to construct the 3 test scenarios (incompatibility alert, no-zonificación regression, Ñuñoa-style no-usos regression) that didn't exist naturally in the 2 available dev projects — all reverted after each check. Confirmed live: the INTEG-01 alert fires automatically with a real AI-generated citation ("El destino declarado (CASA HABITACION) no calza con los usos permitidos de la zona UC2/EAa+cm... los usos residenciales están prohibidos", with a working decree link) when `destino_sii` conflicts with `zona_uperm`/`zona_uproh`, and is completely absent (byte-identical panel otherwise) for both `zona_status !== 'encontrado'` and the Ñuñoa-style `usos_disponibles:false` case — confirming the compound guard, not just `zona_status`, actually gates the UI. Copiloto's "Diagnóstico OGUC" skill ran end-to-end with a real OpenAI call and returned a complete, correctly structured response with 4 article citations — no crash from the new `seccionZonificacion()` prompt injection. Due Diligence tab loads without error (full AI regeneration to observe a live PRC citation was not run — costly, and this project's existing report topic isn't about zoning — the `resolverRefNormativa` PRC branch and 3-way type threading were already code-verified by 12-02's executor and the plan-checker). `lib/via-tramitacion.ts` and its determinism test suite (14/14 passing) untouched throughout the whole phase. All 12 phases across the 3 milestones this session touched (v1.2 Dashboard, v1.3 Army of Skills, v1.4 Zonificación) are now marked complete in ROADMAP.md/STATE.md/REQUIREMENTS.md.

- [v1.6] **Roadmap creado (2026-08-02)** — 3 fases derivadas del research (`research/SUMMARY.md`), numeración 13-15 (continúa desde 12, último número formal de v1.4 — v1.5 y las sesiones ad-hoc posteriores no usaron fases GSD, ver MILESTONES.md/PROJECT.md Key Decisions). Fase 13 (Detalle): refactor de `evaluarOportunidad()` a función pura + ficha `/oportunidades/[id]` — sienta el contrato de datos (`muestra_n` declarado, helper de fecha compartido, propagación de UF ya calculado) que evita que las fases siguientes reintroduzcan bugs ya corregidos en el proyecto (benchmark circular, timezone). Fase 14 (Comparación): reusa los widgets ya resueltos por listing en la Fase 13, estado tipado `{operacion, tipoPropiedad, ids}` persistido en la URL para prevenir estructuralmente mezclar tipo/operación. Fase 15 (Informe): vista de impresión de lo que Detalle/Comparación ya muestran en pantalla — decisión de arquitectura del informe (`@media print` vs. `jspdf`+`html2canvas`) deliberadamente diferida a confirmarse al inicio de esa fase, no forzada en el roadmap. 15/15 requisitos v1.6 mapeados 1:1 por bloque (DETA-01..07→13, COMPA-01..04→14, INFO-01..04→15), sin huérfanos ni duplicados.

- [13-01] **`evaluarOportunidad()` extraída (2026-08-02, commits `e97818d` test / `025e3f0` feat)** — función pura exportada desde `lib/mercado-locales-server.ts`, fuente única de verdad del scoring de `reasonCodes` (`below_p25_ufm2`/`below_p25_uf` mutuamente excluyentes vía `else if`, `price_drop_7d` comparando solo las últimas dos entradas de `historialReciente`, no el mínimo/máximo de la ventana de 7 días — comportamiento preservado byte a byte del código inline que reemplaza). Cubierta por `tests/unit/evaluar-oportunidad.test.ts` (9/9 casos). `obtenerOportunidadesMercadoLocales()` sin cambio de firma ni comportamiento — confirmado en sus 5 call sites (`oportunidades/page.tsx`, `reportes-mercado/route.ts`, `dashboard/page.tsx` ×2, `mercado-inmobiliario-copiloto.ts`). Desbloquea planes 13-03 en adelante (ficha de detalle y comparables reutilizan esta función en vez de reimplementar el cálculo). Sin deviations. Ver 13-01-SUMMARY.md.

- [13-02] **3 utilidades compartidas construidas (2026-08-02, commits `017fc96`/`c3e920a`/`182dd0e`)** — `lib/formato-fecha.ts` (`formatFechaCorta`, movida verbatim desde `oportunidades/page.tsx:26-31`, esa página NO fue tocada — 13-07 hará el swap al import compartido). `streamConContexto(instructions, input)` agregada a `lib/ai.ts` junto a `streamConBusquedaWeb`, pero **sin** el `tools: [{type: 'web_search_preview'}]` — estructuralmente imposible que el modelo busque en vivo (no es solo instrucción de prompt), `max_output_tokens: 3000` (vs 12000 de la versión con búsqueda). `lib/resumen-oportunidad-prompts.ts` (`ResumenOportunidadContexto` + `buildSystemResumenOportunidad()` + `buildUserQueryResumenOportunidad()`) — narra bandas P25/mediana/P75, comparables e historial ya calculados server-side, marca explícitamente "no disponible" en vez de fabricar cualquier campo `null`. Ningún archivo existente fuera de `lib/ai.ts` modificado. Sin deviations (único hallazgo: error de tsc preexistente en `tests/unit/evaluar-oportunidad.test.ts`, ajeno a este plan, de la ejecución paralela de 13-01 — fuera de alcance, no corregido acá). Desbloquea 13-04 (resumen IA) y 13-05/13-06/13-07 (formateo de fecha). Ver 13-02-SUMMARY.md.

- [13-03] **Data layer de la ficha de detalle construida (2026-08-02, commits `08ef64e`/`7f24561`)** — `obtenerOportunidadPorId(id)` (listing individual, status='activo' O 'dado_de_baja', bandas y reasonCodes vía `evaluarOportunidad()` de 13-01), `obtenerComparablesOportunidad(params)` (comparables reales por match exacto comuna+tipo+operación, consulta directa a `mercado_locales_listings` — NO reutiliza `obtenerOportunidadesMercadoLocales()` porque esa descarta listings sin reasonCodes), `obtenerHistorialPrecioListing(listingId)` (historial completo, no acotado a 7 días), `REASON_LABEL_DETALLE` + `REASON_LABEL` re-exportado. Todo agregado a `lib/mercado-locales-server.ts`, ningún archivo nuevo. Desbloquea 13-05/13-06/13-07. Ver 13-03-SUMMARY.md.

- [13-04] **DETA-06 completo end-to-end salvo wiring final (2026-08-02, commits `8ed59c6`/`0530389`)** — `app/api/oportunidades-resumen/route.ts` (ruta SSE flat POST, mismo orden de guardas que `app/api/tasacion/route.ts`: `aiAuthGuard` → `checkRateLimit` → `recordUsage` antes de streamear, usa `streamConContexto` de 13-02 exclusivamente — confirmado sin rastro de `streamConBusquedaWeb`, sin sección avaluoFiscal/SII). `components/mercado-inmobiliario/oportunidad-detalle/resumen-tab.tsx` (`<ResumenTab contexto={...}>` client component, sin `useEffect` de auto-disparo — el resumen ejecutivo IA SOLO se genera con click explícito en "Generar resumen ejecutivo", por decisión bloqueada en `13-CONTEXT.md`; streamea vía `leerEventosSSE`, renderiza con `InformeEjecutivo`, error/loading aislados sin afectar el resto de la ficha). Sin deviations. Falta solo que 13-07 arme el `ResumenOportunidadContexto` real (con datos de 13-01/13-03) y lo pase a `<ResumenTab>` desde la página de detalle. Ver 13-04-SUMMARY.md.

- [13-05] **PosicionamientoTab construido (2026-08-02, commits `2d02c39`/`a1eb931`)** — componente presentacional puro (DETA-02 + DETA-07): banda de precio vs. cohorte con banner amber prominente (markup reutilizado literal de `app/(dashboard)/patentes/page.tsx`) cuando falta banda o hay fallback citywide, `muestra_n` declarada en texto visible, y rentabilidad implícita de zona con badge violet "Estimado de zona" (usa `calcularCapRate()` de `lib/calculadora-inversion.ts` tal cual, mismos supuestos de vacancia 7%/opex 15%) — se renderiza siempre, incondicional a `operacion`. Ningún archivo existente modificado. Desbloquea 13-07. Ver 13-05-SUMMARY.md.

- [13-06] **HistorialTab + ComparablesTab construidos (2026-08-02, commits `6f2b881`/`8950c72`)** — `historial-tab.tsx` (DETA-03 + DETA-04): días publicado, historial de precio completo o mensaje explícito si vacío, cada `reasonCode` explicado en detalle vía `REASON_LABEL_DETALLE`, señales cruzadas de expansión de cadenas y tendencia constructiva INE cuando existen (degradan omitiendo el bloque, sin mensaje explícito de "sin señal" — a diferencia de comparables/rentabilidad, no son dato central de la ficha). `comparables-tab.tsx` (DETA-05): mini-cards clickeables hacia `/mercado-inmobiliario/oportunidades/[id]`, SIEMPRE visible — mensaje de insuficiencia y grilla real coexisten cuando hay exactamente 1 comparable, nunca se oculta un dato real ni se rellena con datos fuera de criterio. Ejecutado en paralelo con 13-05, sin conflicto de archivos. Ningún archivo existente modificado. Sin deviations. Desbloquea 13-07 (última tab pendiente de integrar en la página real). Ver 13-06-SUMMARY.md.

- [13-07] **Fase 13 COMPLETA — ficha de detalle integrada de punta a punta (2026-08-02, commits `006e428`/`0e0b1e3`)** — `app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx` (Server Component nuevo): un único `await Promise.all([...])` al tope trae oportunidad, bandas de arriendo+venta, comparables, historial y señales cruzadas antes de componer las 4 tabs (orden Posicionamiento→Resumen→Historial→Comparables, "el dato duro antes que la narrativa" per CONTEXT.md); `notFound()` reservado exclusivamente para id inexistente, `status='dado_de_baja'` renderiza su propio banner rojo explícito (nunca 404). `app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx`: eliminadas las copias locales de `REASON_LABEL`/`formatFechaCorta` en favor de los exports compartidos de 13-02/13-03, agregado link "Ver ficha completa" por card coexistiendo con el link externo al aviso original. Checkpoint humano (Task 3, 7 pasos end-to-end: links de la lista, header+4 tabs, N= explícito + banner/badge de Posicionamiento, streaming bajo demanda del Resumen, Historial, navegación entre Comparables, 404 real) aprobado por el usuario sin hallazgos. Verificación final: `tsc --noEmit` limpio, `npm run build` sin errores (ruta `[id]` dinámica confirmada), `vitest run tests/unit/evaluar-oportunidad.test.ts` 9/9 en verde. Sin deviations. DETA-01 a DETA-07 completos y navegables de punta a punta. Ver 13-07-SUMMARY.md.

- [14-03] **Fase 14 COMPLETA — ruta y tabla de comparación cerrando COMPA-01 a COMPA-04 (2026-08-02, commits `07a5a06`/`0d2a10b`)** — `components/mercado-inmobiliario/comparacion/tabla-comparacion.tsx` (Server Component presentacional nuevo, no reutiliza los tabs de Fase 13): columnas=oportunidades, filas=Precio UF/Precio UF/m²/% vs. mediana cohorte/Superficie/Días publicado/Señales/Rentabilidad implícita de zona, resaltado de "mejor valor" limitado a las 3 primeras filas (dirección inequívoca, Open Questions 1/2 de 14-RESEARCH.md), excluyendo del cálculo cualquier precio inválido (0 fabricado por `construirOportunidadDetalle`), oportunidades `dado_de_baja` incluidas con badge visual. `app/(dashboard)/mercado-inmobiliario/oportunidades/comparar/page.tsx` (Server Component nuevo): validación server-side en cascada — parseo/dedupe UUID → rango 2-5 (Pitfall 4) → fetch batched vía `obtenerOportunidadesPorIds` (14-01) → existencia (banner "N de M ya no existen") → **homogeneidad de tipoPropiedad/operacion, la defensa REAL de COMPA-03** (Pattern 1 de 14-RESEARCH.md — el checkbox de 14-02 es solo capa de UX, `?ids=` es alcanzable por URL directa sin pasarlo) — cualquier fallo en cualquier capa renderiza mensaje explícito y NUNCA llega a `<TablaComparacion>`. Rentabilidad implícita de zona calculada en lote por comuna distinta, no por oportunidad. Checkpoint humano (Task 3, 8 pasos end-to-end) aprobado por el usuario en vivo sin hallazgos: checkbox→"Comparar (N)"→URL con ids reales→tope de 5 deshabilita checkboxes→tabla con resaltado solo en 3 filas correctas→F5 idéntico (estado en URL, COMPA-04)→1 solo id→"Selección inválida"→mezcla tipo/operación por URL manual→"Comparación inválida" (COMPA-03 confirmado en sentido fuerte)→dado_de_baja incluido con badge. Sin deviations. `npx tsc --noEmit` y `npm run build` limpios. DETA-01..07 (Fase 13) + COMPA-01..04 (Fase 14) completos — Fase 15 (Informe Exportable) sin bloqueadores. Ver 14-03-SUMMARY.md.

- [15-01] **Infraestructura compartida del informe exportable (2026-08-02, commits `d3c2813`/`b4b8e07`/`a8a53c9`)** — hallazgo nuevo no anticipado en 15-RESEARCH.md: ninguno de los 2 precedentes de `@media print` en el proyecto (`clientes/[id]/informe`, `cadenas-comerciales/[id]/compliance`) había sido probado imprimiendo de verdad dentro del layout real — el `<aside>` del sidebar (`components/dashboard/sidebar.tsx`) no tenía `print:hidden`, así que cualquier impresión bajo `(dashboard)` (incluidos los precedentes existentes, no solo los 2 informes nuevos) imprimía con la nav lateral superpuesta; fix de 2 líneas (`print:hidden` en `<aside>`, `print:pl-0` en el `<main>` de `app/(dashboard)/layout.tsx`) aplicado globalmente al grupo de rutas, no solo a las páginas nuevas. `formatTimestampCorto` promovida de `historial-tab.tsx` (función local `formatTimestamp`) a `lib/formato-fecha.ts`, hermana de `formatFechaCorta` — RESEARCH.md Pitfall 3 (date-only vs. timestamptz) ahora resuelto con un solo formateador por caso, sin tercera reimplementación. 4 componentes nuevos en `components/mercado-inmobiliario/informe/`: `PrintButton` (copia casi verbatim del precedente de compliance), `PreparadoPorPara` (INFO-04, 2 inputs controlados sin persistencia — decisión ya resuelta en RESEARCH.md Open Question 3), `PortadaInforme` (embebe `PreparadoPorPara` directamente, satisface INFO-04 por construcción), `MetodologiaInforme` (array de `FuenteMetodologia` genérico, sirve tanto al informe individual de 15-02 como al de comparación de 15-03 sin cambios — declara `usoFallback` explícitamente cuando aplica, nunca omite una comuna sin banda). Sin deviations, `npx tsc --noEmit` limpio. Desbloquea 15-02 y 15-03. Ver 15-01-SUMMARY.md.

- [15-02/15-03] **Fase 15 y milestone v1.6 COMPLETOS — informe individual y de comparación (2026-08-02, commits `eab3286`/`c7132ff`/`9805c8b`)** — 15-02: `app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx` (Server Component nuevo, re-invoca `obtenerOportunidadPorId`/`obtenerComparablesOportunidad` directamente sin API route intermedia — mejora explícita sobre los 2 precedentes `@media print` existentes) + link "Exportar informe" en `[id]/page.tsx` junto a "Ver aviso original". Cierra INFO-01 y las porciones de INFO-03 (vigencia dual: "Generado el" en portada vs. "Última verificación de este dato" visible en el cuerpo junto al precio, Pitfall 4)/INFO-04 (campo "preparado por/para" editable) correspondientes al informe individual. 15-03: `comparar/informe/page.tsx` (re-valida rango 2-5/existencia/homogeneidad tipoPropiedad-operación de forma independiente — Pitfall 5, la ruta es bookmarkable sin pasar por `comparar/page.tsx` — y reutiliza `TablaComparacion` de Fase 14 sin reconstruirla) + link "Exportar informe" en `comparar/page.tsx` usando el set de ids ya-fetcheado. Cierra INFO-02 y las porciones de INFO-03/INFO-04 del informe de comparación. Ambos checkpoints humanos (Task 3) aprobados en vivo por el usuario sin hallazgos — 15-02: navegación completa, portada, guard `precioValido`, vigencia por fila, metodología, Cmd+P sin sidebar/toolbar; 15-03: defensa de Pitfall 5 verificada con URL armada a mano. **Nota de ejecución paralela (sin worktree/branch aislado, `branching_strategy: none`):** el commit `eab3286` quedó atribuido solo al mensaje de 15-02 pero su diff incluye ambos archivos nuevos (`[id]/informe/page.tsx` de 15-02 y `comparar/informe/page.tsx` de 15-03) por un `git add` de la ejecución concurrente — contenido verificado idéntico byte a byte contra lo que cada plan especificaba, sin pérdida de datos, solo atribución cosmética del mensaje de commit. `npx tsc --noEmit` limpio. INFO-01 a INFO-04 completos — Fase 15 y milestone v1.6 (Reportes Profesionales de Oportunidades) cerrados. Ver 15-02-SUMMARY.md y 15-03-SUMMARY.md.

- [16-02] **geocodeComunaCentroide() + lib/cabida-comercial.ts tipos client-safe (2026-08-03, commits `f2f771b`/`60f8aba`/`9816e4c`)** — `lib/geocoding.ts`: nueva `geocodeComunaCentroide(comuna)` junto a `geocodeDireccion()` (mismo archivo, comparte el throttle module-level de 1.1s y `fetchWithTimeout` — 16-RESEARCH.md confirmó que no existía ninguna fuente de coordenadas de comuna en el repo y que reusar `geocodeDireccion` con `direccion=''` producía una query con coma colgante); usa parámetros Nominatim ESTRUCTURADOS (`city=`/`country=Chile`), no el `q=` de texto libre pensado para direcciones calle+número — verificado por test que `has('q')` es `false`. `lib/cabida-comercial.ts` (nuevo, mirror client-safe de `lib/zonificacion.ts`): tipos canónicos `UbicacionCabida`/`UbicacionPrecision`/`IsocronaMetodo`/`IsocronaResultado`/`FormatoComercial`/`AnalisisCabidaComercial`/`CabidaComercialAnalisisResponse` + `consultarCabidaComercial()` fetch helper hacia `/api/cabida-comercial/analisis`; `UbicacionPrecision` deja la unión abierta (`'aproximada' | 'centroide_comuna'`, sin `'exacta'` todavía) para no cerrar a 2 valores hardcodeados algo que un futuro modo standalone por dirección (CABI-03) necesitará extender. Nombre `consultarCabidaComercial` (no `obtenerAnalisisCabidaComercial`, reservado para la función pura server-only de Plan 16-04) sigue el precedente `lookupZonificacion()`/`persistZonificacionParaProyecto()`. Sin deviations, `npx tsc --noEmit` limpio, 2/2 tests en verde. Desbloquea Plan 16-04 (server orchestration) y 16-05 (ruta + UI). Ver 16-02-SUMMARY.md.

- [16-03] **cabida_comercial_cache migración escrita y aplicada en vivo (2026-08-03, commit `436aa6d`)** — `supabase/migrations/20260809_cabida_comercial_cache.sql`: tabla ANGOSTA a propósito (id, lat_r/lng_r `numeric(9,6)`, modo, minutos, isocrona_status/metodo/geometria/proveedor, consultado_el, created_at) — deliberadamente SIN columnas `demografia_*`/`competencia_*` que Fase 17/18 todavía no llenan; se agregarán vía migración ADITIVA cuando corresponda, mismo patrón que `20260730_zonificacion.sql`→`20260730_zonificacion_v2.sql`. Índice único `(lat_r, lng_r, modo, minutos)` para upsert determinista (Plan 16-04). RLS de solo lectura para `authenticated` (`cabida_comercial_cache_read`), sin política de INSERT/UPDATE — escrituras solo vía `createServiceClient()`. 3 CHECK constraints: `isocrona_status IN ('pendiente','encontrado','error')`, `isocrona_metodo IS NULL OR IN ('red_vial','circulo_equivalente')` (nunca indistinguible a nivel de fila entre isócrona real y degradada — evita el bug del Pitfall 1 de 16-RESEARCH.md persistido en DB), `modo IN ('caminando','auto')`. Task 2 (checkpoint human-verify, aplicar la migración) resuelto por el orquestador vía `mcp__supabase__apply_migration` — el agente ejecutor de Task 1 no tenía tools MCP/browser/DATABASE_URL bound en su sesión, mismo fallback ya documentado en `10-01-SUMMARY.md`. Verificado con `SELECT * FROM cabida_comercial_cache LIMIT 1;` sin error "relation does not exist". Sin deviations. Desbloquea Plan 16-04 (upsert contra esta tabla). Ver 16-03-SUMMARY.md.

- [18-01] **Tipos de competencia por formato agregados a lib/cabida-comercial.ts (2026-08-03, commits `340680e`/`bc7eb0e`)** — base tipada de Fase 18, wave 1, plan único del que dependen los 5 planes restantes (Overpass, seed list, geocoding SII, lógica de confianza TDD, orquestador). `CompetidorDetectado`/`ResultadoCompetenciaFormato`/`FuenteCompetidor`/`NivelConfianza` agregados de forma 100% aditiva (verificado con `git diff`, cero líneas eliminadas) al final del archivo ya estable de Phase 16; `AnalisisCabidaComercial.competencia?` opcional hasta que 18-07 lo pueble. `coberturaConocida` y `confianzaGlobal` son obligatorios (no opcionales) en `ResultadoCompetenciaFormato` desde el primer commit — cierra COMPE-05 a nivel de tipo, nunca solo documental. El riesgo de colisión de nombres `FormatoComercial` (mismo nombre, value-set distinto y parcialmente solapado en `lib/terrenos-comercial.ts`, feature no relacionada de terrenos) queda cerrado con un guard automatizado real, no cosmético: `tests/unit/cabida-comercial-tipos.test.ts` usa `@ts-expect-error` para forzar el rechazo de `FormatoTerreno` como `FormatoCabida`; verificado manualmente removiendo el `@ts-expect-error` y confirmando que `npm run typecheck` falla con `TS2322`, luego restaurado. Sin deviations. `npm run typecheck` y `npm run test -- tests/unit/cabida-comercial-tipos.test.ts` (3/3) limpios. Desbloquea toda la wave 2 de Fase 18. Ver 18-01-SUMMARY.md.

- [18-04] **Geocoding on-demand de cadenas_sucursales con cache-through (2026-08-03, commits `e88ca70`/`35eeea8`)** — migración aditiva `lat`/`lng`/`geocodificado_el` (nullable) aplicada en vivo contra Supabase; `obtenerCadenasGeocodificadasPorComuna(comuna)` agregada a `lib/cadenas-sucursales-server.ts`, reusa `geocodeDireccion()` de Fase 16 verbatim (mismo throttle/contrato `{ ok: false }`). Cache-through directo en la fila (no tabla dedicada como `cabida_comercial_cache`) porque cada dirección SII se geocodifica como máximo una vez en su vida útil. `normalizarNombreComuna()` aplicado en JS post-fetch, mismo patrón que `obtenerSenalesExpansionPorComuna()` — `cadenas_sucursales.comuna` viene en MAYÚSCULAS sin tildes del SII. Checkpoint humano verificado en vivo DOS veces (agente ejecutor + orquestador independiente vía queries directas a Supabase): coordenadas reales de Maipú dentro de rango geográfico de Chile, cache-hit instantáneo en la segunda consulta, 3 direcciones no geocodificables excluidas limpiamente sin excepciones. Usuario aprobó "aprobado". Sin deviations. Desbloquea Plan 18-06 (composición con cadenas SII — sustituye tag OSM genérico por nombre real de cadena, "Líder Express"). Ver 18-04-SUMMARY.md.

- [18-05] **Núcleo puro de degradación de confianza — calcularResultadoCompetencia() (2026-08-03, commits `3286596`/`2b2048c`/`51b4f36`)** — TDD RED→GREEN→REFACTOR sobre `lib/competencia-formato.ts`, cierra el pitfall más peligroso de la fase (Pitfall 1 de 18-RESEARCH.md / Pitfall 3 de PITFALLS.md): `competidores.length === 0` NUNCA se lee como "sin competencia confirmada" — `confianzaGlobal` es siempre `'baja'` en ese caso. Tope duro `TOPE_CONFIANZA_GLOBAL = 'media'` en código (constante explícita, no solo comentario) porque `coberturaConocida` es siempre `false` en v1.7 — verificado por el test más importante del plan (competidor con confianza individual `'alta'` nunca produce `confianzaGlobal: 'alta'`). Disclosure construido con constantes nombradas (`DISCLOSURE_GAP_SUPER_MINI`/`DISCLOSURE_GAP_STRIP_POWER`, extraídas en el REFACTOR) que citan el id exacto de `data-sources.yaml` (`sii-nomina-sucursales-holdings-sin-tiendas` para super/mini, `strip-power-centers-chile-seed` para strip/power) y mencionan Unimarc / Grupo Patio / Más Center explícitamente. Función 100% pura (sin fetch/I/O), 9/9 tests y `tsc --noEmit` limpios. Sin deviations. Desbloquea Plan 18-06 (orquestador async que resuelve `CompetidorDetectado[]` desde Overpass/seed list/SII geocodificado y llama a esta función como paso final). Ver 18-05-SUMMARY.md.

- [17-01] **Módulo de población censal — obtenerPoblacionEnPoligono() con URL de ArcGIS corregido (2026-08-03, commits `7789fdf`/`3128360`)** — `lib/censo-manzana-server.ts`: función pura por geometría (nunca por `oportunidadId`), consulta POST en vivo contra el FeatureServer CORRECTO de manzanas Censo 2017, `services9.arcgis.com/kKJR3Qt68ohAWuet` (158.927 manzanas nacionales, 49.974 en RM) — corrige el URL citado a nivel de milestone en STACK.md/ARCHITECTURE.md (`services3.arcgis.com/cTnMkBRk4HWkUCRo`, SOLO 8 comunas de Atacama, CERO cobertura RM, falla silenciosamente con `features:[]` en vez de error). Corrección verificada en vivo dos veces: en `17-RESEARCH.md` y de nuevo en el checkpoint humano de este plan (Task 3), corriendo el mismo polígono de Providencia contra ambos servicios — el correcto retorna manzanas reales con `TOTAL_PERS`/`TOTAL_VIVI` no-cero, el incorrecto retorna HTTP 200 con `features:[]`. Guard de regresión en tests (`not.toContain('services3.arcgis.com/cTnMkBRk4HWkUCRo')`) para que un revert accidental falle en CI. Migración aditiva `demografia_*` (6 columnas: status/total_personas/total_viviendas/manzanas_intersectadas/censo_ano/consultado_el) aplicada en vivo contra `cabida_comercial_cache`, verificada por el orquestador vía `information_schema.columns` (agente ejecutor de Task 1 sin tools MCP bound, mismo fallback de 10-01/16-03). 8/8 tests unitarios. Sin deviations. Plan independiente de Phase 16 — desbloquea Plan 17-03 (cache-through, gateado por disponibilidad de isócrona real de Fase 16). Ver 17-01-SUMMARY.md.

- [17-02] **Consumo macro-zona (EPF + CASEN) — obtenerConsumoEstimado() con datos reales, parciales pero honestos (2026-08-03, commits `ea8be0b`/`0e442fc`/`f385653`)** — `lib/consumo-macro-zona.ts`: tabla estática EPF (IX EPF, INE, oct2021-sept2022) con SOLO 3 de 12-13 categorías CCIF con cifra citable confirmada (Alimentación 21.2%, Vivienda 16.0%, Transporte 15.0%) — el resto queda explícitamente `participacionPct: null` (nunca inventado), expuesto vía `categoriasPendientes` para que la UI lo declare. Tabla `CASEN_POBREZA_POR_COMUNA` con las 36 comunas RM que tienen oportunidades reales hoy en `mercado_locales_listings` (no las ~335 comunas nacionales), cada fila citando `fuenteUrl`+`transcritoEl`. **El checkpoint humano de Task 2 (transcripción CASEN) quedó originalmente bloqueado**: el entorno del executor no resolvía DNS hacia `datasocial.ministeriodesarrollosocial.gob.cl`. El orquestador resolvió el bloqueo sourceando los datos reales directamente desde el dominio hermano `observatorio.ministeriodesarrollosocial.gob.cl` — descargó y parseó en vivo (curl + openpyxl) el XLSX oficial "Estimaciones de Tasa de Pobreza por ingresos por Comuna, SAE 2024" (`storage/docs/pobreza-comunal/2024/SAE_ingresos_2024.xlsx`), confirmó que es una estructura XLSX real (no un número inventado) y que las 36 comunas objetivo están TODAS presentes en la fuente — 0 pendientes, 0 interpoladas. `tasaPobrezaPersonas` se almacena como número-porcentaje (ej. `10.17` = 10.17%), consistente con el tipo ya fijado en Task 1, deliberadamente distinto de la convención fracción-0-1 de EPF (no se introdujo una segunda convención). 4/4 tests unitarios (dato real, comuna ausente → `null` sin lanzar, `categoriasPendientes`, normalización de nombre). Nueva entrada `epf-casen-consumo-estimado` en `data-sources.yaml` documentando ambos gaps. Sin deviations de código (Reglas 1-3); el único desvío fue de infraestructura (DNS), resuelto por el flujo de checkpoint humano ya previsto en el plan. Desbloquea Plan 17-03/17-04 (wiring de `obtenerConsumoEstimado()` dentro del análisis de cabida comercial). Ver 17-02-SUMMARY.md.

- [17-03] **Wiring de demografía en obtenerAnalisisCabidaComercial() — BLOQUEADO por prerequisito de Phase 16 (2026-08-03, sin commits de código)** — plan gateado por diseño (clonado del patrón de 18-07) sobre `lib/cabida-comercial-server.ts`, artefacto que debe construir Phase 16/Plan 16-04. Task 1 (check explícito del prerequisito, ANTES de tocar cualquier archivo) corrió en vivo: `ls lib/cabida-comercial-server.ts` → `ARCHIVO NO EXISTE`; `grep -n "export async function obtenerAnalisisCabidaComercial" lib/cabida-comercial-server.ts` → `FUNCION NO ENCONTRADA`. Resultado ESPERADO — Phase 16 sigue paused desde 16-01 (403 de HeiGIT/ORS, ver arriba), Plan 16-04 (wave 2/3 de Fase 16) todavía no se ejecuta. Siguiendo la instrucción explícita del plan: se DETUVO la ejecución en ese punto, sin crear `lib/cabida-comercial-server.ts` desde cero, sin stub, sin función parcial fabricada — Task 2 (wiring de `obtenerPoblacionEnPoligono()` + `obtenerConsumoEstimado()` dentro de `obtenerAnalisisCabidaComercial()`) queda sin ejecutar. Cero archivos de código tocados (`git status --short` antes/después idéntico salvo el propio SUMMARY/STATE). Listo para reintentarse sin ningún cambio adicional apenas Phase 16/16-04 complete — en ese momento solo falta este plan y, tras él, Plan 17-04 (visibilidad UI). Ver 17-03-SUMMARY.md.

- [18-06] **Orquestador async obtenerCompetenciaPorFormato() (2026-08-03, commits `64406f2`/`bd6f681`)** — agregado a `lib/competencia-formato.ts` (mismo archivo que `calcularResultadoCompetencia()` de Plan 18-05, sin tocarla): ramifica por formato — `strip_center`/`power_center` llaman `obtenerCompetidoresSeedList()` directamente (síncrono); `supermercado`/`minimarket` corren `obtenerCompetidoresOverpass()` y `obtenerCadenasGeocodificadasPorComuna()` en `Promise.all` (dos recursos externos independientes — Overpass API vs. Nominatim — con throttles independientes, verificado con un test que dispara ambos mocks con promesas controladas antes de resolver ninguna, confirmando que no es secuencial). Cruce espacial COMPE-06: para cada POI OSM, busca la sucursal SII geocodificada más cercana dentro de 150m (`turf.distance`) — con match, sustituye `nombre`/`fuente` por el dato real de cadena y sube `confianza` individual a `'alta'` (corroboración independiente, Pitfall 3 de PITFALLS.md); sin match, conserva el POI de Overpass tal cual. El resultado final siempre pasa por `calcularResultadoCompetencia()` de 18-05 — no reimplementa la degradación de confianza. Standalone: cero dependencia de `lib/cabida-comercial-server.ts` (Fase 16, no existe todavía) — es la pieza que Plan 18-07 va a importar y llamar. 5 tests nuevos + 9 preexistentes de 18-05, 14/14 en verde; `tsc --noEmit` limpio. Sin deviations, plan ejecutado exactamente como estaba escrito. Desbloquea la lógica de negocio de Plan 18-07 (integración gateada por Fase 16). Ver 18-06-SUMMARY.md.

- [19-01] **Núcleo puro de veredicto — calcularVeredictoCabida() con banding por terciles (2026-08-03, commits `87e238b`/`46c049d`/`2cd20f7`)** — TDD RED→GREEN→REFACTOR sobre `lib/veredicto-cabida.ts` (archivo nuevo, plan completamente ungateado — construido contra tipos YA reales de Fases 17/18, sin depender de `lib/cabida-comercial-server.ts`/`lib/isocrona-server.ts` ni de que Phase 16/17-03/18-07 completen). Función pura de 3 estados (`evidencia_de_espacio`/`mercado_parece_cubierto`/`evidencia_insuficiente`, VERE-01) que SIEMPRE trae `estado`+`confianza` juntos en la misma estructura (VERE-02). **Decisión LOCKED del founder aplicada literalmente:** el corte entre los dos primeros estados NUNCA es un threshold absoluto hardcodeado — la función recibe `percentiles: PercentilesGapScore | null` (p33/p66 + `muestraN`) como tercer parámetro, calculados por el futuro llamador (Plan 19-03, gateado) desde gap scores reales en `cabida_comercial_cache`. Cold-start explícito modelado: hoy CERO análisis reales existen, así que `percentiles` puede ser `null`, y con `muestraN < MUESTRA_MINIMA` (10, elección de ingeniería documentada como tal) el veredicto es SIEMPRE `evidencia_insuficiente` con `razonInsuficiencia: 'muestra_comparativa_insuficiente'` — nunca compara contra terciles poco confiables. `gapScore` (competidores/1000hab, VERE-04) es `null` únicamente en los 2 modos de falla de datos base (demografia/competencia undefined, o población no utilizable) — nunca fabricado, pero SÍ se reporta como número real en confianza degradada y cold-start (el dato existe, lo que falta es contexto para concluir). Isócrona degradada a círculo fuerza `confianza: 'baja'` incondicionalmente. Tope duro `TOPE_CONFIANZA_VEREDICTO = 'media'` verificado por el test más importante del plan (confianzaGlobal de competencia hipotética `'alta'` nunca produce confianza `'alta'` en el veredicto — mismo criterio defensivo que `TOPE_CONFIANZA_GLOBAL` de Plan 18-05, aplicado en profundidad aunque el upstream ya cape). Tipo local `DemografiaYConsumo`/`AnalisisParaVeredicto` espejo exacto del contrato de 17-03-PLAN.md (compone `PoblacionCensoResultado`+`ConsumoEstimadoResultado`, ya reales) porque `lib/cabida-comercial.ts` todavía no declara `demografia?` (Plan 17-03 sigue bloqueado) — documentado inline que Plan 19-03 debe reconciliar shapes cuando 17-03 corra. 14/14 tests, `tsc --noEmit` limpio. Único desvío: fixture de `consumo` en el test (copiado del skeleton del plan) le faltaban 3 campos requeridos de `ConsumoEstimadoResultado` real (`comuna`/`nivelGeografico`/`fuente`) — fix mecánico de tipos, cero cambio de lógica. Desbloquea Plan 19-03 (orquestador gateado, importará esta función sin modificaciones) y da contexto de tipos a Plan 19-04 (tab). Ver 19-01-SUMMARY.md.

- [16-01] **Segundo intento bloqueado — ORS/HeiGIT sigue en 403 horas después de crear la key, hipótesis de propagación descartada (2026-08-03, sin commits de código, ver 16-01-SUMMARY.md)** — reverificación en vivo (POST real a `/v2/isochrones/foot-walking` con coordenada de Providencia) contra 3 combinaciones — `api.openrouteservice.org` raw, `api.heigit.org` raw, `api.heigit.org` Bearer — todas devolvieron `403 {"error":"Access to this API has been disallowed"}`, idéntico byte a byte al primer intento (`b6deec9`) pese a que ya pasaron varias horas desde que se creó la key (~00:10 → reverificado ~09:xx). Cero código fabricado (ni el script de diagnóstico, ni `lib/isocrona-server.ts`, ni su test) porque Task 2 del plan nunca produjo un payload 200 real que justifique escribir un Zod schema. **Próxima acción es del usuario, no del agente:** revisar el dashboard de HeiGIT (verificación de email pendiente, scope de la key limitado a otro servicio que no sea Isochrones, o paso de activación no estándar) o contactar soporte — no relanzar el ejecutor sin haber confirmado un `curl` manual en 200 primero, ya van dos intentos idénticos.

## Session Continuity

Last session: 2026-08-03
Stopped at: Plan 16-01 reintentado y bloqueado de nuevo — reverificación en vivo confirma 403 persistente de ORS/HeiGIT (3/3 combinaciones probadas), cero código fabricado, ver 16-01-SUMMARY.md. Requiere acción manual del usuario (dashboard HeiGIT o soporte) antes de un tercer intento. Plan 19-01 (núcleo puro de veredicto, `calcularVeredictoCabida()` en `lib/veredicto-cabida.ts`) completo — 3/3 tasks (RED/GREEN/REFACTOR), 14/14 tests en verde, `tsc --noEmit` limpio, 1 fix mecánico de tipos en fixture de test (ver deviations en 19-01-SUMMARY.md). Ejecutado en paralelo con Plan 19-02 (mapa) — ambos wave 1, `depends_on: []`. Próximo paso disponible: Plan 19-03 (orquestador, gateado por Phase 16/17-03/18-07) o Plan 19-04 (tab), o retomar Plan 18-07/18-08 — todos siguen gateados por Phase 16.
Resume file: None

---

Nota histórica (2026-07-30, preservada): Phase 11 (Vista de Zonificación en el Proyecto) fully complete — all 8 plans done, final checkpoint (11-08 Task 2) verified live by the orchestrator using a real browser session (discovered and used the project's `BYPASS_AUTH=true` dev-login mechanism — see accumulated context above, this unblocks all future manual/browser testing too). 3 minor non-blocking findings documented in 11-08-SUMMARY.md. Also fixed an unrelated pre-existing production bug along the way (20260705_proyectos_sii.sql migration was never applied live). Phase 12 Wave 1 (12-01/02/03) complete. Phase 12 Wave 2 (12-04) Task 1 automated verification complete (tsc/vitest/git-diff all clean, commit a8b6075) — Task 2 (Phase 12's final blocking human-verify checkpoint) was later resolved live by the orchestrator, closing milestone v1.4 (see Accumulated Context above).
