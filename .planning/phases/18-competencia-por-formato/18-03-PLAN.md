---
phase: 18-competencia-por-formato
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - scripts/geocode-strip-power-seed.mjs
  - lib/strip-power-centers-chile.ts
  - .planning/data-sources.yaml
  - tests/unit/strip-power-centers-chile.test.ts
autonomous: true

must_haves:
  truths:
    - "Para strip center/power center, el sistema detecta competidores usando una lista curada a mano con coordenadas reales — nunca una tag OSM que no existe para este formato (COMPE-02, COMPE-04)"
    - "Ningún lat/lng de la lista curada es inventado — todos vienen de una llamada real de geocoding (script) o son explícitamente null cuando la dirección fuente es 'no confirmada'"
    - "La lista curada documenta sus propios gaps conocidos (Grupo Patio, Más Center) con la misma disciplina que el gap de Unimarc ya documentado (locked decision #4)"
  artifacts:
    - path: "lib/strip-power-centers-chile.ts"
      provides: "STRIP_POWER_CENTERS_CHILE — array estático git-versionado con lat/lng reales pre-geocodificados + obtenerCompetidoresSeedList()"
      contains: "export const STRIP_POWER_CENTERS_CHILE"
    - path: ".planning/data-sources.yaml"
      provides: "Entrada strip-power-centers-chile-seed documentando el gap de cobertura (Grupo Patio, Más Center)"
      contains: "strip-power-centers-chile-seed"
    - path: "scripts/geocode-strip-power-seed.mjs"
      provides: "Script one-off que produjo los lat/lng reales committeados — auditable, no una caja negra"
  key_links:
    - from: "lib/strip-power-centers-chile.ts"
      to: "Nominatim (vía scripts/geocode-strip-power-seed.mjs, geocodificado UNA VEZ al commitear, no en runtime)"
      via: "misma query estructurada 'direccion, comuna, Santiago, Chile' que lib/geocoding.ts geocodeDireccion()"
      pattern: "lat: -3"
---

<objective>
Convertir `.planning/research/SEED-STRIP-POWER-CENTERS-CHILE.md` (documento de investigación ya escrito, ~19 filas RM con confianza por fila) en `lib/strip-power-centers-chile.ts` — un array TypeScript estático, git-versionado, con lat/lng REALES pre-geocodificados una sola vez (nunca fabricados), siguiendo el mismo patrón editorial que `CADENAS_RUT_CONOCIDOS` en `lib/scrapers/sii-nomina-sucursales.ts`.

Purpose: COMPE-04 exige detección de strip/power center vía lista curada porque no existe tag OSM ni fuente pública con direcciones para estos formatos en Chile — este plan construye esa lista, la geocodifica con disciplina de "nunca fabricar" (mismo rigor que el resto del repo), y documenta honestamente sus gaps conocidos (Grupo Patio ~91-158 propiedades, Más Center ~30 strip centers — ninguno de los dos tiene un solo activo nombrado en el seed doc).

Output: `lib/strip-power-centers-chile.ts` con `STRIP_POWER_CENTERS_CHILE[]` + `obtenerCompetidoresSeedList()`, entrada nueva en `data-sources.yaml`, script de geocoding auditable en `scripts/`.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/SEED-STRIP-POWER-CENTERS-CHILE.md
@.planning/phases/18-competencia-por-formato/18-RESEARCH.md
@lib/cabida-comercial.ts
@lib/scrapers/sii-nomina-sucursales.ts
@lib/geocoding.ts
@scripts/seed-petshop.mjs
@scripts/check-data-sources.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Geocodificar las direcciones reales del seed doc (script auditable, ejecutado una vez)</name>
  <files>scripts/geocode-strip-power-seed.mjs</files>
  <action>
ANTES de escribir nada: correr `ls lib/strip-power-centers-chile.ts scripts/geocode-strip-power-seed.mjs 2>/dev/null` y revisar si ya existen (otro agente de esta sesión, "seed-strip-power-centers", pudo haber avanzado en esto en paralelo). Si ya existen con contenido real (lat/lng no-null, no placeholders), leerlos completos y saltar directo a Task 2 usando esos datos en vez de re-geocodificar — documentar esa decisión en el SUMMARY. Si no existen o están vacíos, continuar con este task normalmente.

Crear `scripts/geocode-strip-power-seed.mjs` (plain Node, mismo estilo que `scripts/seed-petshop.mjs` — sin dependencia de tsx/ts-node, este repo no los tiene instalados). El script NO importa `lib/geocoding.ts` (es un módulo TS con paths `@/...` no resolubles desde un script `.mjs` plano) — replica la misma llamada Nominatim con los mismos parámetros/User-Agent, duplicación deliberada y aceptable para un script one-off (mismo patrón que otros scripts de `scripts/`):

```javascript
// Uso: node scripts/geocode-strip-power-seed.mjs
// Geocodifica las direcciones REALES (no "no confirmada") de
// .planning/research/SEED-STRIP-POWER-CENTERS-CHILE.md Tablas 1 y 2 — mismo
// proveedor/throttle/User-Agent que lib/geocoding.ts (duplicado acá porque
// este script no puede importar TS con paths @/ directamente).

const FILAS = [
  // Copiar CADA fila con confianza 'Alta' o 'Media' y dirección NO "no confirmada"
  // de las Tablas 1 y 2 del seed doc — { nombre, direccion, comuna, formato, confianza }
  { nombre: 'Punta Blanca Maipú (Los Pajaritos)', direccion: 'Av. Los Pajaritos 1.948', comuna: 'Maipú', formato: 'strip_center', confianza: 'alta' },
  // ... resto de filas con dirección real (ver seed doc completo — Punta Blanca x8,
  // BTG Pactual x9, Strip center Camino Lonquén, BCenter Alto Jahuel, Paseo Los
  // Trapenses — omitir filas "no confirmada" y las de confianza 'Baja' sin dirección)
]

async function geocode(direccion, comuna) {
  const q = `${direccion}, ${comuna}, Santiago, Chile`
  const params = new URLSearchParams({ q, format: 'json', limit: '1', addressdetails: '1', countrycodes: 'cl' })
  const res = await fetch(`https://nominatim.openstreetmap.org/search?${params}`, {
    headers: { 'User-Agent': 'PermisoHub/1.0 (+https://permisohub.cl; contacto@permisohub.cl)', Accept: 'application/json' },
  })
  const results = await res.json()
  if (!Array.isArray(results) || results.length === 0) return null
  return { lat: parseFloat(results[0].lat), lng: parseFloat(results[0].lon), displayName: results[0].display_name }
}

for (const fila of FILAS) {
  const geo = await geocode(fila.direccion, fila.comuna)
  console.log(JSON.stringify({ ...fila, ...geo }))
  await new Promise((r) => setTimeout(r, 1100)) // mismo throttle 1.1s que lib/geocoding.ts — recurso público compartido
}
```

Correr `node scripts/geocode-strip-power-seed.mjs` y CAPTURAR la salida real (redirigir a un archivo temporal si es útil, ej. `node scripts/geocode-strip-power-seed.mjs > /tmp/seed-geocoded.jsonl`). Estos son los lat/lng REALES que Task 2 debe copiar literalmente — nunca inventar o aproximar un valor si el script no lo produjo.
  </action>
  <verify>El script corre sin lanzar excepciones; su salida (stdout capturado) tiene una línea JSON por fila con `lat`/`lng` numéricos reales (rango Chile: lat entre -18 y -56, lng entre -76 y -66) para la mayoría de las filas — alguna puede fallar (`geo === null`) si Nominatim no resuelve esa dirección exacta, lo cual es aceptable y se refleja como `lat: null, lng: null` en Task 2, no como un error del script.</verify>
  <done>scripts/geocode-strip-power-seed.mjs existe, corrió contra Nominatim real, y su salida capturada contiene lat/lng reales para la mayoría de las ~19 filas con dirección conocida del seed doc.</done>
</task>

<task type="auto">
  <name>Task 2: lib/strip-power-centers-chile.ts + obtenerCompetidoresSeedList() + entrada en data-sources.yaml</name>
  <files>lib/strip-power-centers-chile.ts, .planning/data-sources.yaml, tests/unit/strip-power-centers-chile.test.ts</files>
  <action>
Crear `lib/strip-power-centers-chile.ts`:

```typescript
// Lista curada a mano de strip centers y power centers en Chile (RM) — COMPE-04.
// No existe tag OSM ni fuente pública con direcciones para estos formatos en
// Chile (ver .planning/research/SEED-STRIP-POWER-CENTERS-CHILE.md, investigación
// 2 ago 2026). Mismo patrón editorial que CADENAS_RUT_CONOCIDOS
// (lib/scrapers/sii-nomina-sucursales.ts): array estático, git-versionado,
// editado por PR cuando el equipo confirma un nuevo centro — NO una tabla con
// pipeline de ingesta (nada la actualiza en cron).
//
// lat/lng geocodificados UNA VEZ vía scripts/geocode-strip-power-seed.mjs
// (<fecha real de ejecución>) contra Nominatim real — nunca fabricados. Filas
// con direccion: null (seed doc dice "no confirmada") tienen lat/lng: null a
// propósito — excluidas del matching espacial, visibles solo para auditoría.
//
// GAPS CONOCIDOS (ver data-sources.yaml → strip-power-centers-chile-seed):
// Grupo Patio (~91-158 propiedades) y Más Center (~30 strip centers activos)
// NO tienen ni un solo activo nombrado en esta lista — un conteo de 0 para
// strip_center/power_center en una comuna donde alguno de estos dos operadores
// tiene presencia real NO significa "confirmado: no hay competencia".

export type StripPowerConfianza = 'alta' | 'media' | 'baja'

export interface StripPowerCenterSeed {
  nombre: string
  formato: 'strip_center' | 'power_center'
  operador: string
  comuna: string
  direccion: string | null
  lat: number | null
  lng: number | null
  fuente: string
  confianza: StripPowerConfianza
}

export const STRIP_POWER_CENTERS_CHILE: StripPowerCenterSeed[] = [
  // Poblar con TODAS las filas de SEED-STRIP-POWER-CENTERS-CHILE.md Tablas 1 y 2
  // (Punta Blanca x8, BTG Pactual x9 strip + Paseo Los Trapenses power, Cimenta
  // Puente Alto, Strip center Camino Lonquén, strip/power de Más Center y Cenco
  // Malls con dirección "no confirmada" → lat/lng: null). lat/lng REALES desde
  // la salida capturada de Task 1 — copiar literal, no redondear ni inventar.
  // Ejemplo de una fila (usar el valor REAL producido por el script, este es
  // solo el shape):
  {
    nombre: 'Punta Blanca Maipú (Los Pajaritos)',
    formato: 'strip_center',
    operador: 'Punta Blanca Inversiones',
    comuna: 'Maipú',
    direccion: 'Av. Los Pajaritos 1.948',
    lat: -33.5115, // ejemplo — REEMPLAZAR con el valor real del script
    lng: -70.7676, // ejemplo — REEMPLAZAR con el valor real del script
    fuente: 'https://puntablanca.cl/comercial',
    confianza: 'alta',
  },
  // ... resto de las filas
]
```

Agregar la función de consulta (usa `turf.distance` y opcionalmente `turf.booleanPointInPolygon`, mismo `@turf/turf` que Plan 18-02 ya instaló):
```typescript
import * as turf from '@turf/turf'
import type { CompetidorDetectado, FormatoComercial } from '@/lib/cabida-comercial'

const CONFIANZA_STRIP_POWER_A_NIVEL: Record<StripPowerConfianza, CompetidorDetectado['confianza']> = {
  alta: 'media',   // tope: la lista es curada a mano y con gaps conocidos (Grupo Patio, Más Center) — nunca 'alta' a nivel de competidor individual, por completa que parezca la fila fuente
  media: 'media',
  baja: 'baja',
}

export function obtenerCompetidoresSeedList(
  formato: 'strip_center' | 'power_center',
  origen: { lat: number; lng: number },
  radioM: number,
  isocronaGeometria?: GeoJSON.Polygon | GeoJSON.MultiPolygon | null
): CompetidorDetectado[] {
  const puntoOrigen = turf.point([origen.lng, origen.lat])
  return STRIP_POWER_CENTERS_CHILE
    .filter((c) => c.formato === formato && c.lat != null && c.lng != null)
    .map((c) => {
      const punto = turf.point([c.lng as number, c.lat as number])
      const distanciaM = turf.distance(puntoOrigen, punto, { units: 'meters' })
      return { c, punto, distanciaM }
    })
    .filter(({ distanciaM, punto }) => {
      if (distanciaM > radioM) return false
      if (isocronaGeometria && !turf.booleanPointInPolygon(punto, isocronaGeometria)) return false
      return true
    })
    .map(({ c, distanciaM }): CompetidorDetectado => ({
      nombre: c.nombre,
      formato: c.formato as FormatoComercial,
      fuente: 'seed_list',
      lat: c.lat as number,
      lng: c.lng as number,
      distanciaM: Math.round(distanciaM),
      confianza: CONFIANZA_STRIP_POWER_A_NIVEL[c.confianza],
      direccionLabel: c.direccion ?? undefined,
    }))
}
```

Agregar a `.planning/data-sources.yaml` (mismo shape/formato YAML que las entradas `type: static-kb` existentes — revisar el archivo para el estilo exacto de indentación/campos antes de escribir):
```yaml
- id: strip-power-centers-chile-seed
  name: "Lista curada a mano — strip centers y power centers en Chile (RM)"
  type: static-kb
  owner_files: [lib/strip-power-centers-chile.ts]
  trigger: none
  called_from: []
  freshness_sla_days: null
  last_verified: 2026-08-02
  monitoring: none
  duplicate_of: null
  status: active
  notes: "COMPE-04 — no existe tag OSM ni fuente pública con direcciones para strip/power center en Chile. Seed manual (.planning/research/SEED-STRIP-POWER-CENTERS-CHILE.md, investigación 2 ago 2026) con ~19 filas RM, lat/lng geocodificados una vez vía scripts/geocode-strip-power-seed.mjs. GAP CONOCIDO: Grupo Patio (~91-158 propiedades, líder del mercado ~15% share) y Más Center/IFB Inversiones (~30 strip centers activos en RM) no tienen NINGÚN activo nombrado en esta lista — sus sitios corporativos bloquearon el scraping automatizado (403) o no publican listado propiedad-por-propiedad. Un conteo de 0 competidores strip_center/power_center en una comuna donde alguno de estos dos operadores podría tener presencia NO debe leerse como 'confirmado: sin competencia' — ver lib/competencia-formato.ts (Plan 18-05) que cita este id explícitamente."
```

Escribir `tests/unit/strip-power-centers-chile.test.ts` cubriendo: (a) toda fila con `direccion !== null` tiene `lat`/`lng !== null` (guard contra un geocoding silenciosamente faltante), (b) `obtenerCompetidoresSeedList()` filtra correctamente por formato y radio con un origen sintético, (c) ninguna fila de `STRIP_POWER_CENTERS_CHILE` tiene `confianza: 'alta'` en el resultado de `obtenerCompetidoresSeedList` (el cap está funcionando).
  </action>
  <verify>`npm run test -- tests/unit/strip-power-centers-chile.test.ts` pasa; `npm run typecheck` pasa; `npm run check:data-sources` (si aplica a static-kb) no reporta la nueva entrada como huérfana.</verify>
  <done>lib/strip-power-centers-chile.ts tiene el array completo con lat/lng reales (no placeholders sin reemplazar) + obtenerCompetidoresSeedList(); data-sources.yaml documenta el gap; los 3 casos de test pasan.</done>
</task>

</tasks>

<verification>
`npm run test -- tests/unit/strip-power-centers-chile.test.ts && npm run typecheck` pasan. `grep -c "lat: -3" lib/strip-power-centers-chile.ts` (o similar) confirma que hay valores de latitud reales de Chile, no placeholders `0`/`null` en filas con dirección conocida. `grep "strip-power-centers-chile-seed" .planning/data-sources.yaml` confirma la entrada nueva.
</verification>

<success_criteria>
Para strip_center/power_center, el sistema puede detectar competidores curados a mano con coordenadas reales, con el gap de cobertura (Grupo Patio, Más Center) documentado con la misma disciplina que el gap de Unimarc ya existente en data-sources.yaml.
</success_criteria>

<output>
After completion, create `.planning/phases/18-competencia-por-formato/18-03-SUMMARY.md`
</output>
