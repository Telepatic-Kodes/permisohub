---
phase: 18-competencia-por-formato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cabida-comercial.ts
  - tests/unit/cabida-comercial-tipos.test.ts
autonomous: true

must_haves:
  truths:
    - "Todo resultado de competencia por formato tiene, desde el primer commit, un campo obligatorio de cobertura conocida y confianza global — nunca opcional (COMPE-05)"
    - "El código de Fase 18 nunca puede confundir FormatoComercial de cabida-comercial con el FormatoComercial no relacionado de terrenos-comercial.ts (locked decision #6)"
  artifacts:
    - path: "lib/cabida-comercial.ts"
      provides: "CompetidorDetectado, ResultadoCompetenciaFormato, FuenteCompetidor, NivelConfianza types; AnalisisCabidaComercial extendido con competencia? opcional"
      contains: "export interface ResultadoCompetenciaFormato"
    - path: "tests/unit/cabida-comercial-tipos.test.ts"
      provides: "Guard automatizado contra la colisión de nombres FormatoComercial"
  key_links:
    - from: "lib/cabida-comercial.ts"
      to: "lib/terrenos-comercial.ts"
      via: "NO import — tipos deliberadamente no compartidos, distinción verificada por test de tipos"
      pattern: "FormatoComercial"
---

<objective>
Extender `lib/cabida-comercial.ts` (ya construido en Phase 16, estable) con los tipos que Fase 18 necesita para representar competidores detectados por formato: `CompetidorDetectado`, `ResultadoCompetenciaFormato`, `FuenteCompetidor`, `NivelConfianza`. Esta es la base tipada sobre la que corren TODOS los demás planes de Fase 18 (Overpass, seed list, geocoding SII, lógica de confianza) — se planifica primero y en su propia wave porque 3 planes paralelos de la wave siguiente dependen de ella.

Purpose: Sin estos tipos como contrato compartido desde el día 1, cada módulo de detección (Overpass, seed list, SII) inventaría su propia forma de resultado y la composición final (Plan 18-06) tendría que reconciliar formas incompatibles. También es donde se cierra, de forma verificable y no solo documental, el riesgo de colisión de nombres `FormatoComercial` identificado en la investigación de fase (`lib/terrenos-comercial.ts` exporta un tipo con el mismo nombre y un value-set distinto para una feature no relacionada).

Output: `lib/cabida-comercial.ts` con los 4 tipos nuevos + `AnalisisCabidaComercial.competencia?` opcional (additive, no rompe consumidores actuales de Phase 16). Un test que falla en tiempo de compilación si alguna vez se intenta unificar/confundir los dos `FormatoComercial`.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-competencia-por-formato/18-RESEARCH.md
@lib/cabida-comercial.ts
@lib/terrenos-comercial.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender lib/cabida-comercial.ts con los tipos de competencia</name>
  <files>lib/cabida-comercial.ts</files>
  <action>
Agregar, de forma ADITIVA (no tocar ningún tipo/función existente de Phase 16 — `UbicacionCabida`, `IsocronaResultado`, `FormatoComercial`, `consultarCabidaComercial` quedan intactos), al final del archivo:

```typescript
export type FuenteCompetidor = 'osm' | 'seed_list' | 'sii_geocodificado'
export type NivelConfianza = 'alta' | 'media' | 'baja'

export interface CompetidorDetectado {
  nombre: string                  // nombre real de cadena (seed_list / sii_geocodificado) o tag crudo de OSM (osm)
  formato: FormatoComercial       // reusa el FormatoComercial YA definido en ESTE archivo — nunca importar el de terrenos-comercial.ts
  fuente: FuenteCompetidor
  lat: number
  lng: number
  distanciaM: number
  confianza: NivelConfianza       // por-competidor: ej. una fila seed_list con direccion:null nunca llega a 'alta'
  direccionLabel?: string
}

export interface ResultadoCompetenciaFormato {
  formato: FormatoComercial
  competidores: CompetidorDetectado[]
  coberturaConocida: boolean      // false = la(s) fuente(s) subyacentes para ESTE formato son conocidas como incompletas
  confianzaGlobal: NivelConfianza // NUNCA derivar solo de competidores.length (COMPE-05) — ver Plan 18-05
  disclosure: string              // línea human-readable, renderizada siempre junto al conteo — nunca omitida
  consultadoEl: string
}
```

Y extender `AnalisisCabidaComercial` (campo NUEVO, opcional — no rompe a quien todavía no lo lee):
```typescript
export interface AnalisisCabidaComercial {
  formato: FormatoComercial
  isocrona: IsocronaResultado
  competencia?: ResultadoCompetenciaFormato   // NUEVO (Fase 18) — opcional hasta que Plan 18-07 lo pueble
  generadoEl: string
}
```

Agregar un comentario de bloque INMEDIATAMENTE antes de `CompetidorDetectado` (mismo estilo que los comentarios existentes del archivo) advirtiendo explícitamente sobre la colisión de nombres:
```typescript
// ADVERTENCIA: lib/terrenos-comercial.ts TAMBIÉN exporta un tipo llamado
// FormatoComercial ('local' | 'strip_center' | 'power_center') para una
// feature no relacionada (potencial de desarrollo de terrenos por
// superficie). Tiene un value-set DISTINTO y PARCIALMENTE solapado con el
// FormatoComercial de este archivo ('supermercado' | 'minimarket' |
// 'strip_center' | 'power_center'). CUALQUIER archivo de Fase 18 debe
// importar FormatoComercial SOLO desde '@/lib/cabida-comercial' — nunca
// desde '@/lib/terrenos-comercial'. Ver tests/unit/cabida-comercial-tipos.test.ts.
```

NO modificar `lib/terrenos-comercial.ts` — permanece intacto, es de un dominio no relacionado (Terrenos).
  </action>
  <verify>`npm run typecheck` pasa sin errores nuevos; `grep -n "export interface ResultadoCompetenciaFormato" lib/cabida-comercial.ts` encuentra la definición.</verify>
  <done>lib/cabida-comercial.ts exporta los 4 tipos nuevos + AnalisisCabidaComercial.competencia?, compila limpio, y los exports/tipos previos de Phase 16 no cambiaron de forma (mismo nombre, misma forma).</done>
</task>

<task type="auto">
  <name>Task 2: Test de colisión de tipos FormatoComercial (guard automatizado, no solo comentario)</name>
  <files>tests/unit/cabida-comercial-tipos.test.ts</files>
  <action>
Crear `tests/unit/cabida-comercial-tipos.test.ts` siguiendo el estilo de `tests/unit/geocoding-comuna-centroide.test.ts` (vitest, `describe`/`it`/`expect`). El test debe:

1. Importar `FormatoComercial` como `FormatoCabida` desde `@/lib/cabida-comercial` y confirmar en runtime que los 4 valores objetivo (`'supermercado'`, `'minimarket'`, `'strip_center'`, `'power_center'`) son asignables (test simple con un array tipado).
2. Importar `FormatoComercial` como `FormatoTerreno` desde `@/lib/terrenos-comercial` y usar un `// @ts-expect-error` para confirmar en TIEMPO DE COMPILACIÓN que un valor válido de `FormatoTerreno` (`'local'`) NO es asignable a `FormatoCabida` — si algún día alguien "simplifica" unificando ambos tipos, este `@ts-expect-error` deja de tener error que suprimir y **falla la compilación/typecheck**, deteniendo el merge.
3. Importar `CompetidorDetectado` y `ResultadoCompetenciaFormato` desde `@/lib/cabida-comercial` y construir un objeto literal mínimo válido de cada uno (compila = la forma es correcta, no hace falta lógica de negocio en este test).

Ejemplo de estructura (adaptar nombres/imports reales, no copiar literal si difiere):
```typescript
import { describe, expect, it } from 'vitest'
import type { FormatoComercial as FormatoCabida, CompetidorDetectado, ResultadoCompetenciaFormato } from '@/lib/cabida-comercial'
import type { FormatoComercial as FormatoTerreno } from '@/lib/terrenos-comercial'

describe('FormatoComercial — colisión de nombres cabida-comercial vs terrenos-comercial', () => {
  it('cabida-comercial.FormatoComercial incluye los 4 formatos objetivo de Fase 18', () => {
    const valores: FormatoCabida[] = ['supermercado', 'minimarket', 'strip_center', 'power_center']
    expect(valores).toHaveLength(4)
  })

  it('terrenos-comercial.FormatoComercial NO es intercambiable con cabida-comercial.FormatoComercial (tipos deliberadamente distintos)', () => {
    const valorTerreno: FormatoTerreno = 'local'
    // @ts-expect-error 'local' no es un FormatoComercial válido en cabida-comercial — si este error deja de existir, alguien unificó los dos tipos por error
    const cruzado: FormatoCabida = valorTerreno
    expect(valorTerreno).toBe('local')
    void cruzado
  })

  it('CompetidorDetectado y ResultadoCompetenciaFormato tienen la forma esperada (compila = correcto)', () => {
    const competidor: CompetidorDetectado = {
      nombre: 'Santa Isabel',
      formato: 'supermercado',
      fuente: 'osm',
      lat: -33.45,
      lng: -70.66,
      distanciaM: 350,
      confianza: 'media',
    }
    const resultado: ResultadoCompetenciaFormato = {
      formato: 'supermercado',
      competidores: [competidor],
      coberturaConocida: false,
      confianzaGlobal: 'media',
      disclosure: 'test',
      consultadoEl: new Date().toISOString(),
    }
    expect(resultado.competidores).toHaveLength(1)
  })
})
```
  </action>
  <verify>`npm run test -- tests/unit/cabida-comercial-tipos.test.ts` pasa (3 tests verdes); `npm run typecheck` pasa (el `@ts-expect-error` debe seguir siendo necesario — si se elimina el error real, tsc reporta "Unused '@ts-expect-error' directive" y el typecheck falla, que es la señal de alarma deseada).</verify>
  <done>El test corre y pasa; borrar deliberadamente el `@ts-expect-error` y correr `npm run typecheck` produce un error de compilación (verificado manualmente una vez, luego restaurado) — confirma que el guard es real, no cosmético.</done>
</task>

</tasks>

<verification>
`npm run typecheck && npm run test -- tests/unit/cabida-comercial-tipos.test.ts` — ambos pasan. `git diff lib/cabida-comercial.ts` muestra solo adiciones (ningún tipo/función de Phase 16 modificado o eliminado).
</verification>

<success_criteria>
lib/cabida-comercial.ts expone los 4 tipos nuevos de competencia listos para ser importados por los 5 planes restantes de Fase 18 (Overpass, seed list, geocoding SII, lógica de confianza TDD, orquestador). El riesgo de colisión de nombres documentado en la investigación de fase queda cerrado con un test que falla si se reintroduce.
</success_criteria>

<output>
After completion, create `.planning/phases/18-competencia-por-formato/18-01-SUMMARY.md`
</output>
