---
phase: 18-competencia-por-formato
plan: 06
type: execute
wave: 3
depends_on: ["18-02", "18-03", "18-04", "18-05"]
files_modified:
  - lib/competencia-formato.ts
  - tests/unit/competencia-formato-orquestador.test.ts
autonomous: true

must_haves:
  truths:
    - "El usuario (vía la función) puede pedir competencia para uno de los 4 formatos objetivo y obtener el resultado correcto según el formato (COMPE-01)"
    - "El nombre real de cadena reemplaza el tag OSM crudo cuando un competidor OSM tiene una sucursal SII geocodificada suficientemente cerca (COMPE-06, ej. 'Líder Express' en vez de 'supermarket')"
    - "Las llamadas a Overpass y a Nominatim (geocoding SII) corren en paralelo, no una detrás de otra — dos recursos externos independientes con throttles independientes"
  artifacts:
    - path: "lib/competencia-formato.ts"
      provides: "obtenerCompetenciaPorFormato(ubicacion, formato, isocronaGeometria?, radioM?) — orquestador async que compone Overpass + seed list + SII geocoding + calcularResultadoCompetencia"
      exports: ["obtenerCompetenciaPorFormato"]
  key_links:
    - from: "lib/competencia-formato.ts (obtenerCompetenciaPorFormato)"
      to: "lib/overpass-competencia.ts (obtenerCompetidoresOverpass)"
      via: "llamado para supermercado/minimarket, en Promise.all junto al geocoding SII"
      pattern: "obtenerCompetidoresOverpass\\("
    - from: "lib/competencia-formato.ts (obtenerCompetenciaPorFormato)"
      to: "lib/cadenas-sucursales-server.ts (obtenerCadenasGeocodificadasPorComuna)"
      via: "cruce espacial por proximidad (turf.distance) contra los POIs de Overpass, para sustituir nombre real de cadena"
      pattern: "obtenerCadenasGeocodificadasPorComuna\\("
    - from: "lib/competencia-formato.ts (obtenerCompetenciaPorFormato)"
      to: "lib/strip-power-centers-chile.ts (obtenerCompetidoresSeedList)"
      via: "llamado para strip_center/power_center"
      pattern: "obtenerCompetidoresSeedList\\("
    - from: "lib/competencia-formato.ts (obtenerCompetenciaPorFormato)"
      to: "lib/competencia-formato.ts (calcularResultadoCompetencia)"
      via: "paso final — envuelve la lista compuesta de competidores en el ResultadoCompetenciaFormato con confianza degradada"
      pattern: "calcularResultadoCompetencia\\("
---

<objective>
Implementar `obtenerCompetenciaPorFormato()` en `lib/competencia-formato.ts` (mismo archivo donde Plan 18-05 dejó la función pura `calcularResultadoCompetencia`) — el orquestador ASYNC que compone las 3 fuentes de datos ya construidas en waves anteriores (Overpass, seed list, geocoding SII) en un único resultado por formato, incluyendo el cruce espacial que sustituye un tag OSM genérico por el nombre real de cadena (COMPE-06).

Purpose: Esta es la pieza de composición que Plan 18-07 (gateada por Phase 16) simplemente va a IMPORTAR Y LLAMAR — toda la lógica de negocio real (qué fuente usar por formato, cómo cruzar OSM con SII, cómo aplicar la degradación de confianza) vive acá, completamente standalone y testeable, sin tocar `lib/cabida-comercial-server.ts` (que todavía no existe). Esto es lo que hace posible que Fase 18 esté genuinamente lista para ejecutar antes de que Fase 16 termine.

Output: `obtenerCompetenciaPorFormato(ubicacion, formato, isocronaGeometria?, radioM?)` — función única que un futuro `obtenerAnalisisCabidaComercial()` puede llamar con una sola línea.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-competencia-por-formato/18-RESEARCH.md
@lib/cabida-comercial.ts
@lib/overpass-competencia.ts
@lib/strip-power-centers-chile.ts
@lib/cadenas-sucursales-server.ts
@lib/competencia-formato.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: obtenerCompetenciaPorFormato() — composición + cruce espacial OSM↔SII</name>
  <files>lib/competencia-formato.ts</files>
  <action>
Agregar a `lib/competencia-formato.ts` (NO tocar `calcularResultadoCompetencia`, ya construida y testeada en Plan 18-05):

```typescript
import type { UbicacionCabida, FormatoComercial, CompetidorDetectado } from '@/lib/cabida-comercial'
import { obtenerCompetidoresOverpass } from '@/lib/overpass-competencia'
import { obtenerCadenasGeocodificadasPorComuna } from '@/lib/cadenas-sucursales-server'
import { obtenerCompetidoresSeedList } from '@/lib/strip-power-centers-chile'
import * as turf from '@turf/turf'

const RADIO_COMPETENCIA_DEFAULT_M = 3000
const RADIO_MATCH_CADENA_SII_M = 150 // distancia máxima para considerar que un POI OSM y una fila SII geocodificada son la MISMA tienda física

export async function obtenerCompetenciaPorFormato(
  ubicacion: UbicacionCabida,
  formato: FormatoComercial,
  isocronaGeometria?: GeoJSON.Polygon | GeoJSON.MultiPolygon | null,
  radioM: number = RADIO_COMPETENCIA_DEFAULT_M
): Promise<ResultadoCompetenciaFormato> {
  let competidores: CompetidorDetectado[]

  if (formato === 'strip_center' || formato === 'power_center') {
    competidores = obtenerCompetidoresSeedList(formato, ubicacion, radioM, isocronaGeometria)
  } else {
    // supermercado / minimarket — Overpass y geocoding SII son DOS recursos
    // externos independientes (Overpass API vs Nominatim) con throttles
    // independientes: correrlos en paralelo, no secuencial (anti-patrón
    // documentado en 18-RESEARCH.md, mismo criterio que
    // app/api/zonificacion/lookup/route.ts ya usa).
    const [todosOsm, cadenasGeocodificadas] = await Promise.all([
      obtenerCompetidoresOverpass(ubicacion.lat, ubicacion.lng, isocronaGeometria),
      obtenerCadenasGeocodificadasPorComuna(ubicacion.comuna),
    ])

    const deEsteFormato = todosOsm.filter((c) => c.formato === formato)

    // Cruce espacial: para cada POI OSM, buscar la sucursal SII geocodificada
    // más cercana dentro de RADIO_MATCH_CADENA_SII_M — si hay match, sustituir
    // el nombre/tag genérico por el nombre real de cadena y subir confianza
    // individual a 'alta' (corroboración independiente, ver Pitfall 3 de
    // PITFALLS.md: "exigir corroboración con al menos una señal independiente
    // antes de alcanzar confianza ALTA").
    competidores = deEsteFormato.map((poi) => {
      const puntoPoi = turf.point([poi.lng, poi.lat])
      let mejorMatch: { cadena: string; distanciaM: number } | null = null
      for (const c of cadenasGeocodificadas) {
        const d = turf.distance(puntoPoi, turf.point([c.lng, c.lat]), { units: 'meters' })
        if (d <= RADIO_MATCH_CADENA_SII_M && (!mejorMatch || d < mejorMatch.distanciaM)) {
          mejorMatch = { cadena: c.cadena, distanciaM: d }
        }
      }
      if (!mejorMatch) return poi
      return { ...poi, nombre: mejorMatch.cadena, fuente: 'sii_geocodificado' as const, confianza: 'alta' as const }
    })
  }

  return calcularResultadoCompetencia(competidores, formato)
}
```

Agregar un comentario de cabecera documentando el tradeoff aceptado de throttle no compartido entre `lib/overpass-competencia.ts` y `lib/terrenos-ubicacion.ts` (Pitfall 2 de 18-RESEARCH.md) — una línea, no repetir todo el análisis.
  </action>
  <verify>`npm run typecheck` pasa; `grep -n "Promise.all" lib/competencia-formato.ts` confirma que Overpass y geocoding SII corren en paralelo, no secuencial.</verify>
  <done>obtenerCompetenciaPorFormato existe, exportada, rama correctamente por formato, cruza OSM↔SII por proximidad, y delega la degradación de confianza a calcularResultadoCompetencia sin reimplementarla.</done>
</task>

<task type="auto">
  <name>Task 2: Tests del orquestador (mocks de las 3 fuentes)</name>
  <files>tests/unit/competencia-formato-orquestador.test.ts</files>
  <action>
Con `vi.mock('@/lib/overpass-competencia', ...)`, `vi.mock('@/lib/cadenas-sucursales-server', ...)`, `vi.mock('@/lib/strip-power-centers-chile', ...)` (mismo patrón de mocking que otros tests del repo que mockean sus dependencias de I/O), cubrir:

1. `formato: 'strip_center'` → llama `obtenerCompetidoresSeedList`, NUNCA llama `obtenerCompetidoresOverpass` ni `obtenerCadenasGeocodificadasPorComuna`.
2. `formato: 'supermercado'` → llama AMBOS `obtenerCompetidoresOverpass` y `obtenerCadenasGeocodificadasPorComuna`, y ambos se disparan antes de que ninguno resuelva (verificar con mocks que retornan promesas controladas — confirma el `Promise.all`, no secuencial).
3. Un POI OSM mockeado a <150m de una cadena SII geocodificada mockeada → el competidor resultante tiene `nombre` de la cadena real, `fuente: 'sii_geocodificado'`, `confianza: 'alta'`.
4. Un POI OSM mockeado a >150m de cualquier cadena SII → el competidor resultante conserva su `nombre`/`fuente` original de Overpass (sin match).
5. El resultado final siempre pasa por `calcularResultadoCompetencia` (verificar `coberturaConocida: false` en el resultado, reflejando lo que Plan 18-05 ya garantiza).
  </action>
  <verify>`npm run test -- tests/unit/competencia-formato-orquestador.test.ts` pasa (los 5 casos); `npm run test -- tests/unit/competencia-formato.test.ts` (de Plan 18-05) sigue pasando (no se rompió la función pura).</verify>
  <done>Los 5 casos de composición/cruce espacial/paralelismo pasan; el archivo de tests de Plan 18-05 sigue en verde.</done>
</task>

</tasks>

<verification>
`npm run test -- tests/unit/competencia-formato-orquestador.test.ts tests/unit/competencia-formato.test.ts && npm run typecheck` pasan.
</verification>

<success_criteria>
Dado lat/lng/comuna reales y un formato objetivo, el sistema produce un ResultadoCompetenciaFormato completo — competidores con nombre real cuando hay match SII, confianza degradada correctamente — usando únicamente código ya construido en waves anteriores, sin ninguna dependencia de lib/cabida-comercial-server.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/18-competencia-por-formato/18-06-SUMMARY.md`
</output>
