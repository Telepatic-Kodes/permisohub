---
phase: 18-competencia-por-formato
plan: 05
type: tdd
wave: 2
depends_on: ["18-01"]
files_modified:
  - lib/competencia-formato.ts
  - tests/unit/competencia-formato.test.ts
autonomous: true

must_haves:
  truths:
    - "Un conteo de 0 competidores nunca se traduce en confianza alta/confirmada — siempre 'baja' con disclosure explícita (COMPE-05)"
    - "coberturaConocida es 'false' para los 4 formatos objetivo en v1.7, porque las 2 fuentes subyacentes (roster SII, seed list strip/power) tienen gaps documentados — nunca 'true' por default silencioso"
    - "confianzaGlobal nunca es 'alta' en v1.7 — ambas fuentes son conocidas como incompletas, así que el tope real es 'media', sin importar cuántos competidores se encuentren"
    - "El disclosure cita el id exacto de la entrada de data-sources.yaml correspondiente (sii-nomina-sucursales-holdings-sin-tiendas para super/mini, strip-power-centers-chile-seed para strip/power) — nunca restatea el hecho de forma independiente"
  artifacts:
    - path: "lib/competencia-formato.ts"
      provides: "calcularResultadoCompetencia(competidores, formato) — función PURA (sin I/O) que implementa la degradación de confianza de COMPE-05"
      exports: ["calcularResultadoCompetencia"]
  key_links:
    - from: "lib/competencia-formato.ts"
      to: ".planning/data-sources.yaml"
      via: "cita el id de la entrada correspondiente en el texto de disclosure, en comentario de código"
      pattern: "sii-nomina-sucursales-holdings-sin-tiendas|strip-power-centers-chile-seed"
---

<objective>
Implementar, vía TDD (RED → GREEN → REFACTOR), la función PURA `calcularResultadoCompetencia(competidores, formato)` en `lib/competencia-formato.ts` — el núcleo de la lógica de degradación de confianza de COMPE-05. Esta función recibe una lista ya resuelta de `CompetidorDetectado[]` (sin hacer ningún fetch/I/O propio) y produce el `ResultadoCompetenciaFormato` completo, con `coberturaConocida`/`confianzaGlobal`/`disclosure` correctamente derivados — nunca `competidores.length === 0` interpretado como "confirmado: sin competencia".

Purpose: Este es el pitfall más peligroso identificado en toda la investigación de fase (Pitfall 1 de 18-RESEARCH.md, Pitfall 3 de PITFALLS.md a nivel de milestone): la implementación "obvia" (`competidores.length === 0 ? 'sin competencia' : ...`) produce falsos negativos sistemáticos justo donde el dato es más débil (Unimarc no está en el roster SII; Grupo Patio/Más Center no están en el seed list). Es lógica con I/O bien definido (`CompetidorDetectado[] + FormatoComercial → ResultadoCompetenciaFormato`) — candidato TDD según la heurística del proyecto (`expect(fn(input)).toBe(output)` se puede escribir ANTES de escribir `fn`).

Output: `calcularResultadoCompetencia()` puro, 100% testeable sin red/DB, listo para que Plan 18-06 lo use como la última pieza de su orquestador async.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-competencia-por-formato/18-RESEARCH.md
@.planning/research/PITFALLS.md
@lib/cabida-comercial.ts
@lib/mercado-locales-server.ts
@tests/unit/evaluar-oportunidad.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Tests para calcularResultadoCompetencia()</name>
  <files>tests/unit/competencia-formato.test.ts</files>
  <action>
Crear `tests/unit/competencia-formato.test.ts` (vitest, mismo patrón que `tests/unit/evaluar-oportunidad.test.ts`: `import { describe, expect, it } from 'vitest'`) importando `calcularResultadoCompetencia` desde `@/lib/competencia-formato` (todavía no existe — el test debe FALLAR en este punto, es el RED).

Firma esperada (documentar en comentario arriba del describe):
```ts
function calcularResultadoCompetencia(
  competidores: CompetidorDetectado[],
  formato: FormatoComercial
): ResultadoCompetenciaFormato
```

Reglas a codificar en los tests (derivadas de COMPE-05 y de Pitfall 3 de PITFALLS.md):

1. `coberturaConocida` es SIEMPRE `false` en v1.7 — tanto para supermercado/minimarket (gap Unimarc en el roster SII) como para strip_center/power_center (gap Grupo Patio/Más Center en el seed list). No hay ningún caso donde sea `true`.
2. `confianzaGlobal` cuando `competidores.length === 0`: siempre `'baja'`, sin excepción.
3. `confianzaGlobal` cuando `competidores.length > 0`: el MÍNIMO de las `confianza` individuales (orden `'baja' < 'media' < 'alta'`), con un TOPE DURO de `'media'` — nunca `'alta'` a nivel global en v1.7, porque `coberturaConocida` es siempre `false`.
4. `disclosure`: string no vacío que, para `'supermercado'`/`'minimarket'`, contiene literalmente la substring `'sii-nomina-sucursales-holdings-sin-tiendas'` y la palabra `'Unimarc'`; para `'strip_center'`/`'power_center'`, contiene literalmente `'strip-power-centers-chile-seed'` y (`'Grupo Patio'` o `'Más Center'`). El texto debe distinguir explícitamente el caso de 0 competidores del caso con competidores (no el mismo texto genérico para ambos).
5. `consultadoEl`: string ISO parseable (`new Date(resultado.consultadoEl).toString() !== 'Invalid Date'`).
6. `competidores` en el resultado es el mismo array de entrada, sin filtrar/reordenar.

Casos de test obligatorios:
- `calcularResultadoCompetencia([], 'supermercado')` → `coberturaConocida: false`, `confianzaGlobal: 'baja'`, disclosure contiene `'sii-nomina-sucursales-holdings-sin-tiendas'` y `'Unimarc'`.
- `calcularResultadoCompetencia([], 'strip_center')` → disclosure contiene `'strip-power-centers-chile-seed'` y (`'Grupo Patio'` o `'Más Center'`).
- `calcularResultadoCompetencia([competidorConfianzaAlta], 'supermercado')` → `confianzaGlobal: 'media'` (NUNCA `'alta'` — el test más importante del plan; falla si alguien quita el cap más adelante).
- `calcularResultadoCompetencia([competidorMedia, competidorBaja], 'minimarket')` → `confianzaGlobal: 'baja'` (mínimo, no promedio ni el primero del array).
- `calcularResultadoCompetencia([competidorMedia], 'power_center')` → `confianzaGlobal: 'media'`, disclosure con id/formato correctos de strip/power.
- Test explícito y separado: para CUALQUIER combinación de formato/competidores usada arriba, `coberturaConocida === false` (no dejarlo implícito solo en los asserts de otros casos).
- `competidores` de salida `=== competidores` de entrada (mismo array, no una copia filtrada).

Usar `CompetidorDetectado`/`FormatoComercial` reales importados de `@/lib/cabida-comercial` (Plan 18-01) para construir los fixtures — no objetos `any`.
  </action>
  <verify>`npx vitest run tests/unit/competencia-formato.test.ts` — DEBE FALLAR (import error, `calcularResultadoCompetencia` no existe todavía). Confirmar que el fallo es por ausencia de la función, no por un typo del test.</verify>
  <done>Archivo de test creado con los ~7 casos descritos, falla en rojo por ausencia de la función.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Implementar calcularResultadoCompetencia()</name>
  <files>lib/competencia-formato.ts</files>
  <action>
Crear `lib/competencia-formato.ts` con la implementación mínima que hace pasar los tests de Task 1:

```typescript
import type { CompetidorDetectado, ResultadoCompetenciaFormato, FormatoComercial, NivelConfianza } from '@/lib/cabida-comercial'

const NIVEL_ORDEN: Record<NivelConfianza, number> = { baja: 0, media: 1, alta: 2 }
const TOPE_CONFIANZA_GLOBAL: NivelConfianza = 'media' // v1.7: coberturaConocida es SIEMPRE false — nunca 'alta' a nivel global

const ES_FORMATO_SUPER_MINI = (formato: FormatoComercial) => formato === 'supermercado' || formato === 'minimarket'

function construirDisclosure(formato: FormatoComercial, count: number): string {
  const prefijo = count === 0
    ? 'No se encontraron competidores en el área de influencia, pero'
    : `Se encontraron ${count} competidor${count === 1 ? '' : 'es'} en el área de influencia, pero`

  if (ES_FORMATO_SUPER_MINI(formato)) {
    return `${prefijo} la cobertura de la fuente es parcial: Unimarc no está en el roster SII (ver data-sources.yaml → sii-nomina-sucursales-holdings-sin-tiendas). Un conteo bajo o cero NO confirma ausencia de competencia.`
  }
  return `${prefijo} la cobertura de la fuente es parcial: Grupo Patio y Más Center no tienen ningún activo nombrado en la lista curada (ver data-sources.yaml → strip-power-centers-chile-seed). Un conteo bajo o cero NO confirma ausencia de competencia.`
}

/**
 * Función PURA — no hace fetch/I/O. Recibe competidores ya resueltos por las
 * 3 fuentes (Overpass, seed list, SII geocodificado — ver Plan 18-06) y
 * produce el resultado final con confianza degradada. COMPE-05: nunca deja
 * que competidores.length === 0 se lea como "confirmado: sin competencia".
 */
export function calcularResultadoCompetencia(
  competidores: CompetidorDetectado[],
  formato: FormatoComercial
): ResultadoCompetenciaFormato {
  const confianzaGlobal: NivelConfianza =
    competidores.length === 0
      ? 'baja'
      : (() => {
          const minima = competidores.reduce(
            (min, c) => (NIVEL_ORDEN[c.confianza] < NIVEL_ORDEN[min] ? c.confianza : min),
            competidores[0].confianza
          )
          return NIVEL_ORDEN[minima] > NIVEL_ORDEN[TOPE_CONFIANZA_GLOBAL] ? TOPE_CONFIANZA_GLOBAL : minima
        })()

  return {
    formato,
    competidores,
    coberturaConocida: false, // v1.7: ambas fuentes (roster SII, seed list strip/power) tienen gaps documentados para los 4 formatos — nunca true
    confianzaGlobal,
    disclosure: construirDisclosure(formato, competidores.length),
    consultadoEl: new Date().toISOString(),
  }
}
```
  </action>
  <verify>`npx vitest run tests/unit/competencia-formato.test.ts` pasa en verde (todos los casos, incluido el crítico de "alta capada a media"). `npx tsc --noEmit` limpio.</verify>
  <done>calcularResultadoCompetencia existe, exportada, y los ~7 casos de test pasan.</done>
</task>

<task type="auto">
  <name>Task 3 (REFACTOR): Extraer textos de disclosure a constantes nombradas</name>
  <files>lib/competencia-formato.ts</files>
  <action>
Sin cambiar comportamiento (tests deben seguir en verde), mejorar legibilidad extrayendo los fragmentos de disclosure citando explícitamente los ids de `data-sources.yaml` a constantes nombradas al tope del archivo:

```typescript
const DISCLOSURE_GAP_SUPER_MINI =
  'Unimarc no está en el roster SII (ver data-sources.yaml → sii-nomina-sucursales-holdings-sin-tiendas)'
const DISCLOSURE_GAP_STRIP_POWER =
  'Grupo Patio y Más Center no tienen ningún activo nombrado en la lista curada (ver data-sources.yaml → strip-power-centers-chile-seed)'
```

Y usarlas dentro de `construirDisclosure()` en vez del texto inline. Esto hace que un futuro cambio de gap (ej. si Unimarc alguna vez se resuelve) tenga un solo lugar para editar, y que un `grep` por el id de data-sources.yaml encuentre la cita fácilmente.
  </action>
  <verify>`npx vitest run tests/unit/competencia-formato.test.ts` sigue en verde (mismo resultado que Task 2, cero regresiones). `npx tsc --noEmit` limpio.</verify>
  <done>Los textos de disclosure viven en constantes nombradas, citando los ids de data-sources.yaml de forma grep-eable; comportamiento idéntico, tests siguen en verde.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/unit/competencia-formato.test.ts` pasa (todos los casos). `npx tsc --noEmit` limpio. `git log --oneline -3` (si se commitea por task) muestra la progresión RED→GREEN→REFACTOR.
</verification>

<success_criteria>
Ningún resultado de competencia por formato puede, en v1.7, reportar `confianzaGlobal: 'alta'` ni `coberturaConocida: true` — el tope está en el código, verificado por test, no solo documentado. Un conteo de 0 siempre viene con disclosure explícita citando la fuente exacta del gap.
</success_criteria>

<output>
After completion, create `.planning/phases/18-competencia-por-formato/18-05-SUMMARY.md`
</output>
