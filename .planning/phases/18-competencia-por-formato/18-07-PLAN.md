---
phase: 18-competencia-por-formato
plan: 07
type: execute
wave: 4
depends_on: ["18-06"]
files_modified:
  - lib/cabida-comercial-server.ts
autonomous: true

must_haves:
  truths:
    - "Cuando lib/cabida-comercial-server.ts existe (Phase 16 completa), obtenerAnalisisCabidaComercial() incluye el campo competencia poblado — cerrando COMPE-01 a COMPE-06 de punta a punta"
    - "Si lib/cabida-comercial-server.ts NO existe todavía, este plan se detiene explícitamente sin fabricar un stub ni fallar silenciosamente — deja un registro claro de por qué no se ejecutó"
  artifacts:
    - path: "lib/cabida-comercial-server.ts"
      provides: "obtenerAnalisisCabidaComercial() con el campo competencia poblado vía lib/competencia-formato.ts — SOLO si el archivo ya existe al momento de ejecutar este plan"
      contains: "obtenerCompetenciaPorFormato"
  key_links:
    - from: "lib/cabida-comercial-server.ts (obtenerAnalisisCabidaComercial)"
      to: "lib/competencia-formato.ts (obtenerCompetenciaPorFormato)"
      via: "una sola llamada, reusando la geometría de isócrona ya calculada en la misma función — sin recalcular la isócrona"
      pattern: "obtenerCompetenciaPorFormato\\("
---

<objective>
ÚNICO plan de Fase 18 que toca `lib/cabida-comercial-server.ts` — el archivo que Phase 16 (Plan 16-04, todavía no ejecutado a la fecha en que se planificó Fase 18) debe construir. Este plan está deliberadamente aislado y gateado: es la última wave, depende de que Plan 18-06 (`obtenerCompetenciaPorFormato`) ya exista, y su PRIMER paso es verificar que el prerequisito de Phase 16 se cumplió antes de tocar nada.

Purpose: Wiring genuino de composición final — `obtenerAnalisisCabidaComercial(lat, lng, formato)` debe retornar el campo `competencia` (opcional, agregado en Plan 18-01) poblado. Pero este archivo no existía al momento de investigar/planificar Fase 18 (verificado en vivo, ver 18-RESEARCH.md User Constraints) y Phase 16 está pausada esperando `ORS_API_KEY` (ver STATE.md). Ejecutar este plan sin el prerequisito produciría, en el mejor caso, un error de archivo no encontrado, y en el peor, la tentación de crear un stub fabricado — exactamente el tipo de atajo que este repo prohíbe explícitamente. Por eso el Task 1 es un check explícito, no una asunción.

Output: Si Phase 16 ya completó `lib/cabida-comercial-server.ts`: el campo `competencia` queda poblado en `obtenerAnalisisCabidaComercial()`, cerrando la Fase 18 de punta a punta. Si no: el plan se detiene limpiamente, documentado, listo para reintentarse más tarde sin cambios.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-competencia-por-formato/18-RESEARCH.md
@.planning/STATE.md
@lib/cabida-comercial.ts
@lib/competencia-formato.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar prerequisito de Phase 16 ANTES de tocar cualquier archivo</name>
  <files></files>
  <action>
Correr, en este orden exacto, y NO proceder al Task 2 si cualquiera de los dos falla:

```bash
ls lib/cabida-comercial-server.ts 2>/dev/null && echo "ARCHIVO EXISTE" || echo "ARCHIVO NO EXISTE"
grep -n "export async function obtenerAnalisisCabidaComercial" lib/cabida-comercial-server.ts 2>/dev/null || echo "FUNCION NO ENCONTRADA"
```

**Si el archivo NO existe, o la función `obtenerAnalisisCabidaComercial` no se encuentra:** este es el resultado ESPERADO si Phase 16 (específicamente Plan 16-04) todavía no terminó de ejecutarse — a la fecha en que se planificó Fase 18, Phase 16 estaba pausada esperando `ORS_API_KEY` en `.env.local` (ver `.planning/STATE.md`, sección "Current Position"). Esto NO es un error de este plan ni de Fase 18.

En ese caso: DETENER la ejecución de este plan acá mismo. NO crear `lib/cabida-comercial-server.ts` desde cero, NO crear un stub, NO fabricar una función parcial. Registrar explícitamente en el SUMMARY de este plan (ver `<output>`) que el plan quedó BLOQUEADO por el prerequisito de Phase 16, con la fecha del intento y el resultado exacto de los dos comandos de arriba, y que debe reintentarse corriendo `/gsd:execute-phase 18` de nuevo una vez que Phase 16 (Plan 16-04 específicamente) esté completo — en ese punto solo falta este plan, ningún otro plan de Fase 18 necesita re-ejecutarse.

**Si el archivo existe Y la función se encuentra:** proceder al Task 2 normalmente.
  </action>
  <verify>El resultado de los 2 comandos queda registrado literalmente (copiado, no parafraseado) en el SUMMARY de este plan, sin importar cuál de los dos casos ocurrió.</verify>
  <done>Está determinado, con evidencia de comando real (no supuesto), si el prerequisito de Phase 16 se cumple — y si no se cumple, el plan se detuvo sin fabricar nada.</done>
</task>

<task type="auto">
  <name>Task 2: Wire obtenerCompetenciaPorFormato() dentro de obtenerAnalisisCabidaComercial() — SOLO si Task 1 confirmó el prerequisito</name>
  <files>lib/cabida-comercial-server.ts</files>
  <action>
Este task SOLO se ejecuta si Task 1 confirmó que `lib/cabida-comercial-server.ts` existe y exporta `obtenerAnalisisCabidaComercial`. Si Task 1 detuvo el plan, este task NO se ejecuta — el plan termina en Task 1.

Leer `lib/cabida-comercial-server.ts` completo primero para entender la firma real de `obtenerAnalisisCabidaComercial(lat, lng, formato)` construida por Phase 16 (puede diferir en detalle de lo documentado en `.planning/phases/16-ubicacion-e-isocrona-motor-desacoplado/16-04-PLAN.md`, que es un plan, no código ejecutado — confiar en el archivo real, no en el plan).

Agregar la llamada a `obtenerCompetenciaPorFormato()` (de `lib/competencia-formato.ts`, Plan 18-06) DENTRO de `obtenerAnalisisCabidaComercial()`, reusando la geometría de isócrona que esa misma función ya calculó en esta ejecución (no volver a calcular la isócrona ni a llamar `obtenerIsocrona()` una segunda vez) y la `comuna` de la ubicación ya resuelta:

```typescript
// Dentro de obtenerAnalisisCabidaComercial(lat, lng, formato), después de que
// la isócrona (o el fallback circulo_equivalente) ya está resuelta:
const competencia = await obtenerCompetenciaPorFormato(
  { lat, lng, comuna, precision, direccionLabel, fuenteTexto }, // UbicacionCabida ya disponible en este scope
  formato,
  isocrona.geometria
)

return {
  formato,
  isocrona,
  competencia, // NUEVO — Fase 18
  generadoEl: new Date().toISOString(),
}
```

Ajustar el nombre exacto de las variables al código real leído (no asumir nombres exactos sin confirmarlos primero contra el archivo). Si `obtenerAnalisisCabidaComercial` ya tiene algún tipo de cache-through (ej. `cabida_comercial_cache`, construido en Plan 16-03) que persiste el resultado, confirmar si `competencia` debe entrar en esa misma fila cacheada o mantenerse fuera del cache (competencia cambia con frecuencia distinta a la isócrona — decidir con criterio propio si Phase 16 no dejó esto explícito, documentando la decisión en el SUMMARY).
  </action>
  <verify>`npm run typecheck` pasa; `npm run test` (suite completa) sigue en verde; una llamada real a `obtenerAnalisisCabidaComercial()` para una ubicación/formato conocidos retorna `analisis.competencia` poblado (no `undefined`).</verify>
  <done>obtenerAnalisisCabidaComercial() retorna el campo competencia poblado, usando la geometría de isócrona ya calculada, sin duplicar cálculos ni romper ningún test/consumidor existente de Phase 16.</done>
</task>

</tasks>

<verification>
Si Phase 16 estaba completa al momento de ejecutar: `npm run typecheck && npm run test` pasan, y una llamada real a `obtenerAnalisisCabidaComercial()` trae `competencia` poblado con datos reales (no mockeados) para al menos una ubicación conocida. Si Phase 16 NO estaba completa: el SUMMARY documenta el bloqueo con evidencia de comando real, cero archivos fueron modificados, y el plan queda listo para reintentarse sin cambios adicionales.
</verification>

<success_criteria>
Fase 18 queda cerrada de punta a punta (COMPE-01 a COMPE-06) el día que Phase 16 complete — sin que ese día requiera ninguna planificación nueva, solo re-ejecutar este plan.
</success_criteria>

<output>
After completion, create `.planning/phases/18-competencia-por-formato/18-07-SUMMARY.md` — DEBE indicar explícitamente si el plan se completó de punta a punta o si quedó bloqueado esperando Phase 16 (con la evidencia de comando del Task 1 incluida literalmente).
</output>
