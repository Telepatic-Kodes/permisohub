---
phase: 18-competencia-por-formato
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260810_cadenas_sucursales_geocoding.sql
  - lib/cadenas-sucursales-server.ts
  - tests/unit/cadenas-sucursales-geocoding.test.ts
autonomous: false

must_haves:
  truths:
    - "El sistema puede resolver lat/lng reales para las sucursales de cadenas conocidas (Walmart Líder/Líder Express, SMU Alvi/Super10) de una comuna dada, geocodificando solo una vez por dirección (COMPE-06)"
    - "Una segunda consulta de la misma comuna no vuelve a pagar el throttle de Nominatim — las coordenadas quedan cacheadas en la fila"
  artifacts:
    - path: "supabase/migrations/20260810_cadenas_sucursales_geocoding.sql"
      provides: "Columnas nullable lat, lng, geocodificado_el en cadenas_sucursales (aditivo, no rompe la ingesta mensual existente)"
      contains: "ALTER TABLE cadenas_sucursales"
    - path: "lib/cadenas-sucursales-server.ts"
      provides: "obtenerCadenasGeocodificadasPorComuna(comuna) — geocodifica on-demand y persiste cache-on-the-row"
      exports: ["obtenerCadenasGeocodificadasPorComuna"]
  key_links:
    - from: "lib/cadenas-sucursales-server.ts"
      to: "lib/geocoding.ts"
      via: "geocodeDireccion() reusado verbatim, mismo throttle/contrato { ok: false } que Phase 16 ya usa"
      pattern: "geocodeDireccion\\("
    - from: "lib/cadenas-sucursales-server.ts"
      to: "cadenas_sucursales (tabla)"
      via: "UPDATE ... SET lat, lng, geocodificado_el — cache-through, nunca re-geocodifica una fila ya resuelta"
      pattern: "\\.update\\(\\{ lat"
---

<objective>
Extender `lib/cadenas-sucursales-server.ts` (tabla `cadenas_sucursales` ya en producción, 605 direcciones reales de Walmart/SMU) con geocoding on-demand por comuna, cacheado directamente en la fila. Esto le da a Fase 18 la pieza que permite mostrar el nombre REAL de cadena (COMPE-06, ej. "Líder Express" en vez de un tag OSM crudo) cuando se cruce contra los resultados de Overpass en el plan de composición (18-06).

Purpose: `cadenas_sucursales` hoy solo tiene texto (`calle`/`numero`/`comuna`), no coordenadas — sin esto, es imposible cruzar espacialmente un POI de OSM contra una sucursal SII conocida. Geocodificar es lento (Nominatim, 1.1s/request) así que debe cachearse permanentemente en la fila, no recalcularse en cada apertura del tab (Pitfall 4 de la investigación de fase).

Output: Migración aditiva aplicada en vivo (mismo patrón que 16-03, vía Supabase MCP) + `obtenerCadenasGeocodificadasPorComuna(comuna)` con cache-through verificado con datos reales.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-competencia-por-formato/18-RESEARCH.md
@lib/cadenas-sucursales-server.ts
@lib/geocoding.ts
@lib/scrapers/instrumentos-ipt.ts
@supabase/migrations/20260808_cadenas_sucursales.sql
@supabase/migrations/20260809_cabida_comercial_cache.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración aditiva — lat/lng/geocodificado_el en cadenas_sucursales</name>
  <files>supabase/migrations/20260810_cadenas_sucursales_geocoding.sql</files>
  <action>
Crear la migración con columnas NULLABLE (nadie las tiene todavía, la ingesta mensual existente en `correrIngestaCadenasSucursales()` sigue funcionando sin tocarlas):

```sql
ALTER TABLE cadenas_sucursales
  ADD COLUMN lat numeric,
  ADD COLUMN lng numeric,
  ADD COLUMN geocodificado_el timestamptz;

COMMENT ON COLUMN cadenas_sucursales.lat IS 'Geocodificado on-demand por comuna (Fase 18, COMPE-06) via lib/geocoding.ts geocodeDireccion() — null hasta la primera consulta de esa comuna.';
COMMENT ON COLUMN cadenas_sucursales.lng IS 'Ver lat.';
COMMENT ON COLUMN cadenas_sucursales.geocodificado_el IS 'Timestamp del geocoding — null si nunca se geocodificó. La ingesta mensual (correrIngestaCadenasSucursales) NO toca estas 3 columnas al re-upsertear direcciones ya conocidas.';
```

Aplicar la migración en vivo contra el proyecto Supabase real usando `mcp__supabase__apply_migration` (mismo procedimiento que Plan 16-03 usó para `cabida_comercial_cache`, documentado en STATE.md) — no dejarla solo escrita en disco sin aplicar.
  </action>
  <verify>Vía `mcp__supabase__list_tables` o `mcp__supabase__execute_sql` (`select column_name from information_schema.columns where table_name='cadenas_sucursales'`), confirmar que `lat`, `lng`, `geocodificado_el` existen en la tabla real de Supabase, no solo en el archivo SQL.</verify>
  <done>La migración existe en supabase/migrations/ Y está aplicada en el proyecto Supabase real (verificado por query directa a information_schema, no solo por la existencia del archivo).</done>
</task>

<task type="auto">
  <name>Task 2: obtenerCadenasGeocodificadasPorComuna() con cache-through</name>
  <files>lib/cadenas-sucursales-server.ts</files>
  <action>
Agregar a `lib/cadenas-sucursales-server.ts` (archivo existente, no crear uno nuevo — ya tiene `createServiceClient`, `CADENAS_RUT_CONOCIDOS`, `normalizarNombreComuna` importados):

```typescript
import { geocodeDireccion } from '@/lib/geocoding'

export interface CadenaGeocodificada {
  cadena: string
  lat: number
  lng: number
  direccionLabel: string
}

/**
 * Geocodifica on-demand las sucursales VIGENTES de una comuna que todavía no
 * tienen lat/lng, persiste el resultado en la fila (cache-through — NUNCA
 * vuelve a geocodificar una fila con lat/lng ya poblados), y retorna todas
 * las filas de la comuna con coordenadas (ya cacheadas + recién geocodificadas
 * en esta llamada). Nunca lanza — una fila que falla su geocoding simplemente
 * no aparece en el resultado, mismo contrato que geocodeDireccion().
 */
export async function obtenerCadenasGeocodificadasPorComuna(
  comuna: string
): Promise<CadenaGeocodificada[]> {
  const supabase = createServiceClient()
  const cadenasActivas = CADENAS_RUT_CONOCIDOS.filter((c) => c.estado === 'activo').map((c) => c.cadena)

  const { data: filas, error } = await supabase
    .from('cadenas_sucursales')
    .select('id, cadena, calle, numero, comuna, lat, lng')
    .eq('vigente', true)
    .in('cadena', cadenasActivas)

  if (error || !filas) return []

  const comunaNormalizada = normalizarNombreComuna(comuna)
  const filasDeLaComuna = filas.filter((f) => normalizarNombreComuna(f.comuna) === comunaNormalizada)

  const resultado: CadenaGeocodificada[] = []
  for (const fila of filasDeLaComuna) {
    if (fila.lat != null && fila.lng != null) {
      resultado.push({ cadena: fila.cadena, lat: fila.lat, lng: fila.lng, direccionLabel: `${fila.calle} ${fila.numero}` })
      continue
    }
    // No geocodificada todavía — geocodificar UNA vez, persistir, luego usar.
    const geo = await geocodeDireccion(`${fila.calle} ${fila.numero}`, fila.comuna)
    if (!geo.ok || geo.lat === undefined || geo.lng === undefined) continue

    await supabase
      .from('cadenas_sucursales')
      .update({ lat: geo.lat, lng: geo.lng, geocodificado_el: new Date().toISOString() })
      .eq('id', fila.id)

    resultado.push({
      cadena: fila.cadena,
      lat: geo.lat,
      lng: geo.lng,
      direccionLabel: geo.displayName ?? `${fila.calle} ${fila.numero}`,
    })
  }
  return resultado
}
```

Nota de diseño (agregar como comentario sobre la función): usa `normalizarNombreComuna()` en JS después de traer todas las filas activas, mismo patrón ya aceptado en `obtenerSenalesExpansionPorComuna()` — no un `.eq('comuna', ...)` directo, porque `cadenas_sucursales.comuna` viene en MAYÚSCULAS sin tildes del SII.

Escribir además `tests/unit/cadenas-sucursales-geocoding.test.ts` con `vi.mock` de `@/lib/geocoding` y de un cliente Supabase falso (siguiendo el patrón de mocks ya usado en tests existentes del repo para `createServiceClient`) cubriendo: (a) una fila con lat/lng ya poblados NO llama a `geocodeDireccion` (verifica el cache-through, Pitfall 4), (b) una fila sin lat/lng llama a `geocodeDireccion` UNA vez y luego persiste vía `.update()`, (c) `geo.ok === false` no lanza y la fila simplemente no aparece en el resultado.
  </action>
  <verify>`npm run test -- tests/unit/cadenas-sucursales-geocoding.test.ts` pasa; `npm run typecheck` pasa.</verify>
  <done>obtenerCadenasGeocodificadasPorComuna existe, exportada, cachea en la fila, y los 3 casos del test unitario (cache-hit, primer geocoding, fallo silencioso) pasan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación humana — geocoding real de sucursales SII conocidas con cache-through</name>
  <files>lib/cadenas-sucursales-server.ts</files>
  <action>
Confirma en vivo lo construido por Task 1 y Task 2: `obtenerCadenasGeocodificadasPorComuna(comuna)` geocodificando sucursales SII conocidas con coordenadas reales, cacheadas en la fila de Supabase.

Pasos:
1. Confirma que `SUPABASE_SERVICE_ROLE_KEY` está en `.env.local` (ya debería estarlo — usado por otros scripts del repo).
2. Corre en una consola Node/REPL local (o un script temporal) contra una comuna real con sucursales conocidas, ej.:
   ```
   import { obtenerCadenasGeocodificadasPorComuna } from './lib/cadenas-sucursales-server'
   const r = await obtenerCadenasGeocodificadasPorComuna('Maipú')
   console.log(r)
   ```
   (O usa `mcp__supabase__execute_sql` para primero confirmar `select comuna, count(*) from cadenas_sucursales where vigente group by comuna order by count(*) desc limit 10` y elegir una comuna con filas reales.)
3. Confirma que el resultado trae `lat`/`lng` reales (dentro de Chile — lat entre -18 y -56, lng entre -76 y -66), no `0`/`NaN`/valores fabricados.
4. Vuelve a correr la MISMA consulta para la MISMA comuna — debe ser notablemente más rápida (cache-hit, sin volver a pegarle a Nominatim) y `select lat, lng, geocodificado_el from cadenas_sucursales where comuna ilike '%MAIPU%' and geocodificado_el is not null limit 5` en Supabase debe mostrar filas con `geocodificado_el` poblado.
  </action>
  <verify>Los 4 pasos anteriores pasan sin hallazgos bloqueantes. Cualquier desviación (coordenadas fabricadas/nulas, segunda llamada tan lenta como la primera) se reporta explícitamente en vez de darse por aprobada.</verify>
  <done>Usuario escribe "aprobado", o describe los problemas encontrados para una ronda de corrección posterior.</done>
</task>

</tasks>

<verification>
Migración aplicada en Supabase real (no solo en disco). `npm run test -- tests/unit/cadenas-sucursales-geocoding.test.ts` pasa. Checkpoint humano confirma coordenadas reales de al menos una comuna con sucursales SII conocidas.
</verification>

<success_criteria>
Cualquier comuna con sucursales de Walmart/SMU en `cadenas_sucursales` puede resolverse a lat/lng reales bajo demanda, con la primera consulta pagando el costo de Nominatim y todas las siguientes siendo instantáneas — la pieza que Plan 18-06 necesita para sustituir un tag OSM genérico por "Líder Express" real.
</success_criteria>

<output>
After completion, create `.planning/phases/18-competencia-por-formato/18-04-SUMMARY.md`
</output>
