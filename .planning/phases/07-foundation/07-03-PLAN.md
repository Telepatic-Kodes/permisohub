---
phase: 07-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/sheet.tsx
autonomous: false

must_haves:
  truths:
    - "El componente Sheet está disponible para importar desde @/components/ui/sheet"
    - "La importación named export { Sheet, SheetContent, SheetHeader, SheetTitle } funciona sin errores de TypeScript"
  artifacts:
    - path: "components/ui/sheet.tsx"
      provides: "Sheet drawer component scaffolded by shadcn CLI"
      exports: ["Sheet", "SheetContent", "SheetHeader", "SheetTitle", "SheetDescription", "SheetFooter", "SheetClose", "SheetTrigger", "SheetOverlay", "SheetPortal"]
  key_links:
    - from: "components/ui/sheet.tsx"
      to: "shadcn style base-nova"
      via: "npx shadcn@latest add sheet"
      pattern: "SheetContent|SheetHeader|SheetTitle"
---

<objective>
Scaffold the Sheet component using the shadcn CLI. The Sheet is required by Phase 8 SKILL-01 as the slide-in drawer container for the Copiloto IA panel. Without it, any import from @/components/ui/sheet will fail at build time.

Purpose: Unblock Phase 8 by making the Sheet component available.
Output: components/ui/sheet.tsx scaffolded and ready to import.
</objective>

<execution_context>
@/home/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sheet component via shadcn CLI</name>
  <files>components/ui/sheet.tsx</files>
  <action>
Run the shadcn CLI to scaffold the Sheet component. The project uses shadcn v4.11.0 with style "base-nova" (confirmed in components.json).

Command to run from /Users/tomas/Estefanía/permisohub:
```
npx shadcn@latest add sheet
```

The CLI will:
- Scaffold components/ui/sheet.tsx
- Possibly install new npm dependencies (accept any prompts — if it adds @radix-ui/react-dialog or vaul, those are correct and needed)

After the CLI completes, open components/ui/sheet.tsx and check which primitive it imports:
- If it imports from "@base-ui/react/..." — the base-nova registry had a sheet component; no further action needed
- If it imports from "@radix-ui/..." — the CLI fell back to a Radix implementation; this is functionally correct. The component will work for the copiloto drawer. Note this in the summary.

Do NOT manually rewrite the scaffolded component to use different primitives. The shadcn output is authoritative regardless of which primitive layer it uses.

Verify the file exports at minimum: Sheet, SheetContent, SheetHeader, SheetTitle.
If the scaffolded file is missing any of these named exports, add stub re-exports that match the API Phase 8 will use:
- Sheet: the root component (handles open/onOpenChange state)
- SheetContent: the panel body (slides in from a side)
- SheetHeader: a div wrapper for title + description
- SheetTitle: the accessible title element

Do NOT add any business logic, styling beyond what the CLI generates, or import from Phase 8 files that don't exist yet.
  </action>
  <verify>grep -n "export" components/ui/sheet.tsx | grep -E "Sheet|SheetContent|SheetHeader|SheetTitle" (must show at least 4 matches) && npx tsc --noEmit 2>&1 | grep -i sheet (must return no errors)</verify>
  <done>components/ui/sheet.tsx exists and exports Sheet, SheetContent, SheetHeader, SheetTitle. TypeScript strict mode produces no errors for this file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Sheet exports and TypeScript compilation</name>
  <files>components/ui/sheet.tsx</files>
  <action>
The shadcn CLI scaffolded components/ui/sheet.tsx in Task 1. Verify it is correct before Phase 8 begins using it.

Steps:
1. Check the file exists: ls components/ui/sheet.tsx
2. Check exports are present: grep -n "export" components/ui/sheet.tsx
   Expected: at minimum Sheet, SheetContent, SheetHeader, SheetTitle
3. Run TypeScript check: npx tsc --noEmit
   Expected: zero new errors (pre-existing errors unrelated to sheet.tsx are not blocking)
4. Note which primitive layer the CLI used (base-ui or radix) — include in the summary.
5. If the CLI installed new npm packages, run npm install to confirm the lockfile is consistent.
  </action>
  <verify>grep -n "export" components/ui/sheet.tsx returns at least Sheet, SheetContent, SheetHeader, SheetTitle. npx tsc --noEmit exits without sheet-related errors.</verify>
  <done>Sheet component is present, TypeScript is clean, primitive layer is documented in summary.</done>
</task>

</tasks>

<verification>
- components/ui/sheet.tsx exists
- File exports: Sheet, SheetContent, SheetHeader, SheetTitle (and ideally SheetDescription, SheetFooter, SheetClose, SheetTrigger)
- npx tsc --noEmit produces no errors attributable to sheet.tsx
- package.json and package-lock.json are consistent (npm install succeeds if new deps were added)
</verification>

<success_criteria>
- components/ui/sheet.tsx exists with named exports for Sheet, SheetContent, SheetHeader, SheetTitle
- The import `import { Sheet, SheetContent, SheetHeader, SheetTitle } from "@/components/ui/sheet"` would resolve without TypeScript errors
- No manual rewriting of the scaffolded output was needed (or if it was, it is documented in the summary)
</success_criteria>

<output>
After completion, create .planning/phases/07-foundation/07-03-SUMMARY.md
Include in the summary: which primitive layer the CLI used (base-ui vs radix), which npm packages were added if any, and whether any manual adjustments were made.
</output>
