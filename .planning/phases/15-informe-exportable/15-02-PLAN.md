---
phase: 15-informe-exportable
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx
  - app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Usuario puede abrir un link 'Exportar informe' desde la ficha de detalle de una oportunidad y llegar a una vista imprimible con portada, cuerpo y metodología/fuentes (INFO-01)"
    - "El informe individual muestra la fecha de generación (portada) Y la fecha de última verificación del dato (visible en el cuerpo, no solo en el pie) — dos vigencias distintas, nunca una sola fecha genérica (INFO-03, Pitfall 4)"
    - "El campo 'preparado por/para' es editable en la portada del informe individual (INFO-04)"
    - "Un precio con precioValido=false nunca se muestra como '0 UF' en el informe (Pitfall 2) — se muestra el mensaje explícito 'Precio no disponible en moneda reconocida'"
    - "Al usar Cmd+P / vista previa de impresión, el toolbar (botón Imprimir) y la barra lateral de navegación desaparecen; el cuerpo del informe queda en una hoja A4 limpia"
  artifacts:
    - path: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx"
      provides: "Server Component nuevo — re-invoca obtenerOportunidadPorId + obtenerComparablesOportunidad directamente (sin API route intermedia), compone PortadaInforme + cuerpo + MetodologiaInforme"
      min_lines: 90
  key_links:
    - from: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx"
      to: "lib/mercado-locales-server.ts"
      via: "await obtenerOportunidadPorId(id) — llamada server-to-server directa"
      pattern: "obtenerOportunidadPorId"
    - from: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx"
      to: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx"
      via: "Link href hacia /oportunidades/{id}/informe — punto de entrada real desde la ficha existente"
      pattern: "/informe"
---

<objective>
Construir el informe exportable/imprimible de UNA oportunidad individual (INFO-01, INFO-03, INFO-04), como ruta hermana `/oportunidades/[id]/informe`, y darle un punto de entrada real desde la ficha de detalle existente (`[id]/page.tsx`) — sin un link visible, la ruta existiría pero nadie la encontraría.

Purpose: Cierra INFO-01 (portada+cuerpo+metodología para una oportunidad), y aporta la mitad de INFO-03/INFO-04 (la otra mitad la construye Plan 15-03 para el caso de comparación). Usa exclusivamente `@media print` + `window.print()` (decisión de arquitectura resuelta en 15-RESEARCH.md Open Question 1 — adoptada como default, NO jsPDF/html2canvas, NO se extiende `lib/informe-pdf.ts` que es 100% Due Diligence sin capa neutra reutilizable) y NO incluye ningún gráfico Recharts (15-RESEARCH.md Open Question 2 — `Histograma`/`RankingBarChart`/`KpiCard`/`DistribucionDonut` usan `ResponsiveContainer`, comportamiento no verificado bajo `@media print` en este repo; el cuerpo se construye con tablas/texto plano, suficiente para cumplir INFO-01 tal como está redactado).
Output: `app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx` (nuevo) + link "Exportar informe" en `[id]/page.tsx` (modificado).
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-informe-exportable/15-RESEARCH.md
@.planning/phases/15-informe-exportable/15-01-SUMMARY.md

@app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx
@lib/mercado-locales-server.ts
@lib/scrapers/mercado-locales-common.ts
@lib/formato-fecha.ts
@components/mercado-inmobiliario/informe/portada-informe.tsx
@components/mercado-inmobiliario/informe/metodologia-informe.tsx
@components/mercado-inmobiliario/informe/print-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build /oportunidades/[id]/informe/page.tsx</name>
  <files>app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx</files>
  <action>
Create the new Server Component route, following the exact pattern already established by `[id]/page.tsx` (Fase 13, read in full during planning) and `cadenas-comerciales/[id]/compliance/page.tsx` (the `@media print` precedent): `params: Promise<{ id: string }>`, `export const dynamic = "force-dynamic"` (matches `[id]/page.tsx:21`, guarantees `new Date()` for `generadoEl` is a real render-time timestamp, never cached), `notFound()` if `obtenerOportunidadPorId(id)` returns null.

Data fetching — re-invoke the data layer directly, no API route (RESEARCH.md Pattern 1, an explicit improvement over both existing `@media print` precedents which both fetch via API routes):
```ts
const oportunidad = await obtenerOportunidadPorId(id)
if (!oportunidad) notFound()
const comparables = await obtenerComparablesOportunidad({
  comuna: oportunidad.comuna,
  operacion: oportunidad.operacion,
  tipoPropiedad: oportunidad.tipoPropiedad,
  excludeId: oportunidad.id,
  precioUfM2Objetivo: oportunidad.precioUfM2Normalizado,
  precioUfObjetivo: oportunidad.precioUfNormalizado,
})
```
(same args shape as `[id]/page.tsx:39-46`). Do not fetch `bandasArriendo`/`bandasVenta`/rentabilidad implícita de zona, señales de expansión, or tendencias de construcción — those power the `ResumenTab`/`PosicionamientoTab` AI narrative on the live page, out of scope for a v1 printable methodology-first report; `oportunidad.bandas` (already resolved by `obtenerOportunidadPorId`) is enough for INFO-01's methodology section.

Print CSS — same `<style>{...}</style>` block as the `compliance/page.tsx` precedent (`.print\\:hidden { display: none !important }`, `body { background: white }`, `@page { size: A4 portrait; margin: 15mm 12mm; }`).

Body composition, top to bottom inside a `mx-auto max-w-[210mm] bg-white p-8 text-gray-900 print:p-0` wrapper:
1. `print:hidden` toolbar row: a label + `<PrintButton />` (from Plan 15-01).
2. `<PortadaInforme titulo={oportunidad.titulo} subtitulo={...comuna · tipo · operación...} generadoEl={new Date()} />` — `subtitulo` built from `TIPO_PROPIEDAD_LABEL[oportunidad.tipoPropiedad].singular` (import from `@/lib/scrapers/mercado-locales-common`, same as `[id]/page.tsx:52`) and `oportunidad.operacion === "venta" ? "Venta" : "Arriendo"`.
3. If `oportunidad.status === "dado_de_baja"`: a red banner (same tone as `[id]/page.tsx:116-126`) noting the aviso is no longer active, with the "dado de baja" date via `formatTimestampCorto(oportunidad.dadoDeBajaEl)`.
4. Price summary section — **guard on `precioValido` exactly like `[id]/page.tsx:132`** (Pitfall 2, this is the highest-risk single line in this task): `{oportunidad.precioValido ? \`${fmt(oportunidad.precioUfNormalizado)} UF\` : "Precio no disponible en moneda reconocida"}`, plus UF/m² if non-null. Directly below the price, a visible line: "Última verificación de este dato: {formatTimestampCorto(oportunidad.ultimaVezVistoEl)}" — this is INFO-03's per-row vigency, must live in the body per Pitfall 4 ("informe individual: una línea visible en el cuerpo, no solo en el pie"), not buried only in the methodology section.
5. "Posicionamiento vs. cohorte" section: if `oportunidad.bandas` exists, a simple 3-row table (P25 / Mediana / P75, UF, `fmt(...)`); if null, an amber note "Sin banda de mercado disponible para esta comuna/tipo/operación."
6. If `oportunidad.reasonCodes.length > 0`: a "Señales" section, bulleted list using `REASON_LABEL_DETALLE[code] ?? code` (import from `lib/mercado-locales-server`, same as `HistorialTab`).
7. If `comparables.length > 0`: a "Comparables" table (título / UF / UF·m²) from the `comparables` array fetched above.
8. `<MetodologiaInforme fuentes={[{ comuna: oportunidad.comuna, operacion: oportunidad.operacion, tipoPropiedad: oportunidad.tipoPropiedad, bandas: oportunidad.bandas }]} />` (Plan 15-01) — single-entry array, satisfies INFO-01's "sección de metodología/fuentes."

Use a local `fmt(n: number)` helper identical to `[id]/page.tsx:23-25` (`n.toLocaleString("es-CL", { maximumFractionDigits: 2 })`).
  </action>
  <verify>`npx tsc --noEmit` passes. Manually inspect the route file: `grep -n "precioValido" app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx` shows the guard used before rendering `precioUfNormalizado`. `grep -n "formatTimestampCorto\|formatFechaCorta" ...` shows both formatters used (timestamptz for `ultimaVezVistoEl`/`dadoDeBajaEl`/`generadoEl`, date-only for `bandas.statsDate` inside `MetodologiaInforme`).</verify>
  <done>`/mercado-inmobiliario/oportunidades/{existing-id}/informe` renders a Server-rendered page with portada, price/positioning/comparables body, and methodology section, using real data from `lib/mercado-locales-server.ts` — no mock data, no API route indirection.</done>
</task>

<task type="auto">
  <name>Task 2: Wire entry point from the existing ficha de detalle</name>
  <files>app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx</files>
  <action>
Without this, the new route from Task 1 is unreachable from the app's UI (it would only work if a user typed the URL by hand). Add an "Exportar informe" link in the price card header of `[id]/page.tsx` (lines 128-147, the same `<div className="flex items-start justify-between gap-3">` that already contains "Ver aviso original"), pointing to the new route.

Add `import Link from "next/link"` and add `Printer` to the existing `lucide-react` import (currently just `ExternalLink`). Wrap "Ver aviso original" and the new link in a shared flex container so both sit together:

```tsx
<div className="flex flex-col items-end gap-1.5">
  <a href={oportunidad.url} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 text-sm text-primary hover:underline">
    Ver aviso original <ExternalLink className="size-3.5" />
  </a>
  <Link href={`/mercado-inmobiliario/oportunidades/${oportunidad.id}/informe`} className="inline-flex items-center gap-1 text-sm text-primary hover:underline">
    <Printer className="size-3.5" /> Exportar informe
  </Link>
</div>
```
Replace the existing bare `<a>` at lines 138-145 with this block (same visual container it already sits inside, `flex items-start justify-between gap-3` at line 129 stays unchanged — only its second child changes from a single `<a>` to this 2-link column).
  </action>
  <verify>`npx tsc --noEmit` passes. `grep -n "oportunidades/\${oportunidad.id}/informe" "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx"` matches.</verify>
  <done>Visiting any oportunidad's ficha de detalle shows a visible "Exportar informe" link next to "Ver aviso original," navigating to the new informe route built in Task 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación humana del informe individual (INFO-01, INFO-03, INFO-04)</name>
  <files>app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/informe/page.tsx</files>
  <action>
Confirma en vivo el informe imprimible de una oportunidad individual, incluyendo su punto de entrada real desde la ficha de detalle y su comportamiento al imprimir.

Pasos:
1. Ve a `/mercado-inmobiliario/oportunidades` y abre cualquier oportunidad (click en una card).
2. En la ficha de detalle, confirma que aparece el link "Exportar informe" junto a "Ver aviso original".
3. Click en "Exportar informe" — debe navegar a `/mercado-inmobiliario/oportunidades/{id}/informe`.
4. En el informe, confirma: portada con título, comuna/tipo/operación, "Generado el {fecha de hoy}", y 2 campos editables "Preparado por"/"Preparado para" — escribe algo en ambos y confirma que se ve el texto tipeado.
5. Confirma que el precio se muestra correctamente (o el mensaje "Precio no disponible..." si la oportunidad elegida tiene precio inválido — si ninguna a mano lo tiene, prueba con otra oportunidad o acepta verificación de código).
6. Confirma la línea "Última verificación de este dato: {fecha}" visible en el cuerpo (no solo en metodología).
7. Confirma la sección "Metodología y fuentes" al final, con muestra N, UF usado y "banda vigente al {mes/año}".
8. Presiona Cmd+P (o el botón "Imprimir") — en la vista previa de impresión, confirma que: (a) la barra lateral de navegación NO aparece, (b) el botón "Imprimir" tampoco aparece, (c) el contenido queda limpio en una hoja A4 vertical.
  </action>
  <verify>Los 8 pasos anteriores pasan sin hallazgos bloqueantes. Cualquier desviación se reporta explícitamente en vez de darse por aprobada.</verify>
  <done>Usuario escribe "aprobado", o describe los problemas encontrados para una ronda de corrección posterior.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` clean. Navegación real: lista → ficha → "Exportar informe" → informe renderizado con datos reales (no mock). Impresión real (Cmd+P) sin sidebar ni toolbar.
</verification>

<success_criteria>
INFO-01, y las porciones de INFO-03/INFO-04 correspondientes al informe individual, verificadas en vivo por el usuario (checkpoint aprobado).
</success_criteria>

<output>
After completion, create `.planning/phases/15-informe-exportable/15-02-SUMMARY.md`
</output>
