---
phase: 12-integracion-con-motores-de-decision
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Las tres integraciones (vía de tramitación, due diligence, copiloto) funcionan juntas en la app real — verificado por un humano, no solo por tsc/eslint"
    - "lib/via-tramitacion.ts no cambió durante toda la fase y sus tests de determinismo siguen pasando exactamente igual"
    - "Un proyecto sin dirección geocodificable, sin cobertura, o sin zonificación consultada se comporta en vía de tramitación, due diligence y copiloto exactamente igual que antes del milestone (criterio de éxito 4 de la fase — sin regresiones)"
  artifacts: []
  key_links: []
---

<objective>
Checkpoint humano final de la Phase 12 (y del milestone v1.4): confirma en vivo, con browser real, que las tres integraciones aditivas (INTEG-01 vía, INTEG-02 due diligence, INTEG-03 copiloto) funcionan como se especificó y que ningún proyecto sin datos de zonificación sufrió regresión — antes de dar por completa la fase.

Purpose: los planes 12-01/02/03 tocan un motor de vía determinista ya probado en producción, un pipeline de Due Diligence con IA, y los prompts del copiloto — exactamente el tipo de superficie (render condicional, llamadas IA en background, comportamiento distinto según datos de zona presentes/ausentes) que `gsd-executor` no puede verificar sin browser. Sigue el mismo patrón que el checkpoint final de Phase 11 (11-08-PLAN.md Task 2).
Output: confirmación humana de que Phase 12 — y el milestone v1.4 completo — no tiene regresiones y cumple sus 4 criterios de éxito.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-integracion-con-motores-de-decision/12-RESEARCH.md
@.planning/phases/12-integracion-con-motores-de-decision/12-01-SUMMARY.md
@.planning/phases/12-integracion-con-motores-de-decision/12-02-SUMMARY.md
@.planning/phases/12-integracion-con-motores-de-decision/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificación automatizada — determinismo + build limpio</name>
  <files></files>
  <action>
Antes del checkpoint humano, confirma automáticamente lo que SÍ se puede automatizar (regla "automation-first": el checkpoint verifica DESPUÉS de la automatización, no la reemplaza):

1. `npx vitest run tests/unit/via-tramitacion.test.ts` — DEBE pasar sin ninguna modificación al archivo de test ni al resultado esperado. Esta es la prueba dura de que `recomendarVia()`/`pasoSiguiente()` no se alteraron durante toda la fase (criterio de éxito 1).
2. `npx tsc --noEmit` sobre todo el proyecto — DEBE estar limpio, confirmando que los tres planes de integración (via-decision.tsx/pmo-panel.tsx, due-diligence.ts/route.ts, copiloto/route.ts) tipan correctamente en conjunto.
3. `git diff --stat lib/via-tramitacion.ts` — DEBE estar vacío en todo el rango de commits de la Phase 12 (`git log --oneline -- lib/via-tramitacion.ts` no debe mostrar commits nuevos de esta fase).
4. Si cualquiera de los 3 chequeos falla, DETENTE aquí y reporta el fallo — no continúes al checkpoint humano hasta que estén limpios.
  </action>
  <verify>Los 3 comandos anteriores se ejecutaron y sus resultados (pass/fail) quedan registrados en el SUMMARY de este plan.</verify>
  <done>vitest de via-tramitacion pasa sin cambios, tsc limpio en todo el proyecto, y lib/via-tramitacion.ts no tiene commits nuevos en esta fase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificación humana end-to-end de las 3 integraciones + no-regresión</name>
  <files></files>
  <action>
Qué se construyó, para contexto antes de verificar: Phase 12 conecta la zonificación PRC (Phase 10-11, ya en producción) como señal aditiva citada en tres motores existentes — (a) `ViaDecision` ahora muestra una alerta citada cuando el destino declarado no calza con los usos permitidos de la zona, sin tocar el árbol determinista de `via-tramitacion.ts`; (b) `due-diligence.ts` puede citar la zona (fuente `'PRC'`) como fundamento de un hallazgo cuando detecta esa misma incoherencia; (c) los prompts de diagnóstico OGUC y checklist del copiloto reciben los usos permitidos/prohibidos de la zona como contexto adicional. Las tres integraciones comparten el mismo guard (`zona_status === 'encontrado' && zona_usos_disponibles === true`) y son estrictamente aditivas: un proyecto sin ese dato disponible debe comportarse exactamente igual que antes del milestone.

Usa el dev-auth bypass del proyecto (`BYPASS_AUTH=true`, `app/auth/dev-login/route.ts`) para navegar con sesión real, igual que en el checkpoint de Phase 11.

Pasos a verificar:
1. Arranca el dev server (`npm run dev`) e inicia sesión.
2. Abre un proyecto con zona resuelta (`zona_status: 'encontrado'`, `zona_usos_disponibles: true`) cuyo `destino_sii` NO calce con los usos permitidos de su zona (si no tienes uno a mano, edita temporalmente el `destino_sii` de un proyecto ya con zona resuelta a algo claramente incompatible, ej. "INDUSTRIA PESADA" en una zona residencial). En la pestaña PMO / vía de tramitación, confirma que aparece automáticamente (sin clic) la alerta citada de incompatibilidad, con su justificación y un link o texto de fuente de la zona.
3. En ese mismo proyecto, genera (o revisa, si ya existe) un Due Diligence — confirma que el informe no falla y que, si la IA detectó la incoherencia, aparece un hallazgo con una cita que se comporta igual que las citas OGUC/LGUC/DDU existentes (link a la fuente, sin cambios visuales necesarios en `CitaBadges`).
4. En ese mismo proyecto, abre el copiloto IA y genera el diagnóstico OGUC y/o el checklist (si el checklist ya existía para este proyecto, prueba con un proyecto nuevo sin checklist previo para forzar la primera generación) — confirma que ambos corren sin error. No es necesario verificar el contenido exacto de la respuesta de la IA, solo que no hay errores ni comportamiento roto.
5. Abre un proyecto SIN zonificación consultada (`zona_status: 'pendiente'` o ausente) o de una comuna sin cobertura — confirma que vía de tramitación, Due Diligence y copiloto se comportan EXACTAMENTE igual que antes del milestone: ninguna alerta, ninguna cita PRC, ningún error, ningún prompt distinto (sin regresiones — criterio de éxito 4).
6. Abre un proyecto en Ñuñoa o cualquiera con `zona_usos_disponibles: false` (zona encontrada pero sin usos) — confirma que tampoco aparece alerta ni cita PRC (el guard compuesto excluye este caso, no solo `zona_status`).
  </action>
  <verify>Los 6 pasos anteriores se comportan como se describe, verificados directamente en el navegador.</verify>
  <done>El humano confirma con "approved", o describe exactamente qué paso falló y qué se observó en su lugar — resolver cualquier hallazgo reportado antes de dar por completa la Phase 12 (y el milestone v1.4).</done>
</task>

</tasks>

<verification>
- Task 1 (automatizado) limpio antes de pasar a Task 2.
- Task 2 (checkpoint humano) confirma las 3 integraciones funcionando end-to-end y ausencia de regresión en proyectos sin datos de zonificación.
</verification>

<success_criteria>
Phase 12 completa: los 4 criterios de éxito de la fase (alerta citada en vía, cita PRC en due diligence, contexto de zona en copiloto, sin regresión para proyectos sin zonificación) están implementados, cableados y verificados por un humano en la app real.
</success_criteria>

<output>
After completion, create `.planning/phases/12-integracion-con-motores-de-decision/12-04-SUMMARY.md`
</output>
