---
phase: 12-integracion-con-motores-de-decision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/due-diligence.ts
  - app/api/ai/due-diligence/route.ts
autonomous: true

must_haves:
  truths:
    - "Cuando el proyecto tiene zona utilizable (zona_status='encontrado', zona_usos_disponibles=true) y la IA detecta incoherencia entre destino_sii y los usos permitidos/prohibidos de la zona, puede producir un hallazgo cuyo refNormativa incluye una cita fuente='PRC' resuelta directamente desde los datos de zona del proyecto (INTEG-02)"
    - "Cuando el proyecto NO tiene zona utilizable, ninguna cita PRC aparece — se descarta silenciosamente (continue), nunca se construye una cita a medias"
    - "Las citas OGUC/LGUC/DDU existentes se resuelven exactamente igual que antes (mismo getArticuloById, mismo verificado/etiqueta/url) — este plan no toca esa rama"
    - "components/proyecto/due-diligence-report.tsx (CitaBadges) sigue renderizando cualquier cita, incluida PRC, sin ningún cambio de código — es genérico sobre `fuente`"
  artifacts:
    - path: "lib/due-diligence.ts"
      provides: "type FuenteHallazgo = FuenteNormativa | 'PRC'; RefNormativa.fuente: FuenteHallazgo; ProyectoContexto extendido con destino_sii + zona_*; resolverRefNormativa(raw, proyecto) con rama PRC; buildSynthesisPrompt() incluye sección '## Zonificación (PRC)' cuando aplica"
      exports: ["FuenteHallazgo", "RefNormativa", "ProyectoContexto", "synthesizeDueDiligence"]
    - path: "app/api/ai/due-diligence/route.ts"
      provides: "ProyectoRow y el objeto proyectoContexto en procesar() extendidos con destino_sii + los mismos campos zona_* (select('*') ya los trae; solo faltaba el narrowing de tipos)"
  key_links:
    - from: "app/api/ai/due-diligence/route.ts (procesar)"
      to: "lib/due-diligence.ts (synthesizeDueDiligence)"
      via: "objeto proyectoContexto: ProyectoContexto con los campos zona_* poblados desde la fila de Supabase"
      pattern: "zona_uperm|zona_status"
    - from: "lib/due-diligence.ts (resolverRefNormativa, fuente==='PRC')"
      to: "proyecto.zona_codigo / zona_nombre / zona_fuente_url"
      via: "construcción directa de la cita — NUNCA getArticuloById, que no conoce PRC"
      pattern: "fuenteRaw === 'PRC'"
---

<objective>
Implementa INTEG-02: `due-diligence.ts` puede fundamentar un hallazgo citando la zona del proyecto como fuente `'PRC'` — un tipo local nuevo en `RefNormativa.fuente`, resuelto directamente desde los datos de zona ya persistidos en el proyecto (nunca desde la base curada OGUC/LGUC/DDU), cuando la síntesis IA detecta incoherencia entre el destino declarado (`destino_sii`) y los usos permitidos/prohibidos de la zona.

Purpose: dar al informe de Due Diligence una tercera fuente de fundamento normativo — la zonificación real del proyecto — sin ensuciar `normativa-retrieval.ts` (que NO sabe resolver PRC) ni el render genérico de citas que ya existe.
Output: `lib/due-diligence.ts` con el tipo `FuenteHallazgo`, `ProyectoContexto` ampliado, y `resolverRefNormativa()` con rama PRC; `app/api/ai/due-diligence/route.ts` con `ProyectoRow`/`proyectoContexto` ampliados en lockstep (Pitfall 5 del research).
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-integracion-con-motores-de-decision/12-RESEARCH.md
@lib/due-diligence.ts
@lib/normativa-retrieval.ts
@lib/zonificacion-format.ts
@app/api/ai/due-diligence/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FuenteHallazgo, ProyectoContexto ampliado, rama PRC en resolverRefNormativa y prompt</name>
  <files>lib/due-diligence.ts</files>
  <action>
1. Importa `fixMojibakeArcGIS` desde `@/lib/zonificacion-format` (nueva import, junto a las de `@/lib/ai` y `@/lib/normativa-retrieval`).

2. Agrega el tipo local (NO extiendas `FuenteNormativa` en `lib/normativa-retrieval.ts` — forzaría a `getArticuloById`/`getContextoNormativo`/`flagUnverifiedCita` a manejar una fuente que no pueden resolver, ver Anti-Pattern del research):
```typescript
export type FuenteHallazgo = FuenteNormativa | 'PRC'
```
Cambia `RefNormativa.fuente: FuenteNormativa` a `fuente: FuenteHallazgo`.

3. Extiende `ProyectoContexto` (todos opcionales, igual que el resto de la interfaz) con `destino_sii` (necesario para que el prompt pueda comparar contra los usos de la zona — hoy `ProyectoContexto` no lo tiene) y los campos `zona_*`:
```typescript
export interface ProyectoContexto {
  nombre: string
  direccion?: string | null
  municipio?: string | null
  rol_sii?: string | null
  tipo?: string | null
  destino_sii?: string | null
  zona_status?: string | null
  zona_usos_disponibles?: boolean | null
  zona_codigo?: string | null
  zona_nombre?: string | null
  zona_uperm?: string | null
  zona_uproh?: string | null
  zona_fuente_url?: string | null
}
```

4. Agrega una sección de zonificación a `buildSynthesisPrompt()`, con el MISMO guard compuesto usado en Phase 11/12 (`zona_status === 'encontrado' && zona_usos_disponibles === true` — nunca solo `zona_status`, ver Pitfall 1 del research: el caso Ñuñoa tiene `zona_status: 'encontrado'` pero sin texto que comparar), aplicando `fixMojibakeArcGIS` a `zona_uperm`/`zona_uproh`/`zona_nombre` ANTES de inyectarlos en el prompt (el research marca esto como pitfall obligatorio para todo código nuevo de esta fase):
```typescript
function seccionZonaProyecto(proyecto: ProyectoContexto): string {
  const utilizable = proyecto.zona_status === 'encontrado' && proyecto.zona_usos_disponibles === true
  if (!utilizable) return ''
  const uperm = fixMojibakeArcGIS(proyecto.zona_uperm) ?? '(sin dato)'
  const uproh = fixMojibakeArcGIS(proyecto.zona_uproh) ?? '(sin dato)'
  const nombre = fixMojibakeArcGIS(proyecto.zona_nombre)
  return `\n## Zonificación (PRC) del proyecto\n- Zona: ${proyecto.zona_codigo ?? ''}${nombre ? ` — ${nombre}` : ''}\n- Usos permitidos: ${uperm}\n- Usos prohibidos: ${uproh}\n- Destino declarado (SII): ${proyecto.destino_sii ?? 'no informado'}\n`
}
```
Llama a esta función dentro de `buildSynthesisPrompt(extracts, proyecto)` e inyecta su resultado justo después de la sección "## Contexto normativo (OGUC · LGUC · DDU)" (antes de `${REGLAS_CITACION}`).

5. Actualiza la instrucción 4 del prompt (fundamento normativo) para mencionar la opción PRC, y el ejemplo de JSON al final para incluir `PRC` como fuente válida. Reemplaza:
```
4. FUNDAMENTO NORMATIVO — obligatorio: por cada hallazgo, en "refNormativa", cita el/los artículo(s) del CONTEXTO NORMATIVO que lo fundan, usando el "id" EXACTO tal como aparece (OGUC/LGUC: el número de artículo, ej. "5.1.2"; DDU: el id "ddu-###" o el número). Cita SOLO ids presentes en el contexto. Si NINGÚN artículo del contexto funda el hallazgo, deja "refNormativa": [] (se mostrará como "sin fundamento verificado" — es preferible a inventar).
```
por:
```
4. FUNDAMENTO NORMATIVO — obligatorio: por cada hallazgo, en "refNormativa", cita el/los artículo(s) del CONTEXTO NORMATIVO que lo fundan, usando el "id" EXACTO tal como aparece (OGUC/LGUC: el número de artículo, ej. "5.1.2"; DDU: el id "ddu-###" o el número). Cita SOLO ids presentes en el contexto. Si además, según la sección "Zonificación (PRC) del proyecto" (cuando esté presente), el DESTINO DECLARADO no calza con los usos permitidos de la zona (o coincide con un uso prohibido), agrega un hallazgo adicional citando "fuente": "PRC" (el "id" puede ser cualquier valor, ej. "zona" — se resuelve automáticamente contra los datos de zona del proyecto, no contra este contexto). Si NINGÚN artículo del contexto funda un hallazgo y no aplica PRC, deja "refNormativa": [] (se mostrará como "sin fundamento verificado" — es preferible a inventar).
```
Y actualiza el ejemplo de salida JSON al final de `buildSynthesisPrompt`, cambiando `"fuente": "OGUC|LGUC|DDU"` por `"fuente": "OGUC|LGUC|DDU|PRC"`.

6. Cambia la firma de `resolverRefNormativa` para recibir el contexto del proyecto y bifurcar en `'PRC'` ANTES de la validación `FUENTES_VALIDAS` (que solo cubre OGUC/LGUC/DDU y debe seguir intacta para esa rama):
```typescript
function resolverRefNormativa(raw: RawRefNormativa[] | undefined, proyecto: ProyectoContexto): RefNormativa[] {
  if (!Array.isArray(raw)) return []
  const out: RefNormativa[] = []
  const zonaUtilizable = proyecto.zona_status === 'encontrado' && proyecto.zona_usos_disponibles === true
  for (const r of raw) {
    const fuenteRaw = typeof r.fuente === 'string' ? r.fuente.toUpperCase() : null
    if (fuenteRaw === 'PRC') {
      if (zonaUtilizable) {
        const nombre = fixMojibakeArcGIS(proyecto.zona_nombre)
        out.push({
          fuente: 'PRC',
          id: proyecto.zona_codigo ?? 'zona',
          etiqueta: `Zona ${proyecto.zona_codigo ?? ''}${nombre ? ` — ${nombre}` : ''}`.trim(),
          url: proyecto.zona_fuente_url ?? undefined,
          verificado: true,
        })
      }
      // zona no utilizable: descarta la cita silenciosamente, no empuja nada a `out`.
      continue
    }
    const fuente = fuenteRaw && FUENTES_VALIDAS.includes(fuenteRaw as FuenteNormativa) ? (fuenteRaw as FuenteNormativa) : null
    const id = typeof r.id === 'string' ? r.id.trim() : ''
    if (!fuente || !id) continue
    const art = getArticuloById(fuente, id)
    if (art) {
      out.push({ fuente, id: art.id, etiqueta: art.etiqueta, url: urlDeCitable(art), verificado: art.verificado })
    } else {
      out.push({ fuente, id, etiqueta: flagUnverifiedCita(`Art. ${id} ${fuente}`), verificado: false })
    }
  }
  return out
}
```
(Nota: la rama OGUC/LGUC/DDU de arriba es idéntica en efecto a la actual — solo reordenada para insertar la rama PRC antes; verifica que el comportamiento para esas tres fuentes sea byte-a-byte el mismo que hoy.)

7. Actualiza el único call site, dentro de `synthesizeDueDiligence`, para pasar `proyecto`:
```typescript
refNormativa: resolverRefNormativa(h.refNormativa, proyecto),
```
  </action>
  <verify>`npx tsc --noEmit` limpio. `grep -n "FuenteHallazgo\|fuenteRaw === 'PRC'" lib/due-diligence.ts` muestra los cambios. Confirma con lectura del archivo que la rama OGUC/LGUC/DDU sigue usando `getArticuloById`/`urlDeCitable`/`flagUnverifiedCita` exactamente como antes.</verify>
  <done>Un hallazgo con `refNormativa: [{fuente:'PRC', id:'zona'}]` producido por la IA se resuelve, cuando la zona es utilizable, a una cita con `verificado: true` construida desde los datos de zona del proyecto; se descarta silenciosamente cuando la zona no es utilizable. Las citas OGUC/LGUC/DDU no cambian de comportamiento.</done>
</task>

<task type="auto">
  <name>Task 2: Enhebrar destino_sii + zona_* en el route de Due Diligence</name>
  <files>app/api/ai/due-diligence/route.ts</files>
  <action>
El `select('*')` de la línea 216 ya trae todas las columnas `zona_*` y `destino_sii` de la fila de Supabase — falta ensanchar el tipo `ProyectoRow` y el objeto `proyectoContexto` literal en `procesar()` para que TypeScript los deje pasar (Pitfall 5 del research: sin esto, `proyecto.zona_uperm` sería un error de tipo, que es justamente la red de seguridad — arréglalo ensanchando, no ignorando el error).

Extiende la interfaz `ProyectoRow` (líneas 37-44):
```typescript
interface ProyectoRow {
  id: string
  nombre: string
  direccion: string | null
  municipio: string | null
  rol_sii: string | null
  tipo: string | null
  destino_sii: string | null
  zona_status: string | null
  zona_usos_disponibles: boolean | null
  zona_codigo: string | null
  zona_nombre: string | null
  zona_uperm: string | null
  zona_uproh: string | null
  zona_fuente_url: string | null
}
```

Extiende el objeto `proyectoContexto` dentro de `procesar()` (líneas 166-172):
```typescript
const proyectoContexto: ProyectoContexto = {
  nombre: proyecto.nombre,
  direccion: proyecto.direccion,
  municipio: proyecto.municipio,
  rol_sii: proyecto.rol_sii,
  tipo: proyecto.tipo,
  destino_sii: proyecto.destino_sii,
  zona_status: proyecto.zona_status,
  zona_usos_disponibles: proyecto.zona_usos_disponibles,
  zona_codigo: proyecto.zona_codigo,
  zona_nombre: proyecto.zona_nombre,
  zona_uperm: proyecto.zona_uperm,
  zona_uproh: proyecto.zona_uproh,
  zona_fuente_url: proyecto.zona_fuente_url,
}
```
No se necesita ningún otro cambio en este archivo — la query, el `after()`, la creación del reporte y el resto de `procesar()` quedan igual.
  </action>
  <verify>`npx tsc --noEmit` limpio (confirma que `ProyectoRow` satisface los campos que `ProyectoContexto` ahora exige opcionalmente, y que no quedó ningún campo del literal sin tipo). `grep -n "zona_uperm\|destino_sii" app/api/ai/due-diligence/route.ts` muestra las tres apariciones (interfaz, literal, comentario si aplica).</verify>
  <done>ProyectoRow y proyectoContexto incluyen destino_sii y los 7 campos zona_* en lockstep con lib/due-diligence.ts — la síntesis recibe el contexto de zona completo para proyectos que lo tienen.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` y `npx eslint lib/due-diligence.ts app/api/ai/due-diligence/route.ts` limpios.
- `components/proyecto/due-diligence-report.tsx` no requiere ningún cambio — confírmalo con `git diff --stat components/proyecto/due-diligence-report.tsx` vacío al terminar este plan.
- No hay trabajo de base de datos (las columnas zona_* ya existen desde Phase 10/11) ni instalaciones npm en este plan.
</verification>

<success_criteria>
INTEG-02 completo: due-diligence.ts puede citar la zona como fuente 'PRC' de un hallazgo cuando corresponde, con el mismo guard de zona utilizable que el resto de la fase, sin degradar las citas OGUC/LGUC/DDU existentes ni requerir cambios en la UI de renderizado (criterios de éxito 2 y 4 de la fase, para esta integración).
</success_criteria>

<output>
After completion, create `.planning/phases/12-integracion-con-motores-de-decision/12-02-SUMMARY.md`
</output>
