---
phase: 12-integracion-con-motores-de-decision
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/ai/copiloto/route.ts
autonomous: true

must_haves:
  truths:
    - "Cuando el proyecto tiene zona utilizable (zona_status='encontrado', zona_usos_disponibles=true), el prompt que arma buildOgucPrompt() y el que arma buildChecklistPrompt() incluyen una sección con los usos permitidos/prohibidos de la zona, reparados de mojibake (INTEG-03)"
    - "Cuando el proyecto NO tiene zona utilizable, ambos prompts son idénticos a los de antes de este plan (la sección nueva devuelve string vacío)"
    - "buildObservacionesPrompt() y buildEstimacionPrompt() no fueron modificados en este plan (decisión ya tomada por el research para la Open Question 1 — alcance literal del requirement)"
  artifacts:
    - path: "app/api/ai/copiloto/route.ts"
      provides: "función seccionZonificacion(p: Proyecto): string, guardada por la condición compuesta de zona utilizable, aplicando fixMojibakeArcGIS a zona_uperm/zona_uproh/zona_nombre; invocada dentro de buildOgucPrompt() y buildChecklistPrompt()"
  key_links:
    - from: "app/api/ai/copiloto/route.ts (buildOgucPrompt, buildChecklistPrompt)"
      to: "seccionZonificacion(p)"
      via: "string interpolado dentro del template, después de la sección '## Datos del proyecto' / '## Proyecto'"
      pattern: "seccionZonificacion"
    - from: "seccionZonificacion"
      to: "lib/zonificacion-format.ts (fixMojibakeArcGIS)"
      via: "reparación de mojibake ANTES de inyectar zona_uperm/zona_uproh/zona_nombre en el prompt (pitfall pre-existente de compatibilidad/route.ts que este plan NO debe repetir)"
      pattern: "fixMojibakeArcGIS"
---

<objective>
Implementa INTEG-03: los skills de diagnóstico OGUC y checklist del copiloto IA reciben el texto de usos permitidos/prohibidos de la zona del proyecto como contexto adicional, cuando el proyecto tiene un resultado de zonificación disponible.

Purpose: que el copiloto razone con la zonificación real del proyecto al diagnosticar cumplimiento normativo (OGUC) y al armar el checklist de documentos — sin tocar `app/api/ai/copiloto/route.ts` más allá de las dos funciones de armado de prompt que el requirement nombra explícitamente, ni los otros dos skills (observaciones/estimación), que el research recomienda dejar fuera de alcance para esta fase.
Output: `seccionZonificacion()` — helper pequeño, guardado, mojibake-safe — inyectado en `buildOgucPrompt()` y `buildChecklistPrompt()`.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-integracion-con-motores-de-decision/12-RESEARCH.md
@app/api/ai/copiloto/route.ts
@lib/zonificacion-format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: seccionZonificacion() en buildOgucPrompt y buildChecklistPrompt</name>
  <files>app/api/ai/copiloto/route.ts</files>
  <action>
Este route ya hace `select('*')` y castea a `const p = proyecto as Proyecto` (líneas 172-182 actuales), así que `p.zona_uperm`/`p.zona_uproh`/`p.zona_status`/`p.zona_usos_disponibles`/`p.zona_codigo`/`p.zona_nombre` ya son type-safe sin ningún cambio en la query (Pitfall 6 del research: este es el caso "fácil", a diferencia de due-diligence).

1. Agrega la importación `import { fixMojibakeArcGIS } from '@/lib/zonificacion-format'` junto a las demás importaciones de `lib/`.

2. Agrega la función helper, ANTES de `buildOgucPrompt` (o junto a las demás funciones `build*Prompt`), con el MISMO guard compuesto usado en el resto de la fase (`zona_status === 'encontrado' && zona_usos_disponibles === true` — nunca solo `zona_status`, por el caso Ñuñoa donde no hay texto que citar):
```typescript
function seccionZonificacion(p: Proyecto): string {
  const utilizable = p.zona_status === 'encontrado' && p.zona_usos_disponibles === true
  if (!utilizable) return ''
  const uperm = fixMojibakeArcGIS(p.zona_uperm) ?? '(sin dato)'
  const uproh = fixMojibakeArcGIS(p.zona_uproh) ?? '(sin dato)'
  const nombre = fixMojibakeArcGIS(p.zona_nombre)
  return `\n## Zonificación (PRC) — ${p.zona_codigo ?? ''}${nombre ? ` ${nombre}` : ''}\nUsos permitidos: ${uperm}\nUsos prohibidos: ${uproh}\n`
}
```

3. En `buildOgucPrompt(p)`, inserta `${seccionZonificacion(p)}` inmediatamente después del bloque `## Datos del proyecto` (después de la línea `- Avalúo fiscal: ...` y antes de `## Artículos OGUC relevantes`), mismo idioma que `statsSection`/`intelSection` en `buildObservacionesPrompt` (secciones condicionales que se interpolan vacías cuando no aplican).

4. En `buildChecklistPrompt(p)`, inserta `${seccionZonificacion(p)}` inmediatamente después del bloque `## Proyecto` (después de la línea `- Expediente: ...` y antes de `Responde SOLO con JSON válido:`).

5. NO toques `buildObservacionesPrompt` ni `buildEstimacionPrompt` — el requirement nombra explícitamente solo "diagnóstico OGUC" y "checklist"; el research resolvió esto como alcance literal para esta fase (Open Question 1), dejando la ampliación a los otros dos skills como trabajo futuro, no de este plan.

6. NO toques el resto del archivo (la query `select('*')`, el `Promise.all` de los 4 skills, la lógica de checklist idempotente, `max_tokens`, etc.) — el único cambio es la inyección de `seccionZonificacion(p)` en dos template strings.
  </action>
  <verify>`npx tsc --noEmit` limpio. `npx eslint app/api/ai/copiloto/route.ts` limpio. `grep -n "seccionZonificacion" app/api/ai/copiloto/route.ts` muestra exactamente 3 apariciones (definición + 2 usos, en buildOgucPrompt y buildChecklistPrompt). `git diff app/api/ai/copiloto/route.ts` confirma que buildObservacionesPrompt y buildEstimacionPrompt no aparecen en el diff.</verify>
  <done>Un proyecto con zona utilizable ve una sección "## Zonificación (PRC)" con usos permitidos/prohibidos reparados de mojibake en los prompts de OGUC y checklist; un proyecto sin zona utilizable produce el mismo prompt de siempre (sección vacía). buildObservacionesPrompt/buildEstimacionPrompt sin cambios.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` y `npx eslint app/api/ai/copiloto/route.ts` limpios.
- No hay trabajo de base de datos ni de MCP ni instalaciones npm en este plan — el route ya hace `select('*')` y ya castea a `Proyecto`, que ya incluye los campos `zona_*` desde Phase 10/11.
</verification>

<success_criteria>
INTEG-03 completo: los skills de diagnóstico OGUC y checklist reciben el contexto de zonificación cuando está disponible, sin afectar los otros dos skills ni proyectos sin zonificación consultada (criterios de éxito 3 y 4 de la fase, para esta integración).
</success_criteria>

<output>
After completion, create `.planning/phases/12-integracion-con-motores-de-decision/12-03-SUMMARY.md`
</output>
