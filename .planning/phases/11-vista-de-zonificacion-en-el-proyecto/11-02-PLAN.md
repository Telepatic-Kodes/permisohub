---
phase: 11-vista-de-zonificacion-en-el-proyecto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/zonificacion-zonas.ts
  - app/api/zonificacion/zonas/route.ts
autonomous: true

must_haves:
  truths:
    - "El arquitecto puede obtener la lista de zonas de una comuna cubierta sin conocer coordenadas ni haber geocodificado nada — insumo directo del fallback manual (ZONE-05)"
    - "La lista de zonas se consulta en vivo contra el mismo FeatureServer ArcGIS ya registrado en lib/zonificacion-comunas.ts, nunca una lista curada a mano que puede quedar obsoleta"
    - "Pedir el detalle de una zona específica retorna sus usos permitidos/prohibidos verbatim, igual que el lookup automático — la selección manual no es una experiencia degradada en cuanto a contenido"
  artifacts:
    - path: "lib/zonificacion-zonas.ts"
      provides: "fetchZonasDisponibles(comunaId) y fetchZonaDetalle(comunaId, zona) — consultas ArcGIS 'distinct values' y 'por zona', sin geometría de punto"
      exports: ["fetchZonasDisponibles", "fetchZonaDetalle"]
    - path: "app/api/zonificacion/zonas/route.ts"
      provides: "GET público (sin auth, mismo patrón que /api/zonificacion/lookup) que sirve el listado de comunas cubiertas y, por comuna, el listado de zonas"
      exports: ["GET"]
  key_links:
    - from: "app/api/zonificacion/zonas/route.ts"
      to: "lib/zonificacion-comunas.ts"
      via: "resolveComunaZonificacion(comuna) / getComunasConCobertura() — mismo registro que ya usa /api/zonificacion/lookup, cero duplicación"
      pattern: "resolveComunaZonificacion|getComunasConCobertura"
    - from: "lib/zonificacion-zonas.ts"
      to: "ArcGIS FeatureServer /query"
      via: "where=1=1&returnDistinctValues=true&returnGeometry=false para el listado; where=<campo zona>='<valor>' para el detalle"
      pattern: "returnDistinctValues"
---

<objective>
Build the data source ZONE-05's manual fallback needs but Phase 10 never built: a way to list the zone codes that exist *within* a covered comuna (Phase 10's registry only says *which comunas* are covered, not what zones live inside them), plus a way to fetch one zone's verbatim usos permitidos/prohibidos once the architect picks it.

Purpose: when geocoding fails or the requested comuna has no ArcGIS coverage, ZONE-05 requires the architect see a real select-from-a-list recovery path, not a dead end. This plan builds the two ArcGIS queries that make that list possible — a "distinct values" query (no point geometry, no geocoding needed) reusing the exact registry (`lib/zonificacion-comunas.ts`) Phase 10 already built and verified against the 4 covered comunas. This is a self-contained, additive query pattern — it does not touch the existing `/api/zonificacion/lookup` route or any Phase 10 file.
Output: `lib/zonificacion-zonas.ts` (two pure fetch functions) and `app/api/zonificacion/zonas/route.ts` (public GET route serving both the comuna list and, given `?comuna=`, that comuna's zone list).
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-vista-de-zonificacion-en-el-proyecto/11-RESEARCH.md
@lib/zonificacion-comunas.ts
@app/api/zonificacion/lookup/route.ts
@lib/rate-limit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ArcGIS distinct-values + zone-detail queries</name>
  <files>lib/zonificacion-zonas.ts</files>
  <action>
Create `lib/zonificacion-zonas.ts`. Two exported async functions, both server-only (plain `fetch`, no Supabase — this is a stateless ArcGIS proxy, same category as the existing lookup route's ArcGIS call):

```typescript
// Consultas ArcGIS complementarias al lookup por punto (app/api/zonificacion/lookup/route.ts):
// aquí NO hay geometría de consulta — se listan valores distintos de zona dentro
// de una comuna, y se trae el detalle de una zona elegida a mano. Fallback manual
// de ZONE-05: reutiliza el mismo registro de lib/zonificacion-comunas.ts, sin
// curación estática que pueda quedar desactualizada.
import { resolveComunaZonificacion } from './zonificacion-comunas'

export interface ZonaListItem {
  zona: string
  nombre: string
}

export interface ZonaDetalle {
  zona: string
  nombre: string
  sector: string | null
  uperm: string | null
  uproh: string | null
  usosDisponibles: boolean
  fuenteUrl: string | null
}

function attrString(attrs: Record<string, unknown>, key: string | undefined): string | null {
  if (!key) return null
  const v = attrs[key]
  return typeof v === 'string' && v.trim() !== '' ? v.trim() : null
}

export async function fetchZonasDisponibles(comunaId: string): Promise<ZonaListItem[] | null> {
  const config = resolveComunaZonificacion(comunaId)
  if (!config) return null

  const url = new URL(`${config.featureServerUrl}/${config.layerIndex}/query`)
  url.searchParams.set('f', 'json')
  url.searchParams.set('where', '1=1')
  url.searchParams.set('outFields', `${config.fieldMap.zona},${config.fieldMap.nombre}`)
  url.searchParams.set('returnDistinctValues', 'true')
  url.searchParams.set('returnGeometry', 'false')
  url.searchParams.set('orderByFields', config.fieldMap.zona)

  const res = await fetch(url.toString(), { signal: AbortSignal.timeout(10_000) })
  if (!res.ok) return null

  const json = (await res.json()) as { features?: { attributes: Record<string, unknown> }[] }
  const seen = new Set<string>()
  const out: ZonaListItem[] = []
  for (const row of json.features ?? []) {
    const zona = attrString(row.attributes, config.fieldMap.zona)
    if (!zona || seen.has(zona)) continue
    seen.add(zona)
    out.push({ zona, nombre: attrString(row.attributes, config.fieldMap.nombre) ?? zona })
  }
  return out
}

export async function fetchZonaDetalle(comunaId: string, zona: string): Promise<ZonaDetalle | null> {
  const config = resolveComunaZonificacion(comunaId)
  if (!config) return null

  const outFields = Object.values(config.fieldMap).join(',')
  const url = new URL(`${config.featureServerUrl}/${config.layerIndex}/query`)
  url.searchParams.set('f', 'json')
  url.searchParams.set('where', `${config.fieldMap.zona}='${zona.replace(/'/g, "''")}'`) // escape de comilla simple — el único caracter de riesgo en un WHERE ArcGIS de texto
  url.searchParams.set('outFields', outFields)
  url.searchParams.set('returnGeometry', 'false')
  url.searchParams.set('resultRecordCount', '1')

  const res = await fetch(url.toString(), { signal: AbortSignal.timeout(10_000) })
  if (!res.ok) return null

  const json = (await res.json()) as { features?: { attributes: Record<string, unknown> }[] }
  const attrs = json.features?.[0]?.attributes
  if (!attrs) return null

  const nombre = attrString(attrs, config.fieldMap.nombre)
  if (!nombre) return null

  return {
    zona,
    nombre,
    sector: attrString(attrs, config.fieldMap.sector),
    uperm: attrString(attrs, config.fieldMap.uperm),
    uproh: attrString(attrs, config.fieldMap.uproh),
    usosDisponibles: config.usosDisponibles, // registry-level flag, igual que el lookup route — nunca derivado de que uperm/uproh vengan vacíos (Pitfall 8)
    fuenteUrl: attrString(attrs, config.fieldMap.url),
  }
}
```

Note `usosDisponibles` comes from the registry config (`comunaConfig.usosDisponibles`), never inferred from whether `uperm`/`uproh` came back empty — same discipline as the existing lookup route, so a manually-selected Ñuñoa zone still correctly shows "usos no disponibles" rather than a false empty-usos-means-no-restrictions read.
  </action>
  <verify>`npx tsc --noEmit` passes. Manual live check (only needs network, no auth/DB): temporarily run `node -e` isn't viable for TS — instead verify via Task 2's route with curl once wired, or trust the direct structural parallel to the already-verified `app/api/zonificacion/lookup/route.ts` query construction (same `featureServerUrl`/`layerIndex`/`fieldMap` usage, already live-verified in Phase 10 for all 4 comunas).</verify>
  <done>Both functions exported, return `null` (not throw) on any ArcGIS/network failure or unknown comuna, never silently return an empty-but-truthy result for a comuna outside the registry.</done>
</task>

<task type="auto">
  <name>Task 2: Public zonas route</name>
  <files>app/api/zonificacion/zonas/route.ts</files>
  <action>
Create `app/api/zonificacion/zonas/route.ts`. Same auth posture as `app/api/zonificacion/lookup/route.ts` (no `createClient()`/`getUser()` — public, rate-limited by IP; this route never exposes tenant data, only public PRC zone lists for a named comuna):

```typescript
import { getComunasConCobertura, resolveComunaZonificacion } from '@/lib/zonificacion-comunas'
import { fetchZonasDisponibles } from '@/lib/zonificacion-zonas'
import { checkRateLimit } from '@/lib/rate-limit'

export const dynamic = 'force-dynamic'

export async function GET(request: Request): Promise<Response> {
  const { searchParams } = new URL(request.url)
  const comuna = searchParams.get('comuna')

  const ip = request.headers.get('x-forwarded-for')?.split(',')[0]?.trim()
    ?? request.headers.get('x-real-ip')
    ?? 'unknown'
  const rateLimit = await checkRateLimit(`zonificacion-zonas:${ip}`)
  if (rateLimit) return rateLimit

  // Sin ?comuna= — devuelve el listado de comunas cubiertas (paso 1 del select
  // en cascada del fallback manual, evita que el cliente necesite importar el
  // registro directamente).
  if (!comuna) {
    return Response.json({
      comunas: getComunasConCobertura().map((c) => ({ comunaId: c.comunaId, tier: c.tier })),
    })
  }

  const config = resolveComunaZonificacion(comuna)
  if (!config) {
    return Response.json({ error: `Sin cobertura ArcGIS para "${comuna}"` }, { status: 404 })
  }

  const zonas = await fetchZonasDisponibles(config.comunaId)
  if (zonas === null) {
    return Response.json({ error: 'No se pudo consultar el listado de zonas' }, { status: 502 })
  }

  return Response.json({ comunaId: config.comunaId, zonas })
}
```
  </action>
  <verify>With a local dev server running: `curl "http://localhost:7891/api/zonificacion/zonas"` returns `{"comunas":[...4 entries...]}`. `curl "http://localhost:7891/api/zonificacion/zonas?comuna=las-condes"` returns `{"comunaId":"las-condes","zonas":[...]}` with real zone codes/names from the live ArcGIS layer. `curl "http://localhost:7891/api/zonificacion/zonas?comuna=temuco"` (uncovered) returns 404 with an explicit error, zero network calls to ArcGIS.</verify>
  <done>Route live-verified against at least one real covered comuna (returns a non-empty zone list) and one uncovered comuna (returns 404, no ArcGIS call). No auth required, matching the public `/api/zonificacion/lookup` precedent.</done>
</task>

</tasks>

<verification>
- `GET /api/zonificacion/zonas` (no params) returns the 4 covered comunas.
- `GET /api/zonificacion/zonas?comuna=las-condes` returns real, distinct zone codes from the live ArcGIS layer (not a static/hardcoded list).
- An uncovered comuna returns 404 without attempting any ArcGIS call.
- `npx tsc --noEmit` and `npx eslint lib/zonificacion-zonas.ts app/api/zonificacion/zonas/route.ts` clean.
</verification>

<success_criteria>
- The manual-fallback UI (built in Plan 11-08) has everything it needs to populate a two-step comuna→zona picker with live data, with zero static curation.
</success_criteria>

<output>
After completion, create `.planning/phases/11-vista-de-zonificacion-en-el-proyecto/11-02-SUMMARY.md`
</output>
