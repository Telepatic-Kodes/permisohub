---
phase: 13-refactor-de-scoring-dashboard-de-detalle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/mercado-locales-server.ts
  - tests/unit/evaluar-oportunidad.test.ts
autonomous: true

must_haves:
  truths:
    - "evaluarOportunidad() es una función pura y exportada que reproduce, para los mismos inputs, exactamente los mismos reasonCodes que el código inline que reemplaza (below_p25_ufm2 y below_p25_uf siguen siendo mutuamente excluyentes vía else-if — nunca ambos a la vez)"
    - "price_drop_7d solo se agrega cuando las DOS ÚLTIMAS entradas de historialReciente (no el mínimo/máximo de la ventana de 7 días) muestran una baja"
    - "obtenerOportunidadesMercadoLocales() sigue devolviendo, para los mismos datos de entrada, resultados idénticos a los de antes del refactor — ninguno de los 5 call sites del proyecto nota diferencia de comportamiento"
  artifacts:
    - path: "lib/mercado-locales-server.ts"
      provides: "evaluarOportunidad() exportada + obtenerOportunidadesMercadoLocales() refactorizada para llamarla en vez de repetir la lógica inline"
      exports: ["evaluarOportunidad"]
    - path: "tests/unit/evaluar-oportunidad.test.ts"
      provides: "Cobertura RED→GREEN de las 4 combinaciones de reasonCodes (below_p25_ufm2, below_p25_uf, price_drop_7d, ninguno) más el caso de ventana de historial con rebote"
  key_links:
    - from: "lib/mercado-locales-server.ts (loop de obtenerOportunidadesMercadoLocales)"
      to: "evaluarOportunidad()"
      via: "llamada de función reemplazando el cálculo inline de reasonCodes (líneas 489-501 actuales)"
      pattern: "evaluarOportunidad\\("
---

<objective>
Extrae la lógica de scoring de `obtenerOportunidadesMercadoLocales()` (hoy inline dentro del loop, `lib/mercado-locales-server.ts:489-501`) a una función pura y exportada `evaluarOportunidad()`, con TDD (RED→GREEN→REFACTOR), preservando EXACTAMENTE el comportamiento actual.

Purpose: este es el prerequisito técnico bloqueante del resto de la fase (CONTEXT.md: "Incluye el refactor de evaluarOportunidad() como fuente única de verdad para scoring — prerequisito técnico, no en discusión"). La ficha de detalle (Plan 13-03 en adelante) necesita calcular reasonCodes para un listing individual y para cada comparable — sin esta extracción, esa lógica se duplicaría y list/detail divergirían con el tiempo (el bug exacto que esta fase existe para prevenir, ver Pitfall A de 13-RESEARCH.md).
Output: `evaluarOportunidad()` exportada desde `lib/mercado-locales-server.ts`, cubierta por tests unitarios; `obtenerOportunidadesMercadoLocales()` sin cambio de comportamiento observable.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-RESEARCH.md
@lib/mercado-locales-server.ts
@tests/unit/histograma-binning.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Tests para evaluarOportunidad()</name>
  <files>tests/unit/evaluar-oportunidad.test.ts</files>
  <action>
Crea `tests/unit/evaluar-oportunidad.test.ts` (mismo patrón que `tests/unit/histograma-binning.test.ts`: `import { describe, expect, it } from 'vitest'`) importando `evaluarOportunidad` desde `@/lib/mercado-locales-server` (todavía no existe — este test debe fallar en este punto, es el RED).

La firma esperada (documéntala en un comentario arriba del describe, ya que la función aún no existe):
```ts
function evaluarOportunidad(params: {
  precioUf: number
  precioUfM2: number | null
  cohortP25Uf: number | null
  cohortP25UfM2: number | null
  historialReciente: { precio_monto: number; capturado_el: string }[]
}): string[]
```

Casos a cubrir (basados en la lógica actual, `lib/mercado-locales-server.ts:489-501`, leída en este mismo plan):

1. `precioUfM2 <= cohortP25UfM2` → `['below_p25_ufm2']` exactamente (no agrega `below_p25_uf` aunque `precioUf <= cohortP25Uf` también sea cierto — el `else if` original hace que sean mutuamente excluyentes).
2. `precioUfM2` es `null` y `precioUf <= cohortP25Uf` → `['below_p25_uf']`.
3. `precioUfM2` no null pero NO califica (`precioUfM2 > cohortP25UfM2`) y `precioUf <= cohortP25Uf` → `['below_p25_uf']` (cae al else).
4. `cohortP25Uf` y `cohortP25UfM2` ambos `null` → nunca agrega `below_p25_ufm2` ni `below_p25_uf`, sin importar cuán bajo sea el precio.
5. `historialReciente` con exactamente 2 entradas donde la última (`historialReciente[1]`) tiene `precio_monto` menor que la primera → incluye `'price_drop_7d'`.
6. `historialReciente` con 3 entradas donde `[0]=100, [1]=50, [2]=80` (bajó y luego subió) → NO incluye `price_drop_7d`, porque solo se comparan las ÚLTIMAS DOS entradas (`80 > 50`), no el mínimo de la ventana — este es el caso que verifica el comportamiento documentado en 13-RESEARCH.md ("no el mínimo/máximo de la ventana").
7. `historialReciente` con 1 sola entrada → nunca agrega `price_drop_7d` (requiere `length >= 2`).
8. Caso combinado: `below_p25_ufm2` + `price_drop_7d` pueden coexistir (son de mecanismos distintos, no se excluyen entre sí) → verifica que el array final tenga ambos.
9. Ningún criterio califica → devuelve `[]` (array vacío, no `null`/`undefined`).

Usa `expect(...).toEqual([...])` para verificar el array completo (orden incluido: `below_p25_ufm2`/`below_p25_uf` siempre antes que `price_drop_7d`, igual que el push order actual).
  </action>
  <verify>`npx vitest run tests/unit/evaluar-oportunidad.test.ts` — DEBE FALLAR (import error, `evaluarOportunidad` no existe todavía). Confirma que el fallo es por import/no-implementación, no por un typo del test.</verify>
  <done>Archivo de test creado con los 9 casos, falla en rojo por ausencia de la función.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Extraer evaluarOportunidad() y refactorizar el loop</name>
  <files>lib/mercado-locales-server.ts</files>
  <action>
Agrega, antes de `obtenerOportunidadesMercadoLocales` (o inmediatamente después de la interfaz `OportunidadMercadoLocal`), la función pura exportada:

```ts
/**
 * Fuente única de verdad para el scoring de "oportunidad" — extraída del
 * loop de obtenerOportunidadesMercadoLocales() para que la ficha de detalle
 * (Fase 13) y los comparables sugeridos puedan reproducir EXACTAMENTE los
 * mismos reasonCodes que la lista, en vez de reimplementar esta lógica y
 * arriesgar que ambas vistas diverjan con el tiempo.
 *
 * below_p25_ufm2 y below_p25_uf son mutuamente excluyentes a propósito
 * (else if, no dos ifs independientes) — preservado tal cual del código
 * original, NO "corregido" a checks independientes.
 */
export function evaluarOportunidad(params: {
  precioUf: number
  precioUfM2: number | null
  cohortP25Uf: number | null
  cohortP25UfM2: number | null
  historialReciente: { precio_monto: number; capturado_el: string }[]
}): string[] {
  const { precioUf, precioUfM2, cohortP25Uf, cohortP25UfM2, historialReciente } = params
  const reasonCodes: string[] = []

  if (precioUfM2 !== null && cohortP25UfM2 !== null && precioUfM2 <= cohortP25UfM2) {
    reasonCodes.push('below_p25_ufm2')
  } else if (cohortP25Uf !== null && precioUf <= cohortP25Uf) {
    reasonCodes.push('below_p25_uf')
  }

  if (historialReciente.length >= 2) {
    const last = historialReciente[historialReciente.length - 1]
    const prev = historialReciente[historialReciente.length - 2]
    if (last.precio_monto < prev.precio_monto) reasonCodes.push('price_drop_7d')
  }

  return reasonCodes
}
```

Luego, dentro del loop de `obtenerOportunidadesMercadoLocales` (líneas 489-501 actuales), reemplaza el bloque completo:

```ts
const reasonCodes: string[] = []
if (precioUfM2 !== null && cohort.p25_uf_m2 !== null && precioUfM2 <= cohort.p25_uf_m2) {
  reasonCodes.push('below_p25_ufm2')
} else if (cohort.p25_uf !== null && precioUf <= cohort.p25_uf) {
  reasonCodes.push('below_p25_uf')
}

const historial = historyByListing.get(listing.id as string) ?? []
if (historial.length >= 2) {
  const last = historial[historial.length - 1]
  const prev = historial[historial.length - 2]
  if (last.precio_monto < prev.precio_monto) reasonCodes.push('price_drop_7d')
}
```

por:

```ts
const reasonCodes = evaluarOportunidad({
  precioUf,
  precioUfM2,
  cohortP25Uf: cohort.p25_uf,
  cohortP25UfM2: cohort.p25_uf_m2,
  historialReciente: historyByListing.get(listing.id as string) ?? [],
})
```

NO toques nada más del loop (el `if (listing.precio_monto === null) continue`, el cálculo de `precioUf`/`precioUfM2`, el `if (reasonCodes.length > 0) { results.push(...) }`, ni el `results.sort(...)` final quedan exactamente iguales).
  </action>
  <verify>`npx vitest run tests/unit/evaluar-oportunidad.test.ts` pasa en verde (9/9). `npx tsc --noEmit` limpio.</verify>
  <done>evaluarOportunidad() existe, exportada, y el loop de obtenerOportunidadesMercadoLocales la usa en vez de lógica inline duplicada.</done>
</task>

<task type="auto">
  <name>Task 3: Verificación de preservación de comportamiento</name>
  <files>lib/mercado-locales-server.ts</files>
  <action>
No hay cambios de código en esta tarea — es una verificación explícita de que el refactor es behavior-preserving para los 5 call sites existentes de `obtenerOportunidadesMercadoLocales` (la firma de la función, sus parámetros y su tipo de retorno `OportunidadMercadoLocal[]` no cambiaron):

```
grep -rn "obtenerOportunidadesMercadoLocales(" --include=*.ts --include=*.tsx app lib
```

Confirma que los 5 sitios siguen siendo: `app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx`, `app/api/reportes-mercado/route.ts`, `app/(dashboard)/dashboard/page.tsx` (×2), `lib/mercado-inmobiliario-copiloto.ts` — y que ninguno requiere cambios (la firma de la función es idéntica a antes de este plan).
  </action>
  <verify>`npx tsc --noEmit` limpio en todo el proyecto (confirma que los 5 call sites siguen tipando correctamente sin modificación). `npx vitest run tests/unit/evaluar-oportunidad.test.ts` sigue en verde.</verify>
  <done>Confirmado: 5/5 call sites intactos, sin cambios de firma, todos los tests pasan.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/evaluar-oportunidad.test.ts` pasa (9 casos).
- `npx tsc --noEmit` limpio.
- `git diff lib/mercado-locales-server.ts` muestra únicamente: la nueva función `evaluarOportunidad` agregada + el bloque de 13 líneas del loop reemplazado por la llamada de 6 líneas — ningún otro cambio en el archivo.
</verification>

<success_criteria>
`evaluarOportunidad()` es la fuente única de verdad para el scoring de oportunidades, cubierta por tests, y `obtenerOportunidadesMercadoLocales()` preserva su comportamiento exacto — desbloquea los planes 13-03 en adelante, que reutilizan esta función para el listing individual y sus comparables.
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-01-SUMMARY.md`
</output>
