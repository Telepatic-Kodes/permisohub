---
phase: 13-refactor-de-scoring-dashboard-de-detalle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/formato-fecha.ts
  - lib/ai.ts
  - lib/resumen-oportunidad-prompts.ts
autonomous: true

must_haves:
  truths:
    - "lib/formato-fecha.ts existe y exporta formatFechaCorta con comportamiento idéntico al que hoy vive inline en oportunidades/page.tsx (mismo formato, mismo manejo de null, mismo `${iso}T00:00:00` + timeZone America/Santiago para campos date-only)"
    - "streamConContexto() en lib/ai.ts NO incluye el tool web_search_preview ni ningún otro tool — el modelo solo puede razonar sobre el texto que se le pasa en `input`, nunca buscar en vivo"
    - "buildUserQueryResumenOportunidad() interpola los campos numéricos reales del contexto (muestra_n, percentiles, comparables) y marca explícitamente como 'no disponible' los campos null, nunca fabrica un valor"
  artifacts:
    - path: "lib/formato-fecha.ts"
      provides: "formatFechaCorta(iso: string | null): string | null, movida verbatim desde oportunidades/page.tsx:26-31"
      exports: ["formatFechaCorta"]
    - path: "lib/ai.ts"
      provides: "streamConContexto(instructions, input) — como streamConBusquedaWeb pero sin tools, para dominios con datos reales ya calculados server-side"
      exports: ["streamConContexto"]
    - path: "lib/resumen-oportunidad-prompts.ts"
      provides: "ResumenOportunidadContexto (tipo del body que viajará a la ruta SSE), buildSystemResumenOportunidad(), buildUserQueryResumenOportunidad(ctx)"
      exports: ["ResumenOportunidadContexto", "buildSystemResumenOportunidad", "buildUserQueryResumenOportunidad"]
  key_links: []
---

<objective>
Construye las tres piezas de utilidad compartida que el resto de la fase necesita y que hoy NO existen: el formateador de fecha compartido (evita duplicar `formatFechaCorta` en la ficha de detalle), la función de streaming de IA sin búsqueda web (DETA-06, corrección de 13-RESEARCH.md respecto a `streamConBusquedaWeb`), y el constructor de prompts del resumen ejecutivo.

Purpose: estas tres piezas son consumidas por planes posteriores (13-04 usa `streamConContexto` + los builders de prompt; 13-05/13-06/13-07 usarán `formatFechaCorta`) pero no dependen de nada de la fase — pueden construirse en paralelo con el refactor de scoring (13-01).
Output: `lib/formato-fecha.ts` (nuevo), `lib/ai.ts` extendido, `lib/resumen-oportunidad-prompts.ts` (nuevo). Ningún archivo existente que ya use `formatFechaCorta` inline se modifica en este plan — eso lo hace 13-07 al conectar la ficha.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-RESEARCH.md
@lib/ai.ts
@lib/tasacion-prompts.ts
@app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/formato-fecha.ts</name>
  <files>lib/formato-fecha.ts</files>
  <action>
Crea `lib/formato-fecha.ts` con la función movida VERBATIM desde `app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx:26-31`:

```ts
/**
 * Formateador compartido de fecha corta (mes+año, es-CL) — extraído de
 * oportunidades/page.tsx antes de escribir el primer componente nuevo que
 * muestre fechas (Fase 13, ficha de detalle) para no duplicar esta lógica.
 *
 * IMPORTANTE (Pitfall D, ver 13-RESEARCH.md): esta función espera un campo
 * DATE-ONLY (ej. `stats_date` de mercado_locales_stats_diarias, o
 * `fecha_registro` de cadenas_sucursales) — por eso fuerza `T00:00:00` antes
 * de parsear, para no perder un día por interpretación UTC. NO la uses para
 * campos `timestamptz` que YA incluyen hora real (`primera_vez_visto_el`,
 * `ultima_vez_visto_el`, `capturado_el`, `dado_de_baja_el`) — esos se
 * parsean con `new Date(iso)` directo, sin el sufijo, o se introduce el
 * bug inverso (forzar medianoche sobre un timestamp real).
 */
export function formatFechaCorta(iso: string | null): string | null {
  if (!iso) return null
  return new Intl.DateTimeFormat("es-CL", { month: "short", year: "numeric", timeZone: "America/Santiago" }).format(
    new Date(`${iso}T00:00:00`)
  )
}
```

NO modifiques `oportunidades/page.tsx` en esta tarea — sigue teniendo su propia copia local por ahora; 13-07 la reemplaza por un import de este archivo compartido.
  </action>
  <verify>`npx tsc --noEmit` limpio. `node -e "require('ts-node/register'); "` no aplica (proyecto usa Next/ESM) — verificación suficiente: el archivo compila y exporta correctamente vía `npx tsc --noEmit`.</verify>
  <done>lib/formato-fecha.ts existe, exporta formatFechaCorta con el mismo comportamiento que la copia inline actual.</done>
</task>

<task type="auto">
  <name>Task 2: streamConContexto() en lib/ai.ts</name>
  <files>lib/ai.ts</files>
  <action>
Agrega, inmediatamente después de `streamConBusquedaWeb` (línea 187 actual), la nueva función:

```ts
// Como streamConBusquedaWeb pero SIN el tool web_search_preview — para
// dominios donde los datos reales ya existen server-side (Fase 13:
// mercado_locales_stats_diarias, comparables, historial de precio) y el rol
// del modelo es narrar/citar esos números, no inventar contexto de mercado
// vía búsqueda. Reusar streamConBusquedaWeb acá dejaría que el modelo
// busque en vivo y devuelva un rango "plausible" pero no anclado a los
// datos reales de PermisoHub — exactamente el riesgo de fabricación que el
// resto del proyecto evita (ver AUDIT-FIDELIDAD-DATOS-2026-07-30.md). Sin
// `tools` en el payload: no es solo una instrucción de prompt (que el
// modelo podría ignorar), es estructuralmente imposible que busque.
export async function streamConContexto(instructions: string, input: string) {
  const ai = getAI()
  if (!ai) throw new Error('OPENAI_API_KEY no configurado')
  return ai.responses.create({
    model: AI_MODEL,
    instructions,
    input,
    stream: true,
    max_output_tokens: 3000,
  })
}
```

`max_output_tokens: 3000` (vs. 12.000 de `streamConBusquedaWeb`) porque el resumen ejecutivo de una sola oportunidad no incluye búsquedas web ni secciones tabulares extensas — es una narrativa corta (ver Task 3 de este plan, tono "so what").
  </action>
  <verify>`npx tsc --noEmit` limpio. `grep -n "tools:" lib/ai.ts` muestra `tools:` únicamente en `streamConBusquedaWeb` y `aiCompleteWithTools` — nunca en `streamConContexto`.</verify>
  <done>streamConContexto existe, exportada, sin tools en el payload.</done>
</task>

<task type="auto">
  <name>Task 3: lib/resumen-oportunidad-prompts.ts</name>
  <files>lib/resumen-oportunidad-prompts.ts</files>
  <action>
Crea `lib/resumen-oportunidad-prompts.ts`, mismo espíritu que `lib/tasacion-prompts.ts` pero para el resumen ejecutivo de una oportunidad de mercado:

```ts
// Prompt builders para el resumen ejecutivo IA de una oportunidad individual
// (DETA-06). A diferencia de Tasación/Due Diligence (lib/tasacion-prompts.ts),
// acá NO hay protocolo de búsqueda web — todos los datos (bandas P25/mediana/
// P75, muestra_n, comparables, historial, reasonCodes) ya fueron calculados
// server-side con datos reales antes de llamar a este builder. El prompt
// existe para NARRAR esos números con criterio de inversionista, no para
// generarlos ni completarlos con conocimiento propio del modelo.

export interface ResumenOportunidadContexto {
  titulo: string
  comuna: string
  tipoPropiedadLabel: string
  operacion: 'arriendo' | 'venta'
  precioUf: number
  precioUfM2: number | null
  reasonCodes: string[]
  muestraN: number
  muestraNComuna: number
  usoFallback: boolean
  p25Uf: number | null
  medianaUf: number | null
  p75Uf: number | null
  p25UfM2: number | null
  medianaUfM2: number | null
  p75UfM2: number | null
  diasPublicado: number
  historialResumen: string
  comparables: { titulo: string; comuna: string; precioUf: number; precioUfM2: number | null }[]
  rentabilidadZonaPct: number | null
  senalExpansion: string | null
  tendenciaConstruccion: string | null
}

function num(n: number | null, sufijo = ''): string {
  return n === null ? 'no disponible' : `${n.toLocaleString('es-CL', { maximumFractionDigits: 2 })}${sufijo}`
}

export function buildSystemResumenOportunidad(): string {
  return `Eres un analista de PermisoHub Mercado Inmobiliario. Escribes resúmenes ejecutivos breves para arquitectos/inversionistas que evalúan un local comercial (u oficina/bodega/industrial) como oportunidad de arriendo o venta.

REGLA CRÍTICA — NUNCA busques información externa ni inventes cifras. TODA la información que necesitas ya viene en el mensaje del usuario, estructurada y verificada server-side contra la base de datos real de PermisoHub. Si un dato viene marcado "no disponible", dilo así explícitamente — nunca lo estimes ni lo completes con tu propio conocimiento del mercado chileno.

Responde en español, tono directo tipo "so what" para alguien que decide rápido — NO un informe formal de tasación. Estructura tu respuesta así:

## 🎯 Resumen Ejecutivo
2-4 frases: ¿por qué esta oportunidad califica (o no) como atractiva, según los datos entregados? Cita al menos una cifra real (precio vs. banda, o comparables, o historial).

## Contexto de Mercado
1-2 frases citando la banda de la cohorte (muestra_n, percentiles) y cómo se posiciona el precio.

## Riesgos y Consideraciones
1-2 frases: qué NO se sabe (muestra chica, sin datos de venta/arriendo cruzado, etc.) — nunca minimices la incertidumbre cuando el contexto la señala explícitamente (ej. usoFallback=true, rentabilidadZonaPct=null).

No agregues secciones adicionales. No uses placeholders entre corchetes.`
}

export function buildUserQueryResumenOportunidad(ctx: ResumenOportunidadContexto): string {
  const bandaTexto = ctx.usoFallback
    ? `banda CITYWIDE de respaldo (la comuna solo tiene N=${ctx.muestraNComuna} avisos, bajo el mínimo de 15 para confiar en su propia banda)`
    : `banda propia de la comuna`

  const comparablesTexto = ctx.comparables.length === 0
    ? 'Sin comparables reales disponibles en esta comuna/tipo/operación todavía.'
    : ctx.comparables
        .map((c) => `- ${c.titulo} (${c.comuna}): ${num(c.precioUf, ' UF')}${c.precioUfM2 !== null ? `, ${num(c.precioUfM2, ' UF/m²')}` : ''}`)
        .join('\n')

  return `## Oportunidad a resumir

- Título: ${ctx.titulo}
- Comuna: ${ctx.comuna}
- Tipo de propiedad: ${ctx.tipoPropiedadLabel}
- Operación: ${ctx.operacion}
- Precio: ${num(ctx.precioUf, ' UF')}${ctx.precioUfM2 !== null ? ` (${num(ctx.precioUfM2, ' UF/m²')})` : ' (sin superficie declarada, no hay UF/m²)'}
- Señales activas (reasonCodes): ${ctx.reasonCodes.length > 0 ? ctx.reasonCodes.join(', ') : 'ninguna'}
- Días publicado: ${ctx.diasPublicado}
- Historial de precio: ${ctx.historialResumen}

## Banda de mercado de su cohorte (${bandaTexto})

- Muestra: N=${ctx.muestraN}
- UF — P25: ${num(ctx.p25Uf)}, mediana: ${num(ctx.medianaUf)}, P75: ${num(ctx.p75Uf)}
- UF/m² — P25: ${num(ctx.p25UfM2)}, mediana: ${num(ctx.medianaUfM2)}, P75: ${num(ctx.p75UfM2)}

## Comparables reales en la misma comuna/tipo/operación

${comparablesTexto}

## Rentabilidad implícita de zona

${ctx.rentabilidadZonaPct === null ? 'No calculable — falta cobertura de arriendo y/o venta para esta comuna×tipo.' : `${num(ctx.rentabilidadZonaPct, '%')} (estimado de zona, no del activo específico — estimado con el cap rate neto genérico, ver desglose ya mostrado en la ficha)`}

## Otras señales

- Expansión de cadenas en la comuna: ${ctx.senalExpansion ?? 'sin señal registrada'}
- Tendencia constructiva histórica (INE): ${ctx.tendenciaConstruccion ?? 'sin señal registrada'}`
}
```
  </action>
  <verify>`npx tsc --noEmit` limpio. `grep -n "web_search\|fetch(" lib/resumen-oportunidad-prompts.ts` no devuelve nada (archivo puramente de construcción de strings, sin llamadas de red).</verify>
  <done>ResumenOportunidadContexto, buildSystemResumenOportunidad y buildUserQueryResumenOportunidad exportados, sin ninguna fabricación de datos null.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpio para los 3 archivos.
- `grep -n "tools:" lib/ai.ts` confirma que `streamConContexto` no tiene tools.
- Ningún archivo existente fuera de `lib/ai.ts` fue modificado en este plan.
</verification>

<success_criteria>
Las tres utilidades compartidas existen y son consumibles por 13-04 (resumen IA) y 13-05/13-06/13-07 (formateo de fecha) sin ninguna dependencia cruzada con el refactor de scoring de 13-01.
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-02-SUMMARY.md`
</output>
