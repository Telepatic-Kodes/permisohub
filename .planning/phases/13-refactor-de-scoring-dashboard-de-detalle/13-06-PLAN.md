---
phase: 13-refactor-de-scoring-dashboard-de-detalle
plan: 06
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - components/mercado-inmobiliario/oportunidad-detalle/historial-tab.tsx
  - components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx
autonomous: true

must_haves:
  truths:
    - "El tab Historial muestra hace cuántos días está publicado el aviso y su historial de precio completo — o un mensaje explícito 'sin cambios registrados' si el historial tiene 0 puntos, nunca un vacío sin explicación"
    - "Cada reasonCode del listing se explica en detalle (texto completo, no solo el badge corto) usando REASON_LABEL_DETALLE, junto con las señales cruzadas (expansión de cadenas, tendencia constructiva) ya usadas en la lista"
    - "El tab Comparables SIEMPRE aparece — cuando hay 0 o 1 comparable real, muestra el mensaje explícito Y además renderiza el/los comparable(s) real(es) que sí existan (nunca oculta datos reales ni rellena con datos fuera de criterio)"
    - "Cada comparable es una mini-card clickeable que enlaza a /mercado-inmobiliario/oportunidades/[id] (su propia ficha), mostrando precio + UF/m² + comuna + badge de reason code"
  artifacts:
    - path: "components/mercado-inmobiliario/oportunidad-detalle/historial-tab.tsx"
      provides: "Tab de historial de precio (DETA-03) + reason codes en detalle + señales cruzadas (DETA-04)"
      exports: ["HistorialTab"]
    - path: "components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx"
      provides: "Tab de comparables sugeridos (DETA-05), mini-cards clickeables o mensaje explícito de insuficiencia"
      exports: ["ComparablesTab"]
  key_links:
    - from: "components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx"
      to: "/mercado-inmobiliario/oportunidades/[id]"
      via: "next/link Link href hacia la ficha del comparable"
      pattern: "oportunidades/\\$\\{"
---

<objective>
Construye los tabs "Historial" (DETA-03 + DETA-04) y "Comparables" (DETA-05) de la ficha de detalle — ambos componentes presentacionales puros, reciben datos ya fetchados por props, sin lógica de red propia.

Purpose: completan, junto con 13-05 (Posicionamiento) y 13-04 (Resumen), las cuatro tabs que CONTEXT.md exige. Se construyen en paralelo con 13-05 porque no comparten archivo ni dependen entre sí — solo de la capa de datos de 13-03 y del formateador compartido de 13-02.
Output: `<HistorialTab>`, `<ComparablesTab>`.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-RESEARCH.md
@lib/mercado-locales-server.ts
@lib/cadenas-sucursales-server.ts
@lib/ine-permisos-server.ts
@app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: HistorialTab — historial de precio + reason codes + señales cruzadas</name>
  <files>components/mercado-inmobiliario/oportunidad-detalle/historial-tab.tsx</files>
  <action>
Crea el archivo:

```tsx
import { Radar, TrendingUp } from "lucide-react"
import type { OportunidadDetalle, PuntoHistorialPrecio } from "@/lib/mercado-locales-server"
import { REASON_LABEL_DETALLE } from "@/lib/mercado-locales-server"
import type { SenalExpansionComuna } from "@/lib/cadenas-sucursales-server"
import type { TendenciaConstruccionComuna } from "@/lib/ine-permisos-server"
import { formatFechaCorta } from "@/lib/formato-fecha"

interface HistorialTabProps {
  oportunidad: OportunidadDetalle
  historial: PuntoHistorialPrecio[]
  senalExpansion: SenalExpansionComuna | null
  tendenciaConstruccion: TendenciaConstruccionComuna | null
}

function formatMonto(precioMonto: number, precioMoneda: string): string {
  return `${precioMonto.toLocaleString("es-CL", { maximumFractionDigits: precioMoneda === "UF" ? 2 : 0 })} ${precioMoneda}`
}

// primera_vez_visto_el/ultima_vez_visto_el/capturado_el son timestamptz — se
// formatean con new Date(iso) directo, SIN el sufijo T00:00:00 que usa
// formatFechaCorta (esa función es solo para campos date-only como
// stats_date). Ver lib/formato-fecha.ts.
function formatTimestamp(iso: string): string {
  return new Intl.DateTimeFormat("es-CL", { day: "2-digit", month: "short", year: "numeric", timeZone: "America/Santiago" }).format(
    new Date(iso),
  )
}

export function HistorialTab({ oportunidad, historial, senalExpansion, tendenciaConstruccion }: HistorialTabProps) {
  const diasPublicado = Math.floor((Date.now() - new Date(oportunidad.primeraVezVistoEl).getTime()) / 86400000)

  return (
    <div className="space-y-5">
      <div className="rounded-lg border border-line-fine bg-card p-4">
        <p className="text-[10px] uppercase tracking-wide text-muted-foreground">Publicado hace</p>
        <p className="num text-xl font-semibold text-primary">{diasPublicado} {diasPublicado === 1 ? "día" : "días"}</p>
        <p className="mt-1 text-[11px] text-muted-foreground">
          Primera vez visto: {formatTimestamp(oportunidad.primeraVezVistoEl)} · Última actualización: {formatTimestamp(oportunidad.ultimaVezVistoEl)}
        </p>
        {oportunidad.status === "dado_de_baja" && oportunidad.dadoDeBajaEl && (
          <p className="mt-1 text-[11px] font-medium text-red-700">Dado de baja el {formatTimestamp(oportunidad.dadoDeBajaEl)}</p>
        )}
      </div>

      <div className="rounded-lg border border-line-fine bg-card p-4">
        <p className="mb-2 text-[10px] uppercase tracking-wide text-muted-foreground">Historial de precio</p>
        {historial.length === 0 ? (
          <p className="text-sm text-muted-foreground">Sin cambios de precio registrados desde que se detectó este aviso.</p>
        ) : (
          <ul className="space-y-1.5">
            {historial.map((p, i) => (
              <li key={`${p.capturadoEl}-${i}`} className="flex items-center justify-between text-sm">
                <span className="text-muted-foreground">{formatTimestamp(p.capturadoEl)}</span>
                <span className="num font-medium text-primary">{formatMonto(p.precioMonto, p.precioMoneda)}</span>
              </li>
            ))}
          </ul>
        )}
      </div>

      <div className="rounded-lg border border-line-fine bg-card p-4">
        <p className="mb-2 text-[10px] uppercase tracking-wide text-muted-foreground">Señales detectadas</p>
        {oportunidad.reasonCodes.length === 0 ? (
          <p className="text-sm text-muted-foreground">
            Este aviso no calificó como oportunidad por precio bajo ni por baja reciente — si llegaste aquí desde comparables,
            es un dato real de la cohorte, no una oportunidad flageada.
          </p>
        ) : (
          <ul className="space-y-2">
            {oportunidad.reasonCodes.map((code) => (
              <li key={code} className="text-sm leading-snug text-foreground">
                <span className="font-medium">{REASON_LABEL_DETALLE[code] ?? code}</span>
              </li>
            ))}
          </ul>
        )}

        {(senalExpansion || tendenciaConstruccion?.tendencia === "creciente") && (
          <div className="mt-3 space-y-1.5 border-t border-line-fine pt-3">
            {senalExpansion && (
              <p className="inline-flex items-start gap-1.5 text-[11px] text-modulo-mercado">
                <Radar className="mt-0.5 size-3 shrink-0" />
                {senalExpansion.cadena} tiene sucursal registrada en {oportunidad.comuna}
                {formatFechaCorta(senalExpansion.fechaRegistro) ? ` (registrada ${formatFechaCorta(senalExpansion.fechaRegistro)})` : ""}
              </p>
            )}
            {tendenciaConstruccion?.tendencia === "creciente" && (
              <p className="inline-flex items-start gap-1.5 text-[11px] text-modulo-mercado">
                <TrendingUp className="mt-0.5 size-3 shrink-0" />
                Actividad constructiva histórica en alza en {oportunidad.comuna} (INE, {tendenciaConstruccion.variacionPct?.toFixed(0)}%)
              </p>
            )}
          </div>
        )}
      </div>
    </div>
  )
}
```

`senalExpansion`/`tendenciaConstruccion` degradan omitiendo el bloque cuando son `null` (mismo criterio que la lista, `oportunidades/page.tsx:152-166`) — a diferencia de comparables/rentabilidad de zona, CONTEXT.md NO exige un mensaje explícito de "sin señal" para estas dos, son un plus opcional, no un dato central de la ficha.
  </action>
  <verify>`npx tsc --noEmit` limpio. `npx eslint components/mercado-inmobiliario/oportunidad-detalle/historial-tab.tsx` limpio.</verify>
  <done>Días publicado, historial completo (o mensaje explícito si vacío), reasonCodes en detalle, señales cruzadas cuando existen.</done>
</task>

<task type="auto">
  <name>Task 2: ComparablesTab — mini-cards o mensaje explícito</name>
  <files>components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx</files>
  <action>
Crea el archivo:

```tsx
import Link from "next/link"
import type { ComparableOportunidad } from "@/lib/mercado-locales-server"
import { REASON_LABEL } from "@/lib/mercado-locales-server"

interface ComparablesTabProps {
  comparables: ComparableOportunidad[]
  comuna: string
  tipoPropiedadLabel: string
}

function fmt(n: number): string {
  return n.toLocaleString("es-CL", { maximumFractionDigits: 2 })
}

export function ComparablesTab({ comparables, comuna, tipoPropiedadLabel }: ComparablesTabProps) {
  const insuficiente = comparables.length < 2

  return (
    <div className="space-y-4">
      {insuficiente && (
        <div className="rounded-lg border border-line-fine bg-muted/40 p-4 text-sm text-muted-foreground">
          No hay suficientes comparables en {comuna} / {tipoPropiedadLabel} todavía
          {comparables.length === 1 ? " — se encontró 1, mostrado abajo." : "."}
        </div>
      )}

      {comparables.length > 0 && (
        <div className="grid grid-cols-1 gap-3 sm:grid-cols-2">
          {comparables.map((c) => (
            <Link
              key={c.id}
              href={`/mercado-inmobiliario/oportunidades/${c.id}`}
              className="block rounded-lg border border-line-fine bg-card p-3.5 transition hover:border-primary/40 hover:shadow-sm"
            >
              <p className="line-clamp-2 text-sm font-medium text-primary">{c.titulo}</p>
              <p className="mt-1 text-xs text-muted-foreground">
                {c.comuna} · {fmt(c.precioUfNormalizado)} UF
                {c.precioUfM2Normalizado !== null ? ` · ${fmt(c.precioUfM2Normalizado)} UF/m²` : ""}
              </p>
              {c.reasonCodes.length > 0 && (
                <div className="mt-2 flex flex-wrap gap-1">
                  {c.reasonCodes.map((code) => (
                    <span
                      key={code}
                      className="rounded-[3px] border border-[var(--blueprint-soft)] bg-[var(--blueprint-soft)] px-1.5 py-0.5 text-[9px] text-[var(--blueprint)]"
                    >
                      {REASON_LABEL[code] ?? code}
                    </span>
                  ))}
                </div>
              )}
            </Link>
          ))}
        </div>
      )}
    </div>
  )
}
```

La sección de mensaje (`insuficiente`) y la grilla de comparables NO son mutuamente excluyentes — cuando `comparables.length === 1`, AMBAS se renderizan (el mensaje explica la insuficiencia, la grilla muestra igual el único comparable real disponible). Cuando `comparables.length === 0`, solo se renderiza el mensaje. Nunca se rellena con datos fuera de criterio para completar un número mínimo.
  </action>
  <verify>`npx tsc --noEmit` limpio. `npx eslint components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx` limpio. Revisión manual: con `comparables=[]` solo se ve el mensaje; con `comparables` de longitud 1, se ven mensaje + 1 card; con longitud ≥2, solo la grilla.</verify>
  <done>ComparablesTab siempre visible, mini-cards clickeables hacia /mercado-inmobiliario/oportunidades/[id], badges de reasonCode, mensaje explícito cuando hay 0 o 1.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpio.
- `npx eslint components/mercado-inmobiliario/oportunidad-detalle/historial-tab.tsx components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx` limpio.
- `grep -n "oportunidades/\${c.id}" components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx` confirma el link de navegación entre fichas.
</verification>

<success_criteria>
DETA-03, DETA-04 y DETA-05 completos como componentes presentacionales, listos para recibir datos reales de 13-07.
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-06-SUMMARY.md`
</output>
