---
phase: 13-refactor-de-scoring-dashboard-de-detalle
plan: 07
type: execute
wave: 4
depends_on: ["13-04", "13-05", "13-06"]
files_modified:
  - app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx
  - app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx
autonomous: false

must_haves:
  truths:
    - "Usuario puede abrir /mercado-inmobiliario/oportunidades/[id] para una oportunidad real y ver las 4 tabs (Posicionamiento, Resumen, Historial, Comparables) todas con datos reales, sin que ninguna espere a la IA para renderizar"
    - "Desde la lista de oportunidades, cada card tiene un link 'Ver ficha completa' hacia su /oportunidades/[id], y el link externo al aviso original sigue existiendo sin reemplazo"
    - "Un id inexistente devuelve 404 (notFound()); un id de un aviso dado_de_baja SÍ abre su ficha y muestra ese estado explícitamente, nunca 404"
    - "El resumen ejecutivo IA solo se genera al hacer click — el resto de la ficha ya está completamente renderizado antes de esa interacción"
  artifacts:
    - path: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx"
      provides: "Server Component que fetch-ea todo lo necesario (obtenerOportunidadPorId, bandas arriendo+venta, comparables, historial, señales) y compone las 4 tabs"
      exports: ["default"]
    - path: "app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx"
      provides: "Import de formatFechaCorta/REASON_LABEL compartidos (elimina las copias locales) + link 'Ver ficha completa' en cada card"
  key_links:
    - from: "app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx"
      to: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx"
      via: "next/link Link href={`/mercado-inmobiliario/oportunidades/${o.id}`} en cada card"
      pattern: "Ver ficha completa"
    - from: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx"
      to: "lib/mercado-locales-server.ts (obtenerOportunidadPorId, obtenerComparablesOportunidad, obtenerHistorialPrecioListing, obtenerBandasMercadoLocales)"
      via: "await Promise.all([...]) al tope del Server Component"
      pattern: "obtenerOportunidadPorId"
    - from: "app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx"
      to: "components/mercado-inmobiliario/oportunidad-detalle/{posicionamiento,resumen,historial,comparables}-tab.tsx"
      via: "composición dentro de <Tabs>/<TabsContent>"
      pattern: "TabsContent"
---

<objective>
Ensambla la ficha de detalle completa: el Server Component `[id]/page.tsx` que fetch-ea todos los datos reales y compone las 4 tabs construidas en 13-04/13-05/13-06, más el wiring de la lista (`oportunidades/page.tsx`) hacia la nueva ruta.

Purpose: es el plan de integración final — todas las piezas (scoring, capa de datos, 4 tabs, streaming IA) ya existen de forma aislada; este plan las conecta en una sola página real y navegable, cerrando DETA-01 completo y el resto de los requisitos de punta a punta.
Output: `/mercado-inmobiliario/oportunidades/[id]` navegable; lista con link "Ver ficha completa".
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-RESEARCH.md
@.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-CONTEXT.md
@lib/mercado-locales-server.ts
@lib/resumen-oportunidad-prompts.ts
@lib/formato-fecha.ts
@components/mercado-inmobiliario/oportunidad-detalle/posicionamiento-tab.tsx
@components/mercado-inmobiliario/oportunidad-detalle/historial-tab.tsx
@components/mercado-inmobiliario/oportunidad-detalle/comparables-tab.tsx
@components/mercado-inmobiliario/oportunidad-detalle/resumen-tab.tsx
@app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx
@components/ui/tabs.tsx
@components/dashboard/page-header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: [id]/page.tsx — Server Component completo</name>
  <files>app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx</files>
  <action>
Crea `app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx`. Server Component `async`, `params: Promise<{id: string}>` (Next 16, `await params` — convención ya usada en `app/api/propiedades-portafolio/[id]/route.ts`, confirmada vigente contra `node_modules/next/dist/docs/`).

Orden de secciones per CONTEXT.md ("el dato duro va antes que la narrativa"): Header → tabs con orden **Posicionamiento → Resumen → Historial → Comparables** (la lista "Resumen | Posicionamiento | Historial | Comparables" del CONTEXT.md es solo el nombre de las 4 tabs, no un mandato de orden — el orden de secciones explícito en la misma sección de CONTEXT.md pone Posicionamiento antes que el resumen IA, y esa es la regla que manda).

```tsx
import { notFound } from "next/navigation"
import Link from "next/link"
import { ExternalLink } from "lucide-react"
import { PageHeader } from "@/components/dashboard/page-header"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import {
  obtenerOportunidadPorId,
  obtenerComparablesOportunidad,
  obtenerHistorialPrecioListing,
  obtenerBandasMercadoLocales,
} from "@/lib/mercado-locales-server"
import { obtenerSenalesExpansionPorComuna } from "@/lib/cadenas-sucursales-server"
import { obtenerTendenciasConstruccionPorComuna } from "@/lib/ine-permisos-server"
import { TIPO_PROPIEDAD_LABEL } from "@/lib/scrapers/mercado-locales-common"
import { PosicionamientoTab } from "@/components/mercado-inmobiliario/oportunidad-detalle/posicionamiento-tab"
import { ResumenTab } from "@/components/mercado-inmobiliario/oportunidad-detalle/resumen-tab"
import { HistorialTab } from "@/components/mercado-inmobiliario/oportunidad-detalle/historial-tab"
import { ComparablesTab } from "@/components/mercado-inmobiliario/oportunidad-detalle/comparables-tab"
import type { ResumenOportunidadContexto } from "@/lib/resumen-oportunidad-prompts"
import { calcularCapRate } from "@/lib/calculadora-inversion"

export const dynamic = "force-dynamic"

function fmt(n: number): string {
  return n.toLocaleString("es-CL", { maximumFractionDigits: 2 })
}

interface Props {
  params: Promise<{ id: string }>
}

export default async function OportunidadDetallePage({ params }: Props) {
  const { id } = await params
  const oportunidad = await obtenerOportunidadPorId(id)
  if (!oportunidad) notFound()

  const [bandasArriendo, bandasVenta, comparables, historial, senalesExpansion, tendenciasConstruccion] = await Promise.all([
    obtenerBandasMercadoLocales(oportunidad.comuna, "arriendo", oportunidad.tipoPropiedad),
    obtenerBandasMercadoLocales(oportunidad.comuna, "venta", oportunidad.tipoPropiedad),
    obtenerComparablesOportunidad({
      comuna: oportunidad.comuna,
      operacion: oportunidad.operacion,
      tipoPropiedad: oportunidad.tipoPropiedad,
      excludeId: oportunidad.id,
      precioUfM2Objetivo: oportunidad.precioUfM2Normalizado,
      precioUfObjetivo: oportunidad.precioUfNormalizado,
    }),
    obtenerHistorialPrecioListing(oportunidad.id),
    obtenerSenalesExpansionPorComuna([oportunidad.comuna]).catch(() => new Map()),
    obtenerTendenciasConstruccionPorComuna([oportunidad.comuna]).catch(() => new Map()),
  ])

  const tipoPropiedadLabel = TIPO_PROPIEDAD_LABEL[oportunidad.tipoPropiedad].singular
  const diasPublicado = Math.floor((Date.now() - new Date(oportunidad.primeraVezVistoEl).getTime()) / 86400000)

  const arriendoUfM2 = bandasArriendo?.medianaUfM2 ?? null
  const ventaUfM2 = bandasVenta?.medianaUfM2 ?? null
  const rentabilidadZonaPct =
    arriendoUfM2 !== null && ventaUfM2 !== null && ventaUfM2 > 0
      ? calcularCapRate({ rentaMensual: arriendoUfM2, precioVenta: ventaUfM2 }).capNeto
      : null

  const historialResumen =
    historial.length === 0
      ? "sin cambios de precio registrados"
      : `${historial.length} punto(s) registrado(s), el más reciente: ${fmt(historial[historial.length - 1].precioMonto)} ${historial[historial.length - 1].precioMoneda}`

  const resumenContexto: ResumenOportunidadContexto = {
    titulo: oportunidad.titulo,
    comuna: oportunidad.comuna,
    tipoPropiedadLabel,
    operacion: oportunidad.operacion,
    precioUf: oportunidad.precioUfNormalizado,
    precioUfM2: oportunidad.precioUfM2Normalizado,
    reasonCodes: oportunidad.reasonCodes,
    muestraN: oportunidad.bandas?.muestraN ?? 0,
    muestraNComuna: oportunidad.bandas?.muestraNComuna ?? 0,
    usoFallback: oportunidad.bandas?.usoFallback ?? false,
    p25Uf: oportunidad.bandas?.p25Uf ?? null,
    medianaUf: oportunidad.bandas?.medianaUf ?? null,
    p75Uf: oportunidad.bandas?.p75Uf ?? null,
    p25UfM2: oportunidad.bandas?.p25UfM2 ?? null,
    medianaUfM2: oportunidad.bandas?.medianaUfM2 ?? null,
    p75UfM2: oportunidad.bandas?.p75UfM2 ?? null,
    diasPublicado,
    historialResumen,
    comparables: comparables.map((c) => ({
      titulo: c.titulo,
      comuna: c.comuna,
      precioUf: c.precioUfNormalizado,
      precioUfM2: c.precioUfM2Normalizado,
    })),
    rentabilidadZonaPct,
    senalExpansion: senalesExpansion.get(oportunidad.comuna)
      ? `${senalesExpansion.get(oportunidad.comuna)!.cadena} tiene sucursal registrada`
      : null,
    tendenciaConstruccion:
      tendenciasConstruccion.get(oportunidad.comuna)?.tendencia === "creciente"
        ? `alza de ${tendenciasConstruccion.get(oportunidad.comuna)!.variacionPct?.toFixed(0)}% en construcción no habitacional (INE, histórico)`
        : null,
  }

  return (
    <div className="flex min-h-screen flex-col">
      <PageHeader
        emoji="📄"
        title={oportunidad.titulo}
        subtitle={`${oportunidad.comuna} · ${tipoPropiedadLabel} · ${oportunidad.operacion === "venta" ? "Venta" : "Arriendo"}`}
        breadcrumbs={[
          { label: "Oportunidades", href: "/mercado-inmobiliario/oportunidades" },
          { label: oportunidad.titulo },
        ]}
      />

      <div className="flex-1 p-8">
        <div className="mx-auto max-w-3xl space-y-6">
          {oportunidad.status === "dado_de_baja" && (
            <div className="flex items-start gap-2.5 rounded-lg border border-red-200 bg-red-50 px-4 py-3">
              <span className="mt-0.5 text-red-500">⚠</span>
              <div className="text-sm">
                <span className="font-semibold text-red-800">Aviso dado de baja</span>
                <span className="ml-1 text-red-700">
                  Este aviso ya no está activo en el portal de origen — los datos que ves son los últimos capturados antes de darse de baja.
                </span>
              </div>
            </div>
          )}

          <div className="rounded-lg border border-line-fine bg-card p-4">
            <div className="flex items-start justify-between gap-3">
              <div>
                <p className="text-2xl font-semibold text-primary num">
                  {oportunidad.precioValido ? `${fmt(oportunidad.precioUfNormalizado)} UF` : "Precio no disponible en moneda reconocida"}
                </p>
                {oportunidad.precioUfM2Normalizado !== null && (
                  <p className="num text-sm text-muted-foreground">{fmt(oportunidad.precioUfM2Normalizado)} UF/m²</p>
                )}
              </div>
              <a
                href={oportunidad.url}
                target="_blank"
                rel="noopener noreferrer"
                className="inline-flex items-center gap-1 text-sm text-primary hover:underline"
              >
                Ver aviso original <ExternalLink className="size-3.5" />
              </a>
            </div>
          </div>

          <Tabs defaultValue="posicionamiento">
            <TabsList>
              <TabsTrigger value="posicionamiento">Posicionamiento</TabsTrigger>
              <TabsTrigger value="resumen">Resumen</TabsTrigger>
              <TabsTrigger value="historial">Historial</TabsTrigger>
              <TabsTrigger value="comparables">Comparables</TabsTrigger>
            </TabsList>

            <TabsContent value="posicionamiento">
              <PosicionamientoTab oportunidad={oportunidad} bandasArriendo={bandasArriendo} bandasVenta={bandasVenta} />
            </TabsContent>

            <TabsContent value="resumen">
              <ResumenTab contexto={resumenContexto} />
            </TabsContent>

            <TabsContent value="historial">
              <HistorialTab
                oportunidad={oportunidad}
                historial={historial}
                senalExpansion={senalesExpansion.get(oportunidad.comuna) ?? null}
                tendenciaConstruccion={tendenciasConstruccion.get(oportunidad.comuna) ?? null}
              />
            </TabsContent>

            <TabsContent value="comparables">
              <ComparablesTab comparables={comparables} comuna={oportunidad.comuna} tipoPropiedadLabel={tipoPropiedadLabel} />
            </TabsContent>
          </Tabs>
        </div>
      </div>
    </div>
  )
}
```

`notFound()` SOLO cuando `obtenerOportunidadPorId` devuelve `null` (id inexistente) — NUNCA cuando `oportunidad.status === 'dado_de_baja'` (eso es un estado válido, renderizado explícitamente con el banner rojo de arriba). Ninguna de las 4 `TabsContent` espera a `ResumenTab` — `ResumenTab` renderiza su propio botón inicial de inmediato, sin bloquear el resto del árbol (Server Component ya entregó todo el HTML antes de que el usuario interactúe con esa tab).
  </action>
  <verify>`npx tsc --noEmit` limpio. `npm run build` completa sin errores (confirma que no hay problemas de límite Server/Client Component entre este archivo y los 4 tabs importados). Navegación manual: abrir `/mercado-inmobiliario/oportunidades/[id-real-de-la-BD]` en dev y confirmar que las 4 tabs muestran datos reales.</verify>
  <done>Ruta /mercado-inmobiliario/oportunidades/[id] funcional end-to-end, 4 tabs pobladas con datos reales, resumen IA bajo demanda sin bloquear el resto.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring de la lista — link "Ver ficha completa" + imports compartidos</name>
  <files>app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx</files>
  <action>
En `app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx`:

1. Elimina el `const REASON_LABEL` local (líneas 16-20) y la función `formatFechaCorta` local (líneas 26-31). Agrega los imports:
```ts
import Link from "next/link"
import { obtenerOportunidadesMercadoLocales, REASON_LABEL } from "@/lib/mercado-locales-server"
import { formatFechaCorta } from "@/lib/formato-fecha"
```
(reemplaza el import existente `import { obtenerOportunidadesMercadoLocales } from "@/lib/mercado-locales-server"` por el de arriba, que ahora también trae `REASON_LABEL`).

2. En el bloque de cada card (líneas ~134-140 actuales, el `<div className="flex items-start justify-between gap-3">` con el título y el precio), agrega el link "Ver ficha completa" DEBAJO del bloque existente de reasonCodes/señales (después del `<div className="mt-2.5 flex flex-wrap gap-1.5">...</div>` que cierra la card), sin tocar el `<a href={o.url}>` externo existente (se mantiene igual):

```tsx
                <div className="mt-2.5">
                  <Link
                    href={`/mercado-inmobiliario/oportunidades/${o.id}`}
                    className="text-xs font-medium text-primary hover:underline"
                  >
                    Ver ficha completa →
                  </Link>
                </div>
```

El resultado final de cada card debe tener AMBOS links: el externo `<a href={o.url}>` (título, apunta al aviso original en el portal) y el nuevo `<Link href="/mercado-inmobiliario/oportunidades/${o.id}">` ("Ver ficha completa", apunta a la ficha interna) — ninguno reemplaza al otro.
  </action>
  <verify>`npx tsc --noEmit` limpio. `npx eslint "app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx"` limpio. `grep -n "function formatFechaCorta\|const REASON_LABEL" "app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx"` no debe devolver nada (confirma que las copias locales fueron eliminadas). `grep -n "Ver ficha completa" "app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx"` confirma el nuevo link.</verify>
  <done>Lista de oportunidades usa los exports compartidos (sin duplicación) y cada card enlaza a su ficha completa, sin perder el link externo original.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación humana end-to-end de la ficha completa</name>
  <files>app/(dashboard)/mercado-inmobiliario/oportunidades/[id]/page.tsx</files>
  <action>
Confirma en vivo lo construido por Task 1 y Task 2: la ficha de detalle completa de una oportunidad individual en `/mercado-inmobiliario/oportunidades/[id]` — header con precio/comuna/tipo/operación, 4 tabs (Posicionamiento con banner de muestra chica + rentabilidad de zona, Resumen ejecutivo IA bajo demanda, Historial de precio + señales, Comparables sugeridos), más el link "Ver ficha completa" desde cada card de la lista. Cubre DETA-01 a DETA-07 completos.

Pasos:
1. Levanta el proyecto (`npm run dev`) y abre `/mercado-inmobiliario/oportunidades` — confirma que cada card tiene el nuevo link "Ver ficha completa" además del link externo al aviso.
2. Haz click en "Ver ficha completa" de una oportunidad real — confirma que carga `/mercado-inmobiliario/oportunidades/[id]` con el header (precio/comuna/tipo/operación) y las 4 tabs.
3. Tab Posicionamiento: confirma que se ve `N=` (muestra_n) explícito, y que si la oportunidad cae en fallback citywide aparece el banner amber prominente. Confirma la sección "Rentabilidad implícita de zona" con el badge violeta "Estimado de zona" y el desglose de ambas bandas (o el mensaje explícito si falta cobertura).
4. Tab Resumen: confirma que NO se generó nada automáticamente — hay que hacer click en "Generar resumen ejecutivo". Haz click y confirma que el texto llega por streaming y termina en el bloque destacado de `InformeEjecutivo`.
5. Tab Historial: confirma días publicado, historial de precio (o mensaje "sin cambios" si no hay), y los reasonCodes explicados en texto largo (no solo el badge).
6. Tab Comparables: confirma que siempre aparece la sección — con datos reales si hay ≥2 comparables, o el mensaje explícito (+ el/los comparable(s) real(es) si hay 1) si no. Haz click en un comparable y confirma que navega a SU propia ficha.
7. Navega directo a un id inexistente (`/mercado-inmobiliario/oportunidades/00000000-0000-0000-0000-000000000000`) — confirma 404.
  </action>
  <verify>Los 7 pasos anteriores pasan sin hallazgos bloqueantes. Cualquier desviación se reporta explícitamente en vez de darse por aprobada.</verify>
  <done>Usuario escribe "aprobado", o describe los problemas encontrados para una ronda de corrección posterior.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpio en todo el proyecto.
- `npm run build` completa sin errores.
- `npx vitest run tests/unit/evaluar-oportunidad.test.ts` sigue en verde (ninguna regresión de scoring introducida por la integración final).
- Checkpoint humano aprobado (task 3).
</verification>

<success_criteria>
Los 7 requisitos DETA-01 a DETA-07 están completos y navegables de punta a punta: ficha en su propia ruta, posicionamiento vs. cohorte con muestra_n explícita, historial + días publicado + reason codes detallados + señales cruzadas, comparables reales o mensaje explícito, resumen ejecutivo IA bajo demanda sin bloquear el resto, y rentabilidad implícita de zona etiquetada como estimado con desglose completo.
</success_criteria>

<output>
After completion, create `.planning/phases/13-refactor-de-scoring-dashboard-de-detalle/13-07-SUMMARY.md`
</output>
