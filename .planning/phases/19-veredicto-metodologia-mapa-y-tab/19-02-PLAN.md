---
phase: 19-veredicto-metodologia-mapa-y-tab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa.tsx
  - app/dev-preview-cabida-mapa/page.tsx
autonomous: false

must_haves:
  truths:
    - "El mapa renderiza el polígono del área de influencia (isócrona o círculo) con un estilo visualmente distinto del mapa de zonificación existente — nunca la misma paleta, para evitar confusión entre los dos tipos de mapa (MAPA-01)"
    - "El mapa renderiza un pin por cada competidor detectado, sin necesidad de manejar coordenadas nulas — CompetidorDetectado.lat/.lng son campos no-opcionales confirmados en lib/cabida-comercial.ts (MAPA-01)"
    - "El componente acepta tanto Polygon como MultiPolygon para la geometría del área de influencia — IsocronaResultado.geometria ya está tipado así, a diferencia de ZonaPolygon de zonificacion-mapa.tsx que solo acepta Polygon"
    - "El componente es renderizable e inspeccionable en un navegador real HOY, sin que exista todavía lib/cabida-comercial-server.ts, lib/isocrona-server.ts, ni el tab de Fase 16 — cero dependencia dura de esos tres artefactos"
  artifacts:
    - path: "components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa.tsx"
      provides: "CabidaComercialMapa: clona la estructura de components/proyecto/zonificacion-mapa.tsx (dynamic import de leaflet, L.geoJSON, L.divIcon) + agrega pines de competidores y fitBounds al polígono"
      contains: "export function CabidaComercialMapa"
  key_links:
    - from: "components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa.tsx"
      to: "lib/cabida-comercial.ts (CompetidorDetectado)"
      via: "import type — prop competidores: CompetidorDetectado[]"
      pattern: "from ['\"]@/lib/cabida-comercial['\"]"
---

<objective>
Construir `cabida-comercial-mapa.tsx` — el componente Leaflet que satisface MAPA-01 (polígono del área de influencia + pines de competidores). Este plan es completamente **ungateado**: clona la estructura ya probada de `components/proyecto/zonificacion-mapa.tsx` (Phase 11, único integración Leaflet de este repo), pero es un componente NUEVO, no una extensión — distinto dominio (isócrona/radio + competidores, no un único polígono de zonificación), mismo criterio que `16-RESEARCH.md` ya recomendó.

Purpose: El componente toma GeoJSON + un array de pines como props planos — no necesita `lib/cabida-comercial-server.ts`, `lib/isocrona-server.ts` ni el tab de Fase 16 para existir ni para verificarse. Se construye y se verifica visualmente HOY contra un fixture armado a mano (un polígono real alrededor de una coordenada conocida de Santiago + 3 pines de competidores ficticios), usando los tipos YA reales de `lib/cabida-comercial.ts` (`CompetidorDetectado`, confirmado con `lat`/`lng` no-opcionales — sin caso de coordenada nula que manejar).

Output: `CabidaComercialMapa` listo para que Plan 19-04 (gateado, más adelante en este mismo Phase) lo importe y le pase datos reales (`analisis.isocrona.geometria`, `analisis.competencia?.competidores ?? []`) el día que Phase 16/18-07 completen. Verificado en un navegador real vía una página de preview temporal, eliminada al final de este plan.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-veredicto-metodologia-mapa-y-tab/19-RESEARCH.md
@components/proyecto/zonificacion-mapa.tsx
@lib/cabida-comercial.ts
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente CabidaComercialMapa</name>
  <files>components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa.tsx</files>
  <action>
Crear `components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa.tsx`, clonando la estructura de `components/proyecto/zonificacion-mapa.tsx` (leído en `<context>`) casi verbatim, con estas diferencias deliberadas:

```typescript
"use client"

import { useEffect, useRef } from "react"
import "leaflet/dist/leaflet.css"
import type { Map as LeafletMap } from "leaflet"
import type { CompetidorDetectado } from "@/lib/cabida-comercial"

interface CabidaComercialMapaProps {
  lat: number
  lng: number
  // A diferencia de ZonaPolygon (zonificacion-mapa.tsx), que solo acepta
  // Polygon: IsocronaResultado.geometria está tipado Polygon | MultiPolygon
  // desde el día 1 (lib/cabida-comercial.ts) — L.geoJSON() maneja ambos
  // nativamente, sin código adicional, pero el tipo del prop debe aceptarlo.
  geometria: GeoJSON.Polygon | GeoJSON.MultiPolygon
  competidores: CompetidorDetectado[]
  className?: string
}

function formatearDistancia(m: number): string {
  return m >= 1000 ? `${(m / 1000).toFixed(1)} km` : `${Math.round(m)} m`
}

export function CabidaComercialMapa({ lat, lng, geometria, competidores, className }: CabidaComercialMapaProps) {
  const containerRef = useRef<HTMLDivElement>(null)
  const mapRef = useRef<LeafletMap | null>(null)

  useEffect(() => {
    if (!containerRef.current) return
    let cancelled = false

    void import("leaflet").then((L) => {
      if (cancelled || !containerRef.current) return

      // Zoom 15 por defecto (vs. 17 de zonificacion-mapa.tsx) — isócronas
      // cubren un área mayor que una sola parcela.
      const map = L.map(containerRef.current).setView([lat, lng], 15)
      mapRef.current = map

      L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>',
        maxZoom: 19,
      }).addTo(map)

      const origenIcon = L.divIcon({
        className: "",
        html: '<div style="width:14px;height:14px;border-radius:50%;background:#2563eb;border:2px solid white;box-shadow:0 0 0 1px rgba(0,0,0,.35)"></div>',
        iconSize: [14, 14],
        iconAnchor: [7, 7],
      })
      L.marker([lat, lng], { icon: origenIcon }).addTo(map)

      // Verde (#16a34a), NO el azul de zonificacion-mapa.tsx (#2563eb) —
      // evitar confundir "área de influencia comercial" con "polígono de
      // zonificación PRC" si un usuario tiene ambos tabs en memoria.
      const areaLayer = L.geoJSON(geometria as GeoJSON.Geometry, {
        style: { color: "#16a34a", weight: 2, fillOpacity: 0.10 },
      }).addTo(map)

      // Pines de competidores — NO existen en zonificacion-mapa.tsx.
      // CompetidorDetectado.lat/.lng son campos no-opcionales (confirmado en
      // lib/cabida-comercial.ts) — sin guard de null necesario acá.
      const pinIcon = L.divIcon({
        className: "",
        html: '<div style="width:12px;height:12px;border-radius:50%;background:#dc2626;border:2px solid white;box-shadow:0 0 0 1px rgba(0,0,0,.35)"></div>',
        iconSize: [12, 12],
        iconAnchor: [6, 6],
      })
      competidores.forEach((c) => {
        L.marker([c.lat, c.lng], { icon: pinIcon })
          .bindPopup(`${c.nombre} — ${formatearDistancia(c.distanciaM)}`)
          .addTo(map)
      })

      // Fit bounds al polígono del área de influencia (no a los pines) — el
      // área es el encuadre principal, los pines son detalle dentro de ella.
      const bounds = areaLayer.getBounds()
      if (bounds.isValid()) map.fitBounds(bounds, { padding: [16, 16] })
    })

    return () => {
      cancelled = true
      mapRef.current?.remove()
      mapRef.current = null
    }
  }, [lat, lng, geometria, competidores])

  return (
    <div className={className}>
      <div ref={containerRef} className="h-[280px] w-full rounded-lg border border-border" />
    </div>
  )
}
```

No agregar `@turf/turf` ni ninguna otra dependencia nueva — `L.geoJSON(...).getBounds()`/`.fitBounds()` ya resuelven el encuadre, mismo criterio que `zonificacion-mapa.tsx` ya usa sin turf (confirmado en `19-RESEARCH.md`, Supporting stack).
  </action>
  <verify>`npx tsc --noEmit` limpio; `npx eslint components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa.tsx` sin errores.</verify>
  <done>CabidaComercialMapa existe, exportado, acepta Polygon|MultiPolygon + competidores, renderiza sin depender de ningún archivo de Fase 16/17/18.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificación visual en vivo contra fixture (sin datos reales — el componente todavía no está wireado a ningún backend)</name>
  <files>app/dev-preview-cabida-mapa/page.tsx</files>
  <action>
Crear una página de preview TEMPORAL, fuera de `(dashboard)`/`(admin)` (no requiere sesión — `middleware.ts` no redirige por auth acá, esa lógica vive en `app/(dashboard)/layout.tsx`), solo para este checkpoint:

```typescript
// app/dev-preview-cabida-mapa/page.tsx
// ARCHIVO TEMPORAL — solo para el checkpoint humano de Plan 19-02. Eliminar
// este archivo (y esta carpeta) después de que el checkpoint sea aprobado.
import { CabidaComercialMapa } from "@/components/mercado-inmobiliario/oportunidad-detalle/cabida-comercial-mapa"
import type { CompetidorDetectado } from "@/lib/cabida-comercial"

const FIXTURE_LAT = -33.4172 // Providencia, Santiago — coordenada real conocida
const FIXTURE_LNG = -70.6106

const FIXTURE_GEOMETRIA: GeoJSON.Polygon = {
  type: "Polygon",
  coordinates: [[
    [-70.620, -33.410],
    [-70.600, -33.410],
    [-70.600, -33.425],
    [-70.620, -33.425],
    [-70.620, -33.410],
  ]],
}

const FIXTURE_COMPETIDORES: CompetidorDetectado[] = [
  { nombre: "Unimarc Providencia", formato: "supermercado", fuente: "sii_geocodificado", lat: -33.415, lng: -70.608, distanciaM: 320, confianza: "alta" },
  { nombre: "Santa Isabel", formato: "supermercado", fuente: "osm", lat: -33.420, lng: -70.605, distanciaM: 480, confianza: "media" },
  { nombre: "Minimarket El Sol", formato: "minimarket", fuente: "osm", lat: -33.412, lng: -70.612, distanciaM: 610, confianza: "media" },
]

export default function DevPreviewCabidaMapaPage() {
  return (
    <div className="p-8">
      <h1 className="mb-4 text-sm font-semibold">Preview — CabidaComercialMapa (fixture, Plan 19-02)</h1>
      <CabidaComercialMapa
        lat={FIXTURE_LAT}
        lng={FIXTURE_LNG}
        geometria={FIXTURE_GEOMETRIA}
        competidores={FIXTURE_COMPETIDORES}
        className="max-w-2xl"
      />
    </div>
  )
}
```
  </action>
  <how-to-verify>
1. Levanta el dev server (`npm run dev`) y abre `http://localhost:3000/dev-preview-cabida-mapa` en el navegador (no requiere login).
2. Confirma que el mapa carga centrado cerca de Providencia, con:
   - Un marcador azul en el punto de origen (lat/lng del fixture).
   - Un polígono VERDE (no azul) delimitando el área de influencia — confirma visualmente que el color es distinto al de `zonificacion-mapa.tsx` (revisa un proyecto real con zonificación si quieres comparar lado a lado).
   - 3 pines ROJOS, uno por cada competidor del fixture, dentro o cerca del polígono.
3. Haz click en uno de los pines rojos — confirma que aparece un popup con el nombre del competidor y su distancia formateada (ej. "Unimarc Providencia — 320 m").
4. Confirma que el mapa hace zoom/encuadre automático al polígono (no queda con el zoom fijo en 15 sin ajustar si el polígono es más grande o más chico que la vista inicial).
5. Revisa la consola del navegador — no debe haber errores.
6. Una vez aprobado: ELIMINA `app/dev-preview-cabida-mapa/page.tsx` (y la carpeta si queda vacía) — es un archivo temporal, no debe quedar en el repo después de este plan.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si el mapa renderiza correctamente (polígono verde + 3 pines rojos + popup + encuadre automático) y confirmas que eliminaste la página de preview, o describe qué falló para iterar.</resume-signal>
</task>

</tasks>

<verification>
`npx tsc --noEmit` limpio. En un navegador real, contra el fixture temporal, el mapa muestra el polígono del área de influencia en un color distinto al de `zonificacion-mapa.tsx`, más un pin por competidor con popup funcional — verificado por un humano, no solo por tipos. La página de preview temporal fue eliminada tras la aprobación.
</verification>

<success_criteria>
`CabidaComercialMapa` renderiza correctamente el polígono del área de influencia (Polygon o MultiPolygon) + pines de competidores, sin ninguna dependencia dura de `lib/cabida-comercial-server.ts`, `lib/isocrona-server.ts` ni el tab de Fase 16 — listo para que Plan 19-04 lo integre con datos reales sin modificaciones a este componente.
</success_criteria>

<output>
After completion, create `.planning/phases/19-veredicto-metodologia-mapa-y-tab/19-02-SUMMARY.md`
</output>
</output>
