---
phase: 14-comparacion-lado-a-lado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mercado-locales-server.ts
autonomous: true

must_haves:
  truths:
    - "obtenerOportunidadPorId(id) sigue devolviendo exactamente el mismo OportunidadDetalle que antes de la extracción — cero cambio de comportamiento observable"
    - "Comparar hasta 5 oportunidades hace 1 query de listings + 1 query de historial de precio + como máximo 1 query de bandas por combinación distinta de comuna×tipoPropiedad×operación presente (nunca 1 query por oportunidad)"
  artifacts:
    - path: "lib/mercado-locales-server.ts"
      provides: "construirOportunidadDetalle() helper interno (no exportado) + obtenerOportunidadesPorIds(ids: string[]): Promise<OportunidadDetalle[]> exportada"
      exports: ["obtenerOportunidadesPorIds"]
  key_links:
    - from: "obtenerOportunidadPorId()"
      to: "construirOportunidadDetalle()"
      via: "llamada directa, reemplazando el bloque de cálculo inline de las líneas 637-673 originales"
      pattern: "construirOportunidadDetalle\\("
    - from: "obtenerOportunidadesPorIds()"
      to: "construirOportunidadDetalle()"
      via: "una llamada por listing dentro de un .map(), reusando el mismo helper que obtenerOportunidadPorId()"
      pattern: "construirOportunidadDetalle\\("
    - from: "obtenerOportunidadesPorIds()"
      to: "mercado_locales_listings / mercado_locales_historial_precio"
      via: "un solo .in('id', ids) y un solo .in('listing_id', ids), nunca un loop de queries por id"
      pattern: "\\.in\\('id', ids\\)"
---

<objective>
Construye `obtenerOportunidadesPorIds(ids: string[])` en `lib/mercado-locales-server.ts` — la función de datos que Fase 13 no dejó construida y que la comparación necesita para traer hasta 5 oportunidades en lote, sin reintroducir N+1.

Purpose: es el prerequisito de datos de todo lo demás en esta fase — tanto la ruta `/oportunidades/comparar` como su tabla dependen de esta función devolviendo `OportunidadDetalle[]` con la misma forma exacta que ya usa la ficha de detalle individual (Fase 13).
Output: `obtenerOportunidadesPorIds(ids)` exportada + `construirOportunidadDetalle()` como helper interno extraído (Open Question 4 de 14-RESEARCH.md, líneas 240-243 — recomendación adoptada: extraer en vez de duplicar el bloque o llamar `obtenerOportunidadPorId()` en loop, porque esto último reintroduce el N+1 que el research de milestone marcó como pitfall a evitar).
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-comparacion-lado-a-lado/14-RESEARCH.md

@lib/mercado-locales-server.ts
@lib/propiedades-portafolio-server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraer construirOportunidadDetalle() de obtenerOportunidadPorId()</name>
  <files>lib/mercado-locales-server.ts</files>
  <action>
En `lib/mercado-locales-server.ts`, extrae el bloque de cálculo de `obtenerOportunidadPorId()` (líneas 637-673 del archivo actual: cálculo de `precioValido`, conversión CLP→UF vía `bandas.ufValorUsado`, `precioUfM2`, y `reasonCodes` vía `evaluarOportunidad()`) a una función privada NO exportada:

```typescript
function construirOportunidadDetalle(
  listing: {
    id: string
    titulo: string
    url: string
    comuna: string
    tipo_propiedad: string
    operacion: string
    status: string
    dado_de_baja_el: string | null
    precio_monto: number | null
    precio_moneda: string | null
    superficie_m2: number | null
    primera_vez_visto_el: string
    ultima_vez_visto_el: string
  },
  bandas: BandasMercadoLocal | null,
  historialReciente: { precio_monto: number; capturado_el: string }[],
): OportunidadDetalle {
  // ... exactamente la lógica de las líneas 637-673 + el objeto de retorno de líneas 675-694,
  // usando `listing.tipo_propiedad as TipoPropiedadComercial` y `listing.operacion as OperacionMercadoLocal`
  // para tipoPropiedad/operacion como ya hace el código actual.
}
```

`historialReciente` reemplaza la llamada interna a `obtenerHistorialPrecioListing(id)` + filtro de 7 días que hoy vive dentro del bloque (líneas 659-663) — el caller (tanto `obtenerOportunidadPorId` como la nueva `obtenerOportunidadesPorIds` de Task 2) es responsable de traer y filtrar el historial ANTES de llamar al helper, para que el helper mismo no haga I/O (permite que `obtenerOportunidadesPorIds` haga una sola query de historial batched en vez de N).

Reescribe `obtenerOportunidadPorId(id)` para que:
1. Siga haciendo su propio fetch de `listing` (SELECT por `id`, sin cambios) y `bandas` (sin cambios).
2. Llame a `obtenerHistorialPrecioListing(id)`, filtre a los últimos 7 días (misma lógica de `sevenDaysAgo` que ya existe), y pase el resultado a `construirOportunidadDetalle(listing, bandas, historialReciente)`.
3. Devuelva directamente el resultado del helper.

NO cambies la firma pública de `obtenerOportunidadPorId(id: string): Promise<OportunidadDetalle | null>` ni el manejo de `listing` nulo/error (sigue devolviendo `null` antes de llegar al helper).
  </action>
  <verify>`npx tsc --noEmit` limpio. Lee el diff de `obtenerOportunidadPorId` y confirma que el objeto `OportunidadDetalle` devuelto para un id real no cambia campo por campo respecto al comportamiento anterior (mismo cálculo, solo reubicado).</verify>
  <done>`construirOportunidadDetalle()` existe como función privada; `obtenerOportunidadPorId()` la usa y sigue exportando la misma firma pública; `npx tsc --noEmit` sin errores nuevos.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar obtenerOportunidadesPorIds(ids) con fetch batched</name>
  <files>lib/mercado-locales-server.ts</files>
  <action>
Agrega, después de `obtenerOportunidadPorId()`, una función exportada nueva:

```typescript
export async function obtenerOportunidadesPorIds(ids: string[]): Promise<OportunidadDetalle[]>
```

Sigue el patrón exacto de `compararPortafolioConMercado()` (`lib/propiedades-portafolio-server.ts:88-97`, ya citado en 14-RESEARCH.md líneas 89-129) adaptado a este archivo:

1. Un solo `.from('mercado_locales_listings').select(...).in('id', ids)` — mismas columnas que ya selecciona `obtenerOportunidadPorId` (línea 626 actual: `id, titulo, url, comuna, tipo_propiedad, operacion, status, dado_de_baja_el, precio_monto, precio_moneda, superficie_m2, primera_vez_visto_el, ultima_vez_visto_el`). Si `error || !listings`, devuelve `[]`.
2. Un solo `.from('mercado_locales_historial_precio').select('listing_id, precio_monto, capturado_el').in('listing_id', ids).gte('capturado_el', sevenDaysAgoIso)` (mismo patrón de filtro por fecha en la query que ya usa `obtenerComparablesOportunidad()`, líneas 781-786 actuales — más eficiente que traer todo el historial y filtrar en JS). Agrupa los resultados en un `Map<string, {precio_monto:number; capturado_el:string}[]>` por `listing_id`.
3. Una caché de bandas por combinación EXACTA `comuna|tipoPropiedad|operacion` (no solo por comuna — los ids podrían llegar heterogéneos si esta función se llama antes de la validación de homogeneidad que hace `/comparar/page.tsx` en el Plan 14-03; cachear por la clave completa es correcto en ambos casos y es el mismo criterio de `compararPortafolioConMercado`):

```typescript
const bandasCache = new Map<string, Promise<BandasMercadoLocal | null>>()
function bandasPara(comuna: string, tipoPropiedad: TipoPropiedadComercial, operacion: OperacionMercadoLocal) {
  const key = `${comuna}|${tipoPropiedad}|${operacion}`
  if (!bandasCache.has(key)) bandasCache.set(key, obtenerBandasMercadoLocales(comuna, operacion, tipoPropiedad))
  return bandasCache.get(key)!
}
```

4. Para cada `listing`, resuelve sus bandas vía `bandasPara(...)`, toma su `historialReciente` del Map del paso 2 (default `[]` si no hay entradas), y llama `construirOportunidadDetalle(listing, bandas, historialReciente)` (el helper de Task 1).
5. Devuelve el array de `OportunidadDetalle` — el orden NO tiene que coincidir con `ids` de entrada (el caller en Plan 14-03 no depende del orden de este array; puede re-ordenar si lo necesita).

NO valides aquí homogeneidad de tipo/operación ni longitud de `ids` (2-5) — esa validación es responsabilidad de `/comparar/page.tsx` (Plan 14-03), esta función es pura capa de datos y debe funcionar para cualquier `ids: string[]` no vacío, incluyendo listings `dado_de_baja` (mismo criterio sin-filtro-de-status que `obtenerOportunidadPorId`, líneas 615-619 actuales — un id de un aviso dado de baja debe seguir resolviendo aquí, la exclusión visual es decisión de UI en Plan 14-03).
  </action>
  <verify>`npx tsc --noEmit` limpio. `grep -n "?? 0" lib/mercado-locales-server.ts` no debe mostrar coincidencias nuevas dentro del bloque agregado (Pitfall 2 de 14-RESEARCH.md — null-goes-last, nunca `?? 0` sobre `precioUfM2Normalizado`). Prueba manual rápida: en una sesión de Node/tsx local o vía script temporal, llama `obtenerOportunidadesPorIds([idReal1, idReal2])` con 2 ids reales de la tabla (obtenidos vía `mcp__supabase__execute_sql` si es necesario) y confirma que el array devuelto tiene 2 elementos con `reasonCodes`/`precioUfNormalizado` no vacíos/no-cero cuando corresponde.</verify>
  <done>`obtenerOportunidadesPorIds(ids)` exportada, hace exactamente 1 query de listings + 1 de historial + como máximo 1 de bandas por combinación distinta comuna×tipoPropiedad×operación; `npx tsc --noEmit` limpio.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` sin errores en todo el proyecto. `obtenerOportunidadPorId` y `obtenerOportunidadesPorIds` ambas exportadas desde `lib/mercado-locales-server.ts` y ambas usan `construirOportunidadDetalle()` internamente (grep confirma 2 call sites).
</verification>

<success_criteria>
`obtenerOportunidadesPorIds(ids: string[])` existe, devuelve `OportunidadDetalle[]` con datos correctos para hasta 5 ids reales, y hace un número de queries acotado (no proporcional 1:1 a `ids.length` salvo para bandas en el peor caso de 5 comunas distintas). `obtenerOportunidadPorId()` no cambió su comportamiento observable.
</success_criteria>

<output>
After completion, create `.planning/phases/14-comparacion-lado-a-lado/14-01-SUMMARY.md`
</output>
