---
phase: 14-comparacion-lado-a-lado
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mercado-inmobiliario/selector-comparacion.tsx
  - app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx
autonomous: true

must_haves:
  truths:
    - "Usuario ve un checkbox por oportunidad en la lista de /oportunidades"
    - "Usuario NO puede marcar una sexta oportunidad cuando ya hay 5 seleccionadas — el checkbox de cualquier ítem no seleccionado queda deshabilitado (defensa de cliente de COMPA-03, capa 1 de 2 — la capa 2, server-side, es Plan 14-03)"
    - "Con 2 o más oportunidades marcadas, aparece un botón/barra 'Comparar (N)'; con 0 o 1 marcadas, no aparece (o aparece deshabilitado)"
    - "Al hacer click en 'Comparar', el navegador va a /mercado-inmobiliario/oportunidades/comparar?ids=id1,id2,... (COMPA-04 — la selección queda en la URL, no en estado que se pierda al recargar)"
  artifacts:
    - path: "components/mercado-inmobiliario/selector-comparacion.tsx"
      provides: "Client island: checkbox por oportunidad + tope de 5 + botón/barra flotante 'Comparar (N)' + router.push hacia /oportunidades/comparar"
      contains: "\"use client\""
    - path: "app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx"
      provides: "Usa <SelectorComparacion> en vez de mapear las cards inline — el Server Component sigue haciendo el fetch de datos, solo delega el render interactivo de la lista"
  key_links:
    - from: "components/mercado-inmobiliario/selector-comparacion.tsx"
      to: "/mercado-inmobiliario/oportunidades/comparar"
      via: "useRouter().push(`/mercado-inmobiliario/oportunidades/comparar?ids=${selectedIds.join(',')}`)"
      pattern: "router\\.push\\(.*comparar"
    - from: "app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx"
      to: "components/mercado-inmobiliario/selector-comparacion.tsx"
      via: "import + <SelectorComparacion oportunidades={oportunidades} ... />"
      pattern: "SelectorComparacion"
---

<objective>
Construye el primer selector interactivo de comparación del módulo (COMPA-01, capa cliente de COMPA-03, COMPA-04): un client island que agrega un checkbox por oportunidad en la lista existente, limita la selección a 5, y navega a `/oportunidades/comparar?ids=...` al confirmar.

Purpose: es la vía normal por la que un usuario arma una comparación — sin este selector, `?ids=` solo sería alcanzable escribiendo la URL a mano. Puede construirse en paralelo con Plan 14-01 (no depende de `obtenerOportunidadesPorIds`, solo construye la URL) y con Plan 14-03 (la ruta `/comparar` acepta `?ids=` directo sin necesitar este selector para poder probarse).
Output: `components/mercado-inmobiliario/selector-comparacion.tsx` nuevo + `oportunidades/page.tsx` modificado para usarlo.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-comparacion-lado-a-lado/14-RESEARCH.md

@app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx
@app/(dashboard)/mercado-inmobiliario/reportes/page.tsx
@components/ui/checkbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear SelectorComparacion (client island con checkbox + tope 5 + botón flotante)</name>
  <files>components/mercado-inmobiliario/selector-comparacion.tsx</files>
  <action>
Crea `components/mercado-inmobiliario/selector-comparacion.tsx` como `"use client"`. Este componente reemplaza el bloque `<div className="space-y-3">{oportunidades.map(...)}</div>` que hoy vive inline en `oportunidades/page.tsx` (líneas 121-167 actuales) — mueve ESE JSX completo (título con link externo, precio, comuna/superficie/UF-m², badges de `reasonCodes` vía `REASON_LABEL`, badges de señales de expansión/tendencia constructiva, link "Ver ficha completa →") dentro de este componente, agregando un `<Checkbox>` (de `@/components/ui/checkbox`) a la izquierda de cada card.

Props (todo lo que el Server Component `oportunidades/page.tsx` ya calculaba y que este componente necesita para renderizar — Next.js RSC serializa `Map` de forma poco fiable entre server/client, así que `senalesExpansion` y `tendenciasConstruccion` deben llegar como objetos planos, no `Map`, convertidos con `Object.fromEntries(...)` en el Server Component antes de pasarlos):

```typescript
interface SelectorComparacionProps {
  oportunidades: Awaited<ReturnType<typeof obtenerOportunidadesMercadoLocales>> // mismo tipo que ya usa page.tsx
  senalesExpansion: Record<string, { cadena: string; fechaRegistro: string }>
  tendenciasConstruccion: Record<string, { tendencia: string; variacionPct: number | null }>
}
```

(Ajusta los tipos exactos de `senalesExpansion`/`tendenciasConstruccion` a lo que realmente devuelven `obtenerSenalesExpansionPorComuna`/`obtenerTendenciasConstruccionPorComuna` — revisa sus firmas en `lib/cadenas-sucursales-server.ts`/`lib/ine-permisos-server.ts` si el tipo inferido no calza directo con `Object.fromEntries`.)

Estado y lógica:
- `const [selectedIds, setSelectedIds] = useState<string[]>([])`
- `const router = useRouter()` (`next/navigation`)
- Función `toggle(id: string)`: si ya está en `selectedIds`, lo saca; si no está Y `selectedIds.length < 5`, lo agrega; si no está y ya hay 5, no hace nada (el checkbox de ese ítem debe estar `disabled` — ver siguiente punto, así que `toggle` nunca debería llamarse en ese estado, pero la guarda es defensiva).
- Cada `<Checkbox>` recibe `checked={selectedIds.includes(o.id)}`, `onCheckedChange={() => toggle(o.id)}`, y `disabled={!selectedIds.includes(o.id) && selectedIds.length >= 5}` — este es el ÚNICO caso real de deshabilitar dentro de esta lista, porque TODOS los ítems que llegan como prop ya comparten el mismo `tipoPropiedad`+`operacion` por construcción (Pattern 1 de 14-RESEARCH.md, líneas 73-81 — `oportunidades/page.tsx` filtra a un solo tipo/operación por render antes de pasar la lista aquí). No hay heterogeneidad que prevenir DENTRO de esta lista — eso es exactamente por lo que la validación server-side de Plan 14-03 es la defensa real, no un checkbox extra acá.
- Barra/botón flotante: cuando `selectedIds.length >= 2`, muestra un botón fijo (ej. `fixed bottom-6 right-6` o una barra `sticky bottom-0` con el resto del layout del proyecto — usa el mismo estilo de `Button` de `@/components/ui/button` y las clases `rounded-lg border-line-fine bg-card` que ya usa el resto del módulo) con texto `Comparar (${selectedIds.length})`. Con menos de 2 seleccionadas, no renderices el botón (no un botón deshabilitado sin contexto — más simple y menos ruido visual).
- `onClick` del botón: `router.push(`/mercado-inmobiliario/oportunidades/comparar?ids=${selectedIds.join(",")}`)`.

Mantén exactamente el mismo JSX visual que ya existe para cada card (mismo `formatUf`, mismo `REASON_LABEL`, mismo texto) — este componente es una migración de ubicación + una capa de selección encima, no un rediseño visual.
  </action>
  <verify>`npx tsc --noEmit` limpio (aunque el componente aún no esté importado en ninguna parte, no debe tener errores de tipos propios).</verify>
  <done>`components/mercado-inmobiliario/selector-comparacion.tsx` existe, exporta `SelectorComparacion`, con checkbox por ítem (deshabilitado solo al tope de 5), botón "Comparar (N)" visible desde 2 seleccionadas, y `router.push` hacia `/oportunidades/comparar?ids=...` con los ids separados por coma.</done>
</task>

<task type="auto">
  <name>Task 2: Wirear SelectorComparacion en oportunidades/page.tsx</name>
  <files>app/(dashboard)/mercado-inmobiliario/oportunidades/page.tsx</files>
  <action>
En `oportunidades/page.tsx`:
1. Elimina el bloque `<div className="space-y-3">{oportunidades.map((o) => (...))}</div>` (líneas 121-167 actuales) — ese JSX ahora vive en `SelectorComparacion` (Task 1).
2. Importa `SelectorComparacion` desde `@/components/mercado-inmobiliario/selector-comparacion`.
3. Convierte `senalesExpansion` y `tendenciasConstruccion` (ambos `Map`, ya calculados en líneas 46-56 actuales) a objetos planos con `Object.fromEntries(...)` justo antes de renderizar — NO cambies cómo se calculan (siguen siendo `Map` para el resto de la página si se necesitaran, solo conviertes en el punto de paso a props).
4. Renderiza `<SelectorComparacion oportunidades={oportunidades} senalesExpansion={Object.fromEntries(senalesExpansion)} tendenciasConstruccion={Object.fromEntries(tendenciasConstruccion)} />` en el lugar donde estaba el bloque eliminado.
5. NO toques el resto de la página (form de filtros, `Histograma`, estado vacío/error) — solo el bloque de cards pasa a ser responsabilidad de `SelectorComparacion`.
  </action>
  <verify>`npx tsc --noEmit` limpio. `npm run build` (o el comando de build del proyecto) compila sin errores. Lectura visual del diff: el form de filtros, el histograma y los estados vacío/error de la página quedan intactos; solo el `.map()` de cards fue reemplazado por `<SelectorComparacion>`.</verify>
  <done>La página `/oportunidades` renderiza `SelectorComparacion` con los datos reales del fetch server-side; `npx tsc --noEmit` y build limpios; ningún otro bloque de la página cambió de comportamiento.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` limpio. Inspección manual del código (no hay entorno de browser en este plan — el checkpoint visual/funcional end-to-end se hace en Plan 14-03 una vez que `/comparar` también existe): `SelectorComparacion` recibe datos reales, no mocks; el `router.push` construye la URL con el formato `?ids=uuid1,uuid2` que `/comparar/page.tsx` (Plan 14-03) espera parsear.
</verification>

<success_criteria>
Usuario puede marcar entre 2 y 5 oportunidades desde la lista y llegar a `/oportunidades/comparar?ids=...` con un click. El checkbox se deshabilita solo al llegar al tope de 5 (COMPA-01 + capa 1 de COMPA-03 + COMPA-04 vía URL).
</success_criteria>

<output>
After completion, create `.planning/phases/14-comparacion-lado-a-lado/14-02-SUMMARY.md`
</output>
