---
phase: 17-demografia-y-consumo
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - lib/cabida-comercial-server.ts
autonomous: true

must_haves:
  truths:
    - "Cuando lib/cabida-comercial-server.ts existe (Phase 16 completa), obtenerAnalisisCabidaComercial() incluye el campo demografia poblado — cerrando DEMO-01 a DEMO-03 de punta a punta a nivel de datos (la UI la cierra Plan 17-04)"
    - "Si lib/cabida-comercial-server.ts NO existe todavía, este plan se detiene explícitamente sin fabricar un stub ni fallar silenciosamente — deja un registro claro de por qué no se ejecutó"
  artifacts:
    - path: "lib/cabida-comercial-server.ts"
      provides: "obtenerAnalisisCabidaComercial() con el campo demografia poblado vía lib/censo-manzana-server.ts + lib/consumo-macro-zona.ts — SOLO si el archivo ya existe al momento de ejecutar este plan"
      contains: "obtenerPoblacionEnPoligono"
  key_links:
    - from: "lib/cabida-comercial-server.ts (obtenerAnalisisCabidaComercial)"
      to: "lib/censo-manzana-server.ts (obtenerPoblacionEnPoligono) + lib/consumo-macro-zona.ts (obtenerConsumoEstimado)"
      via: "obtenerPoblacionEnPoligono() reusa isocrona.geometria ya calculada en la misma función — sin recalcular ni re-consultar la isócrona; obtenerConsumoEstimado() usa la comuna ya resuelta"
      pattern: "obtenerPoblacionEnPoligono\\("
---

<objective>
ÚNICO plan de Fase 17 que toca `lib/cabida-comercial-server.ts` — el archivo que Phase 16 (Plan 16-04, a la fecha de esta planificación todavía no ejecutado) debe construir. Este plan está deliberadamente aislado y gateado, clonando el patrón exacto de Fase 18's Plan 18-07: es la última wave de la parte "de datos" de Fase 17, depende de que Plan 17-01 (`obtenerPoblacionEnPoligono`) y Plan 17-02 (`obtenerConsumoEstimado`) ya existan, y su PRIMER paso es verificar que el prerequisito de Phase 16 se cumplió antes de tocar nada.

Purpose: Wiring genuino de composición final — `obtenerAnalisisCabidaComercial(lat, lng, formato)` debe retornar el campo `demografia` (opcional, siguiendo el mismo patrón aditivo que Fase 18 estableció con `competencia?`) poblado. Pero `lib/cabida-comercial-server.ts` no existía al momento de investigar/planificar Fase 17 (verificado en vivo, ver `17-RESEARCH.md` User Constraints) y Phase 16 sigue pausada esperando que la propagación de la cuenta HeiGIT/ORS se resuelva (ver STATE.md). Ejecutar este plan sin el prerequisito produciría, en el mejor caso, un error de archivo no encontrado, y en el peor, la tentación de crear un stub fabricado — exactamente el atajo que este repo prohíbe explícitamente. Por eso el Task 1 es un check explícito, no una asunción.

Output: Si Phase 16 ya completó `lib/cabida-comercial-server.ts`: el campo `demografia` queda poblado en `obtenerAnalisisCabidaComercial()`, componiendo población censal (Plan 17-01) + consumo estimado (Plan 17-02) sin recalcular la isócrona. Si no: el plan se detiene limpiamente, documentado, listo para reintentarse más tarde sin cambios.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-demografia-y-consumo/17-RESEARCH.md
@.planning/STATE.md
@lib/cabida-comercial.ts
@lib/censo-manzana-server.ts
@lib/consumo-macro-zona.ts
@.planning/phases/18-competencia-por-formato/18-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar prerequisito de Phase 16 ANTES de tocar cualquier archivo</name>
  <files></files>
  <action>
Correr, en este orden exacto, y NO proceder al Task 2 si cualquiera de los dos falla:

```bash
ls lib/cabida-comercial-server.ts 2>/dev/null && echo "ARCHIVO EXISTE" || echo "ARCHIVO NO EXISTE"
grep -n "export async function obtenerAnalisisCabidaComercial" lib/cabida-comercial-server.ts 2>/dev/null || echo "FUNCION NO ENCONTRADA"
```

**Si el archivo NO existe, o la función `obtenerAnalisisCabidaComercial` no se encuentra:** este es el resultado ESPERADO si Phase 16 (específicamente Plan 16-04) todavía no terminó de ejecutarse — a la fecha en que se planificó Fase 17, Phase 16 estaba pausada esperando que la propagación de la cuenta HeiGIT/ORS se resolviera (ver `.planning/STATE.md`, sección "Current Position": `ORS_API_KEY` seteada pero ambos dominios devuelven 403). Esto NO es un error de este plan ni de Fase 17.

En ese caso: DETENER la ejecución de este plan acá mismo. NO crear `lib/cabida-comercial-server.ts` desde cero, NO crear un stub, NO fabricar una función parcial. Registrar explícitamente en el SUMMARY de este plan (ver `<output>`) que el plan quedó BLOQUEADO por el prerequisito de Phase 16, con la fecha del intento y el resultado exacto de los dos comandos de arriba, y que debe reintentarse corriendo `/gsd:execute-phase 17` de nuevo una vez que Phase 16 (Plan 16-04 específicamente) esté completo — en ese punto solo falta este plan (y luego Plan 17-04), ningún otro plan de Fase 17 necesita re-ejecutarse.

**Si el archivo existe Y la función se encuentra:** proceder al Task 2 normalmente.
  </action>
  <verify>El resultado de los 2 comandos queda registrado literalmente (copiado, no parafraseado) en el SUMMARY de este plan, sin importar cuál de los dos casos ocurrió.</verify>
  <done>Está determinado, con evidencia de comando real (no supuesto), si el prerequisito de Phase 16 se cumple — y si no se cumple, el plan se detuvo sin fabricar nada.</done>
</task>

<task type="auto">
  <name>Task 2: Wire obtenerPoblacionEnPoligono() + obtenerConsumoEstimado() dentro de obtenerAnalisisCabidaComercial() — SOLO si Task 1 confirmó el prerequisito</name>
  <files>lib/cabida-comercial-server.ts</files>
  <action>
Este task SOLO se ejecuta si Task 1 confirmó que `lib/cabida-comercial-server.ts` existe y exporta `obtenerAnalisisCabidaComercial`. Si Task 1 detuvo el plan, este task NO se ejecuta — el plan termina en Task 1.

Leer `lib/cabida-comercial-server.ts` completo primero para entender la firma real de `obtenerAnalisisCabidaComercial(lat, lng, formato)` construida por Phase 16 y, si ya se ejecutó, el wiring de `competencia` que Plan 18-07 pudo haber agregado — el código real, no lo documentado en planes, es la fuente de verdad.

Agregar a `lib/cabida-comercial.ts` el campo `demografia?` en `AnalisisCabidaComercial` (mismo patrón aditivo que `competencia?`, Fase 18):

```typescript
// En lib/cabida-comercial.ts, junto a competencia?:
export interface DemografiaYConsumo {
  poblacion: PoblacionCensoResultado   // import type desde lib/censo-manzana-server — OJO: ese archivo es server-only (usa fetchWithTimeout con timeout de red), verificar si lib/cabida-comercial.ts (client-safe) puede importar SOLO el tipo (import type) sin arrastrar el código server-only al bundle cliente. Si el archivo real de Phase 16 ya resolvió un patrón similar para IsocronaResultado/competencia, seguir el mismo criterio exacto.
  consumo: ConsumoEstimadoResultado    // import type desde lib/consumo-macro-zona — ese archivo SÍ es client-safe (sin red, sin server-only imports), se puede importar directo si hace falta.
}

export interface AnalisisCabidaComercial {
  formato: FormatoComercial
  isocrona: IsocronaResultado
  competencia?: ResultadoCompetenciaFormato
  demografia?: DemografiaYConsumo // NUEVO — Fase 17, opcional hasta este plan
  generadoEl: string
}
```

Luego, DENTRO de `obtenerAnalisisCabidaComercial()` en `lib/cabida-comercial-server.ts`, después de que la isócrona (o el fallback `circulo_equivalente`) ya está resuelta:

```typescript
const [poblacion, consumo] = await Promise.all([
  obtenerPoblacionEnPoligono(isocrona.geometria),
  Promise.resolve(obtenerConsumoEstimado(comuna)), // síncrona, pero Promise.all por uniformidad — comuna ya disponible en este scope (UbicacionCabida)
])

return {
  formato,
  isocrona,
  competencia,     // si Fase 18 ya está wireada
  demografia: { poblacion, consumo }, // NUEVO — Fase 17
  generadoEl: new Date().toISOString(),
}
```

Ajustar nombres de variables al código real leído (no asumir sin confirmar contra el archivo). REUSAR `isocrona.geometria` ya calculada — NUNCA volver a llamar `obtenerIsocrona()` ni recalcular el polígono dentro de este wiring (mismo anti-patrón que 18-07 evita para competencia, citado explícitamente en `17-RESEARCH.md` "Anti-Patterns to Avoid").

Si `obtenerAnalisisCabidaComercial` ya tiene cache-through hacia `cabida_comercial_cache` (Plan 16-03/16-04), decidir con criterio propio si el resultado de `demografia.poblacion` debe persistirse en las columnas `demografia_*` agregadas por Plan 17-01 (probablemente sí, mismo criterio que la isócrona — un cálculo caro que vale cachear por `(lat_r, lng_r, modo, minutos)`) o si `demografia.consumo` debe cachearse (probablemente NO — es síncrono, sin costo de red, recalcular es gratis, igual que `17-RESEARCH.md` Pattern 3 ya decidió). Documentar la decisión tomada en el SUMMARY si Phase 16 no dejó esto explícito.
  </action>
  <verify>`npm run typecheck` pasa; `npm run test` (suite completa) sigue en verde; una llamada real a `obtenerAnalisisCabidaComercial()` para una ubicación/formato conocidos retorna `analisis.demografia` poblado (no `undefined`), con `demografia.poblacion.censoAno === 2017` y `demografia.consumo.disclosure` no vacío.</verify>
  <done>obtenerAnalisisCabidaComercial() retorna el campo demografia poblado (población real vía ArcGIS + consumo estimado vía tabla estática), reusando la geometría de isócrona ya calculada, sin duplicar cálculos ni romper ningún test/consumidor existente de Phase 16/18.</done>
</task>

</tasks>

<verification>
Si Phase 16 estaba completa al momento de ejecutar: `npm run typecheck && npm run test` pasan, y una llamada real a `obtenerAnalisisCabidaComercial()` trae `demografia` poblado con datos reales (no mockeados) para al menos una ubicación conocida — `demografia.poblacion.totalPersonas` numérico real, `demografia.consumo.disclosure` presente y no genérico. Si Phase 16 NO estaba completa: el SUMMARY documenta el bloqueo con evidencia de comando real, cero archivos fueron modificados, y el plan queda listo para reintentarse sin cambios adicionales.
</verification>

<success_criteria>
El campo `demografia` de `AnalisisCabidaComercial` queda poblado de punta a punta a nivel de datos (DEMO-01/DEMO-02/DEMO-03 satisfechos por el contrato de tipos y la composición real) el día que Phase 16 complete — sin que ese día requiera ninguna planificación nueva, solo re-ejecutar este plan. La visibilidad en el navegador queda a cargo de Plan 17-04 (gateado adicionalmente por este plan).
</success_criteria>

<output>
After completion, create `.planning/phases/17-demografia-y-consumo/17-03-SUMMARY.md` — DEBE indicar explícitamente si el plan se completó de punta a punta o si quedó bloqueado esperando Phase 16 (con la evidencia de comando del Task 1 incluida literalmente).
</output>
