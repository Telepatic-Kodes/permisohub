---
phase: 17-demografia-y-consumo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/consumo-macro-zona.ts
  - tests/unit/consumo-macro-zona.test.ts
  - .planning/data-sources.yaml
autonomous: false

must_haves:
  truths:
    - "El sistema puede estimar capacidad de gasto por categoría de consumo (EPF) y tasa de pobreza comunal (CASEN) para cualquier comuna, sin ninguna llamada de red en tiempo de request (DEMO-02)"
    - "Cada cifra de consumo declara explícitamente que es un estimado agregado a nivel macro-zona, nunca con precisión de isócrona — campo no-opcional, no un comentario en el código (DEMO-02, DEMO-03)"
    - "Ningún share EPF ni tasa de pobreza CASEN es inventado — cada valor transcrito viene de una fuente oficial citada, con la categoría/comuna marcada explícitamente como 'pendiente' o null cuando el dato real no fue confirmado, nunca rellenado con un número plausible"
  artifacts:
    - path: "lib/consumo-macro-zona.ts"
      provides: "EPF_PARTICIPACION_POR_CATEGORIA (parcial, con categorías pendientes declaradas) + CASEN_POBREZA_POR_COMUNA (comunas RM con oportunidades reales) + obtenerConsumoEstimado(comuna) — pura, sin red"
      exports: ["obtenerConsumoEstimado", "EPF_PARTICIPACION_POR_CATEGORIA", "CASEN_POBREZA_POR_COMUNA"]
  key_links:
    - from: "lib/consumo-macro-zona.ts (obtenerConsumoEstimado)"
      to: "lib/scrapers/instrumentos-ipt.ts (normalizarNombreComuna)"
      via: "mismo helper de normalización ya usado por lib/cadenas-sucursales-server.ts — nunca un .find comuna === comuna directo, sensible a mayúsculas/tildes"
      pattern: "normalizarNombreComuna\\("
---

<objective>
Construir `lib/consumo-macro-zona.ts` — la pieza "estática, sin red" de Fase 17: capacidad de gasto estimada por categoría (EPF) + tasa de pobreza comunal (CASEN), consumida por comuna, cero llamadas externas en tiempo de request. A diferencia de Plan 17-01 (una integración ArcGIS en vivo, nueva), este plan es deliberadamente "no una integración" — `17-RESEARCH.md` confirmó que ni INE (EPF) ni el Ministerio de Desarrollo Social (CASEN) exponen un archivo descargable machine-fetchable, así que la disciplina correcta es una tabla TypeScript estática, git-versionada, transcrita a mano desde fuentes oficiales citadas — mismo patrón editorial que `lib/strip-power-centers-chile.ts` (Fase 18, Plan 18-03).

Purpose: DEMO-02 exige capacidad de gasto por categoría de consumo (ingreso/pobreza comunal vía CASEN + share de categoría vía EPF), explícitamente etiquetada como estimado agregado a nivel macro-zona — nunca con la precisión de la isócrona de Fase 16. Sin este módulo no hay dato de consumo que mostrar.

Este plan es independiente de Phase 16 y de Plan 17-01: `obtenerConsumoEstimado(comuna)` recibe solo un string de comuna (el mismo campo que ya existe en `UbicacionCabida.comuna`, Fase 16), sin geometría ni isócrona.

**Disciplina de "nunca fabricar" aplicada dos veces en este plan:**
1. **EPF**: `17-RESEARCH.md` confirmó solo 3 de las 12-13 categorías CCIF 2018.CL con cifras reales citables (Alimentación 21.2%, Vivienda 16.0%, Transporte 15.0% — IX EPF, oct 2021-sept 2022, INE). Ningún intento adicional de esta sesión (2 WebFetch contra ine.gob.cl) encontró más categorías con cifra exacta citable — los PDFs con el desglose completo no son parseables por fetch automatizado. Decisión explícita (Open Question 2 de `17-RESEARCH.md`, resuelta): shippear con las 3 categorías confirmadas + un array `categoriasPendientes` explícito para las ~9-10 restantes — nunca inventar un share plausible para completarlas.
2. **CASEN**: el portal de datos (`datasocial.ministeriodesarrollosocial.gob.cl`) no resuelve desde este entorno (DNS falla — confirmado en esta sesión, coincide con el hallazgo de `17-RESEARCH.md` sobre dominios INE similares) y no expone un archivo descargable discoverable. La tasa de pobreza comunal real, a diferencia de los shares EPF, NO tiene ningún valor citable todavía en este repo — por eso el Task 2 de este plan es un checkpoint que pide al usuario transcribir cifras reales desde la fuente oficial (`observatorio.ministeriodesarrollosocial.gob.cl/pobreza-comunal` → Data Social, CASEN 2024, metodología SAE), acotado a las 36 comunas RM donde `mercado_locales_listings` tiene oportunidades reales hoy (verificado en vivo vía `mcp__supabase__execute_sql`) — no las ~335 comunas nacionales, que excederían el valor real de esta fase.

Output: `lib/consumo-macro-zona.ts` con datos EPF reales (parciales, honestos) + datos CASEN reales (36 comunas RM, transcritos por el usuario desde la fuente oficial) + `obtenerConsumoEstimado()` probado.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-demografia-y-consumo/17-RESEARCH.md
@lib/cabida-comercial.ts
@lib/strip-power-centers-chile.ts
@lib/scrapers/instrumentos-ipt.ts
@lib/comunas-chile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos + tabla EPF (parcial, honesta) + esqueleto de tabla CASEN + obtenerConsumoEstimado()</name>
  <files>lib/consumo-macro-zona.ts</files>
  <action>
Crear `lib/consumo-macro-zona.ts`:

```typescript
// Capacidad de gasto estimada por categoría (EPF) + tasa de pobreza comunal
// (CASEN) — Fase 17, DEMO-02/DEMO-03. A diferencia de lib/censo-manzana-server.ts
// (Plan 17-01, consulta ArcGIS en vivo), este módulo es ESTÁTICO — cero
// llamadas de red en tiempo de request. Ni INE (EPF) ni el Ministerio de
// Desarrollo Social (CASEN) exponen un archivo descargable machine-fetchable
// (17-RESEARCH.md, Open Question 1; re-confirmado en esta sesión — 2
// WebFetch contra ine.gob.cl no devolvieron el desglose completo por
// categoría, y datasocial.ministeriodesarrollosocial.gob.cl no resuelve DNS
// desde este entorno). Mismo patrón editorial que
// lib/strip-power-centers-chile.ts (Fase 18): array/tabla estática,
// git-versionada, PR-revisada — NO un scraper/cron.

import { normalizarNombreComuna } from '@/lib/scrapers/instrumentos-ipt'

// ---------------------------------------------------------------------------
// EPF — IX Encuesta de Presupuestos Familiares (INE, terreno oct 2021-sept
// 2022, última edición publicada — la X EPF seguía en fase de prueba según
// el propio sitio de INE al momento de esta investigación). Representativa
// SOLO a nivel Gran Santiago / capitales regionales — NUNCA comunal, NUNCA
// con precisión de isócrona (DEMO-02). Clasificación CCIF 2018.CL, 12
// divisiones — solo 3 tienen cifra exacta citable confirmada en esta
// investigación. Las restantes quedan explícitamente pendientes, nunca
// rellenadas con un valor inventado.
// Fuente: INE, comunicado IX EPF ("hogares en Chile gastan más de 1,4
// millones..."), sección de participación por división de gasto — cifras de
// capitales regionales/Gran Santiago.
// ---------------------------------------------------------------------------

export interface EpfCategoria {
  nombre: string
  participacionPct: number | null // null = categoría pendiente de transcripción, NUNCA inventada
}

export const EPF_ANO = 2022 // fin del período de terreno IX EPF (oct 2021-sept 2022)

export const EPF_PARTICIPACION_POR_CATEGORIA: EpfCategoria[] = [
  { nombre: 'Alimentos y bebidas no alcohólicas', participacionPct: 0.212 },
  { nombre: 'Vivienda, agua, electricidad, gas y combustibles', participacionPct: 0.160 },
  { nombre: 'Transporte', participacionPct: 0.150 },
  { nombre: 'Bebidas alcohólicas y tabaco', participacionPct: null },
  { nombre: 'Vestuario y calzado', participacionPct: null },
  { nombre: 'Muebles, artículos para el hogar', participacionPct: null },
  { nombre: 'Salud', participacionPct: null },
  { nombre: 'Información y comunicación', participacionPct: null },
  { nombre: 'Recreación, deporte y cultura', participacionPct: null },
  { nombre: 'Educación', participacionPct: null },
  { nombre: 'Restaurantes y alojamiento', participacionPct: null },
  { nombre: 'Seguros y servicios financieros, cuidado personal y bienes diversos', participacionPct: null },
]
// NOTA: INE agrupa la última división en algunos resúmenes ("diversos") —
// el conteo exacto de divisiones (12 vs 13) varía por fuente, ver
// 17-RESEARCH.md. No tratar el número de filas de este array como un
// hecho estadístico fijo, solo como el mejor listado disponible hoy.

// ---------------------------------------------------------------------------
// CASEN 2024 — pobreza comunal vía metodología SAE (Estimación de Área
// Pequeña), Ministerio de Desarrollo Social y Familia. Cobertura nacional
// real: 335 comunas — este archivo cubre SOLO las comunas RM donde
// mercado_locales_listings tiene oportunidades reales hoy (verificado en
// vivo, 2026-08-03: 36 comunas). Cualquier comuna fuera de esta lista
// retorna tasaPobrezaComunal: null en obtenerConsumoEstimado — NUNCA un
// valor aproximado o heredado de una comuna vecina.
//
// Filas pobladas por Task 2 de este plan (checkpoint humano) — transcritas
// desde observatorio.ministeriodesarrollosocial.gob.cl/pobreza-comunal →
// Data Social, con cita de fecha de consulta. Task 1 deja el array vacío a
// propósito: NO fabricar una sola fila antes de que Task 2 aporte datos
// reales.
// ---------------------------------------------------------------------------

export const CASEN_ANO = 2024

export interface CasenComunaEstimado {
  comuna: string // nombre oficial, mismo formato que lib/comunas-chile.ts
  tasaPobrezaPersonas: number // % — Estimación de Área Pequeña (SAE), CASEN 2024
  fuenteUrl: string
  transcritoEl: string // fecha ISO en que un humano confirmó esta cifra contra la fuente oficial
}

export const CASEN_POBREZA_POR_COMUNA: CasenComunaEstimado[] = [
  // Poblado por Task 2 (checkpoint humano) — ver nota arriba. Array vacío
  // hasta entonces: obtenerConsumoEstimado() ya maneja este caso
  // correctamente (tasaPobrezaComunal: null para toda comuna).
]

export interface ConsumoEstimadoResultado {
  comuna: string
  categorias: EpfCategoria[]
  categoriasPendientes: string[] // nombres de las categorías con participacionPct: null — para que la UI las liste explícitamente (locked decision #4)
  tasaPobrezaComunal: number | null // null si la comuna no está en CASEN_POBREZA_POR_COMUNA
  nivelGeografico: 'macro_zona_gran_santiago' // NUNCA uno solo para todo el objeto sin declararlo — DEMO-02/DEMO-03. Único valor posible en v1 (EPF no tiene desagregación comunal) — union deliberadamente abierta para no hardcodear un boolean si algún día se agrega otra fuente.
  disclosure: string // "estimado agregado a nivel macro-zona, no medido en el área específica" — literal de DEMO-02, nunca omitido
  epfAno: typeof EPF_ANO
  casenAno: typeof CASEN_ANO
  fuente: string
}

const DISCLOSURE_CONSUMO =
  'Estimado agregado a nivel macro-zona (Gran Santiago / capital regional), no medido en el área específica de esta oportunidad — nunca presentado con la precisión de la isócrona.'

/**
 * Pura por comuna — cero llamadas de red, cero caché necesario (recalcular
 * en cada llamada es gratis: son 2 lookups sobre arrays pequeños). Nunca
 * lanza. tasaPobrezaComunal: null si la comuna no aparece en
 * CASEN_POBREZA_POR_COMUNA (fuera de las 36 comunas RM cubiertas hoy) — eso
 * es un estado legítimo, no un error.
 */
export function obtenerConsumoEstimado(comuna: string): ConsumoEstimadoResultado {
  const objetivo = normalizarNombreComuna(comuna)
  const filaCasen = CASEN_POBREZA_POR_COMUNA.find((c) => normalizarNombreComuna(c.comuna) === objetivo)

  return {
    comuna,
    categorias: EPF_PARTICIPACION_POR_CATEGORIA,
    categoriasPendientes: EPF_PARTICIPACION_POR_CATEGORIA.filter((c) => c.participacionPct === null).map((c) => c.nombre),
    tasaPobrezaComunal: filaCasen?.tasaPobrezaPersonas ?? null,
    nivelGeografico: 'macro_zona_gran_santiago',
    disclosure: DISCLOSURE_CONSUMO,
    epfAno: EPF_ANO,
    casenAno: CASEN_ANO,
    fuente: filaCasen
      ? `IX EPF (INE, ${EPF_ANO}) + CASEN ${CASEN_ANO} SAE (Ministerio de Desarrollo Social y Familia) — ${filaCasen.fuenteUrl}`
      : `IX EPF (INE, ${EPF_ANO}) — CASEN ${CASEN_ANO}: comuna sin dato transcrito todavía`,
  }
}
```
  </action>
  <verify>`npx tsc --noEmit` limpio. `grep "participacionPct: null" lib/consumo-macro-zona.ts | wc -l` confirma que hay categorías explícitamente pendientes (no todas rellenadas).</verify>
  <done>lib/consumo-macro-zona.ts existe con la tabla EPF real y parcial (3 confirmadas, resto null), el esqueleto CASEN vacío, y obtenerConsumoEstimado() maneja correctamente el caso "comuna sin dato CASEN" sin lanzar ni fabricar.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Transcripción humana de tasas de pobreza comunal CASEN 2024 (36 comunas RM)</name>
  <files>lib/consumo-macro-zona.ts</files>
  <action>
Ningún intento automatizado de esta investigación (2 WebFetch en esta sesión + los intentos ya documentados en `17-RESEARCH.md`) encontró un archivo descargable o API para las tasas de pobreza comunal CASEN 2024 — la plataforma "Data Social" del Ministerio de Desarrollo Social ni siquiera resuelve DNS desde este entorno. No hay automatización posible aquí sin acceso humano al dashboard oficial.
  </action>
  <how-to-verify>
Pide al usuario (Tomás) que:

1. Visite `https://observatorio.ministeriodesarrollosocial.gob.cl/pobreza-comunal`, seleccione el año 2024, y navegue al dashboard/tabla de tasa de pobreza por personas (metodología SAE) — o, si el observatorio redirige, `https://datasocial.ministeriodesarrollosocial.gob.cl/`.
2. Para las siguientes 36 comunas (todas Región Metropolitana, las que tienen oportunidades reales hoy en `mercado_locales_listings` — verificado en vivo el 2026-08-03), obtenga la tasa de pobreza por personas (%) de CASEN 2024:

Santiago, Maipú, Colina, Recoleta, Huechuraba, San Bernardo, Quilicura, Quinta Normal, Pudahuel, Renca, Macul, Independencia, Ñuñoa, Providencia, Lampa, San Miguel, Estación Central, Las Condes, Vitacura, Lo Barnechea, La Reina, San Joaquín, La Florida, Peñalolén, Cerrillos, Puente Alto, La Cisterna, Conchalí, Buin, La Granja, El Bosque, Pedro Aguirre Cerda, Cerro Navia, Padre Hurtado, Lo Prado, San Ramón.

3. Pegue el resultado en cualquier formato simple (ej. `Santiago: 8.2`, una línea por comuna, o una tabla copiada del dashboard). Si alguna comuna no aparece en el dashboard con una cifra clara, indíquelo explícitamente en vez de omitirlo silenciosamente — esa comuna queda con `tasaPobrezaComunal: null` en el código, un estado legítimo y ya manejado por `obtenerConsumoEstimado()`.
4. Con los datos que el usuario entregue, transcribir cada fila LITERALMENTE (nunca redondear "a ojo" ni interpolar comunas faltantes) dentro de `CASEN_POBREZA_POR_COMUNA` en `lib/consumo-macro-zona.ts` (Task 1), con `fuenteUrl` apuntando a la URL real consultada y `transcritoEl` con la fecha real de esta transcripción. Si el usuario no puede completar las 36 en esta sesión, transcribir las que sí entregó y dejar constancia explícita en el SUMMARY de cuáles quedaron pendientes — nunca fabricar las faltantes para "completar" el array.
  </how-to-verify>
  <resume-signal>Pega las tasas de pobreza comunal (comuna: %) que encontraste en la fuente oficial, o escribe "sin acceso ahora" para dejar el array vacío documentado como pendiente y continuar con el resto del plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Tests unitarios + entrada en data-sources.yaml</name>
  <files>tests/unit/consumo-macro-zona.test.ts, .planning/data-sources.yaml</files>
  <action>
Crear `tests/unit/consumo-macro-zona.test.ts` (función pura, sin mocks de red necesarios) cubriendo:
1. `obtenerConsumoEstimado('Providencia')` (o cualquier comuna que Task 2 haya poblado con dato real) retorna `tasaPobrezaComunal` numérico no-null, `nivelGeografico: 'macro_zona_gran_santiago'`, `disclosure` no vacío conteniendo la palabra "macro-zona".
2. `obtenerConsumoEstimado('Comuna Que No Existe En La Tabla')` retorna `tasaPobrezaComunal: null` sin lanzar.
3. `categoriasPendientes` incluye al menos las categorías EPF sin cifra confirmada (ej. `'Educación'` presente en el array), y NO incluye `'Alimentos y bebidas no alcohólicas'` (que sí tiene cifra real).
4. Normalización: `obtenerConsumoEstimado('PROVIDENCIA')` y `obtenerConsumoEstimado('Providencia')` (si esa comuna tiene fila real) retornan el mismo `tasaPobrezaComunal` — confirma que `normalizarNombreComuna` está siendo usado, no una comparación case-sensitive directa.

Si Task 2 resultó en un array `CASEN_POBREZA_POR_COMUNA` vacío (usuario respondió "sin acceso ahora"), ajustar el test 1 y 4 para usar `expect(...).toBeNull()` documentando la razón en un comentario — nunca fabricar una fila solo para que el test tenga un caso "feliz".

Agregar entrada en `.planning/data-sources.yaml` (mismo shape que `strip-power-centers-chile-seed`):
```yaml
- id: epf-casen-consumo-estimado
  name: "EPF (INE) + CASEN (MDS) — capacidad de gasto y pobreza comunal estimada"
  type: static-kb
  owner_files: [lib/consumo-macro-zona.ts]
  trigger: none
  called_from: []
  freshness_sla_days: null
  last_verified: 2026-08-03
  monitoring: none
  duplicate_of: null
  status: active
  notes: "DEMO-02/DEMO-03 — EPF: solo 3 de 12-13 categorías CCIF 2018.CL con cifra confirmada (Alimentación 21.2%, Vivienda 16.0%, Transporte 15.0%, IX EPF INE oct2021-sept2022) — resto explícitamente 'pendiente de transcripción' (categoriasPendientes), nunca inventado. Ni INE ni datasocial.ministeriodesarrollosocial.gob.cl exponen archivo descargable machine-fetchable (el segundo dominio ni siquiera resuelve DNS desde este entorno). CASEN: tabla acotada a las 36 comunas RM con oportunidades reales en mercado_locales_listings (no las ~335 comunas nacionales) — transcrita a mano por el usuario desde observatorio.ministeriodesarrollosocial.gob.cl/pobreza-comunal (metodología SAE, CASEN 2024). called_from queda vacío hasta el wiring gateado de Plan 17-03/17-04 — mismo criterio que ine-censo-2017-manzana (Plan 17-01), no es un huérfano."
```
  </action>
  <verify>`npm run test -- tests/unit/consumo-macro-zona.test.ts` pasa. `npm run typecheck` pasa. `npm run check:data-sources` no reporta la nueva entrada como huérfana o mal formada.</verify>
  <done>Los 4 casos de test pasan (ajustados honestamente al estado real de CASEN_POBREZA_POR_COMUNA producido por Task 2); data-sources.yaml documenta ambos gaps (EPF parcial, CASEN acotado a 36 comunas) con la misma disciplina que strip-power-centers-chile-seed.</done>
</task>

</tasks>

<verification>
`npm run test -- tests/unit/consumo-macro-zona.test.ts && npm run typecheck` pasan. `obtenerConsumoEstimado(comuna)` es invocable hoy, sin red, sin depender de Phase 16 ni de Plan 17-01, y nunca fabrica un share EPF ni una tasa CASEN — cada valor real transcrito tiene una fuente citada, y cada valor no confirmado queda explícitamente `null`/`pendiente`.
</verification>

<success_criteria>
Dado cualquier comuna RM con oportunidades reales, el sistema puede retornar una estimación de consumo con disclosure de macro-zona no-opcional, categorías EPF (parciales pero honestas) y tasa de pobreza CASEN (real donde el usuario la transcribió, null donde no) — listo para que Plan 17-03 lo componga sin fricción de datos faltantes fabricados.
</success_criteria>

<output>
After completion, create `.planning/phases/17-demografia-y-consumo/17-02-SUMMARY.md` — DEBE indicar explícitamente cuántas de las 36 comunas CASEN quedaron con dato real vs. pendiente, con la respuesta literal del usuario en el checkpoint.
</output>
