---
phase: 17-demografia-y-consumo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260811_cabida_comercial_cache_demografia.sql
  - lib/censo-manzana-server.ts
  - tests/unit/censo-manzana-server.test.ts
  - .planning/data-sources.yaml
autonomous: false

must_haves:
  truths:
    - "El sistema puede calcular población y viviendas dentro de un polígono arbitrario (isócrona real o círculo equivalente) intersectando manzanas del Censo 2017 en vivo contra el servicio ArcGIS CORRECTO (services9.arcgis.com, cobertura RM real) — nunca contra el servicio incorrecto citado en STACK.md/ARCHITECTURE.md a nivel de milestone (services3.arcgis.com/cTnMkBRk4HWkUCRo, cobertura cero en RM)"
    - "obtenerPoblacionEnPoligono() es pura por geometría — nunca acepta oportunidadId ni depende de que lib/cabida-comercial-server.ts exista, funciona hoy con cualquier GeoJSON.Polygon/MultiPolygon válido"
    - "Una consulta real contra una dirección conocida de Providencia retorna manzanas reales con NOM_COMUNA='PROVIDENCIA' — verificado en vivo, no solo por mock"
    - "cabida_comercial_cache tiene columnas demografia_* aplicadas en el proyecto Supabase real, aditivas, sin tocar las columnas de isócrona existentes"
  artifacts:
    - path: "lib/censo-manzana-server.ts"
      provides: "obtenerPoblacionEnPoligono(geometria) — query POST en vivo contra el FeatureServer correcto, agregación TOTAL_PERS/TOTAL_VIVI, nunca lanza"
      exports: ["obtenerPoblacionEnPoligono", "geometriaGeoJsonARings"]
      contains: "services9.arcgis.com/kKJR3Qt68ohAWuet"
    - path: "supabase/migrations/20260811_cabida_comercial_cache_demografia.sql"
      provides: "Columnas aditivas demografia_status/demografia_total_personas/demografia_total_viviendas/demografia_manzanas_intersectadas/demografia_censo_ano/demografia_consultado_el en cabida_comercial_cache"
      contains: "ADD COLUMN demografia_status"
  key_links:
    - from: "lib/censo-manzana-server.ts (obtenerPoblacionEnPoligono)"
      to: "https://services9.arcgis.com/kKJR3Qt68ohAWuet/arcgis/rest/services/Manzanas_censo_2017/FeatureServer/0/query"
      via: "POST con geometry=esriGeometryPolygon, spatialRel=esriSpatialRelIntersects — nunca GET, nunca el URL de services3.arcgis.com/cTnMkBRk4HWkUCRo"
      pattern: "services9\\.arcgis\\.com/kKJR3Qt68ohAWuet"
---

<objective>
Construir el módulo de población censal — la pieza más nueva de Fase 17, y la que la investigación de esta fase corrigió respecto a lo que STACK.md/ARCHITECTURE.md (nivel milestone) citaban como "confirmado en vivo". `lib/censo-manzana-server.ts` expone `obtenerPoblacionEnPoligono(geometria)`, una función pura por geometría (nunca por `oportunidadId`, nunca por `lat`/`lng` directo) que consulta en vivo, vía POST, el FeatureServer de manzanas censales CORRECTO — no el citado a nivel de milestone.

Purpose: `17-RESEARCH.md` verificó en vivo, con una consulta real, que el servicio citado en STACK.md/ARCHITECTURE.md (`services3.arcgis.com/cTnMkBRk4HWkUCRo/.../SHAPES_CENSO_2017/FeatureServer/8`) responde HTTP 200 con schema correcto pero **cubre solo 8 comunas de Atacama — cero cobertura en la Región Metropolitana**, donde vive el 100% de las oportunidades reales de `mercado_locales_listings`. Usar ese URL produciría una feature de población que siempre muestra "0 personas" para toda oportunidad real, sin ningún error visible. El servicio CORRECTO, verificado en vivo en la misma investigación, es `services9.arcgis.com/kKJR3Qt68ohAWuet/arcgis/rest/services/Manzanas_censo_2017/FeatureServer/0` (158.927 manzanas nacionales, 49.974 en RM). Esta es la corrección más importante de `17-RESEARCH.md` y este plan es el ÚNICO lugar donde se implementa la consulta — el URL correcto entra con un comentario explícito citando el hallazgo, para que ningún mantenedor futuro "ayude" revirtiendo al URL de STACK.md.

Este plan es deliberadamente independiente de Phase 16: `obtenerPoblacionEnPoligono()` recibe cualquier `GeoJSON.Polygon`/`MultiPolygon` válido — un círculo de prueba hecho a mano funciona igual que una isócrona real de ORS. La migración aditiva sobre `cabida_comercial_cache` también se aplica ahora, sin depender de que `lib/cabida-comercial-server.ts` exista (mismo precedente que Plan 18-04 de Fase 18: migración aditiva aplicada independiente del wiring final).

Output: `lib/censo-manzana-server.ts` con `obtenerPoblacionEnPoligono()` probado con fixtures + verificado en vivo contra una dirección real de Providencia; migración `demografia_*` aplicada en el proyecto Supabase real.
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-demografia-y-consumo/17-RESEARCH.md
@.planning/PROJECT.md
@.planning/STATE.md
@lib/cabida-comercial.ts
@lib/zonificacion.ts
@lib/zonificacion-geo.ts
@lib/overpass-competencia.ts
@lib/scraper.ts
@supabase/migrations/20260809_cabida_comercial_cache.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración aditiva demografia_* + lib/censo-manzana-server.ts</name>
  <files>supabase/migrations/20260811_cabida_comercial_cache_demografia.sql, lib/censo-manzana-server.ts</files>
  <action>
**1. Migración.** Crear `supabase/migrations/20260811_cabida_comercial_cache_demografia.sql`:

```sql
-- Aditivo sobre cabida_comercial_cache (Fase 16) — mismo patrón que
-- Fase 18's 20260810_cadenas_sucursales_geocoding.sql: migración aplicada
-- independiente de que lib/cabida-comercial-server.ts exista todavía.
-- demografia_status sigue el mismo criterio explícito que isocrona_status —
-- nunca solo nullability (ver 20260809_cabida_comercial_cache.sql).

ALTER TABLE cabida_comercial_cache
  ADD COLUMN demografia_status text NOT NULL DEFAULT 'pendiente',
  ADD COLUMN demografia_total_personas integer,
  ADD COLUMN demografia_total_viviendas integer,
  ADD COLUMN demografia_manzanas_intersectadas integer,
  ADD COLUMN demografia_censo_ano integer,      -- SIEMPRE 2017 hoy, pero explícito y no un literal disperso (DEMO-03)
  ADD COLUMN demografia_consultado_el timestamptz;

ALTER TABLE cabida_comercial_cache
  ADD CONSTRAINT cabida_comercial_cache_demografia_status_check
  CHECK (demografia_status IN ('pendiente', 'encontrado', 'error'));

COMMENT ON COLUMN cabida_comercial_cache.demografia_status IS
  'Independiente de isocrona_status (Fase 16) y competencia_status (Fase 18, si existe) — servicios externos independientes con fallas independientes, mismo criterio que zona_status.';
```

Aplicar en vivo contra el proyecto Supabase real vía `mcp__supabase__apply_migration` (no dejarla solo en disco). Confirmar aplicación real con `mcp__supabase__execute_sql`: `select column_name from information_schema.columns where table_name='cabida_comercial_cache' and column_name like 'demografia_%'` — deben aparecer las 6 columnas.

**2. `lib/censo-manzana-server.ts`.** Crear el módulo:

```typescript
// Población/viviendas dentro de un polígono arbitrario, vía intersección
// espacial en vivo contra el Censo 2017 (manzana). Fase 17 — DEMO-01.
//
// CORRECCIÓN CRÍTICA (17-RESEARCH.md, 2026-08-03): el FeatureServer citado a
// nivel de milestone en STACK.md/ARCHITECTURE.md
// (services3.arcgis.com/cTnMkBRk4HWkUCRo/.../SHAPES_CENSO_2017/FeatureServer/8)
// responde HTTP 200 con schema correcto, pero cubre SOLO 8 comunas de la
// Región de Atacama (3.928 manzanas) — CERO cobertura en la Región
// Metropolitana, donde vive el 100% de mercado_locales_listings. Ese
// servicio NUNCA lanza error para una consulta RM — retorna
// silenciosamente {features: []}, el failure mode más peligroso posible
// (un "0 personas" que parece un dato válido). NO REVERTIR a ese URL sin
// releer 17-RESEARCH.md primero.
//
// El servicio CORRECTO, verificado en vivo con una consulta real de
// polígono contra Providencia (10+ manzanas reales retornadas,
// NOM_COMUNA='PROVIDENCIA'), es:
const CENSO_2017_MANZANA_FEATURESERVER =
  'https://services9.arcgis.com/kKJR3Qt68ohAWuet/arcgis/rest/services/Manzanas_censo_2017/FeatureServer/0/query'
// 158.927 manzanas nacionales, 49.974 en "REGIÓN METROPOLITANA DE SANTIAGO"
// (verificado vía groupByFieldsForStatistics, ver 17-RESEARCH.md Sources).

import { z } from 'zod'
import { fetchWithTimeout } from '@/lib/scraper'

const ManzanaFeatureSchema = z.object({
  attributes: z.object({
    TOTAL_PERS: z.number().int().nullable().optional(),
    TOTAL_VIVI: z.number().int().nullable().optional(),
    MANZENT_I: z.string().nullable().optional(),
    NOM_COMUNA: z.string().nullable().optional(),
  }),
})
const ArcGisCensoResponseSchema = z.object({
  features: z.array(ManzanaFeatureSchema).default([]),
  exceededTransferLimit: z.boolean().optional(),
  error: z.object({ code: z.number().optional(), message: z.string().optional() }).optional(),
})

export interface PoblacionCensoResultado {
  ok: boolean
  totalPersonas: number
  totalViviendas: number
  manzanasIntersectadas: number
  comunasTocadas: string[]
  censoAno: 2017 // NUNCA opcional — DEMO-03
  fuente: 'INE Censo 2017 — manzana censal'
  consultadoEl: string
  paginado: boolean // true si exceededTransferLimit — ver Pitfall 3, 17-RESEARCH.md
  error?: string
}

// GeoJSON Polygon: coordinates ES YA un array de linear rings de [lng,lat] —
// misma forma que Esri rings. MultiPolygon: un array de Polygons, cada uno
// con sus propios rings — Esri acepta múltiples rings en un solo array
// (partes independientes), así que aplanamos. Conversión escrita en UN solo
// lugar (ver lib/zonificacion-geo.ts para la dirección inversa) — no
// reimplementar esto en ningún otro archivo.
export function geometriaGeoJsonARings(
  geometria: GeoJSON.Polygon | GeoJSON.MultiPolygon
): number[][][] {
  if (geometria.type === 'Polygon') return geometria.coordinates
  return geometria.coordinates.flat(1)
}

/**
 * Pura por geometría — nunca acepta oportunidadId ni lat/lng directo (mismo
 * criterio que obtenerCompetenciaPorFormato() de Fase 18). Convierte GeoJSON
 * → Esri rings, consulta el FeatureServer CORRECTO vía POST (nunca GET — un
 * polígono de isócrona real puede exceder límites de longitud de URL),
 * agrega TOTAL_PERS/TOTAL_VIVI. Nunca lanza — retorna { ok:false, ... 0s }
 * ante cualquier fallo de red/parseo, mismo contrato "non-success explícito"
 * que geocodeDireccion().
 */
export async function obtenerPoblacionEnPoligono(
  geometria: GeoJSON.Polygon | GeoJSON.MultiPolygon
): Promise<PoblacionCensoResultado> {
  const consultadoEl = new Date().toISOString()
  const vacio = (error?: string): PoblacionCensoResultado => ({
    ok: !error,
    totalPersonas: 0,
    totalViviendas: 0,
    manzanasIntersectadas: 0,
    comunasTocadas: [],
    censoAno: 2017,
    fuente: 'INE Censo 2017 — manzana censal',
    consultadoEl,
    paginado: false,
    ...(error ? { error } : {}),
  })

  try {
    const rings = geometriaGeoJsonARings(geometria)
    const body = new URLSearchParams({
      geometry: JSON.stringify({ rings, spatialReference: { wkid: 4326 } }),
      geometryType: 'esriGeometryPolygon',
      spatialRel: 'esriSpatialRelIntersects',
      inSR: '4326',
      outSR: '4326',
      outFields: 'TOTAL_PERS,TOTAL_VIVI,MANZENT_I,NOM_COMUNA',
      returnGeometry: 'false',
      f: 'json',
    })

    const res = await fetchWithTimeout(
      CENSO_2017_MANZANA_FEATURESERVER,
      { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body },
      15000
    )
    if (!res.ok) return vacio(`ArcGIS respondió HTTP ${res.status}`)

    const json = await res.json()
    const parsed = ArcGisCensoResponseSchema.safeParse(json)
    if (!parsed.success) return vacio('Respuesta de ArcGIS con schema inesperado')
    if (parsed.data.error) return vacio(parsed.data.error.message ?? 'ArcGIS retornó un error')

    const comunas = new Set<string>()
    let totalPersonas = 0
    let totalViviendas = 0
    for (const f of parsed.data.features) {
      totalPersonas += f.attributes.TOTAL_PERS ?? 0
      totalViviendas += f.attributes.TOTAL_VIVI ?? 0
      if (f.attributes.NOM_COMUNA) comunas.add(f.attributes.NOM_COMUNA)
    }

    return {
      ok: true,
      totalPersonas,
      totalViviendas,
      manzanasIntersectadas: parsed.data.features.length,
      comunasTocadas: [...comunas],
      censoAno: 2017,
      fuente: 'INE Censo 2017 — manzana censal',
      consultadoEl,
      paginado: parsed.data.exceededTransferLimit === true,
    }
  } catch (err) {
    return vacio(err instanceof Error ? err.message : 'Error desconocido consultando ArcGIS')
  }
}
```

Agregar entrada en `.planning/data-sources.yaml` (mismo shape que las entradas `type: api-endpoint` existentes, ej. `enrich-rut` o `dom-digital`):
```yaml
- id: ine-censo-2017-manzana
  name: "INE Censo 2017 — manzana censal (ArcGIS FeatureServer)"
  type: api-endpoint
  owner_files: [lib/censo-manzana-server.ts]
  trigger: manual
  called_from: []
  freshness_sla_days: null
  last_verified: 2026-08-03
  monitoring: none
  duplicate_of: null
  status: active
  notes: "DEMO-01 — CORRECCIÓN CRÍTICA (17-RESEARCH.md): usa services9.arcgis.com/kKJR3Qt68ohAWuet (158.927 manzanas nacionales, 49.974 en RM), NUNCA services3.arcgis.com/cTnMkBRk4HWkUCRo (citado en STACK.md/ARCHITECTURE.md a nivel de milestone — cubre SOLO 8 comunas de Atacama, cero cobertura RM, falla silenciosamente con features:[] en vez de error). Consulta POST en vivo, cache-through pendiente de Plan 17-03 (columnas demografia_* en cabida_comercial_cache). called_from queda vacío hasta que Plan 17-03 (gateado por Phase 16) wiree obtenerPoblacionEnPoligono() dentro de obtenerAnalisisCabidaComercial() — no es un huérfano, es la secuencia esperada de un plan independiente ejecutado antes del wiring final."
```
  </action>
  <verify>`mcp__supabase__execute_sql` confirma las 6 columnas `demografia_*` en `cabida_comercial_cache` (proyecto real, no solo el archivo SQL). `npx tsc --noEmit` limpio para `lib/censo-manzana-server.ts`.</verify>
  <done>Migración aplicada en Supabase real. `lib/censo-manzana-server.ts` exporta `obtenerPoblacionEnPoligono` y `geometriaGeoJsonARings`, usa el URL CORRECTO (`services9.arcgis.com/kKJR3Qt68ohAWuet`), nunca lanza, y tiene el contrato `ok`/ceros explícito ante fallo.</done>
</task>

<task type="auto">
  <name>Task 2: Tests unitarios — agregación, exceededTransferLimit, fallos, y guard contra el URL incorrecto</name>
  <files>tests/unit/censo-manzana-server.test.ts</files>
  <action>
Crear `tests/unit/censo-manzana-server.test.ts`, mockeando `global.fetch` (mismo patrón que `tests/unit/overpass-competencia.test.ts` — `vi.stubGlobal('fetch', ...)`, `afterEach` con `vi.unstubAllGlobals()`). Casos:

1. **Agregación correcta**: fixture con 3 manzanas (`TOTAL_PERS`/`TOTAL_VIVI` reales, 2 con `NOM_COMUNA: 'PROVIDENCIA'`, 1 con `NOM_COMUNA: 'ÑUÑOA'`) → `obtenerPoblacionEnPoligono()` retorna `totalPersonas`/`totalViviendas` sumados correctamente, `manzanasIntersectadas: 3`, `comunasTocadas` con las 2 comunas únicas, `ok: true`.
2. **`features: []`** (respuesta bien formada pero vacía — el failure mode del URL incorrecto) → `ok: true`, todos los totales en 0, `manzanasIntersectadas: 0`. Este caso NO debe confundirse con un error — un polígono real puede genuinamente no tocar ninguna manzana.
3. **`exceededTransferLimit: true`** → `paginado: true` en el resultado.
4. **HTTP no-200 y excepción de red** (ambos casos) → `ok: false`, todos los totales en 0, `error` poblado con un string no vacío, la función NUNCA lanza (envolver la llamada en `expect(...).resolves` no en `expect(() => ...).not.toThrow()`, para probar que efectivamente resuelve).
5. **Guard de regresión de URL** (el caso más importante de este archivo): `expect(fetchMock.mock.calls[0][0]).toContain('services9.arcgis.com/kKJR3Qt68ohAWuet')` en el test de agregación correcta, Y un test dedicado separado: `expect(fetchMock.mock.calls[0][0]).not.toContain('services3.arcgis.com/cTnMkBRk4HWkUCRo')` — este test existe específicamente para que un futuro revert accidental al URL de STACK.md falle en CI, no solo en producción.
6. **`geometriaGeoJsonARings`**: un caso `Polygon` (retorna `coordinates` tal cual) y un caso `MultiPolygon` con 2 polígonos (retorna un array aplanado con los rings de ambos, `result.length === polygon1.rings.length + polygon2.rings.length`).

Usar un polígono de prueba pequeño (bounding box, no una isócrona compleja) — mismo estilo que el usado en `17-RESEARCH.md` (Providencia, -70.618/-33.423 a -70.610/-33.430).
  </action>
  <verify>`npm run test -- tests/unit/censo-manzana-server.test.ts` pasa, incluyendo el test de guard de URL. `npm run typecheck` pasa.</verify>
  <done>Los 6 casos (agregación, vacío-no-error, paginado, fallo-nunca-lanza, guard de URL correcto/incorrecto, conversión de geometría) pasan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación en vivo contra el servicio ArcGIS real — dirección conocida de Providencia + confirmación de que el guard atrapa el URL incorrecto</name>
  <files>lib/censo-manzana-server.ts</files>
  <action>
Ya construido: el módulo con el URL corregido, y los tests unitarios (mockeados) que lo validan. Este task verifica en vivo, contra el servicio ArcGIS real, que (a) el URL correcto retorna manzanas reales para una dirección conocida de Santiago, y (b) el URL incorrecto (el citado en STACK.md/ARCHITECTURE.md) efectivamente falla silenciosamente para la misma consulta — la evidencia concreta de por qué el guard del Task 2 importa.
  </action>
  <how-to-verify>
1. Correr, literalmente, el comando `curl` verificado en `17-RESEARCH.md` (sección "Code Examples") contra el servicio CORRECTO:
```bash
POLY='{"rings":[[[-70.618,-33.423],[-70.610,-33.423],[-70.610,-33.430],[-70.618,-33.430],[-70.618,-33.423]]],"spatialReference":{"wkid":4326}}'
curl -s -G "https://services9.arcgis.com/kKJR3Qt68ohAWuet/arcgis/rest/services/Manzanas_censo_2017/FeatureServer/0/query" \
  --data-urlencode "geometry=${POLY}" --data-urlencode "geometryType=esriGeometryPolygon" \
  --data-urlencode "spatialRel=esriSpatialRelIntersects" --data-urlencode "inSR=4326" --data-urlencode "outSR=4326" \
  --data-urlencode "outFields=TOTAL_PERS,TOTAL_VIVI,MANZENT_I,NOM_COMUNA" --data-urlencode "returnGeometry=false" --data-urlencode "f=json"
```
Confirma que la respuesta trae manzanas reales con `"NOM_COMUNA":"PROVIDENCIA"` y valores `TOTAL_PERS`/`TOTAL_VIVI` numéricos, no cero ni vacío.

2. Correr el MISMO polígono contra el servicio INCORRECTO (el citado en STACK.md/ARCHITECTURE.md, NUNCA usado en el código de este plan):
```bash
curl -s -G "https://services3.arcgis.com/cTnMkBRk4HWkUCRo/arcgis/rest/services/SHAPES_CENSO_2017/FeatureServer/8/query" \
  --data-urlencode "geometry=${POLY}" --data-urlencode "geometryType=esriGeometryPolygon" \
  --data-urlencode "spatialRel=esriSpatialRelIntersects" --data-urlencode "inSR=4326" --data-urlencode "outSR=4326" \
  --data-urlencode "outFields=TOTAL_PERS,TOTAL_VIVI,MANZENT_I,NOM_COMUNA" --data-urlencode "returnGeometry=false" --data-urlencode "f=json"
```
Confirma que retorna HTTP 200 con `"features":[]` — mismo polígono, servicio distinto, resultado vacío sin error. Esta es la prueba viva de por qué el guard del Task 2 (`not.toContain('services3.arcgis.com/cTnMkBRk4HWkUCRo')`) existe: un revert accidental a este URL no produciría ningún error visible, solo ceros silenciosos.

3. Confirma en el código de `lib/censo-manzana-server.ts` (Task 1) que el URL usado en `CENSO_2017_MANZANA_FEATURESERVER` coincide EXACTAMENTE con el del paso 1, no con el del paso 2.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si ambos curls confirman el comportamiento esperado (real vs. vacío) y el código usa el URL correcto, o describe qué no coincidió.</resume-signal>
</task>

</tasks>

<verification>
`npm run test -- tests/unit/censo-manzana-server.test.ts && npm run typecheck` pasan. La migración `demografia_*` está aplicada en el proyecto Supabase real (confirmado por query directa a `information_schema.columns`, no solo por el archivo en disco). El checkpoint humano confirma con curl real que el servicio correcto retorna manzanas de Providencia y que el servicio incorrecto retorna vacío para el mismo polígono.
</verification>

<success_criteria>
`obtenerPoblacionEnPoligono(geometria)` es invocable hoy, de forma independiente, con cualquier polígono GeoJSON válido — sin depender de Phase 16 — y calcula población/viviendas reales usando el servicio ArcGIS con cobertura RM real, con evidencia en vivo de que el servicio incorrecto habría fallado silenciosamente. La migración aditiva `demografia_*` deja `cabida_comercial_cache` lista para que Plan 17-03 (gateado) la use sin fricción de schema.
</success_criteria>

<output>
After completion, create `.planning/phases/17-demografia-y-consumo/17-01-SUMMARY.md`
</output>
