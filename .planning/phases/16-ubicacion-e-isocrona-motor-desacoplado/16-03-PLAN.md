---
phase: 16-ubicacion-e-isocrona-motor-desacoplado
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260809_cabida_comercial_cache.sql
autonomous: false

must_haves:
  truths:
    - "cabida_comercial_cache existe y acepta una fila cacheada por (lat_r, lng_r, modo, minutos)"
    - "isocrona_metodo distingue explícitamente 'red_vial' de 'circulo_equivalente' a nivel de fila — no hay ninguna fila con isocrona real indistinguible de una degradada"
  artifacts:
    - path: "supabase/migrations/20260809_cabida_comercial_cache.sql"
      provides: "DDL de cabida_comercial_cache — tabla ANGOSTA (solo lat/lng/modo/minutos/isocrona_*), sin columnas demografia_*/competencia_* que Fase 17/18 agregarán vía migración aditiva"
      contains: "CREATE TABLE IF NOT EXISTS cabida_comercial_cache"
  key_links: []
---

<objective>
Crear la tabla de caché del motor de cabida comercial — angosta, solo con lo que Fase 16 realmente calcula (ubicación + isócrona), no la tabla ancha que el research a nivel de milestone bosquejó con columnas de demografía/competencia que Fases 17/18 todavía no llenan.

Purpose: Decisión ya tomada por el orquestador (no re-litigar): tabla angosta ahora, columnas `demografia_*`/`competencia_*` se agregan vía migración ADITIVA en Fase 17/18, exactamente como `20260730_zonificacion.sql` → `20260730_zonificacion_v2.sql` agregó `geometria` sin tocar filas existentes. Pre-construir columnas para datos que esta fase no puebla viola la disciplina de este repo.
Output: `supabase/migrations/20260809_cabida_comercial_cache.sql` escrita Y aplicada a la instancia Supabase en vivo (checkpoint — mismo patrón que `10-01-PLAN.md`, la migración original de `zonificacion_cache`).
</objective>

<execution_context>
@/Users/tomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ubicacion-e-isocrona-motor-desacoplado/16-RESEARCH.md
@supabase/migrations/20260730_zonificacion.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración de cabida_comercial_cache</name>
  <files>supabase/migrations/20260809_cabida_comercial_cache.sql</files>
  <action>
Crea `supabase/migrations/20260809_cabida_comercial_cache.sql` con exactamente este contenido (DDL angosta, ya vetada en `16-RESEARCH.md`, sección "Cache table" — no rediseñar):

```sql
-- Motor de cabida comercial (Fase 16): caché de isócronas keyed por
-- ubicación, NO por oportunidadId (CABI-01 — motor desacoplado). Tabla
-- ANGOSTA a propósito: solo lat/lng/modo/minutos/isocrona_* — Fase 17/18
-- agregarán demografia_*/competencia_* vía migración ADITIVA, mismo patrón
-- que 20260730_zonificacion.sql → 20260730_zonificacion_v2.sql. Ver
-- 16-RESEARCH.md "Open Question 2".

CREATE TABLE IF NOT EXISTS cabida_comercial_cache (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  lat_r numeric(9,6) NOT NULL,      -- redondeado a 6 decimales (~11cm), mismo criterio que zonificacion_cache
  lng_r numeric(9,6) NOT NULL,
  modo text NOT NULL,               -- 'caminando' | 'auto'
  minutos integer NOT NULL,

  isocrona_status text NOT NULL DEFAULT 'pendiente',  -- enum explícito, mismo criterio que proyectos.zona_status — nunca solo nullability
  isocrona_metodo text,             -- 'red_vial' | 'circulo_equivalente' — NULL solo mientras status='pendiente'
  isocrona_geometria jsonb,
  isocrona_proveedor text,          -- 'openrouteservice' | NULL (NULL cuando metodo='circulo_equivalente')

  consultado_el timestamptz NOT NULL DEFAULT now(),
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_cabida_comercial_cache_geo
  ON cabida_comercial_cache (lat_r, lng_r, modo, minutos);

ALTER TABLE cabida_comercial_cache ENABLE ROW LEVEL SECURITY;

CREATE POLICY "cabida_comercial_cache_read" ON cabida_comercial_cache
  FOR SELECT TO authenticated USING (true);
-- Sin política de INSERT/UPDATE para authenticated — escrituras solo vía
-- service role (createServiceClient()), mismo patrón que zonificacion_cache.

ALTER TABLE cabida_comercial_cache
  ADD CONSTRAINT cabida_comercial_cache_status_check
  CHECK (isocrona_status IN ('pendiente', 'encontrado', 'error'));

ALTER TABLE cabida_comercial_cache
  ADD CONSTRAINT cabida_comercial_cache_metodo_check
  CHECK (isocrona_metodo IS NULL OR isocrona_metodo IN ('red_vial', 'circulo_equivalente'));

ALTER TABLE cabida_comercial_cache
  ADD CONSTRAINT cabida_comercial_cache_modo_check
  CHECK (modo IN ('caminando', 'auto'));
```

Design notes (no desviar):
- `lat_r`/`lng_r` son `numeric(9,6)`, no `double precision` — mismo motivo que `zonificacion_cache.lat_r`: la UNIQUE INDEX necesita una clave determinista, no float equality.
- `isocrona_metodo` es NULLABLE a nivel de columna PERO el CHECK constraint restringe sus valores no-null a los 2 explícitos — el código en `lib/cabida-comercial-server.ts` (Plan 16-04) nunca debe escribir una fila con `isocrona_status='encontrado'` y `isocrona_metodo=NULL` (eso sería el bug del Pitfall 1 de 16-RESEARCH.md persistido en DB).
- Ningún índice sobre `oportunidadId` — esta tabla deliberadamente no sabe qué es una Oportunidad (CABI-01).
  </action>
  <verify>Archivo existe en `supabase/migrations/20260809_cabida_comercial_cache.sql`; `grep -c "CREATE TABLE IF NOT EXISTS cabida_comercial_cache" supabase/migrations/20260809_cabida_comercial_cache.sql` retorna 1; `grep -c "cabida_comercial_cache_metodo_check" supabase/migrations/20260809_cabida_comercial_cache.sql` retorna 1.</verify>
  <done>Migración escrita con la DDL exacta de arriba: tabla, índice único geo+modo+minutos, RLS de solo lectura, y los 3 CHECK constraints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aplicar la migración</name>
  <files>supabase/migrations/20260809_cabida_comercial_cache.sql</files>
  <action>
Si en esta sesión hay tools `mcp__supabase__*` disponibles (verificado en Fase 10/11: a veces el executor no los tiene bound pero el orquestador sí — ver `.planning/phases/10-motor-de-zonificacion/10-01-SUMMARY.md`), úsalos directamente:
1. `mcp__supabase__apply_migration` con el contenido de `supabase/migrations/20260809_cabida_comercial_cache.sql`.
2. `mcp__supabase__list_tables` — confirmar que `cabida_comercial_cache` aparece con sus columnas.
3. `mcp__supabase__get_advisors` (security) — confirmar que no hay hallazgos nuevos atribuibles a esta tabla.

Si `mcp__supabase__*` NO está disponible en esta sesión, aplica manualmente vía el SQL Editor del dashboard de Supabase (mismo fallback documentado en `10-01-PLAN.md` Task 2):
1. Dashboard → SQL Editor → New Query → pegar y correr el contenido completo del archivo.
2. Table Editor → confirmar que `cabida_comercial_cache` existe con las columnas: id, lat_r, lng_r, modo, minutos, isocrona_status, isocrona_metodo, isocrona_geometria, isocrona_proveedor, consultado_el, created_at.
3. Authentication → Policies → confirmar que `cabida_comercial_cache_read` aparece en la tabla.

Si NINGUNA de las dos rutas está disponible en este entorno de ejecución, documenta explícitamente en el SUMMARY que la migración quedó escrita pero NO aplicada, y márcalo como bloqueante para Plan 16-04 (que hace `upsert` contra esta tabla) — no continúes fingiendo que está aplicada.
  </action>
  <how-to-verify>Query de confirmación: `SELECT * FROM cabida_comercial_cache LIMIT 1;` no debe fallar por "relation does not exist".</how-to-verify>
  <resume-signal>Confirma que la tabla existe en la instancia Supabase en vivo (por MCP o por dashboard) antes de dar este plan por completo.</resume-signal>
</task>

</tasks>

<verification>
`cabida_comercial_cache` existe en la instancia Supabase real (no solo como archivo `.sql` sin aplicar) — verificable vía `mcp__supabase__list_tables` o el Table Editor del dashboard.
</verification>

<success_criteria>
Migración escrita y aplicada; Plan 16-04 puede hacer `upsert` contra `cabida_comercial_cache` sin error "relation does not exist".
</success_criteria>

<output>
After completion, create `.planning/phases/16-ubicacion-e-isocrona-motor-desacoplado/16-03-SUMMARY.md`
</output>
